# 海兔下载器 · 全量测试与代码审查报告

> 测试日期：2026-09-16
> 被测提交：`1c16d02`（main，与工作区一致；`git status` 仅有 3 个历史未跟踪文件，与手册 6.6 描述相符）
> 被测版本：**1.0.6**
> 测试对象：`src/`（App + Core + Core.Tests + UiTests）、`extension/`、`tests/`、`tools/`、`build.ps1`、桌面单文件产物
> 报告性质：**只报告，不修复**（按用户要求，本次未改动任何项目文件）

---

## 〇、结论摘要

**总体结论：主程序（Core + App）功能可用、架构清晰、注释与文档质量高于同类项目；风险集中在「浏览器扩展的接管时序」与「验证脚本本身的可信度」两处。**

| 维度 | 结论 |
| --- | --- |
| 编译 | ✅ `0 warning / 0 error` |
| Core 测试 | ⚠️ **不稳定**：3 次运行 2 失败 1 通过（90 项，失败项固定为 `暂停与恢复`）；根因是**测试夹具缺陷**，不是产品缺陷（已用真机对照实验证明） |
| 扩展逻辑测试 | ✅ 20/20 |
| UI 冒烟（UiTests） | ✅ exit 0，7 张截图齐全；截图内容经像素统计确认为应用本体 |
| 核心功能实测 | ✅ 单文件发行、透明三模式、启动动画、磁力/BT、断点续传、文件名解析、重复检测、限速热更新、孤儿进程清理 —— 均**实测通过** |
| 代码审查 | 发现 **7 项高危、22 项中危、11 项低危**；无「已发生的数据损坏」，但有 3 项会在真实使用中**静默丢失用户的下载/配置** |
| 文档 | 手册与打包指南总体准确；但 §2.5 对 tools 脚本能力的描述与实际不符（见 A-7） |

**最该优先处理的 5 件事**

1. **A-1 / A-3**：扩展「先转发后取消」的接管时序 —— 可能造成同一文件落盘两份且下载历史被抹除；占位去重还会取消本不该接管的下载项。
2. **A-2**：扩展转发不带 Cookie / 浏览器 UA —— 认证类下载会在浏览器副本被取消后必然失败，用户"两头空"。
3. **A-5 / A-6**：`tools/` 里硬编码了**真实 API 密钥**（已入库），以及 UiTests 会**删除用户真实的 `aria2.session`**（正在下载的断点续传状态会丢）。
4. **A-4**：`暂停与恢复` 测试不稳定 → "90/90" 不能作为发布门禁；该用例实际测不出断点续传。
5. **A-7**：透明度验证脚本存在 **DPI 坐标系混用**，其"证据"不成立（功能本身是好的，我已用正确方法复验）。

---

## 一、测试执行情况（可复现）

### 1.1 环境

| 项 | 值 |
| --- | --- |
| 仓库 | `D:\Project\kokonaDown` |
| .NET SDK | 8.0.401 |
| 屏幕 | 1920×1080 @125%（与手册 §11 一致） |
| 应用数据 | `%APPDATA%\KokonaDownloader\` |
| 测试前状态 | 无 `KokonaDownloader.exe`；发现 **1 个残留 `aria2c` 孤儿进程**（已清理，属正常设计内的孤儿，见 §四） |

### 1.2 结果矩阵（期望值取自手册 / 打包指南）

| # | 测试 | 命令 | 文档期望 | 实测 | 判定 |
| --- | --- | --- | --- | --- | --- |
| 1 | 编译 | `dotnet build …App.csproj -c Release -r win-x64` | 0 error | 0 warning / 0 error | ✅ |
| 2 | Core 测试 | `dotnet test …Core.Tests.csproj -c Release` | **90/90** | ①89/90 ②单跑失败 ③**90/90** | ⚠️ 不稳定 |
| 3 | 扩展逻辑测试 | `cscript //nologo tests\test_logic.js` | 20/20 | 20/20 | ✅ |
| 4 | UI 冒烟 | `dotnet run --project …UiTests -c Release` | exit 0 + 7 张截图 | exit 0，7/7 截图 | ✅ |
| 5 | 透明度回归 | `tools\verify_transparency8.ps1` | 三模式可区分 | 脚本自述"生效"；**但证据不可信（A-7）**，已另行复验（§1.3） | ⚠️ |
| 6 | 启动动画 | `tools\probe_windows.ps1` | 启动即出现 | 535 ms 出现可见窗口 336×336（=420 物理 @虚化） | ✅ |
| 7 | 单文件发行 | 桌面 `KokonaSingleFile\KokonaDownloader.exe` + `/api/ping` | 200 | `{"ok":true,"app":"KokonaDownloader","version":"1.0.6"}` | ✅ |
| 8 | 孤儿清理 | 强杀应用后重启 | 日志出现"清理孤儿" | `清理孤儿 aria2 进程 PID=…` 且孤儿被终止 | ✅ |
| 9 | API 端到端（自建） | `node .verify\functest\test.mjs` | — | **23/24**（唯一失败项 = 测试服务器不支持 Range，与第 2 项同源） | ✅ |
| 10 | 断点续传（对照） | `node .verify\functest\range.mjs`（服务器支持 206） | — | 暂停→恢复→**completed 8388608/8388608** | ✅ |
| 11 | 限速热更新 | `node .verify\functest\hotapply.mjs` | — | aria2 `max-overall-download-limit` 1 MB/s→2 MB/s→1 MB/s | ✅ |
| 12 | 多 URL 语义 | `node .verify\functest\multi.mjs` | — | 2 个 URL → **1 个任务、1 个文件**（镜像语义，见 B-1） | ⚠️ |
| 13 | 设置窗口布局 | `.verify\probe_settings_clip.ps1` | — | 物理 680×816（=544×653 DIP），**61 个子元素 0 个越界** | ✅ |

### 1.3 关键实测数据

**（1）`暂停与恢复` 为什么失败 —— 已定位到根因，且证明是测试缺陷**

失败时 `GetTaskAsync` 返回 `Failed`。对同一个测试场景做了 instrumentation（`.verify/diag`，真实 aria2 + 本地服务器），拿到 aria2 原始报错：

```
[aria2] errorCode=8 URI=http://127.0.0.1:59255/pause.bin
[aria2]   -> [HttpResponse.cc:81] errorCode=8 Invalid range header.
          Request: 2965504-5242879/5242880, Response: 0-5242879/5242880
```

即：暂停后恢复时 aria2 发送 `Range:` 续传请求，而 `TestEnv.FileServer`（`TestEnv.cs:75-100`）**不实现 Range / 206**，返回整文件 200 → aria2 判定 range 非法 → 任务失败。

对照实验（同一应用、真实 HTTP 服务器、**支持 206**）：

```
before pause: state=active completed=1884160/8388608
paused:       state=paused
（恢复后发出 8 个 Range 请求，服务器回 206）
final:        state=completed completed=8388608/8388608
```

→ **产品功能正常，测试夹具不完整。** 该用例只在"暂停恰好发生在下载启动前"这种窄时间窗内通过，因此在快机器上表现为随机失败（本次 3 次运行：失败、失败、通过）。

**（2）透明度 —— 用正确坐标系复验，结论：功能真实有效**

项目自带脚本的坐标系混用（见 A-7）使其数据不可采信，故另写 DPI 一致（全部物理像素、48 个采样点、逐点校验"该点属于应用进程"）的复验脚本：

| 模式 | 窗口亮度(均值/范围) | 同点桌面亮度(均值/范围) | 窗口↔桌面 相关系数 | 判定 |
| --- | --- | --- | --- | --- |
| 不透明 | 31 / 28–148 | 105 / 24–194 | **-0.210** | 与背景无关 → 正确不透明 ✅ |
| 磨砂透明 | 69 / 29–148 | 117 / 38–215 | **+0.776** | 随背景变化 → 通透 ✅ |
| 黑色纯透明 | 100 / 33–171 | 106 / 26–250 | **+0.727**（48 点中 13 点与桌面差 ≤6） | 近乎全透 ✅ |

三种模式均 `owned=48/48`（采样点确认属于应用窗口，且与像素读取同一坐标空间）。

**（3）UiTests 截图内容核验**（模型无法看图，改用像素统计 + 主题色比对）

| 截图 | 尺寸 | 主色（Top-1/Top-2） | 结论 |
| --- | --- | --- | --- |
| ui_03 / ui_04 / ui_07 | 1159×692 | `18,31,34`（=#121F22，海雾青 WindowFill）、`31,48,53` | 与应用暗色主题完全一致 |
| ui_05 / ui_06 / ui_07 | 484×282 / 1159×692 | 含 `53,188,203`（=#35BCCB，海雾青强调色） | "完成态"确实出现强调色填充 |
| mode-Opaque.png（透明基线） | 940×560 | `18,31,34`、`31,48,53` | 同为应用本体 |

→ 截图**确为应用窗口内容**（若截到覆盖的 IDE，主色不可能是应用的海洋主题色）。

---

## 二、问题清单

严重度定义：**高**＝会造成用户数据/下载丢失、或让安全/质量门禁失效；**中**＝特定场景下功能错误或明显体验缺陷；**低**＝一致性、卫生、健壮性问题。

### A. 高危（7）

---

#### A-1 扩展「先转发、后取消」存在竞态：小文件会落盘两份，且下载历史被抹除

**位置**：`extension/background.js:258-261`、`background.js:280-283`

```js
258:      // 新建或客户端已有任务（duplicate）都算接收成功：拦截浏览器下载，交给客户端管理
260:      await forwardDownload(s, payload);
261:      await cancelAndErase(item.id);
```

```js
280: async function cancelAndErase(downloadId) {
281:   try { await chrome.downloads.cancel(downloadId); } catch (e) { /* 可能已结束 */ }
282:   try { await chrome.downloads.erase({ id: downloadId }); } catch (e) { /* 忽略 */ }
283: }
```

**问题**：扩展没有使用 `chrome.downloads.onDeterminingFilename`，因此**无法在浏览器开始下载前拦下它**。从 `onCreated` 到 POST 返回要经过 SW 冷启动 + 两次 `storage` 读取（253/254 行）+ 最长 5 s 的 POST 超时（134 行）。对一个小文件，`cancelAndErase` 执行时浏览器**早已下载完成**；而 `chrome.downloads.erase` 只删历史条目、**不删磁盘文件**。

**后果**：磁盘上出现两份文件；浏览器下载历史里那份被抹掉，用户无从追溯，且全程无任何提示。

**建议方向**：改用 `onDeterminingFilename` 同步决策（`preventDefault()`）后再异步转发，转发失败则 `chrome.downloads.download()` 重新交还浏览器；至少在 cancel 后 `chrome.downloads.search` 复查，若已完成则提示用户。

---

#### A-2 扩展转发不带 Cookie / 浏览器 UA：认证类下载会在浏览器副本被取消后必然失败

**位置**：`extension/logic.js:106-114`、`extension/background.js:257-261`；客户端侧 `Aria2Process.cs:83`（固定 `--user-agent=Transmission/2.92`）

```js
110:        var payload = { urls: [url] };
111:        if (name && !isMagnet) payload.filename = name;
112:        if (item.referrer) payload.referer = referrer;
```

**问题**：payload 只有 `urls` / `referrer`。扩展没有 `cookies` 权限，也没有透传请求头；而客户端 aria2 用的是固定非浏览器 UA（`ApiService` 其实**支持** `headers`，`ApiService.cs:21`、`Aria2RpcClient.cs:231`，只是扩展不发）。

**后果**：需要登录态（Cookie）或 UA/防盗链校验的下载，浏览器能下、aria2 不能下。此时扩展已收到 `200 {ok:true,gid}`，于是**取消并抹除**浏览器下载，客户端任务随后失败或落盘一个 HTML 错误页 → 用户"两头空"，且没有任何提示。

**建议方向**：至少透传 `headers: ['User-Agent: <浏览器 UA>']`；或在客户端任务真正 started 后再取消浏览器下载；并把该限制写进扩展说明。

---

#### A-3 占位去重早于 `shouldCapture`：会把"不该接管"的下载项取消并抹除

**位置**：`extension/background.js:245-251`（早于 255 行的 `shouldCapture`）、`background.js:291-295`

```js
245:  const key = KokonaLogic.urlKey(item.url);
246:  // 同步占位去重：会话恢复可能并发触发同一链接的多个下载项
247:  if (forwardingNow.has(key)) {
248:    await cancelAndErase(item.id);
249:    return;
250:  }
251:  forwardingNow.add(key);
```

**问题**：`forwardingNow` 是**自动捕获与手动发送共用**的集合，而"该不该接管"的判定在其后。三种静默失效：

1. 用户在弹窗/右键手动发送 URL X 期间，若浏览器恰好创建了 X 的下载项（例如 autoCapture 已关闭时），该项会在 248 行被取消+抹除；
2. 会话恢复/历史回放项与在途捕获**同 URL** 时，回放项会被取消并从下载历史抹掉 —— 正是"启动防火墙"要保护的东西；
3. 先到者 A 占了坑，A 随后被 `shouldCapture` 判为"不接管"（autoCapture 关、回放、自身 API），而并发到场的**真实新下载 B** 在 248 行被抹除且从未转发 → 下载彻底消失。

**建议方向**：把 `shouldCapture` 判定提到占位之前；对"决定不接管"的项一律不 `erase`；手动发送与自动捕获使用不同的键空间。

---

#### A-4 `暂停与恢复` 用例不稳定，导致"90/90"门禁失效

**位置**：`src/KokonaDownloader.Core.Tests/EngineIntegrationTests.cs:89-109`、夹具 `TestEnv.cs:43-115`

**证据**：3 次运行 2 失败 1 通过；失败信息固定为 `恢复后状态异常: Failed`；instrumentation 抓到 aria2 原文 `errorCode=8 … Invalid range header`（详见 §1.3）。

**影响**：
- 手册 §0/§2 把"Core 测试 90/90"写成接手自检与发布门禁 —— 该门禁实际不可靠（同一提交可能红可能绿）；
- 更实质的是：**断点续传这条主链路实际上没有被自动化覆盖**（用例在夹具下永远走"重启式续传"或直接失败，测不到真实续传）。

**建议方向**：给 `TestEnv.FileServer` 加 Range/206 支持（顺便覆盖 `Content-Range` 分支），并把断言从"状态属于三选一"改成"最终完成 + 落盘字节数一致"。

---

#### A-5 `tools/` 硬编码真实 API 密钥并已入库

**位置**：
- `tools/verify_singlefile_main.ps1:36`
- `tools/verify_singlefile_fallback.ps1:4`
- `tools/verify_desktop_launch.ps1:34`

```powershell
$secret = "a938ca540c347dbdcaf263fe86250661"
```

**证据**：该字符串与 `%APPDATA%\KokonaDownloader\settings.json` 中当前 `ApiSecret` **逐字节一致**（已比对）。`tools/` 是入库目录，仓库为公开 MIT 项目；手册 §7.3 明文禁止在入库文件里放密钥明文（`tools/verify_api_settings.ps1:5` 就是正确写法：从 settings.json 读）。

**影响**：任何拿到仓库的人 / 本机任意进程都持有一份有效凭据。虽然 API 仅绑 127.0.0.1，风险被削弱，但仍是凭据泄露 + 违反自定规范。

---

#### A-6 UiTests 会删除用户真实的 `aria2.session`，并在结束时留下"幻影任务"

**位置**：`src/KokonaDownloader.UiTests/Program.cs:49-50`、`524-536`（`CleanEngineSession`）

```csharp
528:            var session = Path.Combine(AppPaths.EngineWorkDir, "aria2.session");
529:            if (File.Exists(session))
531:                File.Delete(session);
```

**证据 1（数据丢失风险）**：应用恢复任务**只**依赖 aria2 的 `--save-session/--input-file`（`Aria2Process.cs:46-47`）；`tasks.json` 只是元数据。全仓检索无任何"重新入队/恢复下载"逻辑。因此当用户有正在下载的任务时跑一次 UI 宿主，**断点续传状态被直接删除**（且应用先被强杀），无法恢复。

**证据 2（污染用户环境，实测）**：`CleanEngineSession` 只在**启动时**清上一轮，结束时不清。我本次运行后，用户真实会话文件里残留了测试磁力：

```
magnet:?xt=urn:btih:1ae61217055ae9994420526b76cfa5cc69d91d95&dn=kokona-ui-matrix.bin&tr=http%3A%2F%2F127.0.0.1%3A61434%2Fannounce
 gid=b8d10aa1aaea21e8
 dir=…\Temp\kokona_dl_test\…\downloads
```

UIA 实测该应用界面显示为 **`[METADATA]kokona-ui-matrix.bin` / `下载中 0%` / `活动 1`** —— 一个永远连不上（回环 tracker 已关闭）的卡住任务。（证据已另存 `.verify/evidence_leftover_session.txt`；我已把该残留从用户数据中清除，恢复到我接手时的状态。）

**建议方向**：结束时也清理（或把测试专用 task 从 session 中剔除）；不要在真实 `%APPDATA%` 上跑破坏性清理；给用户数据做备份/恢复而非删除。

---

#### A-7 透明度验证脚本坐标系混用（DPI），其"证据"不成立；脚本本身还没有判据

**位置**：`tools/verify_transparency8.ps1:107-141`、`tools/verify_opaque_alpha.ps1:80-139`、`tools/verify_frosted_strength.ps1:85-132`

**问题**：这些脚本都不声明 DPI 感知（无 `SetProcessDPIAware`），而 PowerShell 5.1 宿主在**未触碰 UIA 前**是 DPI-unaware。此状态下 API 不在同一坐标系：

- `GetWindowRect` / `WindowFromPoint` 返回**虚拟化**坐标；
- `Graphics.CopyFromScreen` + `Bitmap.GetPixel` 按**物理**像素索引，且位图只有 1536×864 → 屏幕右下 384×216 物理像素**从未被采集**。

**我的一手证据**（同一进程、同一窗口，仅因中途调用 UIA 而改变）：

```
--- windows BEFORE opening settings ---   （尚未触碰 UIA）
  hwnd=52169584 size=940x560  pos=(154,154)      ← 虚拟化坐标
looking for button label: '设置'                 ← 此处调用 UIA，宿主转为 DPI-aware
--- windows AFTER opening settings ---
  hwnd=52169584 size=1175x700 pos=(192,192)      ← 物理坐标（同一窗口！）
```

即：`940×560`(虚拟) 与 `1175×700`(物理) 是**同一个窗口**。脚本把虚拟坐标当索引去读物理位图，采样点整体偏移。我在实际运行 `verify_transparency8.ps1` 的输出里也能看到症状：不透明模式本应恒定，却出现一个 `250` 的亮点（该点实际落在窗口外/标题区），且与 v1.0.6 文档记录的 `30 30 30 …` **对不上**。

附带问题：
- "app owns 10/10" 用的是 `WindowFromPoint`（虚拟空间），与像素读取空间不同，**它并不能证明采样的像素来自应用窗口**；并且脚本对它只打印、不判定；
- **`verify_transparency8.ps1` 全程没有 PASS/FAIL 与退出码**（`continue` 掉失败分支，最后只打印表格），任何结果都算"跑通"（exit 0）；
- 脚本会把用户真实 `settings.json` 的 `Transparency` 改成最后一种模式且**不恢复**（我实测运行后为 `BlackTransparent`，已手工还原为 `Opaque`）。同类问题见 B-13/B-14；
- 它还会**直接覆盖入库的回归基线图** `artifacts/transparency/mode-*.png`（因为坐标偏移，覆盖后的图与旧基线并不是同一区域）。本次运行后 `git status` 出现 3 个 ` M` 修改，我已用 `git checkout --` 还原。同类"验证脚本污染仓库"的问题也应一并考虑（建议输出到 `.verify/` 或临时目录后再由人确认入库）。

**注**：功能本身经我用正确坐标系复验为**有效**（§1.3-2）。出问题的是"证据链"。

---

### B. 中危（22）

| # | 位置 | 问题 | 影响 / 证据 |
| --- | --- | --- | --- |
| B-1 | `ApiService.cs:327-341` + 实测 | 一次请求里给多个 URL 时，全部塞进**同一个** `AddTaskAsync`（aria2 语义＝镜像 URI） | 实测：POST 2 个不同 URL → 1 个任务、**只落盘 1 个文件**。当前自带扩展只发单 URL，属潜在契约陷阱；`ApiDownloadRequest.Urls` 是数组，外部调用方极易踩 |
| B-2 | `App.xaml.cs:92`、`StandaloneBootstrap.cs:36-108` | 单文件发行版靠 `AppContext.BaseDirectory` 找 `aria2c.exe`/`icons\tray.png`，而该目录是 **%TEMP% 解压目录**；引导逻辑解压到 exe 旁的副本**从未被引用** | 实测：aria2 进程路径为 `%TEMP%\.net\KokonaDownloader\<hash>\aria2c.exe`。即依赖 .NET 宿主把内容自解压到临时目录；若该行为变化 / %TEMP% 不可写或被拦截，引擎与托盘图标会**静默失效**，而 exe 旁的副本不会兜底 |
| B-3 | `background.js:153` vs `301-305` | `forwardDownload` 只回传 `{ok,gid,duplicate}`，**丢掉**客户端磁力场景特有的 `confirm` 字段，可 `manualSend` 却在判断 `r.confirm` | 用户从弹窗发磁力时永远显示"已发送到海兔客户端"，而实际客户端正弹独立确认窗等待点击（实测客户端确实返回 `{"ok":true,"confirm":true}`） |
| B-4 | `popup.js:109-117`、`background.js:387-389` | 盲读剪贴板当密钥 + "离线也保存" | 剪贴板里任何非 URL 文本会被当作密钥写入；若此刻客户端没开（`code==='offline'` 也保存），**原有可用密钥被覆盖**，提示却只说"客户端未启动" |
| B-5 | `background.js:91-115`、`184` | 密钥校验用 `/api/stats`（依赖 aria2 引擎） | 引擎抖动返回 500 时，弹窗说"客户端未启动"、徽标说"密钥错误"，诊断方向被带偏；`/api/settings` 是无引擎依赖的只读端点，更合适 |
| B-6 | `background.js:244-278`、`manifest.json` | 未过滤 `item.incognito` / `item.byExtensionId` | 其他扩展发起的下载会被劫持并抹除历史；无痕下载的 URL 会被明文写入 `tasks.json` |
| B-7 | `background.js`（无 `/api/settings` 调用） | 客户端 `InterceptBrowserDownloads`（`ApiService.cs:226,398`）与扩展自己的 `autoCapture` 是**两个互不知情**的开关 | 在客户端关掉"自动接管"对扩展毫无作用，两者可互相矛盾 |
| B-8 | `background.js:372` vs `386-392` | `connState` 全局竞态：旧设置发起的 `refreshStatus()`（最长 ~4 s）会覆盖新密钥 `test-key` 写入的结果 | 弹窗显示"已连接"，徽标/`get-status` 显示"密钥错误" |
| B-9 | `popup.js:73,130,148,176`、`options.js:23,32` | `chrome.runtime.sendMessage` 未包 try/catch | 扩展更新后页面上下文失效会**同步抛错**，按钮永久禁用、无任何提示（`content.js:14-24` 已有正确写法） |
| B-10 | `background.js:262-273`、`217-227` | 失败通知无去重/退避 | 页面批量触发 N 个下载且客户端离线时，弹 N 条相同通知；客户端有 `NotifiedStore`，扩展没有 |
| B-11 | `logic.js:89-98`、`background.js:255` | 不接管时**完全静默**（无通知、无徽标、无日志）；且 `Date.parse(startTime)` 失败即 `return true`（fail-open） | 用户在"应该被接管"的预期下看到浏览器自己下载了，不知为何；时间格式若变化会让防火墙失效并重下回放项 |
| B-12 | `DownloadEngine.cs:557-593` | `AddTasksAsync` 与 `AddTaskAsync` 行为不一致：**不调用** `DropUrlDerivedFileName`，`TaskMeta` 也**不写** `IsBt/SourceMagnet` | 是 113484f 修复与磁力元数据的"漏网分支"。当前两条入口都不触发（对话框批量不带 FileName；磁力单独走单条分支），属**潜在隐患**：日后有人把磁力或文件名接到批处理路径即复现旧缺陷 |
| B-13 | `tools/verify_opaque_alpha.ps1:69-71`、`verify_frosted_strength.ps1:55-65`、`shoot_transparency_flyout.ps1:104-105`、`verify_transparency8.ps1:89-93` | 直接改写用户真实 `settings.json`（Transparency / FrostedStrength），**无备份、无恢复**（`shoot_*` 的"恢复"写的是硬编码默认值而非原值） | 跑完脚本用户的应用被静默改成磨砂/全透明；实测 `verify_transparency8.ps1` 留下 `BlackTransparent` |
| B-14 | `tools/verify_filename_resolution.ps1:60-70,117-120`、`probe_app_filename.ps1:16-29` | 把用户 `DefaultDownloadDir` 改成 `%TEMP%\kokona_nametest`，且**无 try/finally**：失败/中断路径直接 `exit`，恢复代码不执行；读取用 `Get-Content -Raw`（未指定 UTF-8） | 异常路径下用户下载目录被永久改成临时目录（且该目录不会被清理），后续真实下载可能落到会被系统清理的位置；非 ASCII 路径还可能被写成乱码 |
| B-15 | `tools/probe_aria2_filename.ps1:18,70-71` | `Get-Process aria2c \| Stop-Process -Force` **无差别强杀** | 若用户正开着下载器，会杀掉其引擎子进程、中断在途下载并留下 `.aria2` 控制文件 |
| B-16 | 18 个 tools 脚本中的 17 个 | 只有 `verify_api_settings.ps1` 用 `exit $fail` 传递结论，其余**恒返回 0** | 打印一屏 `FAIL:` 也返回成功，任何自动化/Agent 都会误判"验证通过" |
| B-17 | `Settings/AppSettings.cs:99-117`、`Themes/ThemeService.cs:65-74` | `SettingsStore.Update/Save` 无锁；API 线程（POST /api/settings）与 UI 线程可并发 Update + 写同一个 `.tmp` 再 Move | 内存 `Current` 与磁盘可能不一致；极端时序下设置丢失（UI 侧已做 DispatcherQueue 调度，但存储器本身未加锁） |
| B-18 | `App.xaml.cs:31-39` | `App.Log` 只追加、**无轮转/无上限** | 实测用户 `app.log` 已达 **641,324 行**（含逐任务与逐次轮询日志），长期运行会持续膨胀 |
| B-19 | `MagnetProtocol.cs:14-32` | 每次启动无条件写入 `HKCU\Software\Classes\magnet` | 会**覆盖用户原有的磁力关联**（qbittorrent/uTorrent 等），且设置里没有任何开关或还原入口 |
| B-20 | `ProgressWindow.xaml.cs:207-211` | `GetLastTask()` 用 `.GetAwaiter().GetResult()` 在 **UI 线程同步阻塞** RPC（HttpClient 超时 10 s） | 引擎无响应时点"打开文件夹"会卡界面 |
| B-21 | `WindowEffects.cs:71,90` | `MinSizeMap` 以 hwnd 为键、**只增不减** | 窗口销毁后 hwnd 被系统复用 → 新窗口 `SetMinSize` 命中旧键直接 return，最小尺寸静默失效 |
| B-22 | `UiTests/Program.cs:472-499`、`163-166`、`306-317` | 截图前不校验窗口在最前；限速 POST 失败、切页签失败都只 `WARN`；结束时也不校验 7 张图是否产出 | 可能产出"拍到别的窗口/拍成完成态/文件名与内容不符"的截图而**仍然 exit 0**（本次实测截图内容确为应用本体，风险未发生但判据不成立） |

### C. 低危（11）

| # | 位置 | 问题 |
| --- | --- | --- |
| C-1 | `SettingsWindow.xaml.cs:50` | 设置窗口用 **物理像素** `Resize(680×816)`，而主窗口是 DIP×DPI（`MainWindow.xaml.cs:118-119`）。实测 125% 下为 544×653 DIP。已实测**无元素越界**（61 个子元素 0 越界），故只是与其他窗口约定不一致 |
| C-2 | `tools/verify_singlefile_main.ps1:93-97,111-123` | 每次运行在 `%TEMP%` 残留 `kokona_test_A`（实测 14 文件 / 310.2 MB），脚本自身不清理 |
| C-3 | `tools/verify_singlefile_fallback.ps1:38-55,29,34,59` | `$before` 只打印不参与判定 → 目录里的陈旧载荷即可让 `$after -ge 13` 通过（**假 PASS**）；仍在使用手册 §5.2 已记载会静默失败的 `icacls /remove /deny` 语法 |
| C-4 | `tools/who_covers.ps1:39-41` | 给只读自动变量 `$pid` 赋值失败 → 每个采样点都报告 `process: powershell`（实测复现） |
| C-5 | `tools/*.ps1`（10 个文件） | 硬编码绝对路径与用户名（`D:\Project\kokonaDown\…`、`C:\Users\lumin\Desktop\…`），换机即失效；`UiTests` 反而有 `FindRepoRoot` 可复用 |
| C-6 | `UiTests/Program.cs:45` | 截图写入产品图目录 `screenshot\`（README 引用同一目录），失败时还会留下 `diag_*.png` |
| C-7 | `DownloadEngine.cs:196-197` | `tellWaiting/tellStopped` 上限 1000，超量任务不显示 |
| C-8 | `Core.Tests/ExtensionContractTests.cs` | 未覆盖磁力 `confirm:true` 与 `duplicate:true` 两条响应（正是 B-3 漏掉的契约） |
| C-9 | `extension/logic.js:117-126,53-57` | `urlKey` 为 32 位 djb2（碰撞即误取消下载）；`isOwnApiUrl` 为前缀匹配，漏 `localhost`/无斜杠/异端口 |
| C-10 | `MainWindow.xaml.cs:124-126,529-556` | 900 ms 定时器每次全量拉任务 + 统计（两趟 RPC），空闲时也在轮询 |
| C-11 | `tools/verify_singlefile_*.ps1`、`verify_desktop_launch.ps1` | 版本号 `"1.0.6"` 硬编码在断言里，下个版本即变永久 FAIL |

---

## 三、明确"已验证正常"的部分（避免误伤）

以下均为**实测通过**，建议不要因上面的清单而盲目改动：

| 领域 | 验证方式 | 结论 |
| --- | --- | --- |
| 文件名解析（113484f 的核心修复） | 端到端：`/23_377276` → 302 → `cinebenchr2323.2.zip`，且请求里**故意**带伪名 `filename=23_377276` | ✅ 落盘为真实名，伪名被丢弃；`Content-Disposition` 场景同样正确；显式文件名仍优先；同名冲突自动 `(1)` 且不覆盖旧文件 |
| 断点续传 / 暂停恢复 | 支持 206 的真实服务器 | ✅ 暂停→恢复→完成，字节数一致 |
| 重复任务拦截 | 同一 URL 在下载中再次提交 | ✅ 返回 `duplicate:true` 且不新建 |
| 任务操作 API | pause / resume / remove | ✅ 状态与列表均符合预期 |
| 鉴权与安全 | 无 token → 401；错误 token → 401；`Bearer` 可用；`/api/ping` 免鉴权 | ✅ 与手册一致；`GET /api/settings` **不泄露** `ApiSecret`；`FixedTimeEquals` 常量时间比较 |
| v1.0.5 两个已知小问题 | POST /api/settings 传未知字段 | ✅ 现已返回 200（不再 500）；`GET /api/settings` 已含 `minimizeToTrayOnClose` —— **两处旧疾均已修复** |
| 限速热更新 | 改设置 → 直查 aria2 RPC `getGlobalOption` | ✅ 1 MB/s→2 MB/s→1 MB/s 实时生效 |
| 透明三模式 | DPI 正确的 48 点采样 + 归属校验 | ✅ 不透明无背景相关；磨砂/黑色强相关（见 §1.3） |
| 启动动画 | 顶层窗口轮询 | ✅ 535 ms 出现，420 物理尺寸，主窗口随后接管 |
| 单文件独立运行 | 桌面 exe 实跑 + API | ✅ ping 200 / 版本 1.0.6；引擎从 %TEMP% 启动（见 B-2 的隐患说明） |
| 孤儿进程回收 | 强杀应用 → 重启 | ✅ 日志 `清理孤儿 aria2 进程 PID=…`，孤儿被终止 |
| 正常退出 | `DisposeAsync`（空载 / 下载中）两种场景 | ✅ 均正确终止 aria2（约 4 s，其中 ~3.5 s 是 aria2 自身退出耗时，实测裸 aria2 亦然） |
| 磁力/BT 全链路 | UiTests 回环做种 + 磁力确认 → 下载 → 分片矩阵完成 | ✅ 7 张截图，含 `[METADATA]` 两阶段与做种态 |
| 扩展契约 | 端点 / 头名称 / CORS / ping 语义逐条对照客户端源码 | ✅ `X-Kokona-Secret`、`/api/ping`、`/api/stats`、`/api/download` 均对得上；扩展**从不猜测文件名**（`manualSend` 明确不带） |
| 扩展逻辑单测 | `cscript tests\test_logic.js` | ✅ 20/20 |
| 仓库卫生 | `git status` | ✅ 仅 3 个历史未跟踪文件，与手册 6.6 一致 |
| tools 脚本编码规范 | 逐文件查 BOM 与非 ASCII 字节 | ✅ 含中文的 10 个脚本均带 UTF-8 BOM，其余 8 个纯 ASCII —— 规范被正确遵守 |

---

## 四、本次测试对环境的改动（已全部还原）

测试过程需要临时改动用户真实配置/数据，现均已还原，供复核：

| 项 | 操作 | 现状 |
| --- | --- | --- |
| `settings.json` | `verify_transparency8.ps1` 留下 `Transparency=BlackTransparent` | 已还原为原值 `Opaque`（原文件另存 `.verify/settings_backup.json`） |
| `settings.json` 全局限速 | 热更新验证改为 2 MB/s | 已还原为原值 `1048576` |
| `settings.json` 并发数 | 越界钳制验证写入过 999 | 已还原为 `3` |
| `engine/aria2.session` | UiTests 残留幻影任务 | 已清除（证据另存 `.verify/evidence_leftover_session.txt`） |
| `artifacts/transparency/mode-*.png` | `verify_transparency8.ps1` 覆盖了 3 张入库基线图 | 已 `git checkout --` 还原，工作区无 `M` 改动 |
| 进程 | 测试期间启动的应用/引擎 | 已全部结束，当前无 `KokonaDownloader` / `aria2c` |
| 新增文件 | 全部在 `.verify/`（仓库约定为不提交的调试区），另有本报告 | 未触碰任何源码 |

---

## 五、本次新增的验证工具（均在 `.verify/`，未入库）

| 文件 | 用途 |
| --- | --- |
| `.verify/functest/test.mjs` | 端到端 API 测试（24 项断言：鉴权、设置、文件名解析、重复、任务操作、错误码、磁力） |
| `.verify/functest/range.mjs` | 断点续传对照实验（服务器支持/不支持 206），用于给 A-4 定性 |
| `.verify/functest/hotapply.mjs` | 设置热更新 → 直查 aria2 RPC 验证 |
| `.verify/functest/multi.mjs` | 多 URL 语义验证（发现 B-1） |
| `.verify/verify_transparency_dpi_aware.ps1` | **DPI 一致的**透明度复验（48 点 + 归属校验 + 相关系数） |
| `.verify/probe_settings_window.ps1`、`probe_settings_clip.ps1` | 设置窗口打开与**元素越界**检查 |
| `.verify/diag`、`.verify/diag2` | 暂停失败根因定位、退出路径（是否残留孤儿）验证 |

> 说明：这些脚本是我在 `.verify/`（不提交目录）中新建的，**没有改动仓库内任何文件**。若后续要修 A-4、A-7，可直接复用其中的夹具与采样方法。

---

## 六、声明

- 按用户要求，**本次未做任何修复或功能改动**：仓库内源码、扩展、测试、tools 脚本均保持原样（`git status` 无新增改动）。
- 上一节列出的环境改动均已还原。
- 报告中每条结论都附了文件:行号或实测数据；对**未能复现**的推断（如 B-22 的截图错窗风险）已注明"风险未发生但判据不成立"，未夸大为已发生故障。
- 建议的修复顺序：**A-1/A-3 → A-2 → A-5/A-6 → A-4/A-7 → B-1/B-2/B-12**，其余按模块分批。
