# 海兔下载器（KokonaDownloader）项目文档

> **版本**: 1.0.8  
> **最后更新**: 2026-09-18  
> **文档目的**: 面向未来接手维护者的完整项目交接文档

---

## 目录

1. [项目概述](#1-项目概述)
2. [系统架构](#2-系统架构)
3. [技术栈](#3-技术栈)
4. [目录结构](#4-目录结构)
5. [核心模块详解](#5-核心模块详解)
6. [API 参考](#6-api-参考)
7. [构建与打包](#7-构建与打包)
8. [测试策略](#8-测试策略)
9. [关键设计决策](#9-关键设计决策)
10. [已知问题与限制](#10-已知问题与限制)
11. [维护指南](#11-维护指南)
12. [版本历史](#12-版本历史)

---

## 1. 项目概述

### 1.1 产品定位

海兔下载器是一款基于 Aria2 引擎的 Windows 桌面下载工具，核心卖点是**浏览器集成**：用户在浏览器中点击下载链接，文件自动交给桌面客户端进行多线程高速下载。

### 1.2 核心功能

| 功能 | 说明 |
|------|------|
| 浏览器下载拦截 | 扩展捕获 `downloads.onCreated` 事件，转发给桌面客户端 |
| 多线程下载 | Aria2 引擎，支持断点续传 |
| BT/磁力链接 | 支持磁力链接、.torrent 文件，DHT + PEX + LPD |
| 做种管理 | 可配置分享率/时长，`bt-detach-seed-only` 不占并发配额 |
| 主题系统 | 多种配色 + 深色模式 + 磨砂透明（唯一主题，固定最不透明 100% 档，无其他可选模式） |
| 进度浮窗 | 独立小窗口显示下载进度，不影响主窗口 |
| 系统托盘 | 常驻托盘，显示下载速度，右键快捷菜单 |
| 通知 | 下载完成弹系统通知，一键打开所在文件夹 |
| 单文件发布 | 303 MB 单 exe，运行时文件解压到 `%LOCALAPPDATA%`，exe 目录保持干净 |

### 1.3 技术亮点

- **单文件独立运行**：`StandaloneBootstrap` 在 XAML 初始化前解压嵌入资源，exe 目录零污染
- **快照复用**：轮询快照供 UI 刷新与重复预检共用，减少 RPC 调用
- **墓碑机制**：删除任务后登记墓碑，防止 aria2 会话文件重启后"复活"已删任务
- **伪文件名过滤**：识别 URL 末段重复的伪文件名，交由 aria2 按响应头解析真实文件名
- **常量时间密钥比较**：API 鉴权使用 `FixedTimeEquals` 防时序侧信道

---

## 2. 系统架构

### 2.1 整体架构

```
┌─────────────────┐     HTTP/WebSocket      ┌─────────────────────┐
│  浏览器扩展     │  ═══════════════════════► │  桌面客户端 (WinUI3) │
│  (Manifest V3)  │     (本地 API + 密钥)    │  .NET 8 + WASDK     │
└─────────────────┘                         └──────────┬──────────┘
                                                        │
                                             JSON-RPC over localhost
                                                        │
                                               ┌────────┴────────┐
                                               │   Aria2 引擎    │
                                               │  (aria2c.exe)   │
                                               └─────────────────┘
```

### 2.2 通信协议

| 通道 | 协议 | 端口 | 鉴权 |
|------|------|------|------|
| 扩展 → 客户端 | HTTP REST | 127.0.0.1:16800 | `X-Kokona-Secret` 或 `Authorization: Bearer` |
| 客户端 → Aria2 | JSON-RPC | 127.0.0.1:16801 | `--rpc-secret` |

### 2.3 数据流

```
浏览器下载事件
    │
    ▼
扩展 background.js (MV3 Service Worker)
    │  1. 防火墙基准过滤（历史回放）
    │  2. 并发去重（forwardingNow Set）
    │  3. HTTP POST /api/download
    ▼
ApiService (HttpListener)
    │  1. CORS 处理
    │  2. 鉴权（FixedTimeEquals）
    │  3. 重复检测（FindActiveDuplicatesAsync）
    │  4. 磁力链接 → MagnetConfirmRequested 事件
    ▼
DownloadEngine
    │  1. 伪文件名过滤（DropUrlDerivedFileName）
    │  2. 重名冲突处理（ApplyUniqueFileName）
    │  3. 墓碑撤销（Unmark）
    │  4. aria2.addUri / addTorrent
    ▼
Aria2Process (aria2c.exe)
    │  1. --enable-rpc 后台模式
    │  2. 会话文件持久化（aria2.session）
    │  3. 墓碑过滤（PurgeSessionFile）
    ▼
轮询循环 (PollLoopAsync)
    │  1. multicall 一次拉取 active/waiting/stopped/stats
    │  2. 磁力两阶段处理（followedBy）
    │  3. 重复种子清理（already registered）
    │  4. 状态变化事件推送
    │  5. 快照发布（_latestSnapshot）
    ▼
UI 层 (WinUI 3)
    │  1. 任务列表刷新
    │  2. 进度浮窗
    │  3. 系统通知
    │  4. 托盘图标更新
```

---

## 3. 技术栈

| 组件 | 技术 | 版本 |
|------|------|------|
| 桌面客户端 | C# / .NET 8 | 8.0 |
| UI 框架 | WinUI 3 / Windows App SDK | 1.8.260804001 |
| 浏览器扩展 | JavaScript / Manifest V3 | - |
| 下载引擎 | Aria2 | GPL v2 |
| 客户端 ↔ 扩展 | HTTP REST API | - |
| 客户端 ↔ Aria2 | JSON-RPC over WebSocket | - |
| 托盘图标 | WinForms NotifyIcon | - |
| 测试框架 | xUnit | - |
| 构建工具 | MSBuild / PowerShell | - |

---

## 4. 目录结构

```
kokonaDown/
├── src/
│   ├── KokonaDownloader.App/         # WinUI 3 桌面客户端
│   │   ├── App.xaml / App.xaml.cs    # 应用入口
│   │   ├── MainWindow.xaml / .cs     # 主窗口
│   │   ├── ProgressWindow.xaml / .cs # 进度浮窗
│   │   ├── SettingsWindow.xaml / .cs # 设置窗口
│   │   ├── MagnetConfirmWindow.xaml / .cs # 磁力确认窗口
│   │   ├── NewDownloadDialog.xaml / .cs   # 新建下载对话框
│   │   ├── BtPieceGrid.xaml / .cs    # BT 分片网格
│   │   ├── StandaloneBootstrap.cs    # 单文件引导
│   │   ├── WindowEffects.cs          # 窗口效果（透明度/磨砂）
│   │   ├── Shell.cs                  # 应用外壳
│   │   ├── StartupHelper.cs          # 启动辅助
│   │   ├── MagnetProtocol.cs         # 磁力协议处理
│   │   ├── TrayProgress.cs           # 托盘进度
│   │   ├── app.manifest              # 应用清单
│   │   └── KokonaDownloader.App.csproj
│   ├── KokonaDownloader.Core/        # 核心库
│   │   ├── AppHost.cs                # 应用编排层
│   │   ├── Api/
│   │   │   └── ApiService.cs         # HTTP API 服务
│   │   ├── Engine/
│   │   │   ├── DownloadEngine.cs     # 下载引擎门面
│   │   │   ├── Aria2Process.cs       # aria2c 进程管理
│   │   │   ├── Aria2RpcClient.cs     # JSON-RPC 客户端
│   │   │   ├── BtHashUtil.cs         # BT 哈希工具
│   │   │   ├── TaskStore.cs          # 任务元数据存储
│   │   │   ├── TombstoneStore.cs     # 墓碑存储
│   │   │   └── ...                   # 其他引擎组件
│   │   ├── Settings/
│   │   │   ├── SettingsStore.cs      # 设置存储
│   │   │   └── ...                   # 其他设置组件
│   │   ├── Notifications/
│   │   │   └── ...                   # 通知组件
│   │   └── KokonaDownloader.Core.csproj
│   ├── KokonaDownloader.Core.Tests/  # 单元测试
│   │   ├── ApiContractGapsTests.cs
│   │   ├── ApiServiceTests.cs
│   │   ├── BtHashUtilTests.cs
│   │   ├── BtIntegrationTests.cs
│   │   ├── BtTestInfra.cs
│   │   ├── EngineIntegrationTests.cs
│   │   ├── ExtensionContractTests.cs
│   │   ├── FileNameResolutionTests.cs
│   │   ├── ResumeRangeTests.cs
│   │   ├── Stage2UnitTests.cs
│   │   ├── Stage4UnitTests.cs
│   │   ├── StoreRobustnessTests.cs
│   │   ├── TestCleanup.cs
│   │   ├── TestEnv.cs
│   │   ├── ThemeCatalogTests.cs
│   │   ├── UnitTests.cs
│   │   ├── xunit.runner.json
│   │   └── KokonaDownloader.Core.Tests.csproj
│   └── KokonaDownloader.UiTests/     # UI 冒烟测试
│       ├── Program.cs
│       └── KokonaDownloader.UiTests.csproj
├── extension/                        # 浏览器扩展源码
│   ├── background.js                 # MV3 Service Worker
│   ├── content.js                    # 内容脚本（磁力链接拦截）
│   ├── logic.js                      # 共享逻辑
│   ├── manifest.json                 # MV3 清单
│   ├── options.html / options.js     # 设置页面
│   ├── popup.html / popup.js         # 弹窗
│   └── icons/                        # 扩展图标
├── tests/
│   └── test_logic.js                 # 扩展逻辑单元测试（cscript）
├── tools/                            # 验证脚本
│   ├── verify_api_settings.ps1
│   ├── verify_desktop_launch.ps1
│   ├── verify_filename_resolution.ps1
│   ├── verify_frosted_strength.ps1
│   ├── verify_opaque_alpha.ps1
│   ├── verify_singlefile_fallback.ps1
│   ├── verify_singlefile_main.ps1
│   ├── verify_transparency8.ps1
│   └── ...                           # 其他诊断脚本
├── vendor/aria2/                     # Aria2 引擎二进制
│   └── aria2c.exe
├── screenshot/                       # 产品截图
├── icons/                            # 应用图标
├── release/                          # 打包产物
│   ├── KokonaDownloader_106.exe      # 单文件 exe (303 MB)
│   └── KokonaExtension_106.zip       # 浏览器扩展 (37 KB)
├── build.ps1                         # 一键构建脚本
├── LICENSE                           # MIT 协议
├── NuGet.config                      # NuGet 配置
├── README.md                         # 项目说明
└── 海兔下载器教程.pdf                 # 用户教程
```

---

## 5. 核心模块详解

### 5.1 AppHost（应用编排层）

**文件**: `src/KokonaDownloader.Core/AppHost.cs`

**职责**: 把引擎、设置、API 服务组装成一个整体，UI 层与测试都通过它访问能力。

**关键设计**:
- **设置热更新**: `OnSettingsChanged` 只下发真正变化的字段（全局限速、tracker 列表），避免无关操作触发 RPC
- **Tracker 缓存**: 内存缓存 + 24h 过期，避免每次设置变更都读盘
- **快速关闭**: `FastShutdown()` 强杀引擎进程树 + 同步落盘关键数据，不做网络等待

### 5.2 DownloadEngine（下载引擎门面）

**文件**: `src/KokonaDownloader.Core/Engine/DownloadEngine.cs`

**职责**: 封装 aria2 进程 + RPC + 轮询，向上提供简洁的任务管理接口。

**关键设计**:

#### 轮询优化
- **multicall**: 一次 RPC 拉取 active/waiting/stopped/stats，降低开销
- **快照复用**: `_latestSnapshot` 供 UI 刷新与重复预检共用，减少 RPC 调用
- **空闲退避**: 无任务时轮询间隔从 800ms 拉长到 2500ms

#### 文件名解析
- **伪文件名过滤**: `DropUrlDerivedFileName` 识别 URL 末段重复的伪文件名
- **重名冲突处理**: `ApplyUniqueFileName` 自动追加编号（name.ext → name (1).ext）
- **磁力两阶段**: 元数据任务完成后通过 `followedBy` 指向真实下载任务

#### 重复检测
- **URL 重复**: `FindActiveDuplicatesAsync` 检测未结束任务的 URL 重复
- **BT 重复**: `EnsureBtNotDuplicate` 检测同 infohash 的任务（含做种中）
- **快照失效**: `InvalidateSnapshot()` 在任务集合变化时立即失效快照

#### 墓碑机制
- **登记**: 删除任务时登记 URL+目录哈希到 `tombstones.json`
- **过滤**: 启动 aria2 前用墓碑过滤会话文件，阻断重启后复活；客户端自己记着"早已下载完成且文件仍在"的条目也一并过滤（`DownloadEngine.FinishedSessionEntries`），否则 `--input-file` 会把它当新任务重新加入（新 gid、`排队中 0 B`，看着像历史、其实可能重下一遍）
- **撤销**: 显式重新添加时撤销墓碑，保证之后可以正常恢复/续传

#### 运行期默认值（第八轮 P1-1 → 第九轮修复）
- **目录/线程数是"当前值"而非"启动快照"**: `UpdateRuntimeDefaults(...)` 由 `AppHost.OnSettingsChanged` 在设置改动时立刻调用；`BuildOptions` **每次** `addUri` 都显式下发 `dir`，`MaxConcurrentDownloads` 走 `changeGlobalOption` 热应用
- **同名保护看得见真实目录**: `ApplyUniqueFileName` 用 `_defaultDir` 扫描；名字可靠时客户端自己改成 `x (1).ext`，名字只来自响应头时交给 aria2（逐条 `allow-overwrite=false` ⇒ 自动 `x.1.ext`，绝不覆盖）

#### 本地历史（第八轮 P2-4 → 第九轮修复）
- aria2 **不会**把已完成任务写进 `--save-session`（实测 0 字节），所以完成/失败历史由客户端自己留档：`TaskStore` 的 meta 带终态快照（长度、路径、错误码/原因），`DownloadEngine._history` 在启动时从这些 meta 复原
- 列表 = aria2 实时任务 ∪ 本地历史（同 gid 以实时为准）；上限 `MaxHistoryRows = 2000`，超出按完成时间淘汰最旧并连带删 meta
- 历史行仍带 URL 与 `filePath`，因此"重新下载""删除并删除文件"在重启后依旧可用

#### 失败残留隔离（第八轮 P2-3 → 第九轮修复）
- 判据 `IsUnresumableFailure`：`errorCode=8`（Invalid range header）或文案命中 `Invalid range header` / `Unable to continue download` / `cannot be continued`；**普通网络中断不算**（断点仍有效，不能隔离）
- 动作：路径先 `Path.GetFullPath` 归一 → `x` 改名 `x.partial`（连同 `x.aria2` 控制文件一起挪走）→ 写终态快照 → `removeDownloadResult`
- 必须先清 aria2 的失败结果：否则用户点"继续"会让 aria2 按内存分位图往已改名的文件里"继续写"，产出满是空洞的假完整文件

### 5.3 Aria2Process（aria2c 进程管理）

**文件**: `src/KokonaDownloader.Core/Engine/Aria2Process.cs`

**职责**: aria2c 子进程生命周期管理：启动、健康检查、优雅退出。

**关键设计**:
- **启动参数**: `--enable-rpc` + `--rpc-listen-all=false`（仅本机）
- **会话持久化**: `--input-file` + `--save-session` 保证重启后任务恢复；启动前先过滤会话文件（墓碑 + 已完成，见 §5.2）
- **重试**: `--retry-wait=2 --max-tries=5`（第八轮实测：默认参数下 aria2 对 503 **一次都不重试**，加这两条后同一任务内第 3 次请求即成功；404 两种参数下都只请求 1 次，aria2 视其为永久错误）
- **BT 支持**: DHT + PEX + LPD，`follow-torrent=mem` 避免写 .torrent
- **做种优化**: `bt-detach-seed-only` 让做种任务不占并发配额
- **UA 伪装**: `Transmission/2.92` 避免部分 tracker 封锁 aria2
- **日志优化**: `--show-console-readout=false` 关闭控制台读数刷新（占 96% 日志行）

### 5.4 ApiService（HTTP API 服务）

**文件**: `src/KokonaDownloader.Core/Api/ApiService.cs`

**职责**: 本地 HTTP API 服务，仅绑定 127.0.0.1，供浏览器扩展与主界面外部调用。

**关键设计**:
- **CORS**: 允许任意来源（扩展 origin 为 `chrome-extension://`），处理 OPTIONS 预检
- **鉴权**: `FixedTimeEquals` 常量时间比较，防时序侧信道
- **磁力确认**: 单条磁力链接触发 `MagnetConfirmRequested` 事件，UI 层弹独立确认窗口
- **重复提醒**: 命中重复任务时触发 `DuplicateTaskNoticeRequested` 事件

### 5.5 StandaloneBootstrap（单文件引导）

**文件**: `src/KokonaDownloader.App/StandaloneBootstrap.cs`

**职责**: 单文件独立运行引导，在 XAML 初始化前解压嵌入资源。

**关键设计**:
- **ModuleInitializer**: `[ModuleInitializer]` 先于 XAML 生成的 Main 运行
- **运行时目录**: `%LOCALAPPDATA%\KokonaDownloader\Standalone`
- **增量引导（v1.0.8 第三轮重写）**: 引导清单 `.kokona_bootstrap.json` 记录嵌入 payload 的 SHA256、文件列表与 exe 哈希/大小/mtime——三者一致且文件齐全则**跳过解压**；exe 仅在内容哈希变化时才复制（冷启动不再无条件覆盖 ~305 MB）
- **并发互斥**: 命名互斥量 `Local\KokonaDownloader_Standalone_Bootstrap` 串行化并发冷启动，后到者等锁后复查完整性直接复用，消除"解压撞文件锁→引导静默中止"
- **exe 复制**: 把 exe 复制到运行时目录，从那里带原参数重新拉起
- **路径穿越防护**: `ExtractAll` 检查解压目标必须位于 destDir 之内
- **已知坑**: 本运行时（.NET 8）下 `SHA256.Hash` 未终结前读取会抛 `CryptographicUnexpectedOperationException`，`HashPayload` 必须先 `TransformFinalBlock` 再取哈希（曾导致整个引导被静默吞掉、应用退化为原地运行）

### 5.6 浏览器扩展（MV3 Service Worker）

**文件**: `extension/background.js`

**职责**: 捕获浏览器下载事件，转发给桌面客户端。

**关键设计**:
- **防火墙基准**: `chrome.storage.session` 存储会话级基准，过滤历史回放
- **并发去重**: `forwardingNow` Set 防止并发重复转发同一链接
- **密钥校验**: 定时 ping + 密钥校验，维护连接状态
- **友好通知**: 客户端离线/密钥错误等场景给出友好通知，绝不静默失败

---

## 6. API 参考

### 6.1 端点列表

| 方法 | 路径 | 鉴权 | 说明 |
|------|------|------|------|
| GET | `/api/ping` | 否 | 探测客户端是否在线 |
| GET | `/api/tasks` | 是 | 获取全部任务 |
| GET | `/api/stats` | 是 | 获取全局统计 |
| GET | `/api/settings` | 是 | 获取设置 |
| POST | `/api/download` | 是 | 新建下载任务 |
| POST | `/api/settings` | 是 | 更新设置 |
| POST | `/api/tasks/{gid}/pause` | 是 | 暂停任务 |
| POST | `/api/tasks/{gid}/resume` | 是 | 恢复任务 |
| POST | `/api/tasks/{gid}/remove` | 是 | 删除任务 |
| POST | `/api/tasks/{gid}/redownload` | 是 | 重新下载 |

### 6.2 请求/响应示例

#### GET /api/ping

```json
{
  "ok": true,
  "app": "KokonaDownloader",
  "version": "1.0.8"
}
```

#### POST /api/download

**请求**:
```json
{
  "urls": ["https://example.com/file.zip"],
  "filename": "file.zip",
  "dir": "C:\\Downloads",
  "connections": 16,
  "speedLimit": 0,
  "referer": "https://example.com",
  "headers": ["Authorization: Bearer xxx"]
}
```

**响应**:
```json
{
  "ok": true,
  "gid": "1234567890abcdef"
}
```

**重复任务响应**:
```json
{
  "ok": true,
  "duplicate": true
}
```

**磁力确认响应**:
```json
{
  "ok": true,
  "confirm": true
}
```

#### GET /api/tasks

```json
[
  {
    "gid": "1234567890abcdef",
    "taskNumber": 20260711123456789,
    "name": "file.zip",
    "state": "active",
    "totalLength": 1073741824,
    "completedLength": 536870912,
    "progress": 50.0,
    "downloadSpeed": 1048576,
    "etaSeconds": 512,
    "filePath": "C:\\Downloads\\file.zip",
    "dir": "C:\\Downloads",
    "urls": ["https://example.com/file.zip"],
    "errorMessage": null
  }
]
```

### 6.3 鉴权

所有端点（除 `/api/ping`）必须携带正确密钥：

```
X-Kokona-Secret: <secret>
```

或

```
Authorization: Bearer <secret>
```

密钥在 `settings.json` 的 `apiSecret` 字段中配置。

---

## 7. 构建与打包

### 7.1 开发构建

```powershell
# 一键构建（客户端 + 扩展）
powershell -ExecutionPolicy Bypass -File .\build.ps1

# 产物：
#   dist\KokonaDownloader\KokonaDownloader.exe   桌面客户端
#   dist\KokonaExtension\                        浏览器扩展目录
```

### 7.2 单文件发布

```powershell
# 发布单文件 exe（本仓库设了 EnableMsixTooling=false 并自行嵌入 resources.pri，
# 因此必须跳过 Windows App SDK 的单文件配置校验，否则构建期直接报错）
dotnet publish src\KokonaDownloader.App\KokonaDownloader.App.csproj `
  -c Release -r win-x64 `
  -p:PublishSingleFile=true `
  -p:IncludeNativeLibrariesForSelfExtract=true `
  -p:WindowsAppSDKSingleFileVerifyConfiguration=false `
  -o dist\KokonaSingleFile
```

**产物**: `dist\KokonaSingleFile\KokonaDownloader.exe` (303 MB)

**关键配置** (csproj):
- `PublishSingleFile=true`: 单文件发布
- `IncludeNativeLibrariesForSelfExtract=true`: 原生库自解压
- `EmbedStandalonePayload` 目标: 嵌入 XBF/PRI/aria2c.exe/图标

### 7.3 浏览器扩展打包

```powershell
# 打包扩展为 zip
Compress-Archive -Path extension\* -DestinationPath release\KokonaExtension_106.zip
```

**产物**: `release\KokonaExtension_106.zip` (37 KB)

### 7.4 发布流程

1. **构建**: `build.ps1` 生成开发构建
2. **发布**: `dotnet publish` 生成单文件 exe
3. **打包**: 压缩扩展为 zip
4. **测试**: 运行全量测试套件
5. **上传**: 上传到 GitHub Releases

---

## 8. 测试策略

### 8.1 测试套件

| 套件 | 文件 | 说明 |
|------|------|------|
| Core 单元测试 | `src/KokonaDownloader.Core.Tests/` | 132/136 通过 + 18/18 隔离 |
| 扩展逻辑测试 | `tests/test_logic.js` | 81/81 通过（cscript，九轮新增 3 条接管规则用例） |
| UI 冒烟测试 | `src/KokonaDownloader.UiTests/` | 8 截图 |
| E2E 测试 | `.verify/functest/test.mjs` | 24/24 通过 |
| URL 解析测试 | `.verify/functest/test_url_parsing.mjs` | 19/19 通过 |
| **全链路真实场景回归（第八轮新增）** | `.verify/e2e/`（`site/server.mjs` 测试站 + `stage_*.py` + `chain_lib.py`/`chain_ops.py`/`winx.ps1`） | 真实 Edge + 未打包扩展 + 真实鼠标点击 → 扩展拦截 → `/api/download` → aria2 → 落盘 → UI；用站点请求日志/aria2 日志/app.log 交叉取证。阶段：setup / basic / names / dir / failures / resume / compat / magnet / hidden，逐条结果落在 `.verify/e2e/out/stage_*_results.json` |

### 8.2 测试覆盖

#### Core 单元测试
- **API 契约**: `ApiContractGapsTests.cs`, `ApiServiceTests.cs`
- **BT 哈希**: `BtHashUtilTests.cs`, `BtIntegrationTests.cs`
- **引擎集成**: `EngineIntegrationTests.cs`
- **文件名解析**: `FileNameResolutionTests.cs`
- **断点续传**: `ResumeRangeTests.cs`
- **存储健壮性**: `StoreRobustnessTests.cs`
- **主题目录**: `ThemeCatalogTests.cs`
- **第八轮缺陷回归（九轮新增）**: `RoundEightFixTests.cs` —— 选项构造（逐条 `allow-overwrite`、`continue=false`、`dir` 必发）、不可续传失败判定、终态快照持久化与历史行、真实 aria2 下的目录热更新/同名不覆盖/重启后历史/无 Range 失败隔离+重新下载、会话文件"已完成条目不复活"
- **执行方式**: 见 §11.6"串行执行约定"——`xunit.runner.json` 的 `parallelizeTestCollections=false`（九轮起在 runner 配置层面直接关掉并行：多个用例各起真实 aria2 并抢临时端口，并行会随机撞出 `aria2 RPC 未就绪` 之类的假失败，第八轮记为"并行 flake"）

#### 扩展逻辑测试
- **设置规范化**: `normalizeSettings`
- **URL 解析**: `parseUrl`
- **防火墙基准**: `firewallStamp`
- **并发去重**: `forwardingNow`

#### E2E 测试
- **下载流程**: 新建任务 → 下载 → 完成
- **任务管理**: 暂停/恢复/删除/重新下载
- **设置更新**: 全局限速/主题
- **磁力链接**: 确认窗口 → 下载 → 做种

### 8.3 性能测试

| 指标 | M1 (空闲) | M2 (200 任务) |
|------|-----------|---------------|
| CPU | 3.44% | 2.50% |
| 工作集 | 192.5 MB | 241.5 MB |
| 句柄 | 2193 | 3040 |
| 线程 | 60 | 51 |

**日志速率**: 114 B/s ≈ 9.39 MB/天

**窗口配额**:
- WinPerTask: 0
- HandlePerTask: 9
- WSPerTaskMB: 1.32
- ProgressWinDelta: 0

---

## 9. 关键设计决策

### 9.1 单文件独立运行

**问题**: .NET 单文件发布管线只打包主程序集与原生库，不会搬运 XBF/PRI/aria2c.exe/图标等散文件。

**方案**:
1. `EmbedStandalonePayload` 目标把散文件嵌入 exe 作为资源
2. `StandaloneBootstrap` 在 XAML 初始化前解压到 `%LOCALAPPDATA%\KokonaDownloader\Standalone`
3. 把 exe 复制到运行时目录，从那里带原参数重新拉起

**优势**: exe 目录保持干净（只有 exe 本身），运行时文件在 `%LOCALAPPDATA%`

### 9.2 快照复用

**问题**: UI 定时刷新与"下载前重复预检"各自打 RPC，浪费资源。

**方案**:
- 轮询循环发布 `_latestSnapshot`
- UI 刷新与重复预检直接复用快照（1.2s 窗口）
- 任务集合变化时 `InvalidateSnapshot()` 立即失效

**优势**: 减少 RPC 调用，降低开销

### 9.3 墓碑机制

**问题**: aria2 会话文件每 10 秒落盘，异常退出时已删任务仍留在其中，下次启动 `--input-file` 会把任务复活。

**方案**:
1. 删除任务时登记 URL+目录哈希到 `tombstones.json`
2. 启动 aria2 前用墓碑过滤会话文件
3. 显式重新添加时撤销墓碑

**优势**: 阻断重启后复活，保证删除语义

### 9.4 伪文件名过滤

**问题**: 部分站点用不带扩展名的编号链接指向真实文件，真实文件名只在响应头（Content-Disposition）或 302 重定向目标里。

**方案**:
- `DropUrlDerivedFileName` 识别 URL 末段重复的伪文件名
- 丢弃伪文件名，交由 aria2 按响应头/重定向解析真实文件名

**优势**: 避免固定错误的 `out` 参数，保证真实文件名解析

### 9.5 常量时间密钥比较

**问题**: 普通字符串比较存在时序侧信道，攻击者可通过测量响应时间推断密钥。

**方案**: `FixedTimeEquals` 使用 XOR 累积差异，恒定时间比较。

**优势**: 防时序侧信道

---

## 10. 已知问题与限制

### 10.1 已修复

| 问题 | 修复版本 | 说明 |
|------|----------|------|
| 日志 96% 空白行 | 1.0.6 | `--show-console-readout=false` |
| 单文件 exe 启动崩溃 | 1.0.6 | `ProjectPriFileName=resources.pri` |
| 磁力链接静默建任务 | 1.0.6 | `MagnetConfirmRequested` 事件 |
| 重复种子噪音任务 | 1.0.6 | `EnsureBtNotDuplicate` + 清理 |
| 已删任务重启复活 | 1.0.6 | 墓碑机制 |
| 伪文件名固定 out | 1.0.6 | `DropUrlDerivedFileName` |
| 设置变更触发无关 RPC | 1.0.6 | `OnSettingsChanged` 只下发变化字段 |
| 高并发建窗崩溃（XamlParseException / 原生 0xc000027b） | 1.0.8 (09-19) | 建窗最小间隔 600ms + 激活延迟到下一调度周期 |
| 并发进度小窗完全堆叠 | 1.0.8 (09-19) | Slot 级联偏移（中心 + n×28/+n×20 物理像素） |
| `app.log` 残留 `[aria2]` 前缀空白行（97.5%） | 1.0.8 (09-19) | 捕获 aria2c 输出时跳过空白行 |
| 本地 API 可被任意网页驱动（CORS 通配 `*` + 自定义头鉴权） | 1.0.8 (09-19 二轮) | `IsTrustedOrigin`：只放行扩展/回环来源（用 `Uri.IsLoopback` 防 `127.0.0.1.evil.com` 绕过），受信任来源回显具体 Origin；并轮换已随公开仓库历史外泄的 `ApiSecret` |
| 设置写入触发全量主题重算（每次约 240ms CPU） | 1.0.8 (09-19 二轮) | `ThemeService.Apply` 按值比较 `ResolvedTheme`，主题未变整轮跳过；设置补丁/设置窗保存改为逐字段比对后返回"是否真的变了" |
| `ThemeService._backdrops` 静态字典泄漏关闭窗口对象图 | 1.0.8 (09-19 二轮) | `Unregister` 末尾调用 `ReleaseBackdrop`（影响未自行挂载背景的设置窗/磁力确认窗；进度小窗走复用分支不受影响） |
| 单文件引导每次冷启动无条件全量解压 + 覆盖 ~305 MB exe；并发冷启动撞文件锁后**静默中止**（应用退化为原地运行，exe 目录不再保持干净） | 1.0.8 (09-20 三轮) | 引导清单 `.kokona_bootstrap.json`（payload SHA256 + 文件列表 + exe 哈希/大小/mtime）：一致且齐全则跳过解压；exe 仅内容变化才复制；命名互斥量串行化并发冷启动。另修复 `HashPayload` 未终结即读 `SHA256.Hash` 抛异常导致整个引导被吞掉的隐患（本 .NET 8 运行时行为） |
| `TombstoneStore.Unmark` 批量撤销只移除首个命中墓碑（`Any(Remove)` 短路） | 1.0.8 (09-20 三轮) | 改为逐项 `Remove` 循环；新增回归测试（旧实现下该测试失败：2 条命中剩 1 条） |
| 扩展 `forwardDownload` 丢弃服务端 `confirm` 标志，磁力确认提示分支不可达 | 1.0.8 (09-20 三轮) | 返回值透传 `{ ok, gid, duplicate, confirm }`，`manualSend` 的"请在确认窗确认"分支恢复可达 |
| 轮询每轮为诊断日志构建全量任务签名字符串（471 任务时约 565KB/轮） | 1.0.8 (09-20 三轮) | 改为无分配 FNV-32 整型指纹（gid+state 折叠 + stopped 计数），仅在检测到变化时才构建完整签名串 |
| 本地 API 无并发上限，恶意/失控客户端可无限排队占满连接 | 1.0.8 (09-20 三轮) | `AcceptLoop` 加 64 槽请求门（`SemaphoreSlim.Wait(0)`），超限立即 503 拒绝而非排队挂起 |
| 主窗/设置窗多个事件处理器无异常防护，单点异常可中断 UI 操作链 | 1.0.8 (09-20 三轮) | `OnNewClick`/`OnPauseAllClick`/`OnResumeAllClick`/`OnItemDeleteClick`/`OnBatchDeleteClick`（失败保留选中以便重试）/`SettingsWindow.OnBrowseDir` 加 try/catch + 日志 |
| 磁力确认窗焦点重试是无 try/catch 的 `async void`，且 `await` 之后未复查 `_closed`（四轮 N-1） | 1.0.8 (09-20 五轮) | 整段重试循环包 try/catch + `App.Log`；`await Task.Delay(100)` 之后立即 `if (_closed) return;`，消除"窗口已关闭仍 Focus"的 UI 线程异常路径 |
| `ApiService` 接受循环任何非取消异常即永久 `break` 且无日志；`Task.Run` 委托未执行时门槽位泄漏；`Dispose` 不释放 `_cts`/`_requestGate`（四轮 N-3） | 1.0.8 (09-20 五轮) | `AcceptLoop` 区分 `HttpListenerException`（仅仍在监听且未取消时记日志）/`ObjectDisposedException`（静默）/其他（记完整异常）；`Task.Run` 不再接 `ct`（否则取消会让委托跳过、`finally` 不归还槽位）并自带 catch→记日志+归还槽位+503；`Dispose` 释放 `_cts`/`_requestGate`，`ReleaseGate()` 吞掉关停竞态的 `ObjectDisposedException` |
| 单文件引导清单只比对文件大小（四轮 N-4） | 1.0.8 (09-20 五轮) | 清单 `files[].hash` 记录逐文件 SHA256 + `payloadHash`；`VerifyFiles` 按"存在+尺寸+哈希"三重校验，同尺寸损坏也会被检出并重新解压（实测：把 `resources.pri` 第 1000 字节按位取反、尺寸不变，重启后自愈）；旧清单无哈希时一次性重解压并补齐哈希，之后走"跳过"路径（实测 ping 就绪 1.08 s、mtime 不变） |
| `StandaloneBootstrap` 注释与代码语义相反（四轮 N-5） | 1.0.8 (09-20 五轮) | 注释改为如实描述"引导失败即静默退化为原地运行"，`catch (Exception ex)` 补 `App.Log` 记失败原因 |
| `/api/ping` 版本号硬编码 `Version = "1.0.8"`（四轮 N-6） | 1.0.8 (09-20 五轮) | 改为 `ReadAssemblyVersion()` 读 `AssemblyInformationalVersion`（截断 `+` 之后的源修订号，回退 `AssemblyVersion`）；版本号唯一来源改为 `src\Directory.Build.props`（含 `IncludeSourceRevisionInInformationalVersion=false`），App.csproj 内版本块删除并留指引注释 |
| 仓库根遗留物 `files/`（78 MB）+`infohash.txt`；`artifacts/transparency/`（19 张 PNG、2.2 MB、文档引用 0 次）已被跟踪（四轮 N-7） | 1.0.8 (09-20 五轮) | `.gitignore` 补 `files/`、`infohash.txt`；`artifacts/transparency/` 从索引与工作树删除（git 历史仍可取回，本地已备份到 `.verify\backup\artifacts_transparency_predelete\`） |
| 托盘菜单动作异常完全静默（四轮 N-8） | 1.0.8 (09-20 五轮) | `TrayService.BalloonTipClicked` 与 `TrayMenuHost.NativeItem` 点击回调的 catch 记 `App.Log`；菜单项里的 fire-and-forget 异步动作统一走 `RunLogged(what, start)`，异常不再蒸发 |
| DWM/`SetWindowLong` 返回值被丢弃（CA1806） | 1.0.8 (09-20 五轮) | `WindowEffects.NoteDwmAttrResult` 与 `TrayMenuHost.MakeHostInvisible` 读回校验：失败时各记一次日志（`[effects] ... 设置失败` / `[tray] 托盘宿主隐藏样式未生效`），不刷屏（`Interlocked.Exchange` 单次） |
| `StringContent` 未释放（CA2000）、`_pollCts` 等 IDisposable 字段未释放（CA2213/CA1001） | 1.0.8 (09-20 五轮) | `Aria2RpcClient` 两处改 `using var content`；`DownloadEngine.DisposeAsync` 释放 `_pollCts`；`MainWindow` 实现 `IDisposable`（释放 `_showEvent`/`_dialogGate`，`ShowDialogAsync` 在已释放时直接返回 `None`） |
| P/Invoke 未限定 DLL 搜索路径（CA5392） | 1.0.8 (09-20 五轮) | 新增 `NativeImports.cs`：程序集级 `[assembly: DefaultDllImportSearchPaths(DllImportSearchPath.System32)]`，覆盖 user32/kernel32/shcore/dwmapi/comctl32/shell32（单文件版运行时目录在用户可写路径，属防御纵深） |
| 第三轮修复缺回归护栏（四轮 N-2） | 1.0.8 (09-20 五轮) | 新增 Core 测试：`ApiConcurrencyGateTests`（注入 2 槽闸门，两个"请求体读不完"的裸 TCP 连接占满 → 第三个请求 503 → 断开后恢复 200）、`PollFingerprintTests`（6 项：稳定 + 状态/任务集/NumStopped 变化翻转）、`FileNameResolutionTests.批量添加时同样剔除URL末段伪文件名`；`background.js` 的响应映射抽成 `logic.js:mapDownloadResponse`，cscript 套件从 56 项扩到 77 项（含 503→`busy` 分类） |
| 分析器基线无法固化（四轮 §10-E 第 10 项） | 1.0.8 (09-20 五轮) | 新增 `src\.editorconfig`：按项目路径作用域把 CA2000/CA2213/CA1806/CA5392 设为 `error`（`AnalysisMode=Default` 下默认关闭的规则可按路径强制打开）。实测：一旦某规则的严重度已在 `.editorconfig` 中设定，MSBuild 的 `WarningsAsErrors`/`-warnaserror` **无法再升级它**，故门槛只写在 `.editorconfig`；用一次性探针文件实证四条规则都会以 error 触发（非空转），探针已删除 |
| RPC 响应每轮被物化三次：`ReadAsStringAsync`（>85 KB 直接进 **LOH**）→ `JsonNode` 全量 DOM → 逐元素再序列化；1000 任务规模 = **4 MB 垃圾/轮、34 ms**（六轮开销审计 O-1） | 界面轮询的常驻分配与延迟 | 1.0.8 (09-21 七轮) | 新增 `PostForDocumentAsync`（`ReadAsStreamAsync` + `JsonDocument.ParseAsync`，**整份响应只解析一次**）与文档态 `Aria2MultiCall`（`MultiCallBufferedAsync`，轮询专用，在 `using` 作用域内把数据取完），删除 `ParseResponse(string)`；顺带把 `HttpResponseMessage` 改为显式释放。小回执仍走保留的 `MultiCallAsync`（JsonNode 版）。实测托管分配 **−64 %**（3,995 KB → 1,445 KB / 1000 任务）。护栏：`RpcParsePathTests`（7 项，含"文档态与 JsonNode 版结果一致"）。详见《测试报告_v1.0.8.md》§13 |
| `GET /api/tasks` / `GET /api/stats` 每次都自己打一轮全量 multicall（六轮开销审计 O-10） | 扩展每轮询一次就多一次 458 KB 级 RPC | 1.0.8 (09-21 七轮) | 复用轮询快照：统计与列表**同轮同时间戳**发布（`_latestStats`），读接口先 `TryGetRecentSnapshot` / `TryGetRecentStats`；窗口取 `IdlePollIntervalMs + 500`，**必须严格宽过空闲轮询间隔**（第一版写死 2000 ms < 2500 ms，导致"复用"完全没生效，已加反射断言钉住）；增删/暂停/继续/限速后 `InvalidateSnapshot()`；判重预检**坚持实时读**；排序抽成 `OrderTasks` 供两条路径共用。实测静默态 200 次读：aria2 CPU **125 → 0 ms**、应用 CPU **500 → 234 ms**、总耗时 **344 → 148 ms** |
| 主窗收进托盘后 900 ms 界面刷新定时器照跑（六轮开销审计 O-3；开机进托盘是默认路径，实测常驻 **0.57 %** 单核） | 无人观看时持续烧 CPU | 1.0.8 (09-21 七轮) | 隐藏即停表、可见即恢复：`StopTimerForHidden` / `ResumeRefreshTimer` / `NotifyHidden`（停表三条路：点 X 收托盘、`--minimized` 静默启动、`RefreshAsync` 头部兜底；恢复两条路：`Activated` 与 `ShowMainWindow`，都写日志）。顺带把 `ApplySelectionVisuals` 与 `UpdateBtSummary` 限定为"对应页可见才刷"。实测隐藏态 **0.57 % → 0.13 %**（60 s 分区）、10 s 切片可见 1.72 % vs 隐藏 **0.00 %**。停表判据用新增的 `WindowEffects.WindowVisibleByAnySignal`（`AppWindow.IsVisible` 与 Win32 实际可见性任一为真即算可见），否则会出现"窗口被外部 `ShowWindow` 复原、界面却永久冻住"（实测复现并已修） |

### 10.2 未修复（已知限制）

| 问题 | 影响 | 建议 |
|------|------|------|
| `DownloadEngine.StopAsync` 等待 `_pollTask` 可能挂起 | 退出延迟 | 添加超时机制（二轮审计已核对 `ct` 贯穿 `MultiCallAsync`/`Task.Delay`，未复现；但二轮测试中观察到测试宿主不退出且残留 aria2 进程，与该假设相符，未定位） |
| `Aria2Process.StopAsync` 未删除 `aria2.pid` | 进程泄漏 | 确保删除 pid 文件 |
| 扩展不转发 Cookie/UA | 部分站点下载失败 | 添加 Cookie/UA 转发 |
| `_btHashIndex` 使用 `ConcurrentDictionary` | 内存开销 | 改用普通 Dictionary + 锁 |
| 轮询每轮拉回全部历史任务完整状态 | 471 条历史时约 565KB/轮、30–78ms、空闲 2.81% 单核 | **不建议按条数截断**：实测 `tellStopped(0,N)` 取的是**最旧** N 条（会隐藏新任务）；`keys` 裁剪仅省 5%。如需优化应改 `tellStopped(max(0,numStopped-N),N)` + 本地快照回填（二轮审计 §8-D）。三轮已把每轮诊断签名改为无分配整型指纹，完整签名串仅在检测到变化时构建 |
| 仓库卫生：v1.0.7/v1.0.8 无 git tag；`tools\*.ps1` 过期；本地 `backup\`、`.verify\old_exe_backup\`（319 MB 旧 exe）占盘 | 不影响构建/发布（均 gitignore 或未跟踪） | 三轮审计 L-7：tag 待用户确认（受"禁止擅自发版"约定约束）；`tools\` 按约定只报告不修改；本地清理待用户决定。另核实 `ThemeSwatchPicker.*` **并非死代码**（设置窗"主题配色"区在用，8 套内置调色板），二轮审计 L-7 的删除建议作废 |
| 第四轮审计提出的 8 项（N-1…N-8）与 4 类分析器告警（CA2000/CA2213/CA1806/CA5392） | — | **已于 1.0.8 (09-20 五轮) 全部修复**，见 §10.1 对应行与《测试报告_v1.0.8.md》§11。CA1305/CA2016/CA5350/CA2007 经复核为误报或有意的协议/框架要求，**不修** |
| 第六轮**开销专项审计**（O-1…O-12）：JSON 解析三重物化（1000 任务 = 4 MB 垃圾/轮）、全量历史轮询（1000 条 = 458 KB/轮，aria2 侧再 +2.8 % 单核）、主窗隐藏后 900 ms 定时器仍跑（实测常驻 0.57 % 单核）、`ApplyView` 重建路径 O(n²)、`tasks.json` 美化输出且不剪枝、做种期不退避等 | 当前规模（7 条历史）下绝对值很小：空闲日志 0 行/40 s、WS 稳定 212 MB；但两处**线性项**若涨到 1000 条历史，仅"刷新列表"就要吃掉约 7 % 单核并以约 5 MB/s 分配托管内存 | 本轮**只审计未改码**。改进路线图（P0 三项：解析改 `JsonDocument`、隐藏时停 UI 定时器、`/api/tasks`+`/api/stats` 走快照）与全部实测数据见《测试报告_v1.0.8.md》§12；"明确不建议做"清单（朴素截断 `tellStopped(0,N)`、手写 `Utf8JsonReader`、引导哈希优化等）同样列于 §12-D。**（09-21 七轮更新：P0 三项 O-1 / O-3 / O-10 已落地并实测，见 §10.1 对应三行与《测试报告_v1.0.8.md》§13；O-2、O-4…O-12 仍未做）** |
| 从托盘/任务栏唤醒主窗有时**不显示**（路径：单实例转发 `Local\KokonaDownloader_ShowWindow` → `App.ShowMainWindow` 的 `AppWindow.Show()` → `Presenter.Restore()` → `Activate()` → `ForceForeground()`） | 用户以为程序丢了（其实还在托盘里跑） | 六轮记过 1 次"1 败 1 成"；**七轮同一状态连试 3 次全部失败**，只能靠裸 Win32 `ShowWindow(SW_RESTORE)` 救回，全程无日志可定位。**（2026-09-24 九轮已修）** 做法：把救回手段固化成 `WindowEffects.RevealAndActivate`——`IsIconic` 则 `SW_RESTORE` 否则 `SW_SHOW`，`BringWindowToTop`+`SetForegroundWindow`，再用 `IsWindowVisible && !IsIconic` **验证**，不达标就重试并写日志；`ShowMainWindow` 里 `AppWindow.Show()`/`Presenter.Restore()` 都包 try/catch，第二实例唤醒路径的裸 `catch {}` 换成记录异常类型与消息（报告 §15-B） |

### 10.3 第八轮全链路回归新发现（2026-09-24 发现 → **2026-09-24 第九轮已逐条处置，见 §10.5**；原状态"只报告未修"）

> 方法：真实 Edge + 未打包扩展 + 真实鼠标点击 + 自建测试站（`.verify\e2e\site`）+ 站点请求日志/aria2 日志/app.log 交叉取证。完整数据与复现步骤见《测试报告_v1.0.8.md》§14。

| # | 问题 | 严重度 | 证据 / 根因 |
|---|------|--------|------------|
| **DIR** | 运行中修改"默认下载目录"对已启动的引擎无效：新任务仍写入 aria2 启动时的目录；并连带导致**同名保护失效**（改名逻辑在新目录查重名）与"打开文件夹"指向错误目录 | **P1（数据丢失）** | `Aria2RpcClient.BuildOptions` 仅在 `req.Directory` 非空时下发 `dir`，而自动接管路径传 `dir=null` ⇒ 用 `Aria2Process.cs:45` 的启动期 `--dir`；又因 `--allow-overwrite=true`，实测重复下载同一链接只剩 1 个文件（`DIR7 before=['e2e_conflict_me.bin'] after=[…同…]`）。修复方向：把 `dir` 在**每次** `addUri` 时显式下发为当前设置值 |
| **同名覆盖** | payload 不带 `filename` 时（= 自动接管的常态，因为 `downloads.onCreated` 阶段 `item.filename` 为空）`ApplyUniqueFileName` 按设计直接返回，重复下载**覆盖**已下载文件 | **P1（数据丢失）** | 受控对照：无 `filename` → 下两次只 1 个文件；显式 `filename` → `x.bin` + `x (1).bin`。修复方向：无 `filename` 时也按"目标目录已有同名"做一次改名，或交给 aria2 时带 `--auto-file-renaming=true` |
| **无 Range 续传残留** | 对不支持 `Accept-Ranges` 的服务器"暂停→继续"，任务失败但留下**大小正确、内容损坏**的同名文件（并残留 `.aria2`） | P2（误导） | `app.log`：`errorCode=8 Invalid range header. Request: 5439488-6291455/8388608, Response: 0-8388607/8388608`；落盘 8388608 B 但 sha256 不符。修复方向：失败时改名 `*.partial` |
| **404 处理竞态** | 服务器错误码链接（404/403/500/502/416）有时客户端出现失败任务，有时"什么都没发生" | P3（体验） | 扩展只在下载项 `in_progress` 时接管，Chromium 对错误响应几乎立即置 `interrupted`，两者赛跑 |
| **引擎重试预算小** | 引擎对 404 只发 2 次请求即判失败，短暂抖动要等**约 10 分钟**才由客户端自愈（`17:38:56 Failed` → `17:49:30 Completed`，同 gid） | P3（恢复延迟） | `app.log` + `requests.log` 双向可查；建议给"文件未找到"类错误单独加大重试 |
| **重启后历史丢失** | 客户端重启后，已完成任务从列表消失（API 与 UI 都查不到），只剩失败任务残留 | P2/P3（待与产品预期对齐） | `DIR9 before=25 after=8`；`stage_hidden` I6：重启后 UI 文本中找不到刚完成的文件名 |

其余历史问题在本轮**均未复现**：重定向假文件名、查询串当文件名、`../` 穿越与 `C:\` 绝对路径、非法/控制字符、截断被当成完成、浏览器残留半截文件、双击去重、Range 续传、Chunked、开关关闭后浏览器自行下载、`blob:`/`data:`/导航/表单/自身 API 放行。唯一确认的上游能力边界：**只有 `filename*=UTF-8''…` 而没有 `filename=` 时，aria2 忽略该文件名**（不经浏览器直接用 API 亦复现，非本项目缺陷）。



### 10.5 第九轮：§10.3 的修复与复测（2026-09-24，改码 + 打包 + 全链路复测）

> 完整数据、探针取证与逐条回归见《测试报告_v1.0.8.md》§15。GitHub 同步仍未做（等用户自测后决定）。

| §10.3 条目 | 处置 | 关键实现 / 护栏 |
|-----------|------|----------------|
| 运行中改"默认下载目录"不生效（P1） | **已修** | 每次 `addUri` 显式下发当前 `dir`；`UpdateRuntimeDefaults` 由设置变更事件热应用（目录/线程数/并发）。`RoundEightEngineTests.改默认目录后新任务立刻落到新目录` |
| 重复下载覆盖旧文件（P1） | **已修** | 逐条 `allow-overwrite=false`（BT 仍 `true`）；aria2 自动 `x.1.ext`，客户端名字可靠时自己 `x (1).ext`。`同名文件已存在时新下载自动改名而不是覆盖` |
| 无 Range 续传失败留下"看起来完整"的损坏文件（P2） | **已修** | `IsUnresumableFailure` → 改名 `*.partial`（连 `.aria2` 一起）→ 写终态快照 → 清 aria2 失败结果。`服务器不支持Range时续传失败_残留被隔离为partial且仍可重新下载` |
| 错误码链接"有时什么都没发生"（P3） | **已修** | 扩展接管规则 1 放松：`complete` 仍不接管，新鲜的 `interrupted` 接管（MV3 冷启动时 `error` 会先翻成 `interrupted`）；规则 2 保持严格。`tests\test_logic.js` 81 条断言 |
| 引擎对临时错误不重试（P3） | **定性修正后已修** | 实测：404 是 aria2 的**永久**错误（任何参数都只请求 1 次，客户端不擅自重试死链）；真正的问题是**临时 5xx 一次都不重试** → `--retry-wait=2 --max-tries=5`，同一任务内自愈 |
| 重启后完成态历史丢失（P2） | **已修（本地历史）** | `TaskStore` 终态快照 + `_history`（≤2000），列表 = aria2 实时 ∪ 本地历史。`已完成任务在引擎重启后仍以历史行出现在列表里` |
| 仅 `filename*=UTF-8''` 被忽略（P3） | **不修** | aria2 上游能力边界；要修需在扩展里用 `webRequest` 缓存响应头再显式传 `out` |

**修复过程中新查出的问题（同轮已修）**：aria2 用 `--input-file=<会话文件>` 启动，会话里"其实早就下完"的条目会被**当新任务重新加入**（新 gid、`排队中 0 B`），第八轮 UIA 转储里那条 `e2e_big_50mb.bin` 就是这个假象（当时被误读成"UI 还看得到历史"）。现在启动前一并把这些条目从会话文件里滤掉（判据保守：非 BT + 终态 Completed + 落盘文件仍在）。护栏：`会话文件里早已完成的条目不会在重启后被重新排队`、`启动前过滤会话文件_已删除与早已完成的条目都不再复活`、`stage_dir` 的 `DIR9b/DIR9c`。

**测试基建同轮调整**：`xunit.runner.json` 的 `parallelizeTestCollections` 改 `false`——Core 套件里多个用例会各起一个真实 aria2 并各自抢临时端口，并行时端口 TOCTOU 导致**随机**失败（第八轮记录的"并行 flake"，隔离复跑必过、串行复跑 `170/170` 全绿）。

### 10.4 安全注意事项

| 风险 | 缓解措施 |
|------|----------|
| API 密钥泄露 | 常量时间比较 + 本地回环绑定 + **密钥轮换**（2026-09-19：旧密钥曾在公开仓库历史中出现，已换新；旧值失效） |
| 路径穿越 | `ExtractAll` 检查解压目标 |
| 时序侧信道 | `FixedTimeEquals` |
| CORS 过宽 | **已修复**（2026-09-19 二轮）：原实现回 `Access-Control-Allow-Origin: *`，而鉴权依赖自定义头 → 必触发预检且预检对任意来源放行，任意网页可带密钥调用本地 API。现只放行扩展/回环来源并回显具体 Origin |
| 公开分发资产含密钥截图 | 待人工目视确认 `海兔下载器教程.pdf`（脚本解压可读文本流未见明文，截图无法脚本判读；旧密钥已失效故风险降为信息暴露） |

---

## 11. 维护指南

### 11.1 开发环境

**要求**:
- Windows 10/11 (x64)
- .NET 8 SDK
- Windows App SDK 1.8
- PowerShell 5.1+
- Git

**可选**:
- Chrome/Edge 浏览器（测试扩展）
- Visual Studio 2022（调试）

### 11.2 常用命令

```powershell
# 构建
powershell -ExecutionPolicy Bypass -File .\build.ps1

# 运行单元测试
dotnet test src\KokonaDownloader.Core.Tests\KokonaDownloader.Core.Tests.csproj

# 运行扩展逻辑测试
cscript //nologo tests\test_logic.js

# 运行 UI 冒烟测试
dotnet run --project src\KokonaDownloader.UiTests\KokonaDownloader.UiTests.csproj

# 发布单文件 exe（本仓库设了 EnableMsixTooling=false 并自行嵌入 resources.pri，
# 因此必须跳过 Windows App SDK 的单文件配置校验，否则构建期直接报错）
dotnet publish src\KokonaDownloader.App\KokonaDownloader.App.csproj `
  -c Release -r win-x64 `
  -p:PublishSingleFile=true `
  -p:IncludeNativeLibrariesForSelfExtract=true `
  -p:WindowsAppSDKSingleFileVerifyConfiguration=false `
  -o dist\KokonaSingleFile

# 打包扩展
Compress-Archive -Path extension\* -DestinationPath release\KokonaExtension_106.zip
```

### 11.3 调试技巧

#### 日志位置
- **应用日志**: `%APPDATA%\KokonaDownloader\app.log`
- **Aria2 日志**: `%APPDATA%\KokonaDownloader\engine\aria2.log`
- **会话文件**: `%APPDATA%\KokonaDownloader\engine\aria2.session`

#### 常用诊断脚本
```powershell
# 检查 API 设置
powershell -ExecutionPolicy Bypass -File .\tools\verify_api_settings.ps1

# 检查文件名解析
powershell -ExecutionPolicy Bypass -File .\tools\verify_filename_resolution.ps1

# 检查单文件发布
powershell -ExecutionPolicy Bypass -File .\tools\verify_singlefile_main.ps1
```

#### 进程检查
```powershell
# 检查 aria2c 进程
Get-Process aria2c -ErrorAction SilentlyContinue

# 检查 KokonaDownloader 进程
Get-Process KokonaDownloader -ErrorAction SilentlyContinue

# 检查端口占用
netstat -ano | findstr ":16800"
netstat -ano | findstr ":16801"
```

### 11.4 发版流程

1. **更新版本号**: `src\Directory.Build.props` 中的 `Version`/`AssemblyVersion`/`FileVersion`/`InformationalVersion`（唯一来源，App/Core/测试项目共同继承；`ApiService.Version` 运行时从程序集特性读取，勿在 csproj 或代码里重复定义）
2. **构建**: `build.ps1`
3. **发布**: `dotnet publish src\KokonaDownloader.App\KokonaDownloader.App.csproj -c Release -r win-x64 -p:PublishSingleFile=true -p:IncludeNativeLibrariesForSelfExtract=true -p:WindowsAppSDKSingleFileVerifyConfiguration=false -o dist\KokonaSingleFile`
   （本仓库刻意设 `EnableMsixTooling=false` 并自行嵌入 `resources.pri`，因此必须带最后一项跳过 Windows App SDK 的单文件配置校验，否则构建期直接报错）
4. **打包**: 压缩扩展为 zip
5. **测试**: 运行全量测试套件
6. **提交**: `git commit` + `git push`
7. **上传**: 上传到 GitHub Releases

### 11.5 代码规范

#### C#
- 使用 `ConfigureAwait(false)` 避免 UI 上下文捕获
- 使用 `IAsyncDisposable` 管理异步资源
- 使用 `CancellationToken` 支持取消
- 使用 `Volatile` 保证跨线程可见性
- 使用 `Interlocked` 保证原子操作

#### JavaScript
- 使用 `async/await` 处理异步
- 使用 `chrome.storage` 持久化数据
- 使用 `AbortController` 支持超时
- 使用 `Set` 去重

#### PowerShell
- 使用 `-ExecutionPolicy Bypass` 避免策略限制
- 使用 `try/catch` 处理异常
- 使用 `Write-Host` 输出日志

### 11.6 测试规范

#### 单元测试
- 使用 xUnit 框架
- 测试类命名: `{ClassName}Tests`
- 测试方法命名: `{MethodName}_{Scenario}_{ExpectedResult}`
- 使用 `IAsyncLifetime` 管理异步资源

#### 集成测试
- 使用真实 aria2c 进程
- 使用临时目录隔离测试数据
- 测试后清理临时文件

#### E2E 测试
- 使用 Node.js + HTTP 客户端
- 模拟浏览器扩展行为
- 验证完整下载流程

#### 扩展纯逻辑测试（无需浏览器）
- `extension\logic.js` 不依赖任何扩展 API，被 `background.js`/`popup.js`/`options.js` 与测试工程共用，保证逻辑单一来源；`background.js` 里的**响应映射**（状态码 → `unauthorized/rejected/busy/http`）也已抽成 `KokonaLogic.mapDownloadResponse`
- 用 `cscript //nologo tests\test_logic.js` 直接跑（WSH/JScript，ES3 兼容），当前 81 项断言
- 因此 `logic.js` 有两条硬约束：**只能写 ES3 语法**；**中文注释只能写成块注释**（cscript 按系统 ANSI 码页读源码，以中文收尾的 `//` 行注释其 UTF-8 尾字节会把换行吃掉，导致紧随其后的 `var` 被一起注释掉），用户可见文案一律用 `\u` 转义

#### 静态检查门槛（分析器）
- `src\.editorconfig` 按项目路径作用域把 **CA2000 / CA2213 / CA1806 / CA5392 设为 `error`**（这几条在 `AnalysisMode=Default` 下默认关闭，可按路径单独打开），构建/发布一旦违反即失败
- 实测要点：某规则的严重度一旦写进 `.editorconfig`，MSBuild 的 `WarningsAsErrors` / `-warnaserror` **无法再升级它**，所以门槛只能落在 `.editorconfig` 里（不要再加 MSBuild 侧的 `WarningsAsErrors` 门禁）
- 覆盖范围**故意不含** `KokonaDownloader.Core.Tests` 与 `KokonaDownloader.UiTests`（否则 `dotnet test` 会被测试代码的临时对象写法绊住）
- 复核为误报、**不应修复**：CA1305（整数格式化在各 locale 下实测均 ASCII）、CA2016（`_requestGate.Wait(0)` 本就无回调）、CA5350（`BtHashUtil` 的 SHA1 是 BT v1 infohash 协议规定）、App 层的 CA2007（UI 上下文有意保留）

#### 串行执行约定
- **九轮起直接在 `xunit.runner.json` 里关掉并行**（`parallelizeTestCollections=false`）：并行时多个 fixture 各起一个真实 aria2 并各自抢临时端口，`GetFreePort()` 的 TOCTOU 会随机造成 `aria2 RPC 在 15000ms 内未就绪` 之类与代码无关的失败（第八轮记为"Core 套件并行 flake"）。关掉并行后全套稳定 `170/170`（约 5.9 分钟）
- `.verify\audit\run_tests_serial.ps1` 仍保留：按类逐个跑，便于定位是哪个类引入的不稳定；**新增真实 aria2 测试类时要同步更新该脚本的类清单与"纯单测"排除过滤器**（九轮已加入 `RoundEightEngineTests`）

---

## 12. 版本历史

### 1.0.8 (2026-09-18)

**变更**:
- 移除启动动画（SplashWindow），启动即就绪
- 磨砂透明成为唯一主题：删除其余所有透明预设与可选项，固定最不透明 100% 档（tint 0.98 / luminosity 0.90）
- 启动时直接进入托盘，不再弹出主窗口（`--minimized`）
- 主菜单移除不透明/磨砂相关选项

**修复**:
- 切换主题色时闪黑：`ApplyTransparency` 改为优先复用已挂载的 `DesktopAcrylicController` 原地更新染色（`WindowEffects.TryUpdateAcrylicTint`），不再销毁重建；实测黑帧 7 → 0

**2026-09-19 更新（版本号不变，提交 `eb18305`，GitHub v1.0.8 资产已同步替换）**:
- 完成 v1.0.8 全量测试并出具《测试报告_v1.0.8.md》：下载全场景 52/52、E2E 24/24、扩展契约 56/56、UiTests exit 0、UI 检测 0 控件级问题、性能稳定（空载 CPU 1.15% / WS ~200MB）
- 修复全量测试发现的 3 项问题：
  - 【严重】高并发快速建窗触发未处理 `XamlParseException` 崩溃（`Microsoft.UI.Xaml.dll` 原生故障 0xc000027b fastfail，托管层无法拦截）→ 建窗最小间隔 600ms + `Activate()`/`ForceForeground()` 延迟到下一调度周期，把"建窗风暴"摊平；复测 200 唯一任务 ~10s 连发无崩溃
  - 【中】并发进度小窗完全堆叠（同坐标居中）→ 每窗分配级联槽位（未占用最小槽位，关闭自动释放），按槽位在屏幕中心向右下错开 +28/+20 物理像素×DPI；复测 6 窗 6 个不同坐标
  - 【低】`[aria2]` 日志空白行泛滥（97.5%）→ `Aria2Process` 捕获 stdout/stderr 时跳过空白行（原仅判 `!= null`，空行带前缀绕过 `App.Log` 过滤）；复测 0 空白行
- 单文件 exe 重新发布并部署验证（干净目录自解压迁移正常），GitHub v1.0.8 release 的 `KokonaDownloader_108.exe` 资产替换为修复后构建（319,707,277 字节）

**2026-09-19 更新（第二轮：代码审计 + 安全/性能修复，版本号仍为 1.0.8，本地已重新构建部署）**:
- 审计范围：Core 引擎与 API、App/WinUI 层、浏览器扩展、单文件部署引导全量精读，并对运行中实例做黑盒测量（轮询体积/延迟、设置写入 CPU、空闲 CPU、日志取证、GitHub 仓库与历史）
- **安全【高】**：本地 API 的 CORS 通配 + 公开仓库历史中的实时密钥 → 任意网页可无声投递下载/删任务/改设置。修复：来源白名单（扩展 + 回环，`Uri.IsLoopback` 解析防前缀绕过）、回显具体 Origin、轮换 `ApiSecret`。部署后实测：3 类恶意来源均 403 且无 ACAO、扩展/回环放行、旧密钥 401
- **性能【中】**：任何设置写入都触发全量主题重算（重建约 120 个资源键 × 2 个字典 + 每窗口可视树 Dark→Light→Dark 重求值）。修复：`ResolvedTheme` 按值比较短路 + 设置补丁/设置窗逐字段比对。实测 20 次空改动补丁：127 次窗口级重应用 / 4.75s CPU → **0 次 / 0.27s**（↓94%）
- **内存【低】**：`ThemeService._backdrops` 强键字典从不释放 → 每开一次设置窗、每弹一次磁力确认窗泄漏一个窗口对象图（**修正首轮表述**：进度小窗走复用分支，不受影响）。修复：`Unregister` 调用 `ReleaseBackdrop`，并给该函数里的 `SystemBackdrop = null` 补 try/catch
- **修正首轮结论【轮询负载】**：实测 `tellStopped(0,N)` 返回**最旧** N 条 → 按条数截断会隐藏最新任务，属回归，**不做**；`keys` 裁剪仅省 5%；负载 71.9% 来自 aria2 为每条 split 连接登记的重复 URI。改为记录在案（§10.2）
- 复测：Core **139/139 PASS**（逐类串行；并行首跑 12 项失败系 fixture 竞争）、修复验证 **12/12 PASS**、run.mjs **52/52**、E2E **24/24**、UI 检测 **ISSUES: 0**；新构建日志 0 未处理异常；单文件 exe 重新发布（319,707,277 B，SHA256 `7AC72FD8…796F`）并部署，`StandaloneBootstrap` 搬运与孤儿 aria2 清理均正常
- 记录：审计与修复明细见《测试报告_v1.0.8.md》§8；未修的 10 项低优先级问题见其 §8-F

**2026-09-20 更新（第三轮：§8-F 低优先级项全部处理，版本号仍为 1.0.8，本地已重新构建部署）**:
- L-1…L-10 逐项修复：README 三处过期、进度窗 UI 线程同步等 RPC、批量路径伪文件名、墓碑批量撤销短路、扩展 `confirm` 透传（磁力确认提示分支复活）、单文件引导重写（清单 + 互斥量，冷启动不再无条件覆盖 ~305 MB exe）、轮询诊断签名改无分配整型指纹、API 64 槽请求门（超限 503）、主窗/设置窗事件处理器异常防护；新增墓碑批量撤销回归测试
- **L-7 修正**：`ThemeSwatchPicker.*` 经核实并非死代码（设置窗"主题配色"区在用，8 套内置调色板），不删除；缺 tag / `tools\` 陈旧 / 本地占盘三项记录在案待用户决定
- **隐藏缺陷**：部署验证发现新构建引导静默失效——本 .NET 8 运行时 `SHA256.Hash` 未终结即读抛异常，被 ModuleInitializer 空 catch 吞掉，应用退化为原地运行。修复（先 `TransformFinalBlock`）后复验：全新状态完整解压 + 清单写入 + exe 复制；跳过路径 ping 就绪 1.17 s、零文件重写
- 复测：Core **140/140 PASS**（逐类串行，含新回归测试）、修复验证 **10/10**、run.mjs **52/52**、E2E **24/24**、扩展逻辑（cscript）**56/56**、UI 检测 **ISSUES: 0**；单文件 exe 重新发布（319,711,373 B，SHA256 `03ACF046…B215`）并部署
- 明细见《测试报告_v1.0.8.md》§9

**2026-09-20 更新（第四轮：代码审计，只审不改，版本号仍为 1.0.8）**:
- 方法：主审精读高风险文件全文（引导、API 接受循环、轮询/批处理、磁力确认窗、扩展响应映射、主窗异常路径）+ 编译器静态分析（`AnalysisMode=All`，Core 18 类、App 26 类规则）+ 运行时实测（句柄/内存/API 负载/并发门/引导全流程）；判定纪律：**文档已声明的有意取舍不算缺陷，分析器告警必须逐条复核真伪**。补充说明：曾尝试"五路并行子代理深读"，但五个子代理均超预算无产出被中止，故结论全部出自主审自身方法，覆盖率为抽样性质
- 结论：**未发现高危缺陷**；发现 8 项新问题（N-1…N-8，1 中 + 1 中 + 1 中低 + 5 低），另有 4 类分析器告警经实测判定为**误报或有意的协议/框架要求**（CA1305 整数区域设置格式化、CA2016 非阻塞 `Wait(0)`、CA5350 BT v1 infohash 的 SHA1、CA2007 UI 层上下文）
- 头号建议：`MagnetConfirmWindow.OnActivatedForInitialFocus` 是无 try/catch 的 `async void`（上一批修复漏网）；第三轮修复中仅 L-4 有回归测试，扩展侧 `background.js` 改动完全未纳入 cscript 套件
- 实测亮点：空闲近 11 小时轮询诊断日志 **0 行**（L-8 指纹机制确认生效）、并发门 120 突发 → 75×200 + 45×503、关闭 6 个常驻小窗回收 107 个句柄、部署后 0 未处理异常
- 明细与优先级路线（P0/P1/P2）见《测试报告_v1.0.8.md》§10

**2026-09-20 更新（第五轮：按 §10-E 建议落地改动，版本号仍为 1.0.8）**:
- P0/P1/P2 全部落地（§10-E 第 11 项"长期拆分"按其原文"非必要不做大重构"**有意不做**）：N-1 磁力确认窗异常防护、N-2 回归护栏、N-3 API 接受循环与槽位归还/释放、N-4 引导逐文件哈希、N-5 注释与日志、N-6 版本号单一来源、N-7 仓库卫生、N-8 托盘异常日志，外加 CA2000/CA2213/CA1806/CA5392/CA1001 清理与分析器门槛（`src\.editorconfig`）
- 新增测试资产：Core 侧 `ApiConcurrencyGateTests`（闸门饱和 → 503 → 断开后恢复）、`PollFingerprintTests`（6 项）、批量伪文件名回归测试 1 项；扩展侧把 `background.js` 的响应映射抽成 `logic.js:mapDownloadResponse`，cscript 套件 56 → **77** 项
- 复测：Core **149/149 PASS**（逐类串行）、扩展逻辑 **77/77**、run.mjs **52/52**、E2E **24/24**、UI 检测 **ISSUES: 0**、既有修复回归 **10/10**（+1 skip）、并发门 **64×200 + 56×503 / 74 ms**
- 引导（N-4）实测：把 `resources.pri` 第 1000 字节按位取反（尺寸不变，旧清单会判定"完整"）→ 重启后**自动重新解压自愈**；再重启走校验跳过路径，ping 就绪 **1.08 s** 且 `resources.pri` mtime 不变；清单现含 12 个逐文件 SHA256 + `payloadHash`
- 部署产物：单文件 exe **318,204,760 B**，SHA256 `8A5D043A1ABE98358106AF210CF5AA11CA7E724D780E82203A3D9157D0DFA98D`（桌面版已替换；GitHub Release 资产尚未替换，待用户确认）；`/api/ping` 仍返回 `1.0.8`（现在来自程序集特性）；日志中 0 条 `未处理`/`未生效`/`设置失败`/`引导失败`
- 文档修正：单文件发布命令必须带 `-p:WindowsAppSDKSingleFileVerifyConfiguration=false`（本仓库 `EnableMsixTooling=false`，旧文档命令会直接构建失败）；版本号唯一来源改为 `src\Directory.Build.props`
- 未覆盖项（诚实记录）：托盘菜单宿主/菜单项动作（`TrayMenuHost`）只能人工点开托盘菜单触发，本轮无自动化覆盖，新增的读回校验与异常日志仅有编译期与代码审查证据
- 明细见《测试报告_v1.0.8.md》§11

**2026-09-21 更新（第六轮：开销专项审计，只审不改，版本号仍为 1.0.8）**:
- 方法：把应用真实发出的周期 multicall 原样落盘做负载样本（4,271 B / 458 KB / 587 KB）、解析三路径基准（A 现方案 / B `JsonDocument` / C 手写 `Utf8JsonReader`）、aria2 侧 CPU 采样、主窗"可见 / 收托盘 / 再可见"三分区进程级 CPU 采样
- 结论：**当前规模（7 条历史）绝对开销很小**（空闲 40 s 日志 0 行、WS 稳定 212 MB），但两处**线性项**一旦长到 1000 条历史就不可忽视——仅"刷新列表"就要吃掉约 7 % 单核，并以约 5 MB/s 分配托管内存
- 产出 O-1…O-12 开销清单与 P0/P1/P2 路线图；同时给出"明确**不**建议做"清单：朴素截断 `tellStopped(0,N)`（会隐藏最新任务）、手写 `Utf8JsonReader`（分配与 B 方案相同，白增复杂度）、引导哈希优化
- 明细见《测试报告_v1.0.8.md》§12

**2026-09-21 更新（第七轮：按 §12-D 落地 P0 三项，版本号仍为 1.0.8，本地已重新构建部署）**:
- **O-1**：RPC 响应改"从网络流直接 `JsonDocument.ParseAsync`"（整份只解析一次），轮询走新的文档态 `Aria2MultiCall`；1000 任务每轮托管分配 **3,995 → 1,445 KB（−64 %）** 且不再产生 LOH 字符串；顺带补上 `HttpResponseMessage` 的释放
- **O-10**：`GET /api/tasks` / `GET /api/stats` 复用轮询快照（统计与列表同轮发布）。静默态 200 次读的 A/B（同机、同负载 672,200 B、都等到引擎静默）：aria2 CPU **125 → 0 ms**（读路径彻底不打 RPC）、应用 CPU **500 → 234 ms**、总耗时 **344 → 148 ms**
- **O-3**：主窗隐藏即停界面刷新定时器、可见即恢复（都写日志），并把 BT 概要条/选中视觉限定在对应页可见时刷。托盘常驻 **0.57 % → 0.13 %** 单核（60 s 分区），10 s 切片可见 1.72 % vs 隐藏 **0.00 %**
- **中途抓到的真缺陷**：`ReadSnapshotMaxAgeMs` 第一版 2000 ms **小于**空闲轮询 2500 ms ⇒ 快照复用完全没生效（aria2 CPU 与旧版持平）。改为 `IdlePollIntervalMs + 500` 并新增"新鲜度窗口必须宽过空闲轮询退避"的不变量测试钉住
- 附带两件事：`AppWindow.IsVisible` 与真实窗口可见性会不一致（外部 `ShowWindow` 复原后界面会永久冻住）→ 新增 `WindowEffects.WindowVisibleByAnySignal` 并验收；单实例唤醒主窗 3 次连续失败（**未修**，进 §10.2 未修复清单）
- 新增 11 条测试（`RpcParsePathTests` 7 条含"文档态与 JsonNode 版结果一致"；`SnapshotReadTests`/`SnapshotInvalidationTests` 5 条真引擎）。复测：Core **161/161 PASS**（逐类串行，与 `--list-tests` 的 161 条逐类对账一致）、扩展逻辑 **77/77**、run.mjs **52/52**、E2E **24/24**、既有修复回归 **10/10（+1 skip）**、UI 检测 **ISSUES: 0**
- 部署产物：单文件 exe **318,208,856 B**（比上一版 **+4,096 B**），SHA256 `46D772E6DD8789FF41E489E15B84E092B6DEA30C9A867E39F671CCE28E2CDFF2`；上一版 exe 已备份在 `.verify\old_exe_backup\`（318,204,760 B）可回退
- 明细见《测试报告_v1.0.8.md》§13

**2026-09-24 更新（第八轮：最终打包件的真实场景全链路回归，版本号仍为 1.0.8，**本轮不改产品代码、不同步 GitHub**）**:
- 目的：按用户要求，用"从点击链接 → 扩展拦截 → 本地 API → aria2 → 落盘 → UI 列表"的真实链路，验证**历史各轮遇到过的那些问题是否还会复现**；被测对象是**最终打包件** `dist\KokonaSingleFile\KokonaDownloader.exe`（318,208,856 B，SHA256 `46D772E6…DFF2`，`/api/ping` = 1.0.8）
- 方法：新建测试站 `.verify\e2e\site\server.mjs`（Web 18099 + 内置 BT tracker 26969，含 13 种 Content-Disposition 形态、Range/无 Range/Chunked/截断/抖动 404/Referer 校验、blob:/data:/导航/表单等"不该接管"页面、8 连点批量、20 MiB BT 种子），用真实 Edge（`--load-extension=dist\KokonaExtension`）+ CDP 真实鼠标点击驱动，再用**站点请求日志（按 UA 区分浏览器/aria2）、aria2 原始日志、`app.log`、落盘 sha256、UIA 控件树**五路交叉取证；9 个阶段（setup/basic/names/dir/failures/resume/compat/magnet/hidden）共 400 余条断言
- **未复现**（历史问题确认已修）：重定向假文件名、查询串当文件名、`../` 穿越与 `C:\` 绝对路径、非法/控制字符、截断被当成完成、浏览器残留半截文件、同一链接双击去重、Range 断点续传、Chunked、自动接管开关、`blob:`/`data:`/导航/表单/指向自身 API 的链接放行、托盘隐藏期间引擎继续下载、隐藏停表（O-3）
- **新发现 6 项**（P1×2 / P2×2 / P3×2，**均未修**）：运行中改"默认下载目录"对已启动引擎无效并连带使同名保护失效（P1，数据丢失）、无 `filename` 的自动接管任务重复下载覆盖旧文件（P1）、无 Range 服务器暂停续传后留下"大小正确但损坏"的同名文件（P2）、重启后已完成历史消失（P2/P3）、错误码链接处理竞态（P3）、404 重试预算小导致自愈延迟约 10 分钟（P3）。逐条证据与修复方向见 §10.3 与《测试报告_v1.0.8.md》§14
- ~~唯一确认的上游能力边界：CD 只有 `filename*=UTF-8''…`（无 `filename=`）时 aria2 会忽略该文件名，不经浏览器直接用 API 亦复现~~
  **← 第十轮（§16）推翻：错在我的测试夹具。** 那条头的 ext-value 里圆括号没按 RFC 6266 百分号编码，属非法 ext-value；aria2 对非法 ext-value 是**整条 `Content-Disposition` 作废**。合法的 `filename*`（CJK、空格、语言标记、跟 302）aria2 本来就能解——同一条头只差括号编码，结果就不同（对照实验见《测试报告_v1.0.8.md》§16-C）。
- 交付边界（按用户指示）：**不提交、不推送、不打 tag、不替换 Release 资产**；不出截图，改程序化取证；测试期把 `defaultDownloadDir`/`globalSpeedLimit` 临时改到 `%TEMP%\kokona_e2e\<runTag>`，收尾已按 `state.json` 还原为 `C:\Users\lumin\Downloads` / `1048576`
- 明细见《测试报告_v1.0.8.md》§14

**2026-09-24 更新（第九轮：按 §14-H 落地修复 + 重新打包 + 全链路复测，版本号仍为 1.0.8，**仍不同步 GitHub**）**:
- 用户决策：两条 P2 分别选"实现本地历史"和"失败即隔离为 `.partial`"；仍然不提交/不推送/不打 tag/不换 Release 资产，仍然跳过全部截图（改程序化取证）
- **§10.3 的 6 条全部处置完毕**（详见 §10.5）：目录热更新（每次 `addUri` 显式下发 `dir` + `UpdateRuntimeDefaults` 热应用）、逐条 `allow-overwrite=false` 杜绝覆盖、无 Range 续传失败隔离为 `*.partial` 并清 aria2 失败结果、本地历史（`TaskStore` 终态快照 + `_history` ≤2000）、扩展接管规则 1 放松（新鲜 `interrupted` 也接管）、`--retry-wait=2 --max-tries=5`；~~仅"只有 `filename*` 的 CD"作为上游边界保留不修~~（← 第十轮修掉了，见下面 2026-09-25 条目）
- **修的时候又抓出一个真缺陷并一并修掉**：`--input-file` 会把会话里"早就下完"的条目当新任务重新加入（新 gid、`排队中 0 B`），第八轮 UIA 转储里那条"重启后还能看到 e2e_big_50mb.bin"其实就是它——现在启动前把这些条目从会话文件里过滤掉（判据：非 BT + 终态 Completed + 落盘文件仍在）
- 唤醒窗口（§10.2 老条目）固化为 `WindowEffects.RevealAndActivate`：`SW_RESTORE`/`SW_SHOW` + 置顶 + **用 `IsWindowVisible && !IsIconic` 验证**，不达标重试并写日志
- 测试基建：`xunit.runner.json` 关掉并行（第八轮"并行 flake"的根因是各 fixture 抢临时端口的 TOCTOU），`run_tests_serial.ps1` 类清单加入 `RoundEightEngineTests`；新增 `RoundEightFixTests.cs`（选项构造/失败判定/终态快照/真实 aria2 场景/会话不过滤不复活）与 `.verify\fixtest\aria_probe.mjs`、`retry_probe.mjs` 两个取证探针；`stage_*` 侧同步收紧（`DIR9b/DIR9c`、`C13`、`name_key()`、`I6` 空状态判据、`stage_setup` 跑前清空本轮下载目录）
- 复测（最终干净跑 `.verify\fixtest\e2e_round9g.log`，逐阶段表见《测试报告_v1.0.8.md》§15-E）：Core 全套串行 **172/172**（6 m 1 s）、扩展逻辑 **81/81**、站点自检 **52/52**、全链路 9 阶段 **422 通过 / 6 跳过 / 1 失败**（共 428 条；唯一红是磁力阶段的前置条件 `F0a`，已查明是测试自己的异步删除竞态，非产品行为）、run.mjs **52/52**、functest **24/24**、`verify_fixes.mjs` **10/10**（+1 skip）、UI 检测 **ISSUES: 0**（主窗 81 控件）
  - ⚠️ **第十轮更正其有效性**：那一轮（以及第八轮）链路加载的是 `dist\KokonaExtension`，而该目录自 9-13/9-14 起就是**陈旧副本**（`logic.js` 6,507 B vs 当时源码 8,971 B），因为直接 `dotnet publish` 不会拷扩展、只有 `build.ps1` 会。**涉及扩展行为的链路结论当时跑的是旧扩展代码**；`stage_setup` 现已强制同步并加了 `P0` 核对，重跑结果见下面 2026-09-25 条目
- 收尾：`chain_cleanup.py` 已还原设置（`C:\Users\lumin\Downloads` / `1048576`）、停掉测试站与 Edge/做种机、清掉 `Downloads` 里 6 个泄漏产物；测试沙箱 `%TEMP%\kokona_e2e\145754`（约 986 MB）**故意保留**供核对，下一轮 `setup` 会自动清空
- 部署产物：单文件 exe **318,221,144 B**（比第八轮 **+12,288 B**），SHA256 `7BDDA869FC7FDE2EB798CF6FCEC7B814DFD7895AD372E5B7EEC80F484F4E9055`（`/api/ping` 仍为 1.0.8）；收尾时已把这份 exe 放到 `C:\Users\lumin\Desktop\KokonaSingleFile\KokonaDownloader.exe`（该目录原先不存在，新建；大小与 SHA256 与 `dist\` 一致）；**GitHub Release 资产仍未替换、未提交、未推送、未打 tag**，等用户自测后决定
- 明细见《测试报告_v1.0.8.md》§15

**2026-09-25 更新（第十轮：专项复测"临时链接解析不到真实文件名"→ 推翻上一轮的"上游限制"结论并修复；同时修掉"引擎掉线后永不恢复"；版本号仍 1.0.8，**本轮按用户指示同步 GitHub**）**
- 用户指示：「把能修复的都处理了，然后同步到 GitHub」（此前"只做同步前停手、测试照做"的前置条件已解除；仍然跳过全部截图，改程序化取证）
- **上一轮结论是错的**：`filename*=UTF-8''…` 本身 aria2 完全能解。真正的失败条件是 **ext-value 非法**（最常见就是裸写的圆括号 `报告 (1).pdf`，RFC 6266 的 `attr-char` 不含 `(` `)`），而 aria2 遇到非法 ext-value 会**把整条 `Content-Disposition` 作废**（连同一行里合法的 `filename=` 也不认）；浏览器宽容 ⇒ 用户看到"浏览器里名字对、客户端里名字错"。最小差分实验：同一条头只改"括号有没有百分号编码"，`报告 (1).pdf` ✅ / 落回 URL 末段 ❌。错因见《测试报告_v1.0.8.md》§16-A（我自己的夹具 + 把错误结论固化成绿色的 `B4` 断言）
- **产品修复**（全部在 Core，扩展与 UI 未改）：
  1. 新增 `ContentDispositionParser`：比 aria2 宽容的 RFC 6266 解析（非法 ext-value 照收、优先 `filename*`、只取 basename、拒绝控制字符/Windows 非法字符/`CON`·`COM1` 保留名、剥首尾零宽与 BOM、超长截断保留扩展名、`filename=` 不做百分号解码、`filename=` 里裸 UTF-8 字节还原）
  2. 新增 `RemoteNameResolver`：添加前用 `Range: bytes=0-0` 预解析（跟 302）拿真名并作为 `out` 下发；门控（只在无显式文件名时考虑、带签名参数且 URL 已有扩展名则跳过、**只认 200/206**）；任何失败返回 `null` ⇒ 行为退回修复前
  3. aria2 参数新增 `--content-disposition-default-utf8=true`：`filename="中文.pdf"` 这类**裸 UTF-8 头**不再落成乱码（实测同一份 aria2c 加参数前后：`ä­æ_c18.pdf` → `中文_c18.pdf`）
  4. **引擎掉线自愈**：添加路径（单条 + 批量）遇到连接类异常自动重启 aria2 并重试一次，轮询循环加看门狗；重启失败才报错，且是中文文案（此前恒 500 且泄漏原始 SocketException、永不恢复）
- 测试侧防复发：站点新增 `starencoded`/`nodisptype`/`rawutf8` 三形态；`B4` 改为期待真名并加 `B4b/B4c/B4d`；`stage_setup` **开跑前强制把 `extension\` 同步进 `dist\KokonaExtension`** + 新增 `P0` 逐文件核对（因为链路加载的是 dist 副本，第八/九轮它一直是陈旧副本）；`B6/B7`、`D4` 两处错误判据改为更强的正向断言；站点的 drip 节流改成**按文件共享的令牌桶**（原来只节流 200，aria2 的 206 Range 全绕过去，"慢速文件"实测 24 MB/s 秒完 ⇒ D4 采不到样本），并加自检 `S30` 用 256 KiB Range 请求计时钉死"206 也要过桶"；磁力阶段新增 `F0z`/`F9`（进阶段与收尾都从**引擎侧** `forceRemove` 所有 BT 条目，因为上一轮的做种任务会一直占着 infohash，把新磁力静默吞掉）；新增 `RoundTenNameTests.cs` 44 条（含 5 条真 aria2 端到端，其中一条**杀掉引擎进程**验证自动重启）
- 复测：产品级 23 形态探针（干净目录，走 `POST /api/download`）**20/21 命中服务器真名**（唯一"不符"是服务器名里带零宽字符、我方主动剥掉）；站点自检 **56/56**；Core 全套 **216/216**（6 m 34 s 最终复跑）；扩展逻辑 `cscript //nologo tests\test_logic.js` **81/81**；下载功能全量 `run.mjs` **52/52**（上一轮 50/52 的两条红是 `globalSpeedLimit` 泄漏到用户设置，本轮先 `chain_cleanup` 还原再跑）；全链路 9 阶段（扩展副本已刷新）**461/461，skip 6，失败 0**
- 磁力阶段的两轮红已用取证脚本 `.verify/e2e/diag_magnet_rpc.py` 定性（**不是产品回归**）：客户端列表里看不到的做种任务/磁力孤儿一直占着 aria2 的 infohash 注册表，新磁力 `addUri` 返回 gid 后立刻被 `InfoHash … is already registered` 丢弃，界面留下 0% 幻影行；顺带确认两条产品事实——公共 tracker 列表默认开启（磁力会向 ~20 条公共 tracker announce）、以及"幻影行"这个可用性缺口，均记入报告 §16-G #11/#13 与 §16-H 遗留
- 已知遗留（有意不做）：S3/OSS 式 `?response-content-disposition=…`（名字只在 query 里）仍按 URL 末段；`filename="%E6%8A%A5…"`（普通记法里的百分号字面量）按 RFC **不解码**；带签名参数且 URL 已有扩展名的链接不预解析；自愈守卫目前只覆盖添加路径
- 部署产物：单文件 exe **318,237,528 B**（比第九轮 +16,384 B），SHA256 `6A5BCF9622BDCD7AAD569CFF179F5D8A65255B8C130647008E5450534F4938AC`，`/api/ping` 仍 `1.0.8`；已同步到 `C:\Users\lumin\Desktop\KokonaSingleFile\KokonaDownloader.exe`；扩展包重打为 `release\KokonaExtension_108.zip` **39,369 B**（旧 37,834 B，含 9-24 之后的 `logic.js`；旧包备份 `.bak`）；运行期已用 `Get-CimInstance Win32_Process` 从**真实子进程命令行**确认新参数下发
- GitHub：**已提交并推送**（`main` `e3a1926..d8015d1`，本次推上去两个提交：第四轮审计落地 + 第十轮）；Release `v1.0.8`（id 391289091）两项资产**已原地替换**：`KokonaDownloader_108.exe` 319,711,373 → **318,237,528 B**（上传 http=201，60 s）、`KokonaExtension_108.zip` 37,834 → **39,369 B**（http=201）；`KokonaDownloader-Tutorial.pdf`（288,628 B）未动；沿用 9-18 的"原地替换资产"做法，**没有另打 `v1.0.9` tag**（`/api/ping` 仍是 1.0.8，全链路 `P1` 断言也锁在 1.0.8）
- 明细见《测试报告_v1.0.8.md》§16


### 1.0.6 (2026-07-11)

**新增**:
- 单文件独立运行（303 MB exe）
- 磁力链接确认窗口
- 重复种子检测与清理
- 墓碑机制（防重启复活）
- 伪文件名过滤
- 快照复用（减少 RPC）
- 设置热更新优化

**修复**:
- 日志 96% 空白行
- 单文件 exe 启动崩溃
- 磁力链接静默建任务
- 重复种子噪音任务
- 已删任务重启复活
- 伪文件名固定 out
- 设置变更触发无关 RPC

**测试**:
- Core 132/136 + 18/18 隔离
- Extension 56/56
- UiTests 8 截图
- E2E 24/24
- URL 解析 19/19

### 1.0.5 (2026-07-10)

**新增**:
- BT/磁力链接支持
- 做种管理
- 主题系统
- 进度浮窗

**修复**:
- 断点续传
- 文件名解析
- 设置持久化

### 1.0.4 (2026-07-09)

**新增**:
- 浏览器扩展
- 系统托盘
- 通知

### 1.0.3 (2026-07-08)

**新增**:
- 基础下载功能
- 任务管理

### 1.0.2 (2026-07-07)

**新增**:
- 项目初始化
- Aria2 集成

### 1.0.1 (2026-07-06)

**新增**:
- 项目骨架

### 1.0.0 (2026-07-05)

**新增**:
- 项目创建

---

## 附录

### A. 环境变量

| 变量 | 说明 |
|------|------|
| `%APPDATA%\KokonaDownloader` | 应用数据目录 |
| `%LOCALAPPDATA%\KokonaDownloader\Standalone` | 单文件运行时目录 |
| `%TEMP%\kokona_clean_test` | 干净目录测试 |

### B. 端口

| 端口 | 用途 |
|------|------|
| 16800 | HTTP API（扩展 → 客户端） |
| 16801 | JSON-RPC（客户端 → Aria2） |

### C. 密钥

| 密钥 | 说明 |
|------|------|
| `apiSecret` | API 鉴权密钥（`settings.json`） |
| `rpc-secret` | Aria2 RPC 密钥（同 `apiSecret`） |

### D. 文件位置

| 文件 | 位置 |
|------|------|
| `settings.json` | `%APPDATA%\KokonaDownloader\` |
| `tasks.json` | `%APPDATA%\KokonaDownloader\` |
| `notified.json` | `%APPDATA%\KokonaDownloader\` |
| `trackers.json` | `%APPDATA%\KokonaDownloader\` |
| `app.log` | `%APPDATA%\KokonaDownloader\` |
| `aria2.log` | `%APPDATA%\KokonaDownloader\engine\` |
| `aria2.pid` | `%APPDATA%\KokonaDownloader\engine\` |
| `aria2.session` | `%APPDATA%\KokonaDownloader\engine\` |
| `dht.dat` | `%APPDATA%\KokonaDownloader\engine\` |
| `tombstones.json` | `%APPDATA%\KokonaDownloader\engine\` |

### E. 参考链接

- [Aria2 官方文档](https://aria2.github.io/manual/en/)
- [WinUI 3 文档](https://learn.microsoft.com/windows/apps/winui/)
- [Manifest V3 文档](https://developer.chrome.com/docs/extensions/develop/concepts/extension-manifest-v3)
- [GitHub 仓库](https://github.com/Hariteki/KokonaDownloader)

---

**文档结束**
