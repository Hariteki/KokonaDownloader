# 海兔下载器 · 全量测试补充与性能审查报告（第二轮）

> 测试日期：2026-09-16
> 被测提交：`1c16d02`（main；`git status` 仅 4 个历史未跟踪文件 + 本轮新增测试）
> 被测版本：**1.0.6**
> 报告性质：**新增测试 + 审查**。产品源码（`src/KokonaDownloader.App`、`src/KokonaDownloader.Core`、`extension/`）**一行未改**；
> 本轮只改动/新增了**测试代码与测试夹具**（见第六节改动清单）。
> 上一轮报告（`测试报告-全量测试与代码审查.md`，同一提交）的结论本轮做了复核与量化，标记为「历史项复核」。

---

## 〇、结论摘要

**总体结论：主程序功能面继续实测通过（E2E 24/24、UI 冒烟 8 图、扩展契约全绿）；本轮把测试从 90 项扩到 136 项、扩展断言从 20 扩到 56 条，新增测试抓到 1 个真实缺陷；性能方面的主要问题不在「算得慢」，而在「一个下载任务 = 一个常驻窗口」带来的内存/句柄线性增长，以及一个永不轮转、96% 是空白的日志文件。**

| 维度 | 结论 |
| --- | --- |
| 编译 | ✅ `0 warning / 0 error` |
| Core 测试（原 90 项） | ✅ 90/90（单跑，59s） |
| Core 测试（扩到 136 项） | ⚠️ **134/136**：1 项暴露**真实缺陷**（畸形种子崩溃），1 项是**既有夹具竞态**（BT 位图用例，4 次运行中失败 2 次） |
| 扩展逻辑测试 | ✅ 20/20 → **56/56**（本轮 +36 条断言） |
| UI 冒烟（UiTests） | ✅ exit 0，8 张截图齐全 |
| 端到端（真实应用 + 真实 API） | ✅ **24/24**（上一轮 23/24 的唯一失败已定性为**测试服务器缺陷**，本轮修复夹具后转绿，见 §5.1） |
| 断点续传 | ✅ 新增确定性用例：在途暂停 → 恢复 → 服务端 206 → 续传至完成，内容逐字节一致 |
| 性能/资源 | ❌ 见 §4：**每任务 +1 窗口 / +88 句柄 / +3.28 MB**；空闲 CPU 2.45%（0 任务）→ 5.0%（200 任务）；`app.log` 44.6 MB 且 **96.2% 是空白行**，仍以 ~8 MB/天 增长 |
| 安全 | ❌ 3 个**已入库**的 `tools/*.ps1` 里硬编码着**当前生效的** API 密钥（历史项 A-5，本轮确认仍然live） |

**最该优先处理的 5 件事**

1. **P1 每任务一个进度小窗（新增量化）**：浏览器扩展连下 20 个链接 = 桌面弹 20 个窗口、多占 65 MB 内存、1 762 个句柄；200 个任务实测 **729 MB / 18 000 句柄 / ~200 个窗口**，且窗口在任务完成后**不自动关闭**，每个还带一个 500 ms 轮询（200 窗口 = 400 RPC/s）。
2. **P3 日志无上限（历史项 B-18 量化）**：`app.log` 已 44.6 MB / 641 954 行，其中 **96.2% 是 aria2 控制台刷屏产生的空白行**，空闲时仍以 ~100 B/s（≈8 MB/天）生长，永不轮转。
3. **D1 新缺陷**：`BtHashUtil.FromTorrent` 对畸形 `.torrent` 抛 `IndexOutOfRangeException`（bencode 长度前缀整数溢出），已附最小复现与一行修复。
4. **A-5 密钥入库**：`a938ca540c347dbdcaf263fe86250661` 与用户当前密钥**完全一致**，出现在 3 个已推送的公开仓库文件里。
5. **测试门禁不可信（历史项 A-4 的第二个来源）**：除已修的 Range 夹具缺陷外，`BtIntegrationTests.限速下载中分片位图渐进填充` 存在**独立的夹具竞态**，机制已定位（见 §5.2）。

---

## 一、全量测试执行结果（可复现）

### 1.1 环境

| 项 | 值 |
| --- | --- |
| 仓库 | `D:\Project\kokonaDown` |
| 提交 | `1c16d02` |
| .NET SDK | 8.0.401 |
| PowerShell | 5.1.26100.7920 |
| Node | `D:\DeepSeek\nodejs\node.exe` v24.19.0（**不在 PATH**，`.verify` 下的 mjs 脚本需全路径） |
| 屏幕 / 应用数据 | 1920×1080 @125% / `%APPDATA%\KokonaDownloader\` |

### 1.2 结果矩阵

| # | 测试 | 命令 | 文档期望 | 实测 | 判定 |
| --- | --- | --- | --- | --- | --- |
| 1 | 编译 | `dotnet build …App.csproj -c Release -r win-x64` | 0 error | 0 warning / 0 error（4.9 s） | ✅ |
| 2 | Core（原套件） | `dotnet test …Core.Tests -c Release` | 90/90 | **90/90**（59 s） | ✅ |
| 3 | Core（扩充套件） | 同上 | — | **134/136**（2 m 1 s） | ⚠️ 见 §3/§5 |
| 4 | 扩展逻辑 | `cscript //nologo tests\test_logic.js` | 20/20 | **56/56** | ✅ |
| 5 | UI 冒烟 | `dotnet run --project …UiTests -c Release` | exit 0 + 7 图 | **exit 0 + 8 图** | ✅ |
| 6 | E2E（真实应用） | `node .verify\functest\test.mjs` | — | **24/24**（修夹具前 23/24） | ✅ |
| 7 | 续传对照 | `node .verify\functest\range.mjs` | — | `RESULT: PAUSE/RESUME OK` | ✅ |
| 8 | 限速热更新 | `node .verify\functest\hotapply.mjs` | — | aria2 内 `max-overall-download-limit` 1→2 MB/s 立即生效 | ✅ |
| 9 | 多 URL 语义 | `node .verify\functest\multi.mjs` | — | 2 URL → **1 任务 1 文件**（镜像语义，与新增单测一致） | ✅ |
| 10 | 性能/资源 | `.verify\perf\perf.ps1`、`windowcost.ps1` | — | 见 §4 | ❌ 发现多项 |

> 说明：`tools\verify_transparency8.ps1` 等透明度脚本本轮**未重跑**（需人工目视 + 已有入库基线截图），其 DPI 坐标系问题仍以上一轮报告 A-7 为准。

### 1.3 关键实测

- 原套件 90 项在**安静机器**上 59 s 全绿；扩充后 136 项 2 m 1 s。
- 同一台机器**带负载**（我先前的并发构建/cscript 同时跑）时，会出现额外失败：`ResumeRangeTests`（超时）、`ApiContractGapsTests`（竞态）、`BtIntegrationTests`（位图）。**这本身就是结论**：本套件对 CPU 争用敏感，见 §5.3。

---

## 二、本轮补充的测试（文档与既有套件都没覆盖的部分）

### 2.1 为什么补这些

按维护手册 §7「待办」与上一轮报告的分类，以下内容**没有任何测试覆盖**，本轮逐条补齐：

| 未覆盖项 | 出处 | 风险 |
| --- | --- | --- |
| 断点续传（服务端支持 206 时） | 手册 §7.1、上轮 A-4 | 唯一相关用例「暂停与恢复」实际测不出续传，且不稳定 |
| Range/206 分片下载 | 上轮 §1.3 | 多线程下载链路无断言 |
| `duplicate:true` / `confirm:true` 契约 | 上轮 C-8 | 扩展 background.js 直接依赖这两个字段，无测试 |
| 未知设置字段 / 空补丁容错 | v1.0.5 `FINAL_REPORT` 遗留项 | 曾导致 500，无回归护栏 |
| infohash 工具（磁力/base32/种子/malformed） | 无 | BT 重复预检唯一输入来源，畸形输入未测 |
| 墓碑（Mark/Unmark/会话过滤/上限/损坏） | 无 | 防任务复活机制完全没有测试 |
| TaskStore 防抖/原子性/并发 | 部分 | 断点续传元数据可能丢 |
| SettingsStore 损坏后果（密钥重生成） | 仅「不抛异常」 | 扩展静默失联 |
| NotifiedStore 去重/上限 | 无 | 通知重复 |
| 扩展 `normalizeSettings`/`urlKey`/`isSupportedUrl`/`baseName` | 无 | 用户粘贴配置的容错路径 |

### 2.2 新增测试清单（全部实际运行过）

| 文件 | 用例数 | 覆盖什么 | 结果 |
| --- | --- | --- | --- |
| `src/KokonaDownloader.Core.Tests/TestEnv.cs`（改） | — | FileServer 支持 **HTTP Range/206**、`RangeRequestCount` 计数、慢速下发（drip）参数 | — |
| `ResumeRangeTests.cs`（新） | 2 | ①在途暂停→恢复→206 续传→内容逐字节一致；②多连接分片确实走 206 且内容完整 | ✅ 2/2（单跑） |
| `ApiContractGapsTests.cs`（新） | 9 | duplicate=true 且不重复建任务、单磁力 confirm=true 且不静默建任务、非法 JSON→400、未知字段忽略→200、空补丁不改设置、多 URL 镜像语义、gid 非法格式→404、gid 不存在→400、错误方法→404 | ✅ 9/9 |
| `BtHashUtilTests.cs`（新） | 17 | 磁力 40hex/大写/参数顺序/32 位 base32；8 种无法解析输入→null；种子与构造器 infohash 一致；截断输入不抛异常；**畸形长度前缀不抛异常** | ❌ 16/17（1 项暴露真实缺陷 D1） |
| `StoreRobustnessTests.cs`（新） | 18 | 墓碑：命中剔除/目录不同不误删/撤销/无墓碑不动文件/1000 上限保最近/损坏文件/原子写；TaskStore：防抖落盘/编号保留/并发 200 条/损坏恢复；SettingsStore：损坏回退默认/密钥重生成/原子写/未变更不写；NotifiedStore：去重+上限/损坏；**墓碑与真实 aria2 的目录口径一致性**（集成） | ✅ 18/18 |
| `tests/test_logic.js`（改） | +36 条断言 | normalizeSettings（协议前缀/端口串/host:port/越界/空白/autoCapture/secret）、baseUrl、isSupportedUrl（ftp/magnet/大小写/空白/file/data/空）、isOwnApiUrl（自环/异端口）、baseName（Windows/POSIX/尾分隔符）、urlKey（确定性/区分/去锚点/保留 query/hex）、shouldCapture 补充组合 | ✅ 56/56 |

合计：**Core 90 → 136 项（+46）**；**扩展断言 20 → 56 条（+36）**。

### 2.3 新增测试的产出

- **1 个真实缺陷**（D1，见 §3.1）。
- **1 条现状记录**：`normalizeSettings` 会剥掉 host 里的 `:port` 但**不会采用它**（用户在 host 框粘贴 `127.0.0.1:17000` 会退回默认 16800）；已用断言固定现状并在 §3.2 说明。
- **1 条契约固定**：`/api/download` 的多 URL 是**镜像语义**（一个任务的多个 URI），与 UI 路径（多个 URL → 多个任务）不同；已用单测 + `multi.mjs` 双重固定。

---

## 三、缺陷清单

### 3.1 D1（本轮新发现，真实缺陷）：畸形 `.torrent` 使 `BtHashUtil.FromTorrent` 抛异常

**现象**：`bt.addTorrent` 前的 infohash 计算在畸形 bencode 上抛 `IndexOutOfRangeException`。

**根因**（`src/KokonaDownloader.Core/Engine/BtHashUtil.cs:82-93`）：`ReadString` 用
`pos = start + l` 前只校验 `l < 0`，未校验上界。当长度前缀接近 `int.MaxValue` 时
`start + l` **整数溢出为负数**，随后 `pos <= d.Length` 判为 true，循环里 `data[pos]` 以负下标访问数组。

**最小复现**（新增用例 `BtHashUtilTests.种子_畸形长度前缀不抛异常`）：

```csharp
BtHashUtil.FromTorrent(Encoding.ASCII.GetBytes("d4:name2147483647:x"));
// 期望 null，实际 System.IndexOutOfRangeException: Index was outside the bounds of the array.
```

**影响面**：仅 UI 路径（`NewDownloadDialog` 选 `.torrent` → `AddTorrentAsync` → `CalculateInfoHash`）。
API 不接收种子字节，故外部脚本无法触发。表现是「添加失败: Index was outside the bounds of the array.」，不损坏数据。
**严重度：低**；但它是**用户提供的任意二进制**上的未捕获异常，应从防御性编程角度修掉。

**建议修复（一行）**，`ReadString` 内：

```csharp
if (!int.TryParse(...) || l < 0) return false;
if (l > d.Length - start) return false;   // ← 新增：上界校验，杜绝 start + l 溢出
```

修完 `BtHashUtilTests` 即 17/17。

### 3.2 D2（现状记录，非缺陷但值得产品决策）：host 里的端口被丢弃

`extension/logic.js:26-31` 的注释声称容错「host:port」，实现却只截断不采用。用户在设置页 host 框粘贴
`127.0.0.1:17000` 会得到 host=127.0.0.1 + port=16800（默认），表现为「明明填了却连不上」。
建议：截断时若发现 `:port` 且 port 字段为空/默认，则采用该端口；或在 UI 上明确提示。

### 3.3 历史项复核（上一轮报告的 A/B/C 清单现状）

| 编号 | 上一轮结论 | 本轮复核 |
| --- | --- | --- |
| A-1/A-3 扩展「先转发后取消」时序 | 高危 | **仍然存在**。`background.js:244-251`：`forwardingNow` 占位在 `shouldCapture()` **之前**注册，命中占位时**直接 `cancelAndErase` 且不做任何 `shouldCapture`/`autoCapture` 判断** → 关闭自动接管时，同 URL 的第二个下载项会被静默取消并抹除（既没转发也没留给浏览器）。 |
| A-2 转发不带 Cookie/UA | 高危 | **仍然存在**。`logic.js:106-114` 只发 `urls/filename/referer`；需登录态的下载在浏览器副本被取消后必然失败。 |
| A-4 「暂停与恢复」不稳定 | 中危 | **已定性并修复夹具**：根因是 `TestEnv.FileServer` 不支持 Range。本轮补上 206 后，新增确定性用例通过，E2E 也从 23/24 → **24/24**（§5.1）。**另发现第二个、独立的夹具竞态**，见 §5.2。 |
| A-5 `tools/` 硬编码真实密钥 | 高危 | **仍然存在且确认 live**：`a938ca540c347dbdcaf263fe86250661` 与当前 `settings.json` 的 `apiSecret` **完全一致**，出现在 `tools/verify_desktop_launch.ps1:34`、`verify_singlefile_main.ps1:36`、`verify_singlefile_fallback.ps1:4`，均已推送公开仓库。 |
| A-6 UiTests 删除用户 `aria2.session` | 中危 | **仍然存在**（`Program.cs:524-536` 直接 `File.Delete`，无备份/无恢复）。本次运行前该文件恰为 0 字节故未丢数据（已先备份 10 个文件到 `%TEMP%`）。 |
| A-7 透明度脚本 DPI 混用 | 低危 | 未复验（需目视），以上一轮为准。 |
| B-18 app.log 无上限 | 中危 | **确认并量化**：44.6 MB / 641 954 行，96.2% 为空白行，~8 MB/天（§4.3）。 |
| B-21 `MinSizeMap` 不回收 | 低危 | 仍然存在（`WindowEffects.cs:71`，按 hwnd 累加、无移除）。 |
| C-8 `confirm`/`duplicate` 无测试 | 中危 | **已补**（`ApiContractGapsTests`，9/9 通过）。 |
| C-10 空闲仍轮询 | 中危 | **确认并量化**：0 任务时空闲 CPU 已 2.45%，200 任务时 5.0%（§4.2）。 |

---

## 四、性能与资源开销专项（实测）

> 测量方法：`.verify\perf\perf.ps1`、`.verify\perf\windowcost.ps1`（未入库，`.verify/` 已被 gitignore）。
> 全部为**真实应用 + 真实 aria2 + 回环文件服务器**，采样 30 s；测量后已还原 `settings/tasks/notified/tombstones`。

### 4.1 P1（最严重）：一个下载任务 = 一个常驻窗口

**20 个任务的实测增量**（同一进程内前后对比）：

| 指标 | 0 任务 | 20 任务 | 增量 | 每任务 |
| --- | --- | --- | --- | --- |
| 顶层窗口数 | 6 | 26 | **+20** | **+1 个窗口** |
| 可见窗口数 | 1 | 21 | +20 | +1 |
| **句柄数** | 2 235 | 3 997 | +1 762 | **+88.1** |
| **工作集** | 203.3 MB | 268.9 MB | +65.6 MB | **+3.28 MB** |
| 线程数 | 63 | 69 | +6 | +0.3 |
| 进度小窗创建日志 | 283 | 303 | +20 | +1 |

**200 个任务的实测**（`perf.ps1`）：

| 指标 | 0 任务 | 200 任务 |
| --- | --- | --- |
| 应用空闲 CPU | 2.45 %（单核） | **5.00 %** |
| aria2 空闲 CPU | 0.52 % | 2.71 % |
| 工作集 / 私有内存 | 196.6 MB / 110.4 MB | **729.1 MB / 514.8 MB** |
| 句柄数 | 2 202 | **18 000** |

**机制**（`MainWindow.xaml.cs:232-264` + `DownloadEngine`）：引擎把每个非磁力新任务的 gid 记入 `_newTaskGids`，
轮询首次上报时带 `IsNewTask=true`；UI 收到即为该 gid `new ProgressWindow(...)`。**没有并发上限、没有
「仅用户手动新建才弹」的过滤**，而且任务结束后小窗按设计**不自动关闭**（仅显示「打开文件夹」）。
扩展在后台连发 N 个链接 = 桌面弹 N 个窗口。

**连带成本**：每个 ProgressWindow 还带 ①500 ms 一次 `GetTaskAsync` 轮询（200 窗口 → **400 RPC/s**）、
②一次原生 Acrylic 控制器、③一条 `ThemeService` 注册（每次设置变更都要遍历它）。

**建议**：只对「本会话内**用户主动**发起的单个任务」弹窗（例如 API 来源的打标记后不弹或合并成 1 个）；
对同一批次的多个任务合并为一个「批量进度」窗；给同时存在的窗口数设上限（如 3 个，其余只进托盘/列表）；
任务完成后按设置自动关闭。

### 4.2 P2：空闲轮询成本

| 场景 | 应用 CPU（单核占比） | 说明 |
| --- | --- | --- |
| 0 任务 | **2.45 %** | 完全空闲也在烧 |
| 200 任务 | **5.00 %** | 线性增长 ≈ 每任务 +0.013 % |

**每秒钟的 RPC 账**（`W` = 进度小窗数）：

| 来源 | 频率 | RPC 次数/秒 |
| --- | --- | --- |
| 引擎轮询 `PollLoop`（1 次 multicall 含 4 个子调用） | 800 ms | 1.25 |
| 主界面 `_timer` → `RefreshAsync`（`GetAllTasksAsync` 3 次 + `GetGlobalStat` 1 次） | 900 ms | 4.44 |
| 每个进度小窗 `GetTaskAsync` | 500 ms | 2 × W |
| **合计** | | **≈ 5.7 + 2W** |

W=0 → ≈5.7 次/秒；W=200 → **≈406 次/秒**（全部打到本机 aria2）。

**建议**：①把 `GetAllTasksAsync` 的 3 次单调用改成 **1 次 multicall**（引擎已有 `MultiCallAsync`）；
②小窗不必自己轮询——引擎已经有 `EngineEvent`，订阅即可（可省 2W）；
③空闲（无活动任务）时把间隔退避到 2–3 s；
④轮询 payload 里 `tellStopped(0,1000)` 每次都把最多 1000 个已结束任务的完整状态（含 BT bitfield hex）反序列化一遍，
建议在 `numStopped` 未变化时跳过或减少取数。

### 4.3 P3：`app.log` 无上限，且 96% 是垃圾行

| 指标 | 实测 |
| --- | --- |
| 文件大小 | **46 783 068 B（44.6 MB）** |
| 行数 | **641 954** |
| `[aria2]` 前缀行 | 639 481（**99.6 %**） |
| 其中**纯空白行** | **617 377（96.2 %）** |
| 空白行产生速率 | **0.452 行/秒**（≈39 000 行/天） |
| 空闲时增长 | **100–105 B/s ≈ 8.2–8.65 MB/天**（0 任务与 200 任务几乎相同） |
| 跨度 | 2026-08-31 → 09-16（16 天） |

**根因**：`Aria2Process.cs:112` 把 aria2 的**每一行** stdout 都写进日志：

```csharp
_process.OutputDataReceived += (_, e) => { if (e.Data != null) _log?.Invoke($"[aria2] {e.Data}"); };
```

而 aria2 以 `--quiet=false` 启动（`--console-readout` 默认开启），会**每秒刷新一行**控制台状态（内容多为空白/进度条残影）。
`App.Log` 又是**每行一次 `File.AppendAllText`**（打开→写→关闭），既是磁盘 I/O 也是句柄开销。

**建议**：①过滤 `string.IsNullOrWhiteSpace(e.Data)`；②更彻底地用 `--show-console-readout=false`（或 `--quiet=true`）关掉刷屏，
只保留 `--log=`（level=warn）那份日志；③`app.log` 加**按大小轮转**（如 5 MB × 2 份）并保留一个 `App.Log` 的批量写缓冲。

### 4.4 P4：主题刷新与设置变更的放大效应

- `AppHost.OnSettingsChanged`（`AppHost.cs:61-74`）：**任何**设置变化都会执行 2 次 aria2 RPC（`SetGlobalSpeedLimitAsync` + `SetBtTrackersAsync`）。
  磨砂浓度滑块拖动、切换主题色、改通知开关……都会顺带改一次全局限速与 tracker。
- `ThemeService.Apply()`（`ThemeService.cs:159-171`）：重建约 100 个资源键 + 遍历**所有已注册窗口**（含每个进度小窗）各做
  `ApplyCaption/ApplyTransparency/ForceThemeResourceRefresh`。
- `WindowEffects.ConfigureForWindow`（`WindowEffects.cs:250-275`）：**每次调用都新增 3 个事件处理器**
  （`window.Activated`、`root.ActualThemeChanged`、`window.Closed`）且**从不退订**。窗口存活期内
  `Apply()` 调用 N 次就累积 3N 个处理器；`window.Closed` 里还闭包持有着**已释放的**控制器对象。

**实测**：30 次 `POST /api/settings` 共 105 ms（3.5 ms/次，含 200 个窗口在册）——单次成本不高，
但 **T 次设置变更 × W 个窗口 × 3 个处理器** 的累积是真实的泄漏路径，且每次滑块 tick 都触发全量 `Apply`。

**建议**：①`OnSettingsChanged` 按字段变化决定要发的 RPC（比较前后差值）；
②`ConfigureForWindow` 改为**先退订再订阅**（或在 `ApplyTransparency` 释放控制器时一并移除处理器）。

### 4.5 P5：其余（低）

| 编号 | 问题 | 位置 |
| --- | --- | --- |
| P5-1 | 磨砂滑块每 tick 都 `Settings.Update` → **整份 settings.json 写盘**（无防抖）+ `Changed` 全链路 | `MainWindow.xaml.cs:515-521`、`SettingsStore.Update` |
| P5-2 | 每次 `/api/download` 都做重复预检 → **3 次额外 RPC**（`GetAllTasksAsync`），与轮询快照重复 | `ApiService.cs:283` |
| P5-3 | `_btHashIndex` 每轮询 `new ConcurrentDictionary`（单写者+volatile 交换其实用普通 Dictionary 即可） | `DownloadEngine.cs:42` |
| P5-4 | `_lastStates` 对终态任务不清理，随 aria2 stopped 列表增长 | `DownloadEngine.cs:37` |
| P5-5 | `BtPieceGrid.ParseBits` 每次刷新 `new bool[numPieces]`（10 万分片 = 每次 100 KB 分配，随轮询触发） | `BtPieceGrid.xaml.cs:229` |
| P5-6 | `MinSizeMap` 按 hwnd 只增不减 | `WindowEffects.cs:71` |
| P5-7 | aria2 `stopped` 列表只增不减（应用侧从不 `removeDownloadResult` 已完成任务），会话文件随之变大 | `DownloadEngine` |

---

## 五、测试基础设施问题（本轮新发现）

### 5.1 已修：`FileServer` 不支持 Range（历史项 A-4 根因）

`TestEnv.FileServer` 原先对任何请求都回整文件 200。aria2 恢复/分片时发 `Range: bytes=a-b`，
服务端回 200 → aria2 判 `errorCode=8 Invalid range header` → 任务失败。这就是「暂停与恢复」偶发失败的全部原因。

本轮为夹具补上 206 + `Content-Range` + `Accept-Ranges` + `RangeRequestCount` + 慢速下发（drip）。
**证据链**：

- 修 `test.mjs`（`.verify`，加 Range）前后：`23/24 → 24/24`，失败项从 `resume -> active again state=failed` 变为通过；
  服务器日志显示恢复后 aria2 发出 **8 个 Range 请求**（`bytes=655360-1048575` 等）并全部 206 成功；
- 新增 `ResumeRangeTests`：在途暂停（已下载 >0 且 <总长）→ 恢复 → 断言 `RangeRequestCount` 增长 → 内容逐字节一致。

### 5.2 未修：`BtIntegrationTests.限速下载中分片位图渐进填充` 的夹具竞态

**现象**：4 次全量运行中失败 2 次，失败断言为 `Assert.Equal(16, final.NumPieces)`，**Actual = 1**（50 s）。

**机制**：`PollBtTaskAsync`（`BtIntegrationTests.cs:230-246`）先按「`Gid != metaGid` 且名字等于种子名」找真实任务，
找不到则**回退到元数据任务**。磁力两阶段里，元数据任务完成后会短暂以 `Completed` 留在 `tellStopped`
（引擎随后才 `removeDownloadResult`）；若测试的快照恰好落在这个窗口、且真实任务尚未在同一快照里可见，
回退分支就返回**元数据任务**（`numPieces=1`）并满足 `State == Completed` → 断言失败。

**结论**：产品侧行为正确（同文件用例 1 已断言 followedBy 归并、gid 变化、位图全 1）；这是**夹具竞态**，
但它让「90/90 = 发布门禁」继续不可靠。**建议**：回退分支要求 `t.NumPieces > 1`（或要求 `t.Gid != metaGid` 才判定完成），
并给 `PollBtTaskAsync` 加「已见到真实任务」的状态机。

### 5.3 套件对 CPU 争用敏感

同一套件在**带负载**时（并发跑构建/脚本）会额外失败（`ResumeRangeTests` 超时、`ApiContractGapsTests` 竞态、
`BtIntegrationTests` 位图），安静机器上则通过。原因是多个测试类并行（xUnit 默认按 collection 并行）且大量
真实 aria2 + 回环下载 + 时间敏感断言。**建议**：把 BT/时序敏感用例收进同一个 collection（串行），
或在 `xunit.runner.json` 里关掉并行。

### 5.4 测试残留

- 一次**通过**的全量运行后仍发现 **5 个孤儿 `aria2c`**（`--rpc-secret` 分别为 `resume-secret`/`test-secret-123`/`engine-secret`×3）。
  每个测试用例用**新的 GUID 工作目录**，而 `Aria2Process.CleanupOrphan()` 只看自己工作目录里的 `aria2.pid`，
  所以这些孤儿**永远不会被回收**，会跨运行累积。
- `%TEMP%\kokona_dl_test` 累积了 **39 个**残留工作目录（52 个文件，体积很小 <0.05 MB），
  因为 `Directory.Delete(_workDir, true)` 在 aria2 仍持有文件时会被吞掉异常。本轮已清理。
- **建议**：`DisposeAsync` 里先 `KillNow()` 再删目录；给测试用 aria2 统一命名（如 `--rpc-secret` 带 `kokona-test-` 前缀）
  并在套件启动时按前缀清理历史孤儿。

---

## 六、本轮改动清单（只动测试，未动产品代码）

| 文件 | 改动 | 入库 |
| --- | --- | --- |
| `src/KokonaDownloader.Core.Tests/TestEnv.cs` | FileServer 支持 Range/206、`RangeRequestCount`、`AddFile(..., chunkBytes, chunkDelayMs)` 慢速下发、`WriteBody`/`TryParseRange` | ✅ 是（源码库内） |
| `src/KokonaDownloader.Core.Tests/ResumeRangeTests.cs` | 新增 2 项 | ✅ 是 |
| `src/KokonaDownloader.Core.Tests/ApiContractGapsTests.cs` | 新增 9 项 | ✅ 是 |
| `src/KokonaDownloader.Core.Tests/BtHashUtilTests.cs` | 新增 17 项 | ✅ 是 |
| `src/KokonaDownloader.Core.Tests/StoreRobustnessTests.cs` | 新增 18 项 | ✅ 是 |
| `tests/test_logic.js` | +36 条断言（20 → 56） | ✅ 是 |
| `.verify/functest/test.mjs` | 测试服务器加 Range（**仅夹具**，用于给 A-4 定性） | ❌ 否（`.verify/` 已 gitignore） |
| `.verify/perf/server.mjs`、`perf.ps1`、`windowcost.ps1` | 性能/资源探针 | ❌ 否（`.verify/`） |
| `测试报告-全量测试补充与性能审查.md` | 本报告 | 未跟踪（与上一轮报告一致） |

**注意**：`TestEnv.cs` 的改动会同时影响既有用例——「暂停与恢复」现在实际走 206 续传路径，**语义变强**（不再可能「在下载开始前就暂停」而假通过）。若要让既有门禁保持"原样通过"，无需回退：安静机器上 90 项原套件仍全绿。

**环境还原**：测量期间被改写的 `settings.json` / `tasks.json` / `notified.json` / `tombstones.json` 已由脚本自动还原；
`aria2.session` 已恢复为测量前的 0 字节；39 个残留测试目录与孤儿进程已清理。

---

## 七、建议修复顺序

| 顺序 | 项 | 理由 | 成本 |
| --- | --- | --- | --- |
| 1 | **A-5 密钥脱敏**（tools/ 3 个脚本改为读 `settings.json` 或环境变量）+ 轮换密钥 | 已推送的公开仓库 + 与线上密钥一致 | 10 分钟 |
| 2 | **P1 进度小窗上限/批量合并** | 用户可直接感知：内存、句柄、满屏窗口 | 半天 |
| 3 | **P3 日志过滤空行 + 轮转** | 一条 `if` 即砍掉 96% 的日志量，再加轮转 | 30 分钟 |
| 4 | **D1 `ReadString` 上界校验** | 一行，用户输入上的未捕获异常 | 5 分钟 |
| 5 | **A-1/A-3 占位取消逻辑**（`forwardingNow` 命中时也走 `shouldCapture`/`autoCapture` 判定） | 会静默吞掉用户的浏览器下载 | 1 小时 |
| 6 | **A-6 UiTests 备份/恢复 `aria2.session`** | 保护用户断点续传状态 | 20 分钟 |
| 7 | **§5.2 / §5.3 测试夹具竞态与并行度** | 让「全绿」重新成为可信门禁 | 1–2 小时 |
| 8 | **P2 轮询优化**（multicall 合并 + 小窗订阅事件 + 空闲退避） | 空闲 CPU 与 RPC 量 | 半天 |
| 9 | **P4 设置变更按字段触发 + 事件处理器退订** | 消除长会话下的句柄/内存缓慢增长 | 半天 |
| 10 | A-2（转发带 Cookie/UA，需产品决策）、P5 系列 | 视产品取舍 | — |

---

## 八、声明

- **产品代码零改动**：`src/KokonaDownloader.App`、`src/KokonaDownloader.Core`、`extension/` 本轮均未修改；
  报告中每条结论都附了文件:行号或实测数据。
- **测试代码改动**：新增 4 个测试文件 + 扩展 `TestEnv` 夹具 + 扩写 `tests/test_logic.js`（详见第六节）。
- **当前套件状态**：Core **134/136**（1 项为真实缺陷 D1 的回归护栏，默认保持失败；1 项为既有夹具竞态 §5.2），
  扩展 **56/56**，UiTests exit 0，E2E **24/24**。若希望立刻全绿：按 §7-4 修 D1（一行），并在 §5.2 的夹具回退分支加 `NumPieces > 1` 约束。
- **未复验项**：透明度三模式的像素级证据（A-7）需目视/真实桌面环境，仍以上一轮报告的复验结论为准。

---

## 九、修复记录与复测结果（第三轮，2026-09-16）

> 用户指示"按照测试结果进行修复"后执行。产品代码改动**仅限** §7 建议清单内、且由本轮测试/审查直接证实的项；
> A-2（转发带 Cookie/UA）与 P5-1（设置写防抖）按报告结论**未动**（前者需产品决策，后者会破坏 `Stage2UnitTests.保存与重新加载` 依赖的写穿语义）。

### 9.1 已实施的产品修复（对应 §7 顺序）

| # | 项 | 改动 | 验证 |
| --- | --- | --- | --- |
| 1 | **A-5 密钥脱敏** | `tools\verify_singlefile_main.ps1` / `_fallback.ps1` / `verify_desktop_launch.ps1`：删除硬编码的线上 `apiSecret`（`a938ca…0661`）与 `X-Kokona-Secret` 头，ping 改走免鉴权路径；三个脚本 BOM 完好、`Parser::ParseFile` 0 错误 | 脚本可独立运行；**注意：该密钥已进 git 历史且与线上一致，轮换仍由用户决定** |
| 2 | **P1 进度小窗配额** | `MainWindow.xaml.cs`：`MaxLiveProgressWindows=6` / 突发限流（5s 内最多新建 3 个）/ 超限时回收最旧的已结束窗口复用 | 编译通过；UiTests/E2E 复测（见 9.3） |
| 3 | **P3 日志** | `App.xaml.cs`：`Log` 跳过空白行 + 5MB 轮转（`app.log.1`）；`Aria2Process.cs`：aria2 加 `--show-console-readout=false`（已用 `aria2c --help=#all` 验证参数存在） | 编译通过；性能探针复测（见 9.3） |
| 4 | **D1 `ReadString` 上界校验** | `BtHashUtil.cs`：长度前缀越界（含 int 溢出为负）时返回 false 而非抛异常 | 回归护栏 `种子_畸形长度前缀不抛异常` 通过；`BtHashUtilTests` 17/17 |
| 5 | **A-1/A-3 占位取消逻辑** | `extension/background.js`：`handleDownloadCreated` 先 `shouldCapture` 判定、命中才走 `forwardingNow` 的取消/擦除分支——不再静默吞掉用户下载 | `test_logic.js` 56/56（含新增断言）；E2E 复测 |
| 6 | **A-6 UiTests 会话保护** | `UiTests/Program.cs`：`CleanEngineSession()` 前备份 `aria2.session`，`finally` 里恢复/删除 | UiTests 全流程通过（见 9.3） |
| 7 | **§5.2 夹具竞态** | `BtIntegrationTests.PollBtTaskAsync`：磁力用例只认真实任务（`expectFollowedBy`），不再回退到元数据任务 | 该用例在全量运行中稳定通过 |
| 8 | **P2 轮询优化** | `Aria2RpcClient.TellAllAsync`（一次 multicall 拿齐三类列表）；`DownloadEngine`：轮询发布快照 + 空闲退避 2.5s + 增删任务时 `InvalidateSnapshot()`；`FindActiveDuplicatesAsync` 复用 1.2s 内快照 | `ApiContractGapsTests` 9/9（含"重复链接返回 duplicate"）；全量复测 |
| 9 | **P4 事件退订 + 设置变更按值触发** | `WindowEffects.cs`：`BackdropWiring` 记录并退订 themeChanged/activated/closed 处理器，`DetachBackdropWiring` 供 `ThemeService.ReleaseBackdrop` 调用；`AppHost.cs`：全局限速/tracker 只在**值变化**时发 RPC（带缓存基线） | 编译通过；长会话句柄增长问题消除（探针复测见 9.3） |

### 9.2 测试基建加固（本轮新增，均为测试代码）

- `TestEnv.FileServer`：补 HTTP Range/206（§5.1）；慢速下发改 **async + Task.Delay**（原 `Thread.Sleep` 会占满线程池拖死同进程其它测试——全量运行大面积超时的根因之一）。
- `TestCleanup.cs`（新）：`ProcessExit` 兜底回收 `%TEMP%\kokona_dl_test\**\aria2.pid` 里的残留 aria2c（进程名二次确认，绝不碰用户数据目录）+ 清工作目录。修复"孤儿 aria2 继承 stdout 管道 → `dotnet test | Select-String` 永不返回"。
- `xunit.runner.json`：`maxParallelThreads=1`——本机是日常桌面（常驻重负载应用），8 个引擎类并行会把回环 RPC 拖到超时甚至挂起；串行后峰值负载 = 1 个引擎，套件时长约 4~5 分钟，可接受。
- 7 个 `IAsyncLifetime` 测试类的 `DisposeAsync` 全部改为**逐项判空清理**：高负载下 `InitializeAsync` 中途失败（aria2 15s 未就绪）时，原代码在 `_api.Dispose()` 处 NRE 中断清理 → 引擎轮询循环留在测试进程 + aria2 泄漏（实测一次运行泄漏 5 个）。
- 慢速下发体积压小：`ApiContractGapsTests` 16MB→2MB、`ResumeRangeTests` 8MB→4MB，缩短在途请求占用时间。

### 9.3 复测结果

| 套件 | 结果 | 备注 |
| --- | --- | --- |
| Core 全量（串行） | **132/136**（3 m 10 s） | 4 项均为**负载相关超时**（2×aria2 RPC 15s 未就绪 + 2×HttpClient 10s 超时）；同一批失败类单独重跑 **18/18 全绿（39 s）**，证实为负载相关而非产品回归。详见 §9.4 |
| 扩展逻辑 `test_logic.js` | **56/56** | 含 A-1/A-3 修复后的新断言 |
| App / UiTests 编译 | **0 警告 0 错误**（win-x64） | P1/P2c/P3/P4 全部 UI 层改动可编译 |
| UiTests 宿主 | **exit 0 + 8 图** | ui_01/ui_01b/ui_02~ui_07 齐全；用户 aria2 会话已还原 |
| E2E `.verify\functest\test.mjs` | **24/24** | 真实应用 + 真实 API；含暂停/恢复续传、重复检测、磁力确认流 |
| 性能探针（perf.ps1 / windowcost.ps1） | **显著改善** | 每任务窗口 +1→**+0**、句柄 +88→**+9**（10 倍）、内存 +3.28→**+1.32 MB**（2.5 倍）、日志空白行 96.2%→**0%** 且 5 MB 轮转。详见新报告 §4 |

### 9.4 Core 全量最终数字

**132/136（3 m 10 s）**。4 项失败全部为**负载相关超时**：

| 失败用例 | 错误 | 单独重跑 |
| --- | --- | --- |
| `EngineIntegrationTests.单文件下载完整流程` | aria2 RPC 15s 未就绪 | ✅ |
| `ResumeRangeTests.在途暂停后恢复_服务端支持206时续传至完成且内容一致` | HttpClient 10s 超时 | ✅ |
| `TombstoneEngineContractTests.删除任务后墓碑哈希与会话文件过滤口径一致` | HttpClient 10s 超时 | ✅ |
| `FileNameResolutionTests.临时名URL按ContentDisposition落盘真实文件名` | aria2 RPC 15s 未就绪 | ✅ |

同一批 4 个类单独重跑 **18/18 全绿（39 s）**。裸 `aria2c` 启动 + RPC 应答实测 528~568 ms，远小于 15s 预算。**结论：功能面全绿，4 项超时为测试基建在带负载桌面机器上的启动/轮询超时，非产品缺陷。** 完整复测与性能收益量化见新报告 `测试报告-第三轮修复后全量复测.md`。
