# KokonaDownloader 磁力链接 / 种子下载功能 — 可行性与技术评估报告

> 评估日期：2026-08-31
> 评估范围：BitTorrent / 磁力链接（magnet:）下载支持
> 参考对象：Motrix（<https://github.com/agalwood/Motrix）>
> 本报告为**方案评估文档**，不包含实施代码。

***

## 摘要（结论先行）

**结论：完全可行，且成本远低于预期。推荐采用「aria2 原生 BT 能力」方案（Motrix 同款技术路线）。**

本项目内置的 `vendor\aria2\aria2c.exe`（1.37.0）在编译时已启用 **BitTorrent** 与 **Metalink** 支持（已实测 `--version` 验证）。这意味着：

* ✅ **零新增二进制** —— 不需要引入任何新的可执行文件或下载引擎

* ✅ **零新增依赖** —— 不需要任何新的 NuGet 包 / npm 包

* ✅ **新增体积 ≈ 0 MB** —— 对安装包体积无影响；aria2 BT/DHT 的运行时内存开销约 10\~20 MB，且复用已存在的进程

* ✅ **Motrix 同款架构路线** —— Motrix 的磁力下载同样是纯 aria2 原生能力，本项目的架构（App → JSON-RPC → aria2）与之完全同构

实现工作量集中在 4 个层面：

1. aria2 启动参数补全（约 10 个 BT 相关参数）
2. RPC 层补一个 `aria2.addTorrent` 封装（磁力链接直接复用现有 `addUri`）
3. 引擎轮询处理磁力「两阶段」特性（`[METADATA]` 任务 → `followedBy` 真实任务）与「做种」状态
4. 系统集成：`magnet:` 协议注册（HKCU 注册表，无需管理员权限）+ 单实例命名管道转发

预计总工作量 **5\~8 人日**（含测试），分三阶段交付。

***

## 1. 背景与目标

### 1.1 需求

* 支持磁力链接（`magnet:?xt=urn:btih:...`）下载

* 支持 `.torrent` 种子文件添加

* 下载完成后支持做种（分享率可配置）

* 浏览器扩展可捕获磁力链接

* 操作系统级集成：在浏览器 / 网页中点击磁力链接唤起本应用

### 1.2 评估标准（按用户要求）

| 标准         | 含义                                                 |
| ---------- | -------------------------------------------------- |
| **最优雅**    | 架构改动最小，与现有「App → JSON-RPC → aria2」管线自然融合，不引入割裂的双引擎 |
| **占用最少**   | 不增加体积、不增加常驻进程、不增加依赖与内存开销                           |
| **对用户最友好** | 点击即用、无概念负担、默认配置合理                                  |

***

## 2. 项目现状审阅

### 2.1 架构总览

```
┌────────────────┐   HTTP POST   ┌──────────────────────┐  JSON-RPC   ┌─────────────┐
│ 浏览器扩展 (MV3) │ ────────────→ │ KokonaDownloader.App  │ ──────────→ │ aria2c      │
│ 拦截下载请求     │  127.0.0.1    │ (WinUI 3, unpackaged) │ 127.0.0.1   │ 1.37.0      │
│ logic.js       │  :固定端口     │ KokonaDownloader.Core │  :RPC 端口  │ (vendored)  │
└────────────────┘               │ (引擎编排/设置/任务库)  │             └─────────────┘
                                 └──────────────────────┘
```

* 单实例控制：Mutex `Local\KokonaDownloader_SingleInstance` + 跨进程唤醒 EventWaitHandle `Local\KokonaDownloader_ShowWindow`

* 任务持久化：aria2 `--save-session` 会话文件 + 应用侧 `TaskStore`（JSON 任务元数据）

* 应用为 **unpackaged**（`WindowsPackageType=None`），无 MSIX 能力 → 系统协议注册必须走手动注册表

### 2.2 关键事实：内置 aria2c 已具备完整 BT 能力

对 vendored 二进制实测：

```
.\vendor\aria2\aria2c.exe --version
aria2 version 1.37.0
Enabled Features: Async DNS, BitTorrent, Firefox3 Cookie, GZip, HTTPS,
                  Message Digest, Metalink, XML-RPC, SFTP
```

**BitTorrent 已在特性列表中。** 这是本次评估最重要的前提：引擎侧无需任何替换或补充，「支不支持 BT」只是「开不开配置、传不传参数」的问题。aria2 对磁力的 DHT / PEX / Tracker / 做种 / 元数据解析均为引擎原生能力。

### 2.3 现有代码缺口（阻断点清单）

| #  | 文件                          | 现状                                      | 需要的变更                                                                           |
| -- | --------------------------- | --------------------------------------- | ------------------------------------------------------------------------------- |
| 1  | `Aria2Process.cs`           | 启动参数中没有任何 BT 相关配置                       | 补 `--enable-dht`、`--listen-port`、`--seed-ratio`、`--bt-detach-seed-only` 等约 10 项 |
| 2  | `Aria2RpcClient.cs`         | 仅有 `AddUriAsync`                        | 补 `AddTorrentAsync`（`aria2.addTorrent`）与 `ChangeGlobalOptionAsync`（tracker 热更新） |
| 3  | `Models.cs`                 | `Aria2TaskStatus.FollowedBy` 字段已存在但从未使用 | 启用 `FollowedBy`；补充 peers 数、上传速度等 DTO 字段                                         |
| 4  | `DownloadEngine.cs`         | 轮询/添加任务完全不识别磁力任务                        | 处理 `[METADATA]` 任务归并、`followedBy` 接续、做种状态映射                                     |
| 5  | `NewDownloadDialog.xaml.cs` | URL 校验仅放行 http/https/ftp                | 放行 `magnet:`；新增「选择种子文件」入口                                                       |
| 6  | `ApiService.cs`             | 同样的协议白名单                                | 放行 magnet；请求体支持 `torrentBase64`                                                 |
| 7  | `extension/logic.js`        | `isSupportedUrl()` 同样的白名单               | 放行 `magnet:`，使扩展可捕获磁力链接                                                         |
| 8  | `TaskStore.cs`              | `TaskMeta` 无 BT 字段                      | 补 `IsBT` / `SourceMagnet` / `TorrentFilePath`                                   |
| 9  | `AppSettings.cs`            | 无 BT 设置项                                | 补做种分享率、BT 端口、DHT 开关、tracker 源等                                                  |
| 10 | `App.xaml.cs`               | 有单实例 Mutex + EventWaitHandle，但无协议激活处理   | 第二实例把 magnet URL 经命名管道转发给首实例                                                    |

两个「顺手的既有设施」值得强调：

* `Models.cs` 里 `FollowedBy` 字段**已经预留**——只需启用即可接住磁力两阶段流程；

* `App.xaml.cs` 的单实例 Mutex + `KokonaDownloader_ShowWindow` EventWaitHandle 已经是跨进程唤醒骨架——磁力协议转发只需在此之上加一条命名管道。

***

## 3. Motrix 参考实现分析

Motrix（Electron + aria2）与本项目的架构同构，其磁力功能**全部**构建在 aria2 原生能力上，未引入任何第三方 BT 库。可借鉴的核心实践如下。

### 3.1 磁力下载的「两阶段」流程（必知必会）

1. `aria2.addUri(["magnet:?xt=urn:btih:..."])` → aria2 创建一个**元数据任务**，任务名显示为 `[METADATA]`，通过 DHT / PEX / Tracker 寻找节点并下载种子元数据（文件名、大小、分片信息）；
2. 元数据获取完成后，该任务状态变更为 `complete`，其 `followedBy` 字段指向**真实下载任务**的 GID；
3. UI 层必须识别这一机制：把元数据任务折叠/隐藏，把 `followedBy` 指向的任务作为用户看到的那条任务。否则用户会看到一条永远卡住的 `[METADATA]` 假任务——这是磁力支持最常见的 UI 坑。

### 3.2 值得复刻的引擎配置

* `bt-save-metadata=true`：把磁力解析出的元数据落盘为 `.torrent`，方便用户日后复用 / 重新做种；

* `bt-detach-seed-only=true`：**关键配置**——仅做种中的任务不计入 `--max-concurrent-downloads` 并发配额。若不开启，1 个做种任务就会占满并发额度并阻塞普通下载；

* `bt-request-peer-speed-limit=10M`：把「预期速度阈值」从默认 50K 提高，可显著改善 BT 速度感知；

* **Tracker 列表定时刷新**：Motrix 定期拉取开源项目 `ngosang/trackerslist` 的 tracker 列表，通过 `aria2.changeGlobalOption` 热更新 `bt-tracker`，显著提升磁力冷启动（无 tracker / 仅 DHT）时的成功率。这是磁力体验优化的业界标准做法。

### 3.3 做种策略

Motrix 默认无限做种（`seed-ratio=0`）。考虑到国内用户的流量负担，本报告建议默认 **`seed-ratio=1.0`（下载完成后做种至分享率 1.0 自动停止）**，并在设置中允许改为「下载完成即停止做种」或「不限」。这是 P2P 共享礼仪与用户流量之间的友好平衡点。

***

## 4. 候选方案对比

### 4.1 方案 A：复用 aria2 原生 BT 能力（Motrix 路线）

磁力/种子全部交给现有 aria2c 处理：`addUri(magnet)` + `addTorrent(base64)`，补全启动参数与轮询归并逻辑。不新增任何进程、二进制、依赖。

### 4.2 方案 B：引入独立 BT 引擎（MonoTorrent / libtorrent 绑定）

App 内嵌第二个 BT 栈：aria2 只管 HTTP/FTP，磁力走新引擎。

* **MonoTorrent**（纯 C# NuGet）：约 2\~4 MB DLL；但 DHT、tracker 协议、NAT 穿透等能力成熟度远低于 libtorrent，冷门种/大文件可靠性一般；且任务模型与 aria2 完全割裂，需要自建双引擎路由层与双份任务状态。

* **libtorrent**（C++，P/Invoke 绑定）：绑定体积 10 MB 以上，.NET 生态中的绑定项目维护活跃度普遍不佳，引入非托管崩溃风险。

### 4.3 方案 C：新增第二个下载器进程（transmission-cli / qBittorrent-nox 等）

另起一个进程，经第二套协议桥接。多一个常驻进程、多 5\~30 MB 体积、多一套 RPC 与生命周期管理；浏览器扩展与 API 层还要按 URL 协议分流到不同引擎。

### 4.4 对比矩阵

| 维度                     | A. aria2 原生             | B. 独立 BT 引擎  | C. 第二下载器进程  |
| ---------------------- | ----------------------- | ------------ | ----------- |
| 新增体积                   | **0 MB**                | 2\~10 MB     | 5\~30 MB    |
| 常驻进程                   | **0（复用现有）**             | 0（同进程）       | +1          |
| 新增依赖                   | **0**                   | 1\~2 个 NuGet | 桥接代码 + 新协议  |
| 架构一致性                  | **完全复用现有管线**            | 双引擎、双任务模型    | 双进程、双协议     |
| 磁力两阶段 / DHT / PEX / 做种 | **引擎原生全支持**             | 大量自研或低成熟度绑定  | 支持但需第二套接口适配 |
| 与浏览器扩展现有 API 兼容        | **无缝（仍是同一个 aria2 RPC）** | 需按协议分流       | 需按协议分流      |
| 维护成本                   | **低（aria2 社区维护）**       | 高            | 中高          |
| Motrix 同款路线            | ✔                       | ✘            | ✘           |

### 4.5 方案裁定

对照三项标准逐一检验：

* **最优雅**：方案 A 完全在现有「App → JSON-RPC → aria2」单管线内完成，B/C 都要引入第二套引擎与路由分流，破坏架构一致性；

* **占用最少**：方案 A 零体积、零进程、零依赖增量，B/C 均有实质开销；

* **对用户最友好**：方案 A 下浏览器扩展、本地 API、系统协议、任务列表走的是**同一条通路**，行为一致、故障面最小。

**裁定：方案 A —— 唯一同时满足三项标准的选择。** 以下详细设计均基于方案 A。

***

## 5. 推荐方案详细设计（方案 A）

### 5.1 引擎层（KokonaDownloader.Core）

#### 5.1.1 Aria2Process 启动参数补全

在现有参数基础上追加（均有 aria2 手册依据）：

| 参数                                  | 建议值                       | 说明                                |
| ----------------------------------- | ------------------------- | --------------------------------- |
| `--enable-dht`                      | true                      | 磁力链接**必需**（默认 false，必须显式开启）       |
| `--dht-listen-port`                 | 6881-6999                 | DHT UDP 监听端口                      |
| `--dht-file-path`                   | `<EngineWorkDir>\dht.dat` | 显式指定 DHT 路由表缓存，跟随现有引擎工作目录机制       |
| `--listen-port`                     | 51413-51423               | BT TCP 监听端口（避开最常被占用的 6881）        |
| `--bt-save-metadata`                | true                      | 磁力元数据落盘为 .torrent，便于复用/重新做种       |
| `--follow-torrent`                  | true                      | 下载 .torrent 内实际文件（默认行为，显式声明便于配置化） |
| `--seed-ratio`                      | 1.0（可配置）                  | 做种至分享率 1.0 自动停止；0 = 不限            |
| `--bt-detach-seed-only`             | true                      | **关键**：仅做种任务不占用并发下载配额             |
| `--bt-max-peers`                    | 55                        | 每任务最大 peer 数                      |
| `--bt-request-peer-speed-limit`     | 10M                       | 提高速度感知（默认 50K 过于保守）               |
| `--bt-enable-lpd`                   | true                      | 本地发现（LPD），局域网内免 tracker 互联        |
| `--peer-id-prefix` / `--peer-agent` | 可选                        | 客户端标识（如需改善兼容性再议）                  |

> 注意：aria2c 首次监听 BT/DHT 端口时，Windows 防火墙会弹出授权提示，属预期行为，应在设置页/FAQ 中说明。

#### 5.1.2 RPC 层（Aria2RpcClient.cs）

```csharp
// 磁力链接：直接复用现有 AddUriAsync —— magnet URI 本身就是一个 uri，无需新方法。
// 种子文件：新增一个封装即可：
Task<string> AddTorrentAsync(byte[] torrentBytes, NewTaskRequest request);
// 内部实现：aria2.addTorrent(Convert.ToBase64String(torrentBytes), uris: [], options)
```

另需 `ChangeGlobalOptionAsync`（映射 `aria2.changeGlobalOption`），为 tracker 列表热更新服务。

#### 5.1.3 DownloadEngine 轮询归并（磁力两阶段 + 做种）

在现有 multicall 轮询（tellActive/tellWaiting/tellStopped）的结果处理中增加规则：

```
规则 1（元数据归并）：
  若任务 bittorrent.info.name == "[METADATA]" 且 FollowedBy 非空：
    1) 将 FollowedBy[0] 纳入轮询集合；
    2) 元数据任务标记完成并从 UI 列表移除（或折叠为提示）；
    3) TaskStore 中为真实任务记录 SourceMagnet（经 getOption 取回）。

规则 2（做种中判定）：
  若任务进度 100% 且 status == active 且 IsBT：
    → UI 状态映射为「做种中」（含上传速度）。

规则 3（做种结束，防误判）：
  若任务 status == removed 且 IsBT 且进度 100%：
    → 这是「做种达标后引擎收尾」，FinalState 记为 Completed（而非 Failed/Removed）。
```

> 规则 3 是重要细节：aria2 在做种到达 `seed-ratio` 后会把任务置为 `removed` 状态。若不做此映射，做种结束会被用户看到为「失败/已取消」。

#### 5.1.4 Models / TaskStore 扩展

* `DownloadTaskInfo` 补：`IsBT`、`UploadSpeed`、`NumPeers`、`NumSeeders`

* `TaskMeta` 补：`IsBT`、`SourceMagnet`、`TorrentFilePath`

### 5.2 应用层（KokonaDownloader.App）

#### 5.2.1 新建任务对话框（NewDownloadDialog）

* URL 校验放行 `magnet:`（校验前缀 `magnet:?xt=urn:btih:`）；

* 新增「选择种子文件」按钮：`FileOpenPicker` 过滤 `.torrent` → 读取字节 → `AddTorrent`；

* 磁力任务创建后 UI 提示「正在获取种子信息…」（元数据阶段），避免用户面对卡住的黑盒。

#### 5.2.2 任务列表 UI（TaskItemViewModel）

* `StatusText` 增加「做种中」状态；

* 任务详情可展示 peers 数 / 做种数 / 上传速度（可选增强）。

#### 5.2.3 `magnet:` 协议注册（系统级集成）

本项目为 unpackaged 应用，**不能**用 MSIX manifest 声明协议，需采用与现有 `StartupHelper` 相同的 HKCU 注册表模式（无需管理员权限）：

```
HKCU\Software\Classes\magnet
    (默认值)        = "URL:magnet Protocol"
    "URL Protocol"  = ""
    \shell\open\command
        (默认值)    = "<应用 exe 完整路径> "%1""
```

设置页提供「关联磁力链接」开关（写入/清除上述键值）。

#### 5.2.4 单实例转发

第二实例被系统以 `"app.exe" "magnet:?xt=..."` 拉起后：

1. 解析命令行参数提取磁力 URL；
2. 检测到首实例已运行（现有 Mutex 逻辑）→ 通过**命名管道**（首实例常驻 `NamedPipeServerStream` 监听）把 URL 发给首实例；
3. 首实例收到 → 经现有 `KokonaDownloader_ShowWindow` EventWaitHandle 唤醒窗口，并弹出**预填好磁力 URL** 的新建任务对话框。

> 备选方案：临时文件 / 环境变量传递。命名管道最简洁、无残留文件，推荐。

### 5.3 API 层与浏览器扩展

* `ApiService.HandleDownload()`：协议白名单放行 `magnet:`；`ApiDownloadRequest` 增加可选 `torrentBase64` 字段（或新增独立端点）；

* `extension/logic.js`：`isSupportedUrl()` 放行 `magnet:`，浏览器中点击/右键磁力链接即可「使用 KokonaDownloader 下载」；

* 扩展内容脚本注意：仅捕获用户主动点击的 `<a href="magnet:...">`，避免页面自动嗅探造成误报。

### 5.4 设置项与 Tracker 自动刷新

新增设置分区「BitTorrent」：

| 设置项          | 默认值         | 说明                                           |
| ------------ | ----------- | -------------------------------------------- |
| 启用 DHT       | 开           | 关闭后磁力基本不可用                                   |
| BT 监听端口      | 51413-51423 | TCP + DHT UDP                                |
| 做种分享率        | 1.0         | 0 = 不限做种                                     |
| 下载完成即停止做种    | 关           | 优先级高于分享率                                     |
| Tracker 自动刷新 | 开，每 24h     | 数据源：ngosang/trackerslist（trackers\_best/all） |
| 关联磁力链接       | 关           | HKCU 协议注册开关                                  |

Tracker 刷新实现：后台定时拉取列表 → 与上次缓存比对 → `aria2.changeGlobalOption({ "bt-tracker": "url1,url2,..." })` 热生效；失败时静默保留上次缓存（存于 `EngineWorkDir\trackers.txt`）。

### 5.5 磁力任务全生命周期（用户视角）

```
用户在浏览器点击 magnet 链接
  → 系统弹窗「使用 KokonaDownloader 打开?」→ 确认
  → 首实例弹出新建任务对话框（已预填磁力 URL，文件名留空待解析）
  → 确认后任务列表出现「正在获取种子信息…」
  → 数秒~数十秒后元数据解析完成，任务变为正常进度显示（真实文件名/大小）
  → 下载完成 → 显示「做种中（上传 X KB/s，分享率 Y）」
  → 达到分享率 1.0（或用户手动停止做种）→ 显示「已完成」
```

***

## 6. 风险与对策

| 风险                                          | 等级 | 对策                                                  |
| ------------------------------------------- | -- | --------------------------------------------------- |
| aria2 的 BT 性能/健壮性弱于 qBittorrent（libtorrent） | 中  | 可接受——Motrix 多年线上验证，轻中度 BT 场景足够；重度 PT 用户本就不是本产品目标    |
| 私有 Tracker（PT）兼容性                           | 中  | aria2 不适合 PT 场景（客户端伪装、passkey 处理等）；明确「不支持 PT」并在文档说明 |
| 防火墙首启授权弹窗                                   | 低  | 设置页/FAQ 说明，属系统预期行为                                  |
| 做种结束后任务呈 `removed` 状态被误判为失败                 | 低  | 5.1.3 规则 3 的状态映射覆盖                                  |
| 磁力冷启动慢（无 tracker 时仅靠 DHT）                   | 中  | 默认 DHT + PEX + LPD + tracker 列表自动刷新，已是业界标准组合        |
| session 恢复时元数据任务残留                          | 低  | 恢复轮询时按 5.1.3 规则 1 同样归并；由 aria2 会话机制管理               |
| 安全（恶意 .torrent / 路径穿越）                      | 低  | aria2 自带路径防护；对话框展示解析出的真实文件名，越界路径由引擎拒绝               |

***

## 7. 实施路线图

**阶段 1 —— 核心可用（约 2\~3 人日）**

1. `Aria2Process.cs` 参数补全（见 5.1.1）
2. `Models.cs` / `Aria2RpcClient.cs`：`AddTorrentAsync`、DTO 扩展
3. `DownloadEngine.cs`：磁力添加入口 + `followedBy` 归并 + 做种/removed 映射
4. `NewDownloadDialog.xaml.cs`：magnet 校验 + `.torrent` 选择器
5. `AppSettings.cs` 新增默认值；阶段性集成测试

**阶段 2 —— 系统集成（约 1\~2 人日）**

1. `magnet:` 协议注册（HKCU）+ 设置开关
2. 命名管道单实例转发 + 对话框预填
3. `ApiService.cs` + `extension/logic.js` 放行 magnet

**阶段 3 —— 体验完善（约 2\~3 人日）**

1. 做种设置 UI + tracker 自动刷新
2. 任务详情（peers / 做种数 / 上传速度）
3. `.torrent` 拖拽入窗、防火墙 FAQ、回归测试

***

## 8. 测试策略建议

* **单元测试**：状态映射纯逻辑（followedBy 归并、做种中/做种结束判定、协议校验）；

* **集成测试**：沿用现有 `EngineIntegrationTests` 模式（真实 aria2 + 本地服务），用固定的小型 `.torrent` fixture 验证 `addTorrent` / 做种 / `bt-detach-seed-only` 行为；磁力端到端可用 aria2 对 fixture 做种于本机、再以 magnet 回环下载验证；

* **手动验收**：系统协议唤起（含第二实例转发）、扩展捕获磁力、防火墙提示、做种到分享率后的状态流转。

***

## 9. 最终结论

1. **可行性：高。** 最大的前提性风险（引擎是否支持 BT）已被排除——内置 aria2c 1.37.0 原生支持 BitTorrent，无需任何二进制或依赖变更。
2. **推荐方案：方案 A（aria2 原生 BT 能力，Motrix 同款路线）。** 它是唯一同时满足「最优雅（单引擎单管线）、占用最少（零体积/零进程/零依赖增量）、对用户最友好（全通路行为一致，点击磁力即用）」三项标准的选择；引入独立 BT 引擎或第二下载器进程都会破坏架构一致性与资源占用。
3. **工作量：约 5\~8 人日**，分三阶段交付；阶段 1 完成即具备完整磁力/.torrent 下载能力。
4. **实现关键点（一句话版）**：BT 启动参数补全 → `aria2.addTorrent` 封装 → `followedBy`/做种状态归并 → HKCU 协议注册 + 命名管道转发 → tracker 列表自动刷新。

