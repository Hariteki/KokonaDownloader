# 用户构建/发布产物
dist/
release/
realese/

# 编译产物
bin/
obj/

# Visual Studio
.vs/
*.user
*.suo

# 日志与临时文件
*.log
_tmp_*
*.tmp

# 扩展打包产物（私钥绝对不能提交）
extension.crx
extension.pem

# 本地 UI 验证脚本与截图、备份、测试站点（不属于仓库内容）
.verify/
backup/
testsite/

# UiTests 宿主每次运行都会重新生成的截图与失败诊断（不是 README 用的产品截图）
screenshot/ui_*.png
screenshot/diag_*.png

# 本地测试夹具与临时记录（第四轮审计 N-7）：
# files/ 是历史测试夹具（78 MB，全仓无任何脚本引用），infohash.txt 是 BT 测试的一次性产物。
files/
infohash.txt
