# 海兔下载器 · 项目维护手册

> **本文是"交接文档"：目标是不读历史对话也能安全接手本项目。**
> 对应提交：`113484f`（main）　编写时间：2026-09-15
> 已发布版本：**v1.0.6**（GitHub Release）　main 上另有 2 个未发版的修复（见第 7 节）
>
> 阅读顺序建议：第 0 节（速览）→ 需要动手时看第 2 节（命令）→ 改代码前看第 4 节（不变量）
> → 写完看第 5 节（踩坑）→ 交付看第 8 节（发版流程）。

---

## 目录

0. [30 秒速览](#0-30-秒速览)
1. [目录地图](#1-目录地图)
2. [命令速查（全部已实测）](#2-命令速查全部已实测)
3. [架构与数据流](#3-架构与数据流)
4. [关键设计决策与不变量（改之前先读）](#4-关键设计决策与不变量改之前先读)
5. [踩坑速查（症状 → 根因 → 正确做法）](#5-踩坑速查症状--根因--正确做法)
6. [当前进度与状态](#6-当前进度与状态)
7. [待办与已知问题](#7-待办与已知问题)
8. [发版流程（v1.0.7 照抄即可）](#8-发版流程v107-照抄即可)
9. [改动指引：常见配方 + 回归矩阵](#9-改动指引常见配方--回归矩阵)
10. [新增验证脚本的写法约定](#10-新增验证脚本的写法约定)
11. [本机环境事实](#11-本机环境事实)
12. [文档索引](#12-文档索引)
13. [附录 A · 术语表](#附录-a--术语表)

---

## 0. 30 秒速览

**项目**：海兔下载器（Kokona Downloader）——Windows 上的 aria2 图形客户端。
浏览器扩展拦截下载 → 本地 HTTP API → 桌面客户端（WinUI 3）→ aria2c 子进程（JSON-RPC）真正下载。

**技术栈**：C# / .NET 8 / WinUI 3（Windows App SDK 1.8）/ 原生 JS 扩展（Manifest V3）/ aria2c（GPL v2，`vendor\aria2\aria2c.exe`）。

**仓库**：`https://github.com/Hariteki/KokonaDownloader`（公开，MIT）。

**三条命令**（PowerShell，工作目录 = 仓库根）：

```powershell
# 1) 编译（-r win-x64 必须，否则 XBF 资源路径不一致）
& "C:\Program Files\dotnet\dotnet.exe" build src\KokonaDownloader.App\KokonaDownloader.App.csproj -c Release -r win-x64

# 2) 核心测试（真实 aria2 + 本地回环服务器，约 1 分钟）→ 期望 90/90
& "C:\Program Files\dotnet\dotnet.exe" test src\KokonaDownloader.Core.Tests\KokonaDownloader.Core.Tests.csproj -c Release

# 3) UI 截图验证宿主（真实应用 + UIA 驱动 + 回环种子，约 3 分钟）→ 期望 7 张截图
& "C:\Program Files\dotnet\dotnet.exe" run --project src\KokonaDownloader.UiTests\KokonaDownloader.UiTests.csproj -c Release
```

**当前状态一句话**：v1.0.6 已发布（透明切换 + 启动动画 + 单文件发行）；main 上追加了"磨砂浓度滑块"和"下载文件名解析修复"两项，**尚未发版**（v1.0.7 候选）；桌面副本已刷新为含文件名修复的新构建。

**接手第一步（自检环境是否可用，逐条打勾）**：

- [ ] `git -C <repo> status --short` → 只有 3 个历史未跟踪文件（见 6.6），无意外改动
- [ ] 第 2 节三条命令都能跑通：build 0 error / Core 测试 90/90 / UiTests 出 7 张截图
- [ ] `cscript //nologo tests\test_logic.js` → 20/20
- [ ] 读第 4 节"不变量"，尤其 4.1（透明度）与 4.3（文件名解析）
- [ ] 确认没有残留进程：`Get-Process KokonaDownloader, aria2c`（应为空）
- [ ] 动代码前先看第 9 节的"配方"，改完按 9.2 矩阵跑对应验证

**三条红线（踩了就出事故）**：

1. **绝不把 `URL 末段`当文件名固定给 aria2**（`out=`）——会覆盖重定向目标/`Content-Disposition` 里的真实文件名。客户端有 `DownloadEngine.DropUrlDerivedFileName` 兜底，测试见 `FileNameResolutionTests`。
2. **绝不提交 `.verify\`**（里面有 GitHub token `gh_token.txt`）、`dist\`、`release\`、`backup\`、`bin\obj`。含中文的 `.ps1` 必须存 **UTF-8 带 BOM**。
3. **不要再用 XAML 的 `MicaBackdrop`/`DesktopAcrylicBackdrop` 做窗口透明**（实测"设了但不透桌面"），必须走原生 `DesktopAcrylicController`（见 4.1）。

---

## 1. 目录地图

| 路径 | 是什么 | 改动要点 |
| --- | --- | --- |
| `src\KokonaDownloader.App\` | WinUI 3 桌面客户端（UI、托盘、窗口、主题） | XAML 改动后必须重编；WinUI3 限制多，先读第 4/5 节 |
| `src\KokonaDownloader.Core\` | 核心库：aria2 封装、设置、任务、通知、API 服务、主题目录 | 与 UI 无关的逻辑放这里，才能被单测覆盖 |
| `src\KokonaDownloader.Core.Tests\` | xUnit 测试（真实 aria2 + 本地 HTTP 文件服务器） | 新增网络/命名/引擎逻辑测试写这里 |
| `src\KokonaDownloader.UiTests\` | **不是** xUnit：控制台程序（`dotnet run`），UIA 驱动真实应用 + 回环做种 + 截产品图 | 只做截图/冒烟，判据是"跑完 exit 0 + 产出截图" |
| `extension\` | 浏览器扩展源码（直接"加载已解压的扩展"即可用） | 无构建步骤；改动后跑 `cscript //nologo tests\test_logic.js` |
| `tests\test_logic.js` | 扩展逻辑单测（WSH/JScript，全 ASCII） | `cscript //nologo tests\test_logic.js` → 期望 20/20 |
| `tools\` | **验证脚本库**（PowerShell，已入库） | 见 2.5 表格；新脚本按第 10 节约定写 |
| `artifacts\transparency\` | 透明度/磨砂的证据截图（已入库，回归基线） | 改透明度相关代码后重跑脚本并更新截图 |
| `docs\单文件打包指南.md` | 单文件 exe 打包/解压机制的权威说明 | 动 csproj 的嵌入/PRI 链前必读 |
| `v1.0.6-进度与待办.md` | v1.0.6 的版本化进度与验证数据（含历史踩坑） | 版本交付记录，按版本累加 |
| `FINAL_REPORT.md` | v1.0.5 单文件修复报告（上一轮交付物，未入库） | 历史资料，已被本文与打包指南吸收 |
| `磁力链接下载功能可行性与技术评估.md` | BT/磁力功能的技术评估（未入库） | 动 BT 功能前可参考 |
| `README.md` | 面向用户/开发者的门面文档 | 发版后如需可更新截图与简介 |
| `build.ps1` | 一键构建（客户端 + 扩展）到 `dist\` | 仅开发用，单文件发版不用它 |
| `vendor\aria2\aria2c.exe` | aria2 引擎二进制（5.39 MB） | 升级引擎时同步 `.csproj` 里的嵌入路径 |
| `icons\` | 应用图标 + `titlebar-tray\`（托盘/窗口图标） | 托盘图标会随发布输出到 `icons\tray.ico|png` |
| `screenshot\` | README 用的产品截图（`ui_*`/`diag_*` 已被 .gitignore 忽略） | UiTests 每次运行都会重写 `ui_*` |
| `.verify\` | **本地草稿区（gitignore）**：GitHub token、release payload、历史调试脚本 | 绝不入库；可随时清理 |
| `dist\ release\ backup\ testsite\ .tmp\ bin\` | 构建产物/备份/测试站点（全部 gitignore） | 可删，勿提交 |

---

## 2. 命令速查（全部已实测）

### 2.1 环境要求

| 项 | 值（本机） |
| --- | --- |
| .NET SDK | 8.0.401（`C:\Program Files\dotnet\dotnet.exe`） |
| PowerShell | 5.1（`C:\Windows\System32\WindowsPowerShell\v1.0\powershell.exe`）——脚本按 5.1 写 |
| Git | MinGit：`C:\Users\lumin\AppData\Local\Programs\MinGit\cmd\git.exe` |
| 浏览器扩展测试 | `cscript.exe`（Windows 自带 WSH） |
| aria2 | 随仓库 `vendor\aria2\aria2c.exe`，发布时嵌进 exe |

### 2.2 构建

```powershell
# 开发构建（bin\Release\net8.0-windows10.0.19041.0\win-x64\KokonaDownloader.exe）
dotnet build src\KokonaDownloader.App\KokonaDownloader.App.csproj -c Release -r win-x64

# 一键构建（客户端目录版 + 扩展目录 → dist\KokonaDownloader、dist\KokonaExtension）
powershell -ExecutionPolicy Bypass -File .\build.ps1
```

### 2.3 测试

```powershell
# 核心测试（xUnit；真实 aria2 + 本地回环 HTTP 服务器；含 BT/磁力/文件名/扩展契约）
dotnet test src\KokonaDownloader.Core.Tests\KokonaDownloader.Core.Tests.csproj -c Release   # 90/90

# 扩展逻辑单测
cd <repo>; cscript //nologo tests\test_logic.js                                             # 20/20

# UI 宿主（截图 + 端到端冒烟；必须先杀掉在跑的应用实例）
dotnet run --project src\KokonaDownloader.UiTests\KokonaDownloader.UiTests.csproj -c Release
```

> 跑任何"启动应用"的脚本前先 `Get-Process KokonaDownloader, aria2c | Stop-Process -Force`，
> 否则单实例互斥会让新实例只触发"显示窗口"事件而不真正新建任务。

### 2.4 打包单文件 exe

```powershell
dotnet publish src\KokonaDownloader.App\KokonaDownloader.App.csproj -c Release -r win-x64 `
  --self-contained true -p:PublishSingleFile=true `
  -p:IncludeNativeLibrariesForSelfExtract=true `
  -p:WindowsAppSDKSingleFileVerifyConfiguration=false `
  -o dist\KokonaSingleFile
# 产物：dist\KokonaSingleFile\KokonaDownloader.exe ≈ 303.5 MB（318,204,760 B @v1.0.6+修复）
# 同目录另有 resources.pri / *.xbf / aria2c.exe / icons\ ——开发便利，分发只给 exe 一个文件
```

### 2.5 验证脚本库（`tools\`）

| 脚本 | 验证什么 | 通过判据 |
| --- | --- | --- |
| `verify_singlefile_main.ps1` | 单文件 exe：内嵌 13 个载荷 + A 干净目录 + B dist 目录 | 13/13 资源命中；13 文件解压；ping 200 v1.0.6+；日志无 XamlParseException |
| `verify_singlefile_fallback.ps1` | C 只读目录回退 `%LOCALAPPDATA%\...\Standalone` | 回退目录 14 文件；新进程从回退目录运行；ping 200 |
| `verify_desktop_launch.ps1` | D 桌面实机启动 | splash 可见；主窗口 940×560；ping 200 |
| `verify_filename_resolution.ps1` | 多站点文件名解析（**每条都以"URL 末段伪名"发送**） | 落盘名 == 独立推导的期望名（CD 优先，其次重定向目标） |
| `probe_aria2_filename.ps1` | 直接问 aria2 会解析出什么名字（排除应用因素） | 输出 resolved name，用于定位"是 aria2 还是应用" |
| `probe_app_filename.ps1` | 端到端：POST /api/download → 任务名 + 落盘名 | 同上；`-FileName <伪名>` 可复现旧 bug |
| `verify_opaque_alpha.ps1` | 不透明模式是否真不透明（白底遮挡 + 6 点采样） | 6 点全部 == 主题 WindowFill 不透明值，devFF ≤ 6 |
| `verify_frosted_strength.ps1` | 磨砂浓度 0/0.5/1 三档 + UIA 滑块存在性 | 平均亮度单调（242>137>61，差 ≥25）；菜单含 Slider 且磨砂模式 enabled |
| `shoot_transparency_flyout.ps1` | 透明度菜单视觉证据 + 滑块禁用态 | 产出 `artifacts\transparency\flyout-{frosted,opaque}.png`；UIA dump 列出菜单元素 |
| `verify_transparency8.ps1` | 三模式渲染差异（每次启动固定一种模式，10 点采样） | 桌面亮处透出（磨砂/黑色）/恒定（不透明） |
| `verify_api_settings.ps1` | 设置 API：GET 字段、POST 持久化、未知字段不 500 | ALL PASS（含恢复原值） |
| `probe_windows.ps1` | 启动动画是否真出现（启动瞬间轮询顶层窗口） | 420 DIP 窗口 `visible=True` |
| `probe_splash_args.ps1` | `--minimized` / `magnet:` 唤起路径的窗口表现 | 无可见主窗口；确认窗 500×430 出现 |
| `diag_windows.ps1` / `who_covers.ps1` / `bring_to_front.ps1` / `uia_dump.ps1` / `compare_mode_shots.ps1` | 诊断辅助（窗口归属、遮挡识别、置前、UIA 树转储、截图像素对比） | 无判据，人工看输出 |

> 所有脚本都：自己杀/起应用、结束时清理进程；采样类脚本会先校验"采样点归属本应用进程"再下结论。

---

## 3. 架构与数据流

### 3.1 三进程模型

```
浏览器(扩展 MV3)                桌面客户端 (KokonaDownloader.exe, WinUI3)
  downloads.onCreated ──HTTP──►  ApiService (HttpListener, 127.0.0.1:16800, X-Kokona-Secret)
  右键/弹窗"发送"   ──HTTP──►        │
                                     ├─ DownloadEngine ──JSON-RPC(ws/http)──► aria2c.exe (127.0.0.1:16801)
                                     ├─ AppHost：设置/引擎/API/通知 装配与热更新
                                     ├─ MainWindow + ProgressWindow + MagnetConfirmWindow + SplashWindow + SettingsWindow
                                     └─ TrayService（WinForms NotifyIcon）
```

- 扩展**不**直接跟 aria2 通信，只发 HTTP 给客户端；客户端把任务交给 aria2 并轮询状态渲染 UI。
- 磁力/种子：命令行 `magnet:` 或 `/api/download` 送来 → **独立确认窗**（主窗口保持隐藏）→ 用户确认后才建任务。
- 通知：`NotificationRules` + `NotifiedStore` 去重，WinRT Toast（`ToastPayload`）。

### 3.2 关键类职责

| 文件 | 职责 |
| --- | --- |
| `Core\AppHost.cs` | 装配 Settings/Engine/Api/通知；`OnSettingsChanged` 里把**全局限速**与 BT tracker 热更新到引擎 |
| `Core\Engine\Aria2Process.cs` | 拼 aria2 启动参数、拉起/看护子进程、清理孤儿进程、会话文件墓碑过滤 |
| `Core\Engine\Aria2RpcClient.cs` | JSON-RPC 封装（addUri/addTorrent/tellStatus/…）；**`options["out"]` 只在 `req.FileName` 非空时设置** |
| `Core\Engine\DownloadEngine.cs` | 任务增删改查、去重/墓碑、BT 做种策略、`ApplyUniqueFileName`（重名自动编号）、**`DropUrlDerivedFileName`（伪名丢弃）**、轮询更新任务名 |
| `Core\Engine\Models.cs` | `NewTaskRequest` / `DownloadTaskInfo` / `EngineConfig` 等模型 |
| `Core\Api\ApiService.cs` | 本地 HTTP API + 鉴权 + CORS；`ApiDownloadRequest`/`ApiSettingsPatch` 契约；ping 里的版本号 |
| `Core\Settings\AppSettings.cs` | 设置模型 + `SettingsStore`（JSON 持久化 + `Changed` 事件）+ `AppPaths`（数据目录） |
| `Core\Themes\ThemeCatalog.cs` | 内置主题配色目录与解析（含对比度约束，有单测） |
| `App\App.xaml.cs` | 启动流程：magnet 参数 → 单实例互斥 → 启动动画 → 主窗口；`--minimized` 只留托盘 |
| `App\StandaloneBootstrap.cs` | `[ModuleInitializer]` 单文件载荷解压/只读回退（见 4.4） |
| `App\Themes\ThemeService.cs` | 主题资源覆盖 + 原生标题栏着色 + **透明度模式/磨砂浓度** 应用（`FrostedParams`） |
| `App\WindowEffects.cs` | 原生 Mica/Acrylic 控制器、`WM_GETMINMAXINFO` 最小尺寸子类化、DWM 标题栏颜色 |
| `App\MainWindow.xaml(.cs)` | 主界面（列表/筛选/BT 页/批量操作/主题与透明度菜单/进度窗管理） |
| `App\MagnetConfirmWindow.xaml(.cs)` | 磁力确认窗（500×430，取消/开始下载） |
| `App\SplashWindow.xaml(.cs)` | 启动动画（420 DIP，无多余文字，最短 1.2s、6s 兜底） |
| `App\ProgressWindow.xaml(.cs)` | 每任务进度浮窗（含 BT 分片方块矩阵 `BtPieceGrid`） |
| `App\Tray\TrayService.cs` | 托盘图标/菜单/进度文字 |

### 3.3 数据与文件位置

| 位置 | 内容 |
| --- | --- |
| `%APPDATA%\KokonaDownloader\settings.json` | 全部设置（见 3.4） |
| `%APPDATA%\KokonaDownloader\tasks.json` | 任务元数据（含 taskNumber、显示名、URL、来源 magnet） |
| `%APPDATA%\KokonaDownloader\notified.json` | 已通知去重记录 |
| `%APPDATA%\KokonaDownloader\trackers.json` | BT tracker 列表缓存（24h TTL） |
| `%APPDATA%\KokonaDownloader\app.log` | 应用日志（透明度/磁力/错误诊断都在这；**排查问题先看它**） |
| `%APPDATA%\KokonaDownloader\engine\` | aria2 工作目录：`aria2.session`（--input/--save-session）、`aria2.log`、`dht.dat` |
| `%LOCALAPPDATA%\KokonaDownloader\Standalone\` | 单文件 exe 在**只读目录**运行时的回退解压目录 |
| `%TEMP%\.net\KokonaDownloader\...` | .NET 单文件 host 的 bundle 解压目录（不是我们的代码） |

### 3.4 设置项（`AppSettings`，JSON 存字符串枚举）

| 键 | 类型/默认 | 生效方式 |
| --- | --- | --- |
| `DefaultDownloadDir` | string（用户 Downloads） | 新建任务时读取 |
| `MaxConcurrentDownloads` | int=3 | 引擎启动参数 |
| `DefaultConnections` | int=8 | 引擎启动参数（`--split`） |
| `ApiPort` / `RpcPort` | int=16800 / 16801 | 启动时监听 |
| `ApiSecret` | string（随机 32 hex） | API 鉴权；扩展需粘贴同一密钥 |
| `NotificationsEnabled` | bool=true | 通知规则 |
| `Theme` | `System`/`Light`/`Dark` | 即时 |
| `ThemeColorId` | string（`system` 或主题 id） | 即时（`ThemeService.Apply`） |
| `Transparency` | `Opaque`/`Frosted`/`BlackTransparent` | 即时（重建原生背景控制器） |
| `FrostedStrength` | double=0.5（0=更透明…1=更不透明） | 即时；仅 `Frosted` 有效，映射见 4.1 |
| `GlobalSpeedLimit` | long=0（B/s） | **热更新**（`aria2.changeGlobalOption`） |
| `LaunchAtStartup` | bool=false | 注册表 Run 项 |
| `MinimizeToTrayOnClose` | bool=true | 关窗行为 |
| `InterceptBrowserDownloads` | bool=true | 扩展自动接管开关（扩展侧设置也在扩展 storage） |
| `BtEnabled` / `BtPort` | bool=true / int=51413 | 引擎启动参数 |
| `BtSeedEnabled` / `SeedRatio` / `SeedTimeMinutes` | true / 1.0 / 0 | 添加 BT 任务时的 `seed-*` 选项 |
| `BtTrackersEnabled` / `BtTrackersUpdatedAt` | true / 时间戳 | 热更新 tracker 列表 |

### 3.5 本地 API（`127.0.0.1:16800`）

鉴权：除 `/api/ping` 外都要 `X-Kokona-Secret: <ApiSecret>` 或 `Authorization: Bearer <ApiSecret>`；CORS 全开（扩展 origin 是 `chrome-extension://`）；`OPTIONS` 预检返回 204。

| 方法 | 路径 | 说明 |
| --- | --- | --- |
| GET | `/api/ping` | 免鉴权；返回 `{ok,app,version}`（version 与 csproj 同步） |
| GET | `/api/tasks` | 任务列表（`gid/taskNumber/name/state/progress/…`） |
| GET | `/api/stats` | 全局统计（速度、活动/等待数、全局限速） |
| GET | `/api/settings` | 设置子集（含 `transparency`/`frostedStrength`/`minimizeToTrayOnClose`） |
| POST | `/api/download` | 新建任务：`urls[]`/`url`/`filename`/`dir`/`connections`/`speedLimit`/`referer`/`headers` |
| POST | `/api/settings` | 部分更新（白名单字段，未知字段忽略不报错） |
| POST | `/api/tasks/{gid}/{pause\|resume\|remove\|redownload}` | 任务操作 |

取密钥（不要写死在文档/脚本里）：

```powershell
$secret = (Get-Content "$env:APPDATA\KokonaDownloader\settings.json" -Raw -Encoding UTF8 | ConvertFrom-Json).ApiSecret
Invoke-RestMethod "http://127.0.0.1:16800/api/ping" -Headers @{ "X-Kokona-Secret" = $secret }
```

### 3.6 扩展与客户端的文件名契约（重要）

- 扩展**只在浏览器已解析出真实文件名时**才带 `filename`（`buildDownloadPayload`），
  **绝不从 URL 猜**（`fileNameFromUrl` 已删除；`tests\test_logic.js` 有专门断言）。
- 不携带 `filename` 时由 aria2 按响应头/重定向解析真实名。
- 客户端另有兜底：`DownloadEngine.DropUrlDerivedFileName` 会丢弃"与 URL 末段同名"的伪名。

---

## 4. 关键设计决策与不变量（改之前先读）

### 4.1 窗口透明度与磨砂浓度

- 三种模式：`Opaque`（无背景控制器 + 实心主题背景）/ `Frosted`（半透明主题色 + Acrylic）/ `BlackTransparent`（黑色薄磨砂）。
- **必须用原生 `DesktopAcrylicController`**（`WindowEffects.TryApplyAcrylicTinted`）：XAML 的
  `MicaBackdrop`/`DesktopAcrylicBackdrop` 在本机实测"设了但不透桌面"（采样点亮度恒定）。
- 切换模式时**先释放上一个控制器**（`ThemeService._backdrops`），否则多个背景叠加。
- `Opaque` 模式**强制 `A=0xFF`**（主题 `WindowFill` 自带 alpha 0xF2=95%，不强制就有 5% 透明）；
  有像素级验证脚本 `verify_opaque_alpha.ps1`（白底遮挡 6 点采样）。
- `Frosted` 的浓度由用户滑块控制，映射在 `ThemeService.FrostedParams`：分段线性
  0.0 → tint 0.12/lum 0.06；0.5 → 0.70/0.55（原观感）；1.0 → 0.98/0.90。
- 透明度菜单是**普通 `Flyout`**（不是 `MenuFlyout`）：WinUI3 的 `MenuFlyoutItem` 没有 `Content`、
  `MenuFlyout.Items` 只收 `MenuFlyoutItemBase`，塞不进滑块；模式行是"仿菜单项按钮 + 代码悬停高亮"。
- `BlackTransparent` 实为"薄磨砂黑"：DWM 没有"无模糊纯透明"公开 API，属已知限制（见 7.3）。

### 4.2 启动动画（时机是全部难点）

- `SplashWindow` 420×420、只有图标与应用名（**不能有多余文字**）。
  注意：它的 `AppWindow.Resize(420, 420)` 传的是**物理像素且未乘 DPI**（与主窗口的换算方式不同），
  本机 125% 缩放下 DPI-unaware 的探针会把它报成 336×336 虚拟像素——写探针时别拿 420 去比对。
  （若要让它在高 DPI 屏上与"420 DIP"的设计意图一致，把 `SplashWindow.SizeAndPlace` 里的 Resize 乘上
  `GetDpiScale()` 即可；属外观微调，非缺陷。）
- 入场动画用**代码构建 Storyboard**：XAML 属性文本无法表达 `EasingFunction`（XBF 会报 Property Not Found）。
- `OnLaunched` 是同步执行的，返回前消息泵不跑 → 在末尾直接 `Dismiss()` 等于"从没显示过"。
  现改为 `DispatcherQueue` 一次性计时器推迟关闭：**最短展示 1.2s**，并有 **6s 兜底**自动关闭。
- `TryCreateSplash()` 失败时会 300ms 重试一次，再失败就直接显示主窗口（不能只记日志）。

### 4.3 下载文件名解析（本项目最容易复发的地方）

三条不变量，任何一条被破坏都会重现"文件名变成编号"的 bug：

1. **不猜名**：客户端不把 URL 末段当文件名；扩展也不发。
2. **服务端优先**：有 `Content-Disposition` 用它的 `filename`；否则用**重定向目标的末段**（302 跟随后的 URL）。
3. **伪名必须丢弃**：`DropUrlDerivedFileName` —— 传入名与任一 URL 末段相同（去查询串、URL 解码、忽略大小写）
   → 视为伪名，改为不设 `out` 交回 aria2；与末段**不同**的显式名（用户手填、浏览器解析结果）保持固定 `out`。
4. 重名冲突：`ApplyUniqueFileName` 仅在**可靠文件名**时把 `out` 固定为 `name (1).ext`，未知名不动。

回归防线：`FileNameResolutionTests`（7 个用例）+ `tools\verify_filename_resolution.ps1`（真实站点）。
`.torrent` 链接是特例：`--follow-torrent=mem` 直接转 BT 任务，不落 `.torrent` 文件（见 7.2）。

### 4.4 单文件 exe 的独立运行（三条路径）

`StandaloneBootstrap.EnsureRuntimeFiles()`（`[ModuleInitializer]`，早于 XAML 初始化）：

1. exe 旁已有 `resources.pri` → 什么都不做（dist 目录/桌面文件夹这种完整布局）。
2. 否则把嵌入资源（9 个 XBF + `resources.pri` + `aria2c.exe` + 2 个托盘图标 = 13 个）解压到 **exe 所在目录**。
3. 该目录只读（如未提权的 Program Files）→ 解压到 `%LOCALAPPDATA%\KokonaDownloader\Standalone`，
   复制 exe 过去并**带原参数重新拉起**，自身 `Environment.Exit(0)`。

必须用 `Environment.ProcessPath` 推导目录，**不能**用 `AppContext.BaseDirectory`（单文件下指向 %TEMP% 解压目录）。
**计时陷阱**：单文件 host 先解压 bundle（本机约 6s）才跑引导逻辑，所以只读回退在启动 6~7s 后才发生——
写探针脚本时至少观察 8s，否则会误判"没有回退"。

### 4.5 单实例、磁力唤起与 `--minimized`

- 单实例：互斥体 `Local\KokonaDownloader_SingleInstance`；第二实例优先用 `MagnetIpc.TrySend` 转发磁力，
  否则触发命名事件 `Local\KokonaDownloader_ShowWindow` 让主窗口现身（UiTests 就是靠它把隐藏的主窗口唤出来的）。
- `magnet:` 命令行或 `/api/download` 单条磁力 → `MainWindow.HandleExternalMagnet` → **独立确认窗**（主窗口保持隐藏）。
- `--minimized` → 主窗口隐藏、只留托盘。
- 这三种路径下启动动画都必须按时自动关闭（有 6s 兜底）。

### 4.6 主题系统

- `ThemeCatalog`（Core，有单测：对比度/亮度约束）产出 `ResolvedTheme`；`ThemeService` 把它写进资源字典。
- **关键坑**：只改 `MergedDictionaries` 里的键，已渲染元素不会重取 `ThemeResource`；因此
  ① 覆盖键同时镜像写入 `Application.Resources` 直属键；② 切换时把根元素主题 Dark→Light→Dark 同帧切一遍
  强制全量重取（`ForceThemeResourceRefresh`，同调度周期内完成、无可见闪烁）。
- 系统强调色变化（`UISettings.ColorValuesChanged`）会重新解析（"跟随系统"主题）。

### 4.7 限速：逐任务 vs 全局

- 逐任务：`NewTaskRequest.SpeedLimit` → aria2 `max-download-limit`（新建下载对话框的"限速"下拉；磁力确认窗没有此项）。
- 全局：设置里的 `GlobalSpeedLimit` → `AppHost.OnSettingsChanged` → `Engine.SetGlobalSpeedLimitAsync`
  → `aria2.changeGlobalOption`，**运行中即时生效**（也作为 aria2 启动参数传入）。UiTests 靠它制造"下载中"中间态。

### 4.8 BT / 磁力

- aria2 以 `--enable-dht --enable-peer-exchange --bt-enable-lpd --follow-torrent=mem`
  `--user-agent=Transmission/2.92 --peer-id-prefix=-TR2920-` 运行（伪装 UA 是为了不被 tracker 封锁）。
- 做种：`bt-detach-seed-only=true`（做种不占并发配额）；`seed-time=0` 表示不做种。
- tracker 列表 `BtTrackersEnabled` 时每 24h 从社区列表刷新（`trackers.json` 缓存），变化时热更新引擎。
- 墓碑（`TombstoneStore`）：删除过的任务在重启后不许"复活"；启动前会用它过滤 `aria2.session`。
- 重复检测：未结束的同 URL/同 infohash 任务会被拦（`DuplicateTaskException`），扩展端据此提示。

---

## 5. 踩坑速查（症状 → 根因 → 正确做法）

### 5.1 WinUI 3 / XAML

| 症状 | 根因 | 正确做法 |
| --- | --- | --- |
| `WMC0011: Unknown member 'Triggers'` / `Unknown type 'Trigger'` | **WinUI3 XAML 不支持 `ControlTemplate.Triggers`** | 用 `VisualStateManager`，或代码里切 `Background`（本项目仿菜单项悬停就是这么做的） |
| `MenuFlyoutItem` 没有 `Content` 定义；`Items.Add(panel)` 报 CS1503 | WinUI3 的 `MenuFlyout.Items` 只接受 `MenuFlyoutItemBase`（UWP 可塞任意元素） | 要自定义内容（滑块等）→ 用普通 `Flyout` + 代码构建内容 |
| XBF 报 `Property Not Found`（如 `EasingFunction`、自定义类型的 `Translation`） | XAML 属性文本解析不了部分类型 | 在代码里构建 Storyboard / 设置 `Translation` |
| 窗口设了 `MicaBackdrop` 却完全不透明 | WinUI3 无 `SystemBackdrop` 时窗口表面本身不透明，根元素 alpha 只跟那块表面混合 | 走原生 `DesktopAcrylicController`（见 4.1） |
| `COMException 0x80000019 Only a single ContentDialog can be open at any time` | 同一时刻只能开一个 `ContentDialog` | 用 `MainWindow._dialogGate`（`SemaphoreSlim`）串行化 |
| 主题色切了但界面不刷新 | 改合并字典里的键不会让已渲染元素重取 `ThemeResource` | 镜像写 `Application.Resources` 直属键 + `ForceThemeResourceRefresh` |
| 设置窗口 `XamlParseException`（缺 `ThemeSwatchPicker.xbf`） | 发布时只拷贝了顶层 xbf，子目录页面漏了 | csproj 里 xbf 用 `**\` 递归拷贝（`CopyXamlResourcesToPublishDir`） |

### 5.2 PowerShell 5.1 与脚本

| 症状 | 根因 | 正确做法 |
| --- | --- | --- |
| 中文 `.ps1` 报字符串/语法错误 | PS 5.1 无 BOM 时按 ANSI 读，中文被拆坏 | **存成 UTF-8 带 BOM**（`[IO.File]::WriteAllText($p,$t,(New-Object System.Text.UTF8Encoding $true))`）；纯 ASCII 脚本可免 |
| `Get-Content -Raw` 读 UTF-8 中文变乱码 | 默认按 ANSI 解码 | `-Encoding UTF8` 或 `[IO.File]::ReadAllText($p,[Text.Encoding]::UTF8)` |
| `New-Object Foo($a+$b, $c+$d)` 报 `op_Addition on Object[]` | PS 5.1 参数解析把 `+` 当运算符 | 用 `[Foo]::new(...)` |
| `[IntPtr]::MinusOne` 传给 P/Invoke 变成 null | 该属性在本环境返回 `$null` | 用 `[IntPtr](-1)` |
| 变量赋值报 `Cannot overwrite variable Host` | `$Host` 是只读自动变量 | 换名（如 `$hostWin`） |
| `Write-Host "..." -f $x` 报错 | `-f` 被当成 `-ForegroundColor` 前缀匹配 | 写 `Write-Host ("..." -f $x)` |
| 内联 `-Command` 里引号/`$` 被外层吞掉 | 外层 shell 预处理 | 一律写脚本文件用 `-File` 调用 |
| `try{}catch{}` 抓不到 `New-Item`/`Remove-Item` 失败 | cmdlet 的错误默认是非终止错误 | 加 `-ErrorAction Stop` 或用 `$?` 判断 |
| `icacls $d /remove /deny "Everyone:(W)"` 静默失败、ACL 残留 | 语法错误（`/remove` 只接受账户名） | **`icacls $d /remove:d Everyone`**；残留的 DENY 会让目录连枚举都失败 |
| 编辑工具报 "file changed since it was read" | 用 pwsh 重存 BOM 后文件 mtime 变了 | 重新 read 再 edit（BOM 重存是幂等的） |

### 5.3 UIA 自动化

| 症状 | 根因 | 正确做法 |
| --- | --- | --- |
| `PropertyCondition` 抛 "must be 'Int32'" | `ProcessIdProperty` 条件值必须是 `Int32` | 传 `[int]$pid`（别传 `[uint32]`） |
| 抓到"错误的窗口"、之后一直超时 | `[Process]::MainWindowHandle` 缓存**首个可见顶层窗口**；现在首个是启动动画窗口（420×420，比主窗口小得多） | 枚举顶层窗口按标题/尺寸/内容挑（主窗口 940×560；UiTests 用"新建下载"文本识别） |
| `FindFirst(RadioButton, "BT")` 找不到 | 页签内容实为"BT下载"，且自定义模板下控件自身 `Name` 为空（文本在内部 `TextBlock`） | 先按文本找元素，再用 `TreeWalker.ControlViewWalker` 向上找目标类型祖先 |
| 隐藏窗口怎么都找不到 | 不可见窗口不在 UIA 树里 | 先让它显示（本项目：再启一个实例触发 `ShowWindow` 事件） |
| 截图/采样采到别人的窗口 | 前台锁定：非前台进程 `SetForegroundWindow` 会失败 | 采样前用 `WindowFromPoint`+PID 校验归属；必要时用"白底窗口垫在后面 + 两次 `SetWindowPos`（自身 HWND_TOP，垫底窗插到自身下方）" |
| `$Host` 冲突（同 5.2） | PowerShell 只读变量 | 换名 |

### 5.4 aria2 / 网络 / 下载站

| 症状 | 根因 | 正确做法 |
| --- | --- | --- |
| 文件名叫成 URL 编号（如 `23_377276`） | 调用方把 URL 末段当 `filename` 发来，被固定成 aria2 的 `out`，覆盖 302 目标/CD | 见 4.3；`DropUrlDerivedFileName` 兜底 + 单测 |
| `curl -I` 拿 403（UA 黑名单） | 部分 CDN（wsyhn 等）拒绝 curl/默认 UA | 用浏览器 UA 或应用同款 `Transmission/2.92`（脚本里做"期望名"推导时注意） |
| `.torrent` 链接"下载失败、没有文件" | `--follow-torrent=mem`：aria2 把种子当 BT 任务直接开始拉，不落 `.torrent`；swarm 不可达就 failed | 预期行为，非文件名 bug（见 7.2） |
| 任务名先显示 URL 末段，后来才变 | 元数据阶段先用 URL 末段占位，aria2 上报真实 `files[0].path` 后轮询覆盖 | 正常；只有"最终落盘名"才是判据 |
| 重启后已删任务复活 | `aria2.session` 残留 | `TombstoneStore.PurgeSessionFile`（启动前过滤），改动任务删除逻辑时别绕过 |

### 5.5 单文件打包 / MSBuild

| 症状 | 根因 | 正确做法 |
| --- | --- | --- |
| 单文件 exe 换目录后 `XamlParseException` | 单文件管线不打包 XBF/PRI/aria2c/图标；`AppContext.BaseDirectory` 指向临时目录 | 嵌入资源 + `StandaloneBootstrap` 解压（见 4.4、`docs\单文件打包指南.md`） |
| exe 改名后启动即崩 | WASDK 1.8+ 按 exe 名找 PRI | `ProjectPriFileName=resources.pri`（固定通用名） |
| 嵌入资源 `CS1508 资源标识符重复`（全塌成 `standalone/`） | 在 `<Target>` 内创建项时 `%(RecursiveDir)%(Filename)` 求值为空 | 用 C# 内联任务（`SetStandaloneLogicalNames`）算 `LogicalName` |
| 干净构建 `MSB3030`（bin 下没有 xbf） | PRI 链 `_GenerateProjectPriConfigurationFiles` 要 bin 下的 xbf，而 XAML 编译器只写 obj | `CopyXbfToBinForPri` 补偿目标 |
| 发布目录缺 xbf/aria2c/图标 | `WindowsPackageType=None` 时 publish 不搬散文件 | `CopyXamlResourcesToPublishDir` 补偿目标 |

### 5.6 Git / 文档 / 流程

| 症状 | 正确做法 |
| --- | --- |
| 中文文件名在 `git status` 里显示成八进制 | 正常（`core.quotepath`）；`git add` 中文名路径直接写字符串即可 |
| LF/CRLF 警告 | 正常（`autocrlf`） |
| 想把草稿脚本/截图入库 | `tools\`（脚本）与 `artifacts\`（证据图）是入库的；`.verify\`、`screenshot\ui_*`、`dist\` 一律不入库 |
| 提交信息风格 | 中文，首行 `fix:`/`feat:`/`docs:` + 一句话；正文分"问题/定位/修复/测试"段落（见 `git log` 最近 6 条） |

---

## 6. 当前进度与状态

### 6.1 版本与提交

| 提交 | 内容 | 状态 |
| --- | --- | --- |
| `113484f` | fix: 下载文件名不再被 URL 末段伪名覆盖 + UiTests 宿主修复 | **已在 main，未发版** |
| `6f0822f` | feat: 磨砂透明度可调（透明度菜单「磨砂浓度」滑块） | **已在 main，未发版** |
| `ce12bc1` | v1.0.6：窗口透明切换 + 启动动画 + 单文件发版 | 已发版（tag `v1.0.6`） |
| `f9d9dd5` / `e374eb8` / `cb800e1` | v1.0.6 收尾文档 / 打包指南 / 单文件独立运行修复 | 已发版 |

版本号需同步的 3 处：`KokonaDownloader.App.csproj`（`Version`/`AssemblyVersion`/`FileVersion`/`InformationalVersion`）、
`Core\Api\ApiService.cs`（ping 的 `Version`）、`extension\manifest.json`（`version`）。

### 6.2 v1.0.6 完成的功能（已发布）

1. **窗口透明切换**：不透明/磨砂透明/黑色纯透明，即时生效、重启保持（原生 Acrylic，不透明强制 A=0xFF）。
2. **启动动画**：SplashWindow 420×420、无多余文字、最短 1.2s、6s 兜底。
3. **单文件发行**：303 MB 单 exe，任意目录可运行，只读目录自动回退。
4. 设置 API 补齐（`transparency`、`minimizeToTrayOnClose`、未知字段不 500）。

### 6.3 main 上未发版的两项

| 项 | 验证数据 |
| --- | --- |
| **磨砂浓度滑块**（`FrostedStrength`） | 白底 6 点采样：0.0→亮度 242 / 0.5→137 / 1.0→61（单调、差 ≥25）；UIA 菜单含 Slider，磨砂模式 enabled=True、不透明模式 False；截图 `artifacts\transparency\flyout-{frosted,opaque}.png` |
| **文件名解析修复** | Core 测试 90/90（+3 新用例）；真实站点 6 PASS（wsyhn 编号链接 ×3、codeload.github.com、api.github.com zipball、7-zip 对照）；桌面单文件 exe 复验通过 |

### 6.4 验证套件最近结果（本次会话实测）

| 套件 | 命令 | 结果 |
| --- | --- | --- |
| Core 测试 | `dotnet test …Core.Tests` | **90/90 通过**（约 55s） |
| 扩展逻辑测试 | `cscript //nologo tests\test_logic.js` | **20/20 通过** |
| UI 宿主 | `dotnet run …UiTests` | **全流程通过**，产出 `ui_01`~`ui_07` 共 7 张截图 |
| 单文件 A/B/C/D | `verify_singlefile_main.ps1` / `_fallback.ps1` / `verify_desktop_launch.ps1` | 全通过（exe 318,204,760 B） |
| 透明度/磨砂 | `verify_opaque_alpha.ps1` / `verify_frosted_strength.ps1` | devFF=0；三档单调 |
| 设置 API | `verify_api_settings.ps1` | ALL PASS |

### 6.5 已发布产物

- **GitHub Release v1.0.6**（id `389103246`，非 draft）：<https://github.com/Hariteki/KokonaDownloader/releases/tag/v1.0.6>
  - `KokonaDownloader_106.exe`（318,200,664 B）※**不含** 6.3 的两项修复
  - `KokonaExtension_106.zip`（37,659 B）、`KokonaDownloader-Tutorial.pdf`（288,628 B）
- **tag**：`v1.0.6`（指向 `ce12bc1`）→ 若发 v1.0.7 需新 tag。
- **桌面副本**（用户自用，非仓库内容）：`C:\Users\lumin\Desktop\KokonaDownloader.exe`
  与 `C:\Users\lumin\Desktop\KokonaSingleFile\` 已刷新为**含文件名修复**的构建（318,204,760 B）。

### 6.6 工作区状态

干净（无未提交改动）。未跟踪且**故意不入库**的 3 个文件：`FINAL_REPORT.md`、
`海兔下载器教程.pdf`、`磁力链接下载功能可行性与技术评估.md`（历史资料；如需入库请显式决定）。

---

## 7. 待办与已知问题

### 7.1 待办（建议优先级）

| 优先级 | 事项 | 说明 / 建议做法 |
| --- | --- | --- |
| **P0** | 发 v1.0.7（把 6.3 两项发出去） | 照第 8 节流程；Release 资产命名沿用 `KokonaDownloader_107.exe` 风格 |
| P1 | 扩展版本与主版本手工同步（3 处） | 建议加一个发版前检查脚本（读 3 处版本号比对） |
| P1 | UiTests 截图输出位置 | 目前写进 `screenshot\`（README 产品截图目录）；建议改到 `artifacts\uitests\` 或加参数 |
| P2 | README 主界面截图仍是 v1.0.4 时期 | 可用最新 `screenshot\ui_07_main_card_matrix_complete.png` 替换 |
| P2 | 无代码签名 | exe 未签名，SmartScreen 会提示"未知发布者"；如需消除要买证书并签名 |
| P2 | 给 `FrostedParams` 加单测 | 它位于 App 工程（非 Core），当前只有像素级脚本验证；可把映射函数挪到 Core 或加 InternalsVisibleTo |
| P2 | 回退目录清理 | `%LOCALAPPDATA%\KokonaDownloader\Standalone` 会随版本累积；可考虑版本化子目录或启动时清理 |

> 已顺手修掉的两处文档/脚本不一致（都是 v1.0.6 加启动动画后遗留的）：
> ① `docs\单文件打包指南.md` 5.1 的资源清单原写"12 个"且缺 `standalone/SplashWindow.xbf`，现改为 13 个并补该行；
> ② `tools\verify_singlefile_main.ps1` 同样只查 12 个，且"解压文件数"只统计顶层目录
> （载荷含 `icons\`、`Themes\` 子目录 → 会误报 `FAIL: only 11 files`），现已改为 13 个资源 + 递归统计。

### 7.2 已知行为（非缺陷，勿"修"）

- **`.torrent` 链接**：`--follow-torrent=mem` 使其直接成为 BT 任务，不落 `.torrent` 文件；
  swarm 不可达时任务 failed 且任务名停在 URL 末段。若产品上想改成"保存 .torrent"，需去掉 `mem` 并调整 UI 预期。
- **任务名短暂显示 URL 末段**：元数据阶段占位，真实路径上报后被覆盖，属预期。
- **黑色纯透明 = 薄磨砂黑**：DWM 无"无模糊纯透明"公开 API；自定义 `SystemBackdrop` 在本项目投影下编译不通
  （`ICompositionSupportsSystemBackdrop` 无 `Compositor`，两套 compositor 不通用）。如需"完全不模糊"要另找方案（分层窗口）。

### 7.3 安全与卫生

- `.verify\gh_token.txt` 是本机 GitHub token（**已 gitignore**）：不要复制进任何入库文件、不要贴进提交信息。
- 历史文档（`v1.0.6-进度与待办.md` 的某条示例命令）里出现过**明文 API 密钥**；新文档一律改为从
  `settings.json` 现读（3.5 节）。若担心泄露，可在客户端设置里重新生成密钥（扩展需重新粘贴）。
- `%APPDATA%\KokonaDownloader\app.log` 会记录路径与 URL，贡献日志前先看一眼是否含隐私信息。

---

## 8. 发版流程（v1.0.7 照抄即可）

> 前置：确保 `%APPDATA%\KokonaDownloader\settings.json` 存在（跑过一次应用），且没有应用/aria2 在跑：
> `Get-Process KokonaDownloader, aria2c -ErrorAction SilentlyContinue | Stop-Process -Force`

**第 1 步 · 改版本号（3 处，必须一致）**

| 文件 | 改什么 |
| --- | --- |
| `src\KokonaDownloader.App\KokonaDownloader.App.csproj` | `Version` / `AssemblyVersion` / `FileVersion` / `InformationalVersion`（+ 注释） |
| `src\KokonaDownloader.Core\Api\ApiService.cs` | ping 的 `Version` 字符串 |
| `extension\manifest.json` | `version` |

**第 2 步 · 测试**

```powershell
dotnet test src\KokonaDownloader.Core.Tests\KokonaDownloader.Core.Tests.csproj -c Release   # 期望 90/90（新增测试后数字会变）
cd <repo>; cscript //nologo tests\test_logic.js                                             # 期望 20/20
dotnet run --project src\KokonaDownloader.UiTests\KokonaDownloader.UiTests.csproj -c Release # 期望 7 张截图
```

**第 3 步 · 打单文件包**（命令见 2.4）→ 确认 `dist\KokonaSingleFile\KokonaDownloader.exe` 大小合理（≈303 MB）。

**第 4 步 · 单文件 A/B/C/D 校验**

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File tools\verify_singlefile_main.ps1      # 13/13 资源 + A + B
powershell -NoProfile -ExecutionPolicy Bypass -File tools\verify_singlefile_fallback.ps1  # C 只读回退（观察 8s+）
# 刷桌面副本后：
powershell -NoProfile -ExecutionPolicy Bypass -File tools\verify_desktop_launch.ps1       # D 桌面启动
```

**第 5 步 · 提交 + 推送 + 打 tag**

```powershell
$git = "C:\Users\lumin\AppData\Local\Programs\MinGit\cmd\git.exe"
& $git -C <repo> add <改动文件…>            # 中文路径直接写；不要 add .verify/
& $git -C <repo> commit -F .verify\commit_msg_xxx.txt   # 中文提交信息，先写进文件避免引号问题
& $git -C <repo> push origin main
& $git -C <repo> tag v1.0.7 <commit>
& $git -C <repo> push origin v1.0.7
& $git -C <repo> ls-remote --tags origin v1.0.7         # 确认远端有 tag
```

**第 6 步 · 造 Release 资产**

- exe：`dist\KokonaSingleFile\KokonaDownloader.exe` → `release\KokonaDownloader_107.exe`
- 扩展：`Compress-Archive -Path extension\* -DestinationPath release\KokonaExtension_107.zip -Force`
  （zip 内必须是 `manifest.json` 在根）
- 教程 PDF：仓库根的 `海兔下载器教程.pdf`

**第 7 步 · 创建 Release + 上传资产**（脚本在 `.verify\`，本地专用）

```powershell
# 1) 写 payload（参考 .verify\release_payload_106.json：tag_name/target_commitish/name/body/draft/prerelease）
#    名字用中文"海兔下载器 v1.0.7"，正文用中文，含"本版新增"与"已知问题"
# 2) POST 创建
$token = [IO.File]::ReadAllText('<repo>\.verify\gh_token.txt').Trim()
$payload = [IO.File]::ReadAllText('<repo>\.verify\release_payload_107.json', [Text.Encoding]::UTF8)
$r = Invoke-RestMethod -Uri "https://api.github.com/repos/Hariteki/KokonaDownloader/releases" -Method Post `
     -Headers @{ Authorization = "token $token"; Accept = "application/vnd.github+json"; "Content-Type" = "application/json" } `
     -Body ([Text.Encoding]::UTF8.GetBytes($payload))
[IO.File]::WriteAllLines('<repo>\.verify\release_id.txt', @($r.id, $r.upload_url))
# 3) 上传资产（改 upload_assets.ps1 里的文件名列表后运行）
powershell -NoProfile -ExecutionPolicy Bypass -File <repo>\.verify\upload_assets.ps1   # 期望每个资产 http=201
```

**第 8 步 · 刷新桌面副本**

```powershell
Copy-Item dist\KokonaSingleFile\KokonaDownloader.exe "$env:USERPROFILE\Desktop\KokonaDownloader.exe" -Force
$sf = "$env:USERPROFILE\Desktop\KokonaSingleFile"
Get-ChildItem $sf -Recurse -File | Remove-Item -Force
Copy-Item dist\KokonaSingleFile\* $sf -Recurse -Force
```

**第 9 步 · 更新文档**

- 在 `v1.0.6-进度与待办.md` 追加本次版本小节（或新建 `v1.0.7-进度与待办.md`），写明：改动、验证数据、Release id、资产大小。
- 更新本手册第 6 节（状态表）与第 7 节（待办勾掉）。
- 提交并推送（文档提交不必再打 tag）。

**第 10 步 · 收尾自检**

- [ ] 远端 `main` == 本地 `main`；tag 已推
- [ ] Release 页面：名字/正文/3 个资产大小正确，非 draft
- [ ] 桌面副本 = 新构建（比字节数）
- [ ] 应用与 aria2 进程已清干净；测试临时目录（`%TEMP%\kokona_*`）已删
- [ ] 被测试脚本改过的设置（`Transparency`、`DefaultDownloadDir`、`GlobalSpeedLimit`）已还原

---

## 9. 改动指引：常见配方 + 回归矩阵

### 9.1 常见改动配方（照着改就不会漏地方）

**配方 A：新增一个设置项**（最容易漏 API 与文档）

1. `Core\Settings\AppSettings.cs`：加属性（给默认值 + 注释说明生效方式）。
2. 生效点：即时生效走 `SettingsStore.Changed`（`ThemeService` / `AppHost.OnSettingsChanged` 已订阅）；
   需要重启引擎的（端口、并发、BT 监听）写进 `EngineConfig` 装配或 `EngineConfigFactory`。
3. `Core\Api\ApiService.cs`：`GET /api/settings` 响应加字段；`ApiSettingsPatch` 加可空属性 + POST 白名单里赋值（**未知字段必须不报错**）。
4. UI：设置窗口（`SettingsWindow.xaml`）或对应 Flyout 里加控件，读 `App.Host.Settings.Current`、写 `Settings.Update`。
5. 文档：本手册 3.4 表格 + 验证脚本（`tools\verify_api_settings.ps1` 的字段断言）。

**配方 B：新增一个窗口/页面**

1. 加 `XxxWindow.xaml` + `.xaml.cs`（继承 `Microsoft.UI.Xaml.Window`，构造函数里 `ThemeService.Register(this)`、关窗时 `Unregister`）。
2. **单文件发布必须能被解压出来**：XBF 是自动递归嵌入的（`EmbedStandalonePayload` 用 `**\*.xbf`），
   但要同步更新 `docs\单文件打包指南.md` 5.1 的资源清单和 `tools\verify_singlefile_main.ps1` 的 `$names`。
3. 用 `WindowEffects.SetMinSize`/`AppWindow.Resize` 固定尺寸时注意 DPI 换算（见 11 节）。
4. 跑 `verify_singlefile_main.ps1` 确认 13→14 个载荷资源都在 exe 里。

**配方 C：新增主题配色**

1. `Core\Themes\ThemeCatalog.cs` 加一条（id/名称/颜色字段），保持该文件顶部注释里的对比度约束。
2. 跑 `dotnet test …Core.Tests`（`ThemeCatalogTests` 会校验 hex 合法性、对比度、亮暗单调性）。
3. UI 自动出现（主题菜单按目录构建），无需改 XAML。

**配方 D：让某个下载站的文件名正确**

1. 先按 4.3 的三条不变量判断属于哪种情形（CD / 302 / 伪名）。
2. 用 `tools\probe_aria2_filename.ps1` 单独问 aria2（排除应用因素），再用 `probe_app_filename.ps1 -FileName <URL末段>` 走应用端到端。
3. 若是新机制（例如 `Content-Disposition: filename*=UTF-8''...`、meta refresh 跳转），在 `FileNameResolutionTests`
   加一个用例（本地服务器可 `AddFile` 自定义响应头、`AddRedirect` 造 302），再改 `DownloadEngine` 相应逻辑。
4. 最后跑 `tools\verify_filename_resolution.ps1`（真实站点）+ 更新本手册 5.4。

**配方 E：改 aria2 启动参数 / BT 行为**

1. `Core\Engine\Aria2Process.cs`（启动参数）、`Core\Engine\Aria2RpcClient.cs`（运行期 RPC）。
2. 跑 `dotnet test …Core.Tests`（`BtIntegrationTests` / `EngineIntegrationTests` 走真实 aria2 + 回环做种）。
3. 注意别破坏：墓碑过滤、`bt-detach-seed-only`、做种参数在**添加任务时**生效（改设置不影响已存在任务）。

### 9.2 回归矩阵

| 我要改… | 先读 | 改完必须跑 |
| --- | --- | --- |
| 文件名/落盘命名 | 4.3 | `dotnet test …Core.Tests`（FileNameResolutionTests）+ `tools\verify_filename_resolution.ps1` |
| 透明度/磨砂/主题 | 4.1、4.6 | `verify_opaque_alpha.ps1` + `verify_frosted_strength.ps1` + `verify_transparency8.ps1`（必要时更新 `artifacts\transparency\*`） |
| 启动流程/启动动画/单实例/磁力确认 | 4.2、4.5 | `probe_windows.ps1`、`probe_splash_args.ps1`、`dotnet run …UiTests` |
| 设置项 / 设置 API | 3.4、3.5 | `verify_api_settings.ps1` + `dotnet test …Core.Tests`（ApiServiceTests/Stage2） |
| 扩展捕获/转发逻辑 | 3.6 | `cscript //nologo tests\test_logic.js` + `dotnet test …Core.Tests`（ExtensionContractTests） |
| csproj / 打包链 / PRI | 4.4、5.5、`docs\单文件打包指南.md` | 重新 publish + `verify_singlefile_main.ps1` + `verify_singlefile_fallback.ps1` |
| aria2 启动参数 / BT | 4.8 | `dotnet test …Core.Tests`（BtIntegrationTests）——注意 BT 测试需要回环基础设施，别在弱网环境误判 |
| UI 布局（主窗口/设置/进度窗） | — | `dotnet run …UiTests`（会重出 `ui_*.png` 供人工比对） |
| 版本号 | 第 8 节 | 第 2、3、4 步 |

---

## 10. 新增验证脚本的写法约定

1. **编码**：含中文必须 UTF-8 **带 BOM**；能用纯 ASCII 就纯 ASCII（如 `probe_aria2_filename.ps1`）。
2. **调用方式**：`powershell.exe -NoProfile -ExecutionPolicy Bypass -File <脚本>`，别用 `-Command`。
3. **时长**：脚本自己负责"杀进程 → 启动 → 等待 → 采样/操作 → 杀进程"；等待应用就绪至少 **9s**（单文件版 bundle 解压约 6s）。
4. **输出**：每项检查打印 `PASS:` / `FAIL:` / `WARN:`，最后可给一行汇总；便于人眼与 AI 快速判断。
5. **采样类**：先校验"采样点归属本应用进程"（`WindowFromPoint` + `GetWindowThreadProcessId`），
   否则容易被别的窗口盖住而得到假结论；需要纯色背景时用"White 窗体垫在后面 + 两次 `SetWindowPos`"。
6. **改设置文件**：改前备份原值、结束时恢复（脚本里直接改 JSON 用正则替换键值即可，注意 `-Encoding UTF8`）。
7. **不留痕**：结束时删临时目录、恢复设置、杀干净进程（`KokonaDownloader` 与 `aria2c` 都要）。
8. **入库位置**：验证脚本放 `tools\`（会提交）；一次性调试脚本放 `.verify\`（不提交）。

---

## 11. 本机环境事实

| 项 | 值 |
| --- | --- |
| 仓库路径 | `D:\Project\kokonaDown` |
| 屏幕 | **1920×1080 @125% 缩放**（DPI-unaware 的 PowerShell/探针看到的是虚拟化后的 1536×864 与 0.8× 坐标——写脚本比对尺寸时必须留意这点） |
| .NET SDK | 8.0.401（`C:\Program Files\dotnet\dotnet.exe`） |
| PowerShell | 5.1.26100.7920（`C:\Windows\System32\WindowsPowerShell\v1.0\powershell.exe`） |
| Git | MinGit `C:\Users\lumin\AppData\Local\Programs\MinGit\cmd\git.exe` |
| 应用数据 | `%APPDATA%\KokonaDownloader\`（settings.json / tasks.json / app.log / engine\） |
| 单文件回退目录 | `%LOCALAPPDATA%\KokonaDownloader\Standalone` |
| 默认端口 | API 16800 / aria2 RPC 16801 / BT 监听 51413 |
| UI 尺寸契约 | 主窗口 940×560 DIP（= 物理 1175×700 @125%，最小尺寸同值，`WindowEffects.SetMinSize`）；启动动画窗口 420×420 **物理**（未乘 DPI）；磁力确认窗 500×430 DIP（= 物理 625×538）；设置窗/进度窗另有布局 |
| 桌面副本 | `%USERPROFILE%\Desktop\KokonaDownloader.exe`、`%USERPROFILE%\Desktop\KokonaSingleFile\` |
| 常用验证环境 | 本机可直连 GitHub / 部分国内下载站（wsyhn 可用，SourceForge 403），aria2 UA 已被设置为 `Transmission/2.92` |

---

## 12. 文档索引

| 文档 | 讲什么 | 什么时候看 |
| --- | --- | --- |
| **本文**（`项目维护手册.md`） | 交接总纲：命令、架构、不变量、踩坑、进度、发版流程 | 接手第一份就看它 |
| `README.md` | 面向用户/开发者的门面（功能、快捷键、架构图、构建） | 对外介绍改这里 |
| `docs\单文件打包指南.md` | 单文件 exe 的嵌入/解压/PRI 机制与验证清单（含 MSBuild 细节） | 动 csproj、打包、PRI 前必读 |
| `v1.0.6-进度与待办.md` | v1.0.6 需求、实现、验证数据、踩坑（含磨砂浓度与文件名修复两节） | 查历史决策与实测数据 |
| `FINAL_REPORT.md` | v1.0.5 单文件独立运行修复报告（未入库） | 查 v1.0.5 那次的根因分析 |
| `磁力链接下载功能可行性与技术评估.md` | BT/磁力功能技术评估（未入库） | 扩展 BT 功能前参考 |
| `海兔下载器教程.pdf` | 用户教程（同步作为 Release 资产，未入库） | 发版需要、或改用户可见流程时 |
| `tools\` 脚本头部注释 | 每个验证脚本"验证什么、判据是什么" | 改完对应功能要验证时 |

---

> **维护本文的约定**：每次发版后更新第 6 节（状态/版本/产物）与第 7 节（待办）；
> 新增踩坑写进第 5 节对应小类；新增验证脚本写进 2.5 表格与第 9 节矩阵。
> 保持"命令可直接复制"与"判据明确"两条：宁可多写一句复现命令，不要只写结论。

---

## 附录 A · 术语表

| 术语 | 含义 |
| --- | --- |
| **aria2 / aria2c** | 第三方下载引擎（子进程），本项目通过 JSON-RPC 驱动它；`vendor\aria2\aria2c.exe` |
| **RPC / JSON-RPC** | 客户端 → aria2 的控制协议（`addUri`/`tellStatus`/`changeGlobalOption`…），端口 16801 |
| **gid** | aria2 给每个下载任务分配的全局 id；本项目所有任务操作都以 gid 为键 |
| **infohash / btih** | BT 种子的唯一标识（magnet 里的 `urn:btih:`），用于磁力去重 |
| **做种（seeding）** | BT 下载完成后继续上传；由 `BtSeedEnabled`/`SeedRatio`/`SeedTimeMinutes` 控制 |
| **tracker** | BT 里帮助 peer 互相发现的服务器；本项目支持自动刷新社区列表（24h TTL） |
| **墓碑（Tombstone）** | "已删除任务"的记录，防止 aria2 重启后从 `aria2.session` 里把任务复活 |
| **out** | aria2 的落盘文件名选项；**本项目最重要的坑点**（见 4.3） |
| **Content-Disposition** | HTTP 响应头，服务器在这里给出真实文件名（`attachment; filename=...`） |
| **载荷（standalone payload）** | 单文件 exe 里内嵌、启动时解压到 exe 旁的 13 个文件（XBF/PRI/aria2c/图标） |
| **XBF / PRI** | WinUI 的编译后 XAML 与资源索引文件；XAML 运行时从 exe 目录加载它们 |
| **DIP** | 设备无关像素（1/96 英寸）；`AppWindow.Resize` 用的是**物理像素**，两者需按 DPI 换算 |
| **UIA** | Windows UI Automation，本项目所有 UI 验证脚本靠它定位/点击控件 |
| **TintOpacity / LuminosityOpacity** | 原生 Acrylic 的"染色层/明度层"不透明度，磨砂浓度滑块映射的就是这两个值 |
| **逐任务限速 / 全局限速** | 前者 `max-download-limit`（新建对话框里选），后者 `max-overall-download-limit`（设置里、可热更新） |
