<Window
    x:Class="KokonaDownloader.App.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:converters="using:KokonaDownloader.App.Converters"
    mc:Ignorable="d"
    Title="海兔下载器">

    <Grid x:Name="RootGrid" Background="{ThemeResource SolidBackgroundFillColorBaseBrush}">
        <Grid.Resources>
            <converters:BoolToVisibilityConverter x:Key="BoolToVis"/>
            <!-- Metro 风格 Tab：纯文字大标题，无圆点无下划线，选中时主题色+加粗 -->
            <Style x:Key="FilterTabStyle" TargetType="RadioButton">
                <Setter Property="MinWidth" Value="0"/>
                <Setter Property="MinHeight" Value="0"/>
                <Setter Property="Padding" Value="12,6"/>
                <Setter Property="FontSize" Value="24"/>
                <Setter Property="FontWeight" Value="Bold"/>
                <Setter Property="Background" Value="Transparent"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="RadioButton">
                            <Grid x:Name="TabRoot" Background="Transparent">
                                <ContentPresenter x:Name="TabContent" Content="{TemplateBinding Content}"
                                                  Padding="{TemplateBinding Padding}"
                                                  HorizontalAlignment="Center" VerticalAlignment="Center">
                                    <ContentPresenter.Foreground>
                                        <SolidColorBrush x:Name="TabBrush" Color="{ThemeResource TabInactiveColor}"/>
                                    </ContentPresenter.Foreground>
                                </ContentPresenter>
                                <VisualStateManager.VisualStateGroups>
                                    <VisualStateGroup x:Name="CheckStates">
                                        <VisualState x:Name="Checked">
                                            <Storyboard>
                                                <ColorAnimation Storyboard.TargetName="TabBrush" Storyboard.TargetProperty="Color"
                                                                To="{ThemeResource TabAccentColor}" Duration="0:0:0.18">
                                                    <ColorAnimation.EasingFunction>
                                                        <CubicEase EasingMode="EaseOut"/>
                                                    </ColorAnimation.EasingFunction>
                                                </ColorAnimation>
                                            </Storyboard>
                                        </VisualState>
                                        <VisualState x:Name="Unchecked">
                                            <Storyboard>
                                                <ColorAnimation Storyboard.TargetName="TabBrush" Storyboard.TargetProperty="Color"
                                                                To="{ThemeResource TabInactiveColor}" Duration="0:0:0.15">
                                                    <ColorAnimation.EasingFunction>
                                                        <CubicEase EasingMode="EaseOut"/>
                                                    </ColorAnimation.EasingFunction>
                                                </ColorAnimation>
                                            </Storyboard>
                                        </VisualState>
                                    </VisualStateGroup>
                                </VisualStateManager.VisualStateGroups>
                            </Grid>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
            <Style x:Key="DangerButtonStyle" TargetType="Button">
                <Setter Property="Background" Value="{ThemeResource SystemFillColorCriticalBrush}"/>
                <Setter Property="Foreground" Value="White"/>
                <Setter Property="BorderThickness" Value="0"/>
                <Setter Property="CornerRadius" Value="{ThemeResource ControlCornerRadius}"/>
                <Setter Property="Padding" Value="{ThemeResource ButtonPadding}"/>
                <Setter Property="MinHeight" Value="32"/>
                <Setter Property="FontFamily" Value="{ThemeResource ContentControlThemeFontFamily}"/>
                <Setter Property="FontSize" Value="{ThemeResource ControlContentThemeFontSize}"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <ContentPresenter x:Name="ContentPresenter"
                                              Background="{TemplateBinding Background}"
                                              Foreground="{TemplateBinding Foreground}"
                                              BorderBrush="{TemplateBinding BorderBrush}"
                                              BorderThickness="{TemplateBinding BorderThickness}"
                                              CornerRadius="{TemplateBinding CornerRadius}"
                                              Padding="{TemplateBinding Padding}"
                                              HorizontalContentAlignment="{TemplateBinding HorizontalContentAlignment}"
                                              VerticalContentAlignment="{TemplateBinding VerticalContentAlignment}"
                                              Content="{TemplateBinding Content}"
                                              ContentTemplate="{TemplateBinding ContentTemplate}"
                                              ContentTransitions="{TemplateBinding ContentTransitions}">
                                <VisualStateManager.VisualStateGroups>
                                    <VisualStateGroup x:Name="CommonStates">
                                        <VisualState x:Name="Normal"/>
                                        <VisualState x:Name="PointerOver">
                                            <Storyboard>
                                                <ObjectAnimationUsingKeyFrames Storyboard.TargetName="ContentPresenter" Storyboard.TargetProperty="Background">
                                                    <DiscreteObjectKeyFrame KeyTime="0" Value="{ThemeResource AppDangerFillHoverBrush}"/>
                                                </ObjectAnimationUsingKeyFrames>
                                            </Storyboard>
                                        </VisualState>
                                        <VisualState x:Name="Pressed">
                                            <Storyboard>
                                                <ObjectAnimationUsingKeyFrames Storyboard.TargetName="ContentPresenter" Storyboard.TargetProperty="Background">
                                                    <DiscreteObjectKeyFrame KeyTime="0" Value="{ThemeResource AppDangerFillPressedBrush}"/>
                                                </ObjectAnimationUsingKeyFrames>
                                            </Storyboard>
                                        </VisualState>
                                        <VisualState x:Name="Disabled">
                                            <Storyboard>
                                                <ObjectAnimationUsingKeyFrames Storyboard.TargetName="ContentPresenter" Storyboard.TargetProperty="Opacity">
                                                    <DiscreteObjectKeyFrame KeyTime="0" Value="0.55"/>
                                                </ObjectAnimationUsingKeyFrames>
                                            </Storyboard>
                                        </VisualState>
                                    </VisualStateGroup>
                                </VisualStateManager.VisualStateGroups>
                            </ContentPresenter>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
        </Grid.Resources>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 标题栏拖拽区：无系统色带，标题融入内容（与设置窗口同款沉浸式样式） -->
        <Border x:Name="TitleDrag" Grid.Row="0" Height="40" Background="Transparent">
            <TextBlock Text="海兔下载器" Margin="24,0,0,0" VerticalAlignment="Center"
                       FontSize="14" FontWeight="SemiBold"
                       Foreground="{ThemeResource TextFillColorPrimaryBrush}"/>
        </Border>

        <!-- 顶部工具栏 -->
        <Grid Grid.Row="1" Padding="16,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Orientation="Horizontal" Spacing="8">
                <Button x:Name="BtnNew" Click="OnNewClick" Style="{ThemeResource AccentButtonStyle}">
                    <StackPanel Orientation="Horizontal" Spacing="6">
                        <FontIcon Glyph="&#xE710;" FontSize="14"/>
                        <TextBlock Text="新建下载"/>
                    </StackPanel>
                </Button>
                <Button x:Name="BtnPauseAll" Click="OnPauseAllClick">
                    <StackPanel Orientation="Horizontal" Spacing="6">
                        <FontIcon Glyph="&#xE769;" FontSize="14"/>
                        <TextBlock Text="全部暂停"/>
                    </StackPanel>
                </Button>
                <Button x:Name="BtnResumeAll" Click="OnResumeAllClick">
                    <StackPanel Orientation="Horizontal" Spacing="6">
                        <FontIcon Glyph="&#xE768;" FontSize="14"/>
                        <TextBlock Text="全部开始"/>
                    </StackPanel>
                </Button>
            </StackPanel>

            <StackPanel Grid.Column="2" Orientation="Horizontal" Spacing="8">
                <Button x:Name="BtnTheme" ToolTipService.ToolTip="主题配色" AutomationProperties.Name="主题配色">
                    <FontIcon Glyph="&#xE790;" FontSize="16"/>
                    <Button.Flyout>
                        <MenuFlyout x:Name="ThemeMenu" Placement="Bottom"/>
                    </Button.Flyout>
                </Button>
                <Button x:Name="BtnSettings" Click="OnSettingsClick" ToolTipService.ToolTip="设置" AutomationProperties.Name="设置">
                    <FontIcon Glyph="&#xE713;" FontSize="16"/>
                </Button>
            </StackPanel>
        </Grid>

        <!-- 筛选与搜索：Tab 栏 + 右侧选择按钮 + 搜索框 -->
        <Grid Grid.Row="2" Padding="16,4" ColumnSpacing="8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0" Orientation="Horizontal" Spacing="2" VerticalAlignment="Bottom">
                <RadioButton x:Name="FilterAll" Content="全部" GroupName="Filter" IsChecked="True" Click="OnFilterChanged" Style="{StaticResource FilterTabStyle}"/>
                <RadioButton x:Name="FilterActive" Content="下载中" GroupName="Filter" Click="OnFilterChanged" Style="{StaticResource FilterTabStyle}"/>
                <RadioButton x:Name="FilterPaused" Content="已暂停" GroupName="Filter" Click="OnFilterChanged" Style="{StaticResource FilterTabStyle}"/>
                <RadioButton x:Name="FilterDone" Content="已完成" GroupName="Filter" Click="OnFilterChanged" Style="{StaticResource FilterTabStyle}"/>
                <RadioButton x:Name="FilterFailed" Content="失败" GroupName="Filter" Click="OnFilterChanged" Style="{StaticResource FilterTabStyle}"/>
                <Border Width="1" Height="16" Margin="8,0" VerticalAlignment="Center"
                        Background="{ThemeResource DividerStrokeColorDefaultBrush}"/>
                <Button x:Name="BtnBatch" ToolTipService.ToolTip="筛选任务" AutomationProperties.Name="筛选任务"
                        Background="Transparent" BorderThickness="0" Padding="8,2" MinHeight="28">
                    <StackPanel Orientation="Horizontal" Spacing="4">
                        <FontIcon Glyph="&#xE71C;" FontSize="13"/>
                        <TextBlock Text="筛选" FontSize="12" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                    </StackPanel>
                    <Button.Flyout>
                        <MenuFlyout Placement="Bottom">
                            <MenuFlyoutItem Text="全选" Click="OnSelectAllClick"/>
                            <MenuFlyoutItem Text="反选" Click="OnInvertSelectionClick"/>
                            <MenuFlyoutItem Text="取消选择" Click="OnClearSelectionClick"/>
                            <MenuFlyoutSeparator/>
                            <MenuFlyoutItem Text="暂停选中" Click="OnBatchPauseClick">
                                <MenuFlyoutItem.Icon>
                                    <FontIcon Glyph="&#xE769;"/>
                                </MenuFlyoutItem.Icon>
                            </MenuFlyoutItem>
                            <MenuFlyoutItem Text="继续选中" Click="OnBatchResumeClick">
                                <MenuFlyoutItem.Icon>
                                    <FontIcon Glyph="&#xE768;"/>
                                </MenuFlyoutItem.Icon>
                            </MenuFlyoutItem>
                            <MenuFlyoutItem Text="删除选中" Click="OnBatchDeleteClick" Foreground="{ThemeResource SystemFillColorCriticalBrush}">
                                <MenuFlyoutItem.Icon>
                                    <FontIcon Glyph="&#xE74D;" Foreground="{ThemeResource SystemFillColorCriticalBrush}"/>
                                </MenuFlyoutItem.Icon>
                            </MenuFlyoutItem>
                        </MenuFlyout>
                    </Button.Flyout>
                </Button>
            </StackPanel>
            <AutoSuggestBox x:Name="SearchBox" Grid.Column="1" PlaceholderText="搜索"
                            QueryIcon="Find" Width="200" MinWidth="120" VerticalAlignment="Center"
                            TextChanged="OnSearchTextChanged"/>
        </Grid>

        <!-- 任务列表：Extended 支持单击选中/Ctrl/Shift 多选，"全选/反选"菜单也可用 -->
        <ListView x:Name="TaskList" Grid.Row="3" Padding="12,0"
                  SelectionMode="Extended" IsItemClickEnabled="False"
                  RightTapped="OnTaskRightTapped">
            <!-- 圆角卡片修复：本 SDK 默认模板中 presenter 的 CornerRadius 绑定的是
                 主题资源键而非控件属性，样式里直接设 CornerRadius 不生效。此模板
                 逐字复制自 generic.xaml 的 DefaultListViewItemStyle，唯一差异是
                 CornerRadius 改为 TemplateBinding，由下方 Setter 提供圆角；
                 Margin 让相邻卡片留出间距、右侧与列表左 Padding 对齐 -->
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                    <Setter Property="MinHeight" Value="76"/>
                    <Setter Property="Padding" Value="4,2"/>
                    <Setter Property="Margin" Value="0,3,12,3"/>
                    <Setter Property="CornerRadius" Value="8"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ListViewItem">
                                <ListViewItemPresenter x:Name="Root"
                                    ContentTransitions="{TemplateBinding ContentTransitions}"
                                    Control.IsTemplateFocusTarget="True"
                                    FocusVisualMargin="{TemplateBinding FocusVisualMargin}"
                                    FocusVisualPrimaryBrush="{TemplateBinding FocusVisualPrimaryBrush}"
                                    FocusVisualPrimaryThickness="{TemplateBinding FocusVisualPrimaryThickness}"
                                    FocusVisualSecondaryBrush="{TemplateBinding FocusVisualSecondaryBrush}"
                                    FocusVisualSecondaryThickness="{TemplateBinding FocusVisualSecondaryThickness}"
                                    SelectionCheckMarkVisualEnabled="{ThemeResource ListViewItemSelectionCheckMarkVisualEnabled}"
                                    CheckBrush="{ThemeResource ListViewItemCheckBrush}"
                                    CheckBoxBrush="{ThemeResource ListViewItemCheckBoxBrush}"
                                    DragBackground="{ThemeResource ListViewItemDragBackground}"
                                    DragForeground="{ThemeResource ListViewItemDragForeground}"
                                    FocusBorderBrush="{ThemeResource ListViewItemFocusBorderBrush}"
                                    FocusSecondaryBorderBrush="{ThemeResource ListViewItemFocusSecondaryBorderBrush}"
                                    PlaceholderBackground="{ThemeResource ListViewItemPlaceholderBackground}"
                                    PointerOverBackground="{ThemeResource ListViewItemBackgroundPointerOver}"
                                    PointerOverForeground="{ThemeResource ListViewItemForegroundPointerOver}"
                                    SelectedBackground="{ThemeResource ListViewItemBackgroundSelected}"
                                    SelectedForeground="{ThemeResource ListViewItemForegroundSelected}"
                                    SelectedPointerOverBackground="{ThemeResource ListViewItemBackgroundSelectedPointerOver}"
                                    PressedBackground="{ThemeResource ListViewItemBackgroundPressed}"
                                    SelectedPressedBackground="{ThemeResource ListViewItemBackgroundSelectedPressed}"
                                    DisabledOpacity="{ThemeResource ListViewItemDisabledThemeOpacity}"
                                    DragOpacity="{ThemeResource ListViewItemDragThemeOpacity}"
                                    ReorderHintOffset="{ThemeResource ListViewItemReorderHintThemeOffset}"
                                    HorizontalContentAlignment="{TemplateBinding HorizontalContentAlignment}"
                                    VerticalContentAlignment="{TemplateBinding VerticalContentAlignment}"
                                    ContentMargin="{TemplateBinding Padding}"
                                    CheckMode="{ThemeResource ListViewItemCheckMode}"
                                    CornerRadius="{TemplateBinding CornerRadius}"
                                    CheckPressedBrush="{ThemeResource ListViewItemCheckPressedBrush}"
                                    CheckDisabledBrush="{ThemeResource ListViewItemCheckDisabledBrush}"
                                    CheckBoxPointerOverBrush="{ThemeResource ListViewItemCheckBoxPointerOverBrush}"
                                    CheckBoxPressedBrush="{ThemeResource ListViewItemCheckBoxPressedBrush}"
                                    CheckBoxDisabledBrush="{ThemeResource ListViewItemCheckBoxDisabledBrush}"
                                    CheckBoxSelectedBrush="{ThemeResource ListViewItemCheckBoxSelectedBrush}"
                                    CheckBoxSelectedPointerOverBrush="{ThemeResource ListViewItemCheckBoxSelectedPointerOverBrush}"
                                    CheckBoxSelectedPressedBrush="{ThemeResource ListViewItemCheckBoxSelectedPressedBrush}"
                                    CheckBoxSelectedDisabledBrush="{ThemeResource ListViewItemCheckBoxSelectedDisabledBrush}"
                                    CheckBoxBorderBrush="{ThemeResource ListViewItemCheckBoxBorderBrush}"
                                    CheckBoxPointerOverBorderBrush="{ThemeResource ListViewItemCheckBoxPointerOverBorderBrush}"
                                    CheckBoxPressedBorderBrush="{ThemeResource ListViewItemCheckBoxPressedBorderBrush}"
                                    CheckBoxDisabledBorderBrush="{ThemeResource ListViewItemCheckBoxDisabledBorderBrush}"
                                    CheckBoxCornerRadius="{ThemeResource ListViewItemCheckBoxCornerRadius}"
                                    SelectionIndicatorCornerRadius="{ThemeResource ListViewItemSelectionIndicatorCornerRadius}"
                                    SelectionIndicatorVisualEnabled="{ThemeResource ListViewItemSelectionIndicatorVisualEnabled}"
                                    SelectionIndicatorBrush="{ThemeResource ListViewItemSelectionIndicatorBrush}"
                                    SelectionIndicatorPointerOverBrush="{ThemeResource ListViewItemSelectionIndicatorPointerOverBrush}"
                                    SelectionIndicatorPressedBrush="{ThemeResource ListViewItemSelectionIndicatorPressedBrush}"
                                    SelectionIndicatorDisabledBrush="{ThemeResource ListViewItemSelectionIndicatorDisabledBrush}"
                                    SelectedDisabledBackground="{ThemeResource ListViewItemBackgroundSelectedDisabled}" />
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </ListView.ItemContainerStyle>
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid Padding="16,12" ColumnSpacing="12" Background="Transparent"
                          DoubleTapped="OnItemDoubleTapped" ToolTipService.ToolTip="双击打开所在位置">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="150"/>
                            <ColumnDefinition Width="110"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <!-- 文件名等信息列（双击整张卡片打开所在位置，事件挂在模板根 Grid） -->
                        <StackPanel Grid.Column="0" Spacing="4">
                            <TextBlock Text="{Binding Name}" FontWeight="SemiBold" TextTrimming="CharacterEllipsis" FontSize="14"/>
                            <TextBlock Text="{Binding StatusText}" FontSize="12" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                            <TextBlock Text="{Binding SizeText}" FontSize="11" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                        </StackPanel>

                        <TextBlock Grid.Column="1" Text="{Binding SpeedText}" VerticalAlignment="Center" FontSize="13"/>
                        <TextBlock Grid.Column="2" Text="{Binding EtaText}" VerticalAlignment="Center" FontSize="12" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>

                        <StackPanel Grid.Column="3" VerticalAlignment="Center" Spacing="3">
                            <StackPanel Orientation="Horizontal" Spacing="4">
                                <Button Padding="6" Click="OnItemPauseClick" Visibility="{Binding PauseVisible, Converter={StaticResource BoolToVis}}" ToolTipService.ToolTip="暂停">
                                    <FontIcon Glyph="&#xE769;" FontSize="13"/>
                                </Button>
                                <Button Padding="6" Click="OnItemResumeClick" Visibility="{Binding ResumeVisible, Converter={StaticResource BoolToVis}}" ToolTipService.ToolTip="继续">
                                    <FontIcon Glyph="&#xE768;" FontSize="13"/>
                                </Button>
                                <Button Padding="6" Click="OnItemOpenClick" ToolTipService.ToolTip="打开文件夹">
                                    <FontIcon Glyph="&#xE8DA;" FontSize="13"/>
                                </Button>
                                <Button Padding="6" Click="OnItemDeleteClick" ToolTipService.ToolTip="删除">
                                    <FontIcon Glyph="&#xE74D;" FontSize="13"/>
                                </Button>
                            </StackPanel>
                            <!-- 过渡小字：点击暂停/继续后立即出现（图标同步乐观切换），引擎确认后消失；
                                 常驻占位（透明）保证行高恒定不跳动 -->
                            <TextBlock Text="{Binding PendingText}" FontSize="10" Height="13" Opacity="{Binding PendingOpacity}"
                                       HorizontalAlignment="Right" Margin="0,0,4,0"
                                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                        </StackPanel>

                        <ProgressBar Grid.Row="1" Grid.ColumnSpan="4" Value="{Binding Progress}" Minimum="0" Maximum="100"
                                     HorizontalAlignment="Stretch" CornerRadius="9" Margin="0,8,0,0"/>

                        <!-- 右上角红绿灯：绿=下载中 黄=暂停/排队 蓝=已完成 红=失败；底层为同色径向光晕 -->
                        <Ellipse Grid.ColumnSpan="4" Width="20" Height="20" HorizontalAlignment="Right" VerticalAlignment="Top"
                                 Margin="0,-14,-16,0" Fill="{Binding StatusGlowBrush}" IsHitTestVisible="False"/>
                        <Ellipse Grid.ColumnSpan="4" Width="10" Height="10" HorizontalAlignment="Right" VerticalAlignment="Top"
                                 Margin="0,-9,-11,0" Fill="{Binding StatusDotBrush}"/>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <!-- 空状态：跨列表+底部状态栏两行，在可视区域中央居中 -->
        <StackPanel x:Name="EmptyState" Grid.Row="3" Grid.RowSpan="2" HorizontalAlignment="Center" VerticalAlignment="Center" Spacing="8" Visibility="Collapsed">
            <FontIcon Glyph="&#xE896;" FontSize="48" Foreground="{ThemeResource TextFillColorDisabledBrush}" HorizontalAlignment="Center"/>
            <TextBlock Text="暂无下载任务" FontSize="16" Foreground="{ThemeResource TextFillColorSecondaryBrush}" HorizontalAlignment="Center"/>
            <TextBlock Text="点击左上角“新建下载”开始" FontSize="12" Foreground="{ThemeResource TextFillColorTertiaryBrush}" HorizontalAlignment="Center"/>
        </StackPanel>

        <!-- 底部状态栏 -->
        <Grid Grid.Row="4" Padding="16,8" ColumnSpacing="24">
            <StackPanel Orientation="Horizontal" Spacing="24">
                <StackPanel Orientation="Horizontal" Spacing="6">
                    <FontIcon Glyph="&#xE896;" FontSize="13" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                    <TextBlock x:Name="GlobalSpeedText" Text="0 B/s" FontSize="13"/>
                </StackPanel>
                <TextBlock x:Name="ActiveCountText" Text="活动 0 · 等待 0" FontSize="13" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                <TextBlock x:Name="ConnStatusText" Text="引擎运行中" FontSize="13" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
