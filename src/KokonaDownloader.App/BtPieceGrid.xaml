<UserControl
    x:Class="KokonaDownloader.App.BtPieceGrid"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:KokonaDownloader.App">
    <UserControl.Resources>
        <local:PieceStateToBrushConverter x:Key="PieceBrushConverter"/>
    </UserControl.Resources>
    <StackPanel Spacing="6">
        <ItemsRepeater x:Name="Repeater" Visibility="Collapsed">
            <ItemsRepeater.Layout>
                <UniformGridLayout Orientation="Horizontal"
                                   MinItemWidth="12" MinItemHeight="12"
                                   MinRowSpacing="3" MinColumnSpacing="3"
                                   ItemsJustification="Start"/>
            </ItemsRepeater.Layout>
            <ItemsRepeater.ItemTemplate>
                <DataTemplate x:DataType="local:PieceVm">
                    <Border Width="{x:Bind Side, Mode=OneWay}" Height="{x:Bind Side, Mode=OneWay}" CornerRadius="2"
                            Background="{x:Bind State, Mode=OneWay, Converter={StaticResource PieceBrushConverter}}"/>
                </DataTemplate>
            </ItemsRepeater.ItemTemplate>
        </ItemsRepeater>
        <TextBlock x:Name="MetaText" FontSize="11" TextWrapping="NoWrap" Visibility="Collapsed"
                   Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
    </StackPanel>
</UserControl>
