# 海兔下载器 v1.0.6 开发进度与待办

> 更新时间：2026-09-15 19:40
> 当前分支提交：`ce12bc1`（v1.0.6 全部改动），已推送 `origin/main`，tag `v1.0.6` 已推送
> 版本号：**1.0.6**（✅ 已发版：GitHub Release v1.0.6 已创建并上传全部资产）

---

## 一、本版需求（用户原话）

1. 主窗口新增**透明主题切换**：在主题色按钮旁边加一个透明按钮，在有主题色的基础上提供
   **黑色纯透明 / 磨砂透明 / 不透明**三种选项；点击后**立即生效**；需要**截图验证**是否生效。
2. 新增**启动动画**：一启动软件立马出现（首次启动等待较久，用户不知道是否点成功）；
   **不能有多余文字**，最多只能有下载器名字。
3. 版本号升到 1.0.6，按 `docs/单文件打包指南.md` 打包，提交/打 tag/发 Release，刷新桌面副本。

---

## 二、已完成

### 1. 版本号 → 1.0.6（已完成）

| 文件 | 改动 |
| --- | --- |
| `src/KokonaDownloader.App/KokonaDownloader.App.csproj` | `Version` / `AssemblyVersion` / `FileVersion` / `InformationalVersion` = 1.0.6，注释同步 |
| `src/KokonaDownloader.Core/Api/ApiService.cs` | `/api/ping` 返回的 `Version` = `"1.0.6"` |
| `extension/manifest.json` | `version` = `1.0.6` |
| `docs/单文件打包指南.md` | 适用版本标注 v1.0.6+、示例版本号、ping 期望值、版本历史新增一行 |

### 2. 窗口透明度切换（功能已完成并实测生效）

- **UI**：`MainWindow.xaml` 在「主题配色」与「设置」之间新增 `BtnTransparency`（图标 E7B3），
  点开 `TransparencyMenu` 三项：`不透明` / `磨砂透明` / `黑色纯透明`，当前项显示对勾（E73E）。
- **设置持久化**：`AppSettings.Transparency`（枚举 `TransparencyMode`，JSON 存字符串，默认 `Opaque`），
  与主题一致走 `SettingsStore.Update → Changed → ThemeService.Apply()`，切换即写盘、重启保持。
- **即时生效**：点击菜单项 → `ThemeService.SetTransparencyMode(mode)` → 全窗口重刷。已用
  UI 自动化模拟真实点击测量（见下方数据）。
- **实现方式（本版重要变更）**：
  最初用 XAML 的 `MicaBackdrop` / `DesktopAcrylicBackdrop`，**实测"设了但桌面不透出来"**：
  窗口采样点亮度恒定（不透明恒 32、磨砂恒 37、黑色恒 0），完全不随窗口后面的内容变化。
  改为本项目进度窗已验证生效的**原生 `DesktopAcrylicController`**（`WindowEffects.TryApplyAcrylicTinted`），
  支持自定义 `TintColor / TintOpacity / LuminosityOpacity / Kind`，并在切换模式时释放上一个控制器
  （`ThemeService._backdrops`）。改完实测三种模式行为正确。

**实测数据**（`tools/verify_transparency8.ps1`，每次启动固定一种模式，采样窗口内 10 个点，
与"杀掉进程后同一坐标的桌面亮度"对比；`app owns 10/10` 表示采样点确实属于本应用窗口）：

| 模式 | 窗口内 10 点亮度 | 桌面底 10 点亮度 | 结论 |
| --- | --- | --- | --- |
| 不透明 | 30 30 30 30 30 30 30 30 30 30 | 232 30 38 27 29 36 171 36 70 64 | 恒定、与背景无关 → **正确的不透明** |
| 磨砂透明 | 58 33 98 51 47 39 86 51 44 46 | 29 34 172 30 32 39 155 39 38 45 | 桌面亮处(172→98、155→86)明显透出 → **磨砂透明生效** |
| 黑色纯透明 | 65 71 28 54 48 54 33 80 161 81 | 24 25 30 24 148 27 35 29 183 81 | 桌面亮处(183→161、148→48)透出、暗处仍暗 → **透明生效** |

**点击即时生效实测**（UI 自动化点击按钮+菜单项；当时用中间版脚本完成，最终回归脚本为 `tools/verify_transparency8.ps1`）：
`不透明→磨砂透明` 画面差 38.7、`磨砂透明→黑色纯透明` 差 5.1、`黑色纯透明→不透明` 差 10.4；
同模式重复采样差异 0（噪声基线为 0，说明差异是真实变化）。
诊断日志（`%APPDATA%\KokonaDownloader\app.log`）确认：`不透明` 根背景 `#F2231A21` 且无控制器，
`磨砂/黑色` 根背景透明且原生控制器已挂载，本机 `mica支持=True acrylic支持=True`。

**验证截图**：`artifacts/transparency/mode-Opaque.png`、`mode-Frosted.png`、`mode-BlackTransparent.png`
（均为窗口区域裁剪，同尺寸可直接对比），另有 `A/B/C-*-before/after/desktop.png` 为逐次点击前后对照。

### 3. 启动动画（功能已完成并实测可见）

- **新增窗口**：`src/KokonaDownloader.App/SplashWindow.xaml` + `.xaml.cs`
  - 420×420、无标题栏、不可缩放、置顶、工作区居中（`DisplayArea.GetFromWindowId(...Nearest)`）；
  - 画面只有：深色底 + 中心微光 + 圆环内的下载箭头图标 + 应用名「海兔下载器」，
    **没有任何说明性文字**（符合要求）；
  - 入场动画在代码里构建 Storyboard（图标淡入 + 0.7→1.0 缩放，应用名延迟 350ms 淡入上浮）；
    XAML 属性文本无法表达 `EasingFunction`（XBF 会报 Property Not Found），必须代码构建。
- **时机修复（关键）**：`OnLaunched` 是同步执行的，返回前 XAML 消息泵不运行，
  原先在 `OnLaunched` 末尾调用的 `Dismiss()` 等于"窗口从没显示过"。
  现改为：`SplashWindow.RequestDismiss()` 用 `DispatcherQueue` 一次性计时器延后关闭，
  保证**最短展示 1.2s**（避免一闪而过像故障），并有 **6s 兜底**自动关闭
  （`ScheduleFallbackClose`，防止异常流程下窗口赖在屏幕上）。
- **实测可见性**（`tools/probe_windows.ps1` 启动瞬间轮询顶层窗口）：
  修复前 420×420 窗口 `visible=False`（从未显示）；修复后 `[2942 ms] hwnd=4983812 visible=True size=420x420 pos=(630,291)`
  —— 启动动画真实显示，位置为工作区居中值。
- **未破坏其他启动路径**：`--minimized`（开机静默到托盘）、`magnet:` 唤起、单实例互斥逻辑均未改动，
  启动动画在这三种情况下都会在时限内自动关闭。

### 4. 其他

- `App.Log` 增加透明度诊断行（模式 / 控制器挂载 / 平台支持 / 根背景色），便于日后排查"透明不生效"。
- 新增免依赖验证工具（`tools/`，PowerShell）：窗口探针、UI 自动化点击、像素采样对比、
  遮挡窗口识别。注意：含中文的 `.ps1` 必须存成 **UTF-8 带 BOM**，否则 PS 5.1 按 ANSI 读会报语法错误。

---

## 三、原待办（✅ 已全部完成）

### A. 打包与发版（按指南执行）

1. **打包 —— ✅ 已完成**：按 `docs/单文件打包指南.md` 执行单文件 publish（exit 0），
   产物 `dist\KokonaSingleFile\KokonaDownloader.exe` = **303.46 MB**（符合指南 ~303 MB 预期）。
   指南第五节验证清单全部通过：
   - **构建验证**：exe 内嵌 12/12 `standalone/` 资源（另含新增 `SplashWindow.xbf`，共 13 个载荷文件）✅
   - **A 干净临时目录**：只放 exe → 13 个载荷文件全部解压到 exe 旁；启动动画可见（见 B.1）；
     ping 200 `version=1.0.6`；日志无 XamlParseException ✅
   - **B dist 目录**（resources.pri 已存在）：跳过解压，正常启动，ping 200 v1.0.6 ✅
   - **C 只读目录**（icacls deny W）：回退 `%LOCALAPPDATA%\KokonaDownloader\Standalone`
     （13 载荷 + exe 副本 = 14 文件），重新拉起后 ping 200 v1.0.6 ✅
     （注意：单文件 host 先要解压 bundle 到 %TEMP%，约 6s 后才执行引导逻辑，观察进程状态要等 8s+）
   - **D 桌面**：随"刷新桌面副本"步骤验证。
2. **提交推送 —— ✅ 已完成**：提交 `ce12bc1`「v1.0.6：窗口透明切换、启动动画与单文件发版」
   （42 个文件，含新增 `SplashWindow.*`、`tools/` 12 脚本、`artifacts/transparency/` 17 截图、进度文档），
   已推送 `origin/main`。
3. **打标签 —— ✅ 已完成**：`git tag v1.0.6 ce12bc1` 并推送（远端确认 `refs/tags/v1.0.6`）。
4. **GitHub Release —— ✅ 已完成**：Release「海兔下载器 v1.0.6」（id 389103246，非 draft），
   https://github.com/Hariteki/KokonaDownloader/releases/tag/v1.0.6 ；三个资产全部 HTTP 201 上传成功：
   `KokonaDownloader_106.exe`（318200664 B）/ `KokonaExtension_106.zip`（37659 B）/
   `KokonaDownloader-Tutorial.pdf`（288628 B）。说明含本版新增与已知问题（黑色纯透明为薄磨砂黑）。
5. **刷新桌面副本 —— ✅ 已完成**：`C:\Users\lumin\Desktop\KokonaDownloader.exe`（318200664 B）与
   `C:\Users\lumin\Desktop\KokonaSingleFile\`（含新增 `SplashWindow.xbf`）均替换为 1.0.6。
   **测试 D —— ✅ PASS**（`tools/verify_desktop_launch.ps1`）：桌面文件夹启动，
   `[536 ms] visible=True size=336x336`（splash）、`[1049 ms]` 主窗口 940x560、ping 200 v1.0.6。

### B. 发布前复测 —— ✅ 全部完成

1. **单文件版启动动画复测 —— ✅ PASS**：干净临时目录只放 exe 启动，
   `[6662 ms] visible=True size=336x336 pos=(504,228)`（420 DIP × DPI 缩放 = 336 物理像素）。
   比开发版（~3s）晚约 3.5s，原因是单文件 host 先解压 bundle 到 %TEMP%——属预期行为，
   动画仍"一启动就出现"（用户视角：点完图标 ~6.5s 内必见画面），时长符合 1.2s 最短展示要求。
2. **启动动画创建失败路径 —— ✅ 代码已覆盖**：`App.xaml.cs` L76–90 已有 300ms 一次性
   DispatcherQueueTimer 重试兜底（首次 `TryCreateSplash()` 失败后重试一次，再失败则直接显示主窗口），
   当前构建未复现失败，无需再改。
3. **`--minimized` / `magnet:` 唤起 —— ✅ PASS**（`tools/probe_splash_args.ps1` 实机）：
   - `--minimized`：无可见窗口（主窗口隐藏、仅托盘），启动动画在时限内自动关闭，不卡屏；
   - `magnet:`：主窗口保持隐藏，`MagnetConfirmWindow`（500x430）~1.08s 出现，确认弹窗正常。
4. **不透明模式的 95% 透明 —— ✅ 已修复并验证**：Opaque 分支现强制 `A=0xFF`
   （`ThemeService`/`WindowEffects`），`tools/verify_opaque_alpha.ps1` 白底遮挡测试：
   6 个采样点全部精确等于 RGB(18,31,34)（海洋主题 WindowFill 不透明值），**devFF=0**，
   不再出现 A=0xF2 混合出的 (30,42,45)。

### C. 已知问题

1. ~~`POST /api/settings` 传入未知字段会返回 500~~ —— **✅ 已修复**：`ApiSettingsPatch` 白名单
   现含 `transparency`；`tools/verify_api_settings.ps1` 全项 PASS（GET 字段完整、POST 持久化、
   未知字段 ok 不 500）。
2. ~~`GET /api/settings` 不返回 `minimizeToTrayOnClose`~~ —— **✅ 已修复**：现正常返回。
3. 透明度的"黑色纯透明"是**薄磨砂黑**（DWM 无"无模糊纯透明"公开 API；
   自定义 `SystemBackdrop` + 半透明色刷在本项目投影下编译不通：
   `ICompositionSupportsSystemBackdrop` 无 `Compositor`、目标属性要求 `Windows.UI.Composition.CompositionBrush`，
   与 `Microsoft.UI.Composition` 两套 compositor 不通用）。如需"完全不模糊"，需另找方案（如分层窗口）。
   —— **保留为文档说明**（Release 说明中注明，不改代码）。

### D. 工作区清理 —— ✅ 已完成

- 已删除：根目录 `screenshot_full.png` / `screenshot_splash.png` / `screenshot_test.png`；
  `tools/` 下 8 个一次性调试脚本（`dbg_*`）与 6 个中间版验证脚本（`verify_transparency` v2–v7，
  被最终版 v8 取代）。
- 已入库：`tools/` 12 个验证/探针脚本 + `artifacts/transparency/` 全部证据截图（回归验证资产）。
- `FINAL_REPORT.md`、`海兔下载器教程.pdf`、`磁力链接下载功能可行性与技术评估.md` 属上版遗留未跟踪文件，本次不动。

---

## 四、复现 / 验证命令速查

```powershell
# 编译（-r win-x64 必须，否则 XBF 资源路径不一致）
dotnet build src\KokonaDownloader.App\KokonaDownloader.App.csproj -c Release -r win-x64

# 启动动画是否真出现（启动瞬间轮询顶层窗口，看 420x420 是否 visible=True）
powershell -NoProfile -ExecutionPolicy Bypass -File tools\probe_windows.ps1

# 三种透明度模式渲染差异（每次启动固定一种模式，采样窗口内 10 点 vs 桌面底）
powershell -NoProfile -ExecutionPolicy Bypass -File tools\verify_transparency8.ps1
# 三张窗口截图的 RGB 对比
powershell -NoProfile -ExecutionPolicy Bypass -File tools\compare_mode_shots.ps1

# 不透明模式是否绝对不透明（白底遮挡 + 6 点采样，期望全部 = 主题 WindowFill 不透明值）
powershell -NoProfile -ExecutionPolicy Bypass -File tools\verify_opaque_alpha.ps1

# 设置 API 自检（GET 字段 / POST 持久化 / 未知字段不 500）
powershell -NoProfile -ExecutionPolicy Bypass -File tools\verify_api_settings.ps1

# 带参启动探针（--minimized / magnet: 唤起路径，轮询顶层窗口）
powershell -NoProfile -ExecutionPolicy Bypass -File tools\probe_splash_args.ps1 --minimized

# 单文件 exe 验证（构建内嵌资源 + A 干净目录 + B dist 目录；C 只读回退见下）
powershell -NoProfile -ExecutionPolicy Bypass -File tools\verify_singlefile_main.ps1
powershell -NoProfile -ExecutionPolicy Bypass -File tools\verify_singlefile_fallback.ps1

# 桌面实机启动验证（测试 D：splash + ping v1.0.6）
powershell -NoProfile -ExecutionPolicy Bypass -File tools\verify_desktop_launch.ps1

# 运行时自检
Invoke-RestMethod http://127.0.0.1:16800/api/ping -Headers @{ "X-Kokona-Secret" = "a938ca540c347dbdcaf263fe86250661" }
Get-Content "$env:APPDATA\KokonaDownloader\app.log" -Tail 40          # 看错误
Select-String -Path "$env:APPDATA\KokonaDownloader\app.log" -Pattern "透明度模式"
```

> 注意：含中文的 `.ps1` 必须存成 **UTF-8 带 BOM**（PS 5.1 按 ANSI 读取会破字符串）；
> `icacls` 移除拒绝项要用 `/remove:d <账户>`，`/remove /deny "…:(W)"` 是无效语法。

---

## 五、踩坑记录（下次省时间）

1. **`Diff` 不能当函数名**：PowerShell 内置别名 `diff` = `Compare-Object`，别名优先级高于函数，
   会静默走成对象比较。
2. **`Write-Host "..." -f $x`** 会把 `-f` 当成 `-ForegroundColor` 前缀匹配而报错，必须写 `Write-Host ("..." -f $x)`。
3. **`out` 参数不能用在 lambda 里**（CS1628）→ `Add-Type` 编译失败后 `[W]::` 全部 `TypeNotFound`，
   表现是"窗口定位静默失效、采样全是桌面"，很容易误判成功能问题。
4. **`[Process]::MainWindowHandle` 会缓存首个可见顶层窗口**，启动阶段拿到的是启动动画窗口，不是主窗口。
   要枚举进程顶层窗口并按面积挑。
5. **前台锁定**：脚本进程非前台时 `SetForegroundWindow` 会失败，应用窗口会被别的窗口盖住，
   抓屏抓到的是上层窗口 —— 必须先确认"采样点归属本应用进程"（`WindowFromPoint` + PID 比对）再采信数据。
6. **WinUI 3 无 `SystemBackdrop` 时窗口表面不透明**：根元素 alpha 只与那块不透明表面混合
   （黑色模式实测恒为 `0,0,0`），想真正透出桌面必须走 backdrop 机制。
7. **中文 `.ps1` 必须带 BOM**（PS 5.1 按 ANSI 读取会破字符串）。
