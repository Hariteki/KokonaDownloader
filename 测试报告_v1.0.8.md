# 海兔下载器 v1.0.8 全量测试报告

- **被测对象**：单文件发布版 `KokonaDownloader.exe`（v1.0.8，win-x64，self-contained，PublishSingleFile）
  - 部署目录：`C:\Users\lumin\Desktop\KokonaSingleFile\`（仅含 exe）
  - 实际运行位置：`%LOCALAPPDATA%\KokonaDownloader\Standalone`（由 `StandaloneBootstrap` 自解压迁移，部署目录保持干净）
- **测试日期**：2026-09-18（首轮全量）；2026-09-19（修复后复测，见 §7）；2026-09-19（第二轮：代码审计 + 安全/性能修复后复测，见 §8）；2026-09-26（第十二轮：版本号升到 **1.0.9** + 重新打包单文件 + 复测，见 §18）
  - 说明：本文件名与标题沿用首轮的 v1.0.8（历史命名），后续各轮按时间追加章节，不代表被测版本号
- **测试环境**：Windows（单屏 1920×1080），.NET 8 / Windows App SDK 1.8.260804001，aria2c 1.37.0
- **配套组件**：MV3 浏览器扩展 v1.0.8；本地 HTTP API `127.0.0.1:16800`（`X-Kokona-Secret`）；aria2 JSON-RPC `127.0.0.1:16801`
- **测试手段**：自建测试网站 + 自动化驱动脚本（Node）、UI Automation（UIA）控件枚举、像素/窗口几何检测、性能采样、日志审计、多套单元/集成/E2E 套件

---

## 一、测试结果汇总

| # | 测试项 | 结果 | 说明 |
|---|--------|------|------|
| 1 | 下载功能全场景（测试网站） | ✅ **52 / 52 PASS** | 鉴权/直链/文件名解析/续传/限速/并发去重/设置/非法请求 |
| 2 | BT 磁力下载 | ✅ PASS | UiTests 回环做种（64 分片）进度矩阵 partial→complete；见 §3.2 说明 |
| 3 | UI 检测（重叠/越界/离屏） | ✅ 0 控件级问题；窗口级重叠已修复 | 并发进度小窗原完全堆叠，已加级联偏移，复测 6 窗 6 个不同位置，见 §5-B / §7 |
| 4 | UiTests 全流程 + 截图 | ✅ exit 0 | 磁力确认流 / 限速下拉 / 进度矩阵 / 8 张截图 |
| 5 | Core 单元+集成套件 | ✅ **139 / 139 PASS** | 逐类串行复测全通过；并行首跑 12 项失败系 fixture 竞争，见 §8-E |
| 6 | 扩展契约套件 | ✅ **56 / 56 PASS** | cscript 驱动 |
| 7 | E2E 功能套件（test.mjs） | ✅ **24 / 24 PASS** | 版本断言已更新为 1.0.8；重定向/CD/续传/去重/生命周期/非法请求/磁力确认 |
| 8 | 性能 / 资源占用 | ✅ 稳定，无内存膨胀 | 见 §3.6 |
| 9 | 日志健康 | ✅ 已修复 | 原 1 次未处理异常崩溃（§5-A）+ `[aria2]` 空白行泛滥（§5-C），均已修复并复测通过，见 §7 |

**总体结论**：v1.0.8 核心下载/扩展/设置功能全部通过；UI 无控件级重叠或越界；资源占用稳定。首轮测试发现的 **1 个严重健壮性缺陷（高并发下未处理 XamlParseException 导致崩溃）**、1 个中等问题（进度小窗堆叠）、1 个低优先级日志卫生问题，**均已在 §7 修复并复测通过**；Core 套件 6 项失败判定为测试基建并行 flake（§5-D），非应用缺陷。

---

## 二、测试范围与方法

1. **测试网站**（`.verify\fulltest\server.mjs`，端口 18080）：覆盖直链 / Content-Disposition / 重定向 / Range 续传 / 慢速限速 / 404 / Chunked / UTF-8 文件名（RFC 5987 `filename*`）/ 多连接 / 自定义头；导出每文件 SHA256 供完整性校验。
2. **下载驱动**（`.verify\fulltest\run.mjs`）：对真实应用 API 发起 A–I 共 9 组 52 项断言，落盘到 `%TEMP%` 并逐文件校验大小 + SHA256。
3. **BT 做种**：TestSite tracker（`192.168.32.35:6969`）+ 本地 aria2c 做种机；UiTests 另用回环做种（8MB / 64 分片）。
4. **UI 检测**（`.verify\fulltest\ui_inspect.ps1` + `uia_lib.ps1`）：枚举每个应用窗口全部后代控件（类型/名称/包围矩形/离屏/启用），检测越界、兄弟控件重叠（重叠率≥0.30）、离屏控件。
5. **UiTests**（`src\KokonaDownloader.UiTests`）：独立启动应用、回环 BT 做种、UIA 驱动磁力确认窗、限速下拉、进度/方块矩阵，产出 8 张截图。
6. **性能采样**：进程 CPU%（CPU 时间差 / 墙钟）+ 工作集（WS），分别在空载、多任务驻留、活跃下载三种状态采样。
7. **日志审计**：对 `%APPDATA%\KokonaDownloader\app.log` 做异常与空白行统计。

---

## 三、详细结果

### 3.1 下载功能全场景（测试网站，52/52 PASS）

| 组 | 覆盖 | 结果 |
|----|------|------|
| A 鉴权与基础 | ping 无鉴权 / 无 token 401 / 错 token 401 / Bearer 兼容 / 版本 1.0.8 | ✅ 5 |
| B 直链完整性 | 1MB / 10MB / 50MB，大小 + SHA256 全对 | ✅ 8 |
| C 文件名解析 | Content-Disposition 真实名 / 重定向取目标名 / 重定向+显式名覆盖 / UTF-8 RFC5987 / 显式 filename 覆盖 | ✅ 10 |
| D 传输形态 | Chunked（无 Content-Length）/ 404 失败态 / 8 连接分片 | ✅ 5 |
| E 生命周期 | 暂停 → 续传（Range 206）→ 删除 | ✅ 6 |
| F 限速 | `changeGlobalOption` 热更新 1MB/s→512KB/s→还原；实测平均速率 ≤ 上限×1.6，耗时达标 | ✅ 4 |
| G 并发+去重 | 并发下载 / 同 URL 去重（duplicate）/ 不同 URL 放行 | ✅ 3 |
| H 设置 API | 并发数 / 连接数 / 限速 / 主题 等 patch 与钳制 | ✅ 6 |
| I 非法请求 | 非法 scheme / 空 body / 越界参数 等 | ✅ 4 |

> 关键验证点：显式 `filename` **覆盖** Content-Disposition 名（符合预期）；非 ASCII 文件名必须走 RFC 5987 `filename*=UTF-8''…`（裸 UTF-8 头值会被 HTTP 栈拒绝）；同 URL 去重基于 1.2s 快照缓存，刚完成的任务约 1.2s 内再提交同 URL 会被判为 duplicate（已记录为设计权衡）。

### 3.2 BT 磁力下载

- **UiTests（权威验证，PASS）**：回环做种机（infohash `0a47678b…`，64 分片）→ 应用经磁力确认窗「开始下载」→ 进度/方块矩阵由 partial 渐进填充至 complete → 主卡片 / BT 页矩阵同步完成。截图 `ui_02/05/06/07` 佐证。
- **独立 LAN 做种测试（未跑通，属测试环境限制，非应用缺陷）**：用 aria2c 对**已存在文件**做种时，aria2 不会信任其自身未下载过的数据（announce 恒为 `left=全量 / uploaded=0`），无法真正对外提供数据，故应用侧 0% 停滞。这是 aria2 做种既有文件的固有限制，与 v1.0.8 代码无关；BT 能力已由 UiTests 回环做种充分验证。

### 3.3 UI 检测（重叠 / 越界 / 离屏）

- 枚举 6 个窗口（主窗 + 5 个进度小窗），共 181 个控件：
  - **越界（OVERFLOW）**：0
  - **窗口内兄弟控件重叠（≥0.30）**：0
  - **离屏控件（OFFSCREEN）**：0
- **发现（窗口级，见 §5-B）**：5 个并发进度小窗全部居中于同一坐标 `760,420 400×192`，**完全堆叠**，用户只能看到最上层一个。

### 3.4 UiTests 全流程（exit 0）

回环做种就绪 → 应用启动 → 磁力预填地址栏 → 确认窗「开始下载」→ 任务创建 → 主窗显示 → 限速下拉可选（500KB/s）→ 进度矩阵 partial→complete → 8 张截图全部产出 → 测试记录清理、aria2 会话还原。

### 3.5 E2E 功能套件（test.mjs，24 / 24 PASS）

版本断言已由 1.0.6 更新为 **1.0.8**。覆盖端到端路径：
- ping 无鉴权 200 / 无 token 401 / 错 token 401 / Bearer 兼容 / 版本 1.0.8
- 重定向下载：按**真实目标名**落盘，丢弃 URL 段假名；内容大小一致
- Content-Disposition 下载：按头内文件名落盘
- 慢速下载 + Range 分片续传（206）
- 活跃中重复提交 → `duplicate:true`
- 暂停 → paused / 恢复 → active / 删除 → 列表移除
- 非法 scheme → 400 / 无 urls → 400 / 未知路由 → 404 / 畸形 JSON → 400
- 单条磁力 → 200 `confirm:true`（不 500，走确认窗）

### 3.6 性能 / 资源占用

| 状态 | CPU% | 工作集 WS | 说明 |
|------|------|-----------|------|
| 空载（无任务） | 1.15% | ~200 MB | 基线，内存还在回收（-10.7MB/15s） |
| 172 任务驻留（空闲） | 4.95% | 228 MB | 多任务记录常驻，仅 +~28MB，无膨胀 |
| 活跃下载（新增 30 + 驻留 172） | 6.77% | 211 MB | 稳定，内存回落，单 aria2c 子进程 |

> 结论：资源占用平稳、无内存泄漏迹象；CPU 在活跃下载时 <7%（多核占比）。**唯一异常是 §5-A 的极端快速建窗崩溃**（200 任务 ~6s 内提交触发），非资源耗尽所致。

### 3.7 日志健康

- `app.log` 共 29,036 行 / 2.24 MB。
- **未处理异常**：1 次 `XamlParseException`（§5-A，导致进程退出）。
- **`[aria2]` 空白行泛滥**：`[aria2]` 行共 26,835 条，其中 **26,554 条（99%）为纯空白/尾随空格**（§5-C）。

---

## 四、套件结果明细

| 套件 | 通过 / 总数 | 备注 |
|------|------------|------|
| Core（单元+集成，xUnit） | 130 / 136 | 6 项失败均为 `aria2 RPC 在 15000ms 内未就绪` / HttpClient 超时；**隔离运行 ApiServiceTests+ResumeRangeTests = 13/13 通过**，判定为并行负载 flake，非应用缺陷（§5-D） |
| 扩展契约（cscript） | 56 / 56 | 全通过 |
| E2E（test.mjs） | 见 §3.5 | 版本断言已更新 1.0.8 |
| UiTests | exit 0 | 全流程 + 8 截图 |

---

## 五、发现的问题

### A.【严重·已修复】高并发快速建窗触发未处理 XamlParseException，应用崩溃（修复见 §7-A）
- **现象**：约 200 个任务在 ~6s 内提交时，`app.log` 记录未处理异常并进程退出：
  ```
  未处理异常 Microsoft.UI.Xaml.Markup.XamlParseException:
  Failed to assign to property 'KokonaDownloader.App.Controls.SquircleBorder.Fill'. [Line: 0 Position: 0]
  ```
- **触发路径**：任务批量创建 → 进度小窗（`ProgressWindow.xaml` 内含 `SquircleBorder Fill="{ThemeResource CardBackgroundFillColorDefaultBrush}"`）快速加载 → 该 ThemeResource 在窗口 XAML 解析瞬间解析失败 → 未捕获 → 崩溃。
- **现有保护不足**：`MaxLiveProgressWindows=6` 限流只限制“同时存活”的小窗数量，无法阻止“快速创建/销毁”循环中某次 XAML 加载命中资源竞态。
- **影响**：极端批量导入（如扩展一次性转发大量链接）可能直接闪退整个应用。
- **建议**：① 进度小窗创建整体包 try/catch，失败时降级为仅主列表展示、不崩进程；② 排查 `CardBackgroundFillColorDefaultBrush` 等主题刷在窗口加载期的解析竞态（与主题切换/透明应用路径的并发）；③ 对建窗做节流/串行化。

### B.【中·已修复】并发进度小窗完全堆叠（同坐标居中）（修复见 §7-B）
- **现象**：多个下载同时进行时，各进度小窗均被 `SizeAndPlaceProgressWindow` 居中到显示区中心（`760,420 400×192`），彼此**完全重叠**，用户只能看到最上层一个。
- **影响**：多任务场景下进度小窗失去“各自可见”的意义。
- **建议**：为并发小窗做级联偏移（如按序号在中心附近错开 24–32px），或改为仅保留 1 个聚合进度窗 + 主列表承载其余。

### C.【低·已修复】`[aria2]` 日志空白行泛滥（修复见 §7-C）
- **现象**：`app.log` 中 `[aria2]` 前缀行 99%（26,554/26,835）为纯空白，来自逐行捕获 aria2c stdout 时把空行也写入。
- **影响**：日志体积膨胀、可读性差、长期可能无界增长。
- **建议**：捕获 aria2c 输出时跳过空白行（或合并连续空行）。

### D.【说明，非缺陷】Core 套件并行 flake
- 全量运行时 6 项因 `aria2 RPC 在 15000ms 内未就绪` 失败；**隔离运行同批测试 13/13 通过**。判定为 xUnit 并行启动多个 aria2c 时的启动超时竞态（测试基建问题），非 v1.0.8 应用缺陷。如需稳定绿，可限制测试类并行度或调大该超时。

---

## 六、结论与建议

- **可发布性**：v1.0.8 核心功能（下载全场景 / BT / 扩展 / 设置 / 主题）全部通过，资源占用稳定，UI 无控件级重叠或越界，**具备发布条件**。
- **首轮发现的 3 项问题（§5-A 崩溃 / §5-B 小窗堆叠 / §5-C 日志空白行）均已于 2026-09-19 修复并复测通过**（§7），严重缺陷已消除。
- **第二轮代码审计（§8）另修复 3 项**：本地 API 可被任意网页驱动（CORS 通配 + 公开仓库历史中的密钥，已收紧来源校验并轮换密钥）、设置写入触发全量主题重算（实测每次约 240ms CPU → 0）、`ThemeService._backdrops` 静态字典窗口泄漏；并**修正 1 项首轮结论**（轮询负载不做截断：实测截断取的是最旧任务，属回归）。
- **可后续迭代**：§5-D Core 测试并行度（限制并行或调大 RPC 就绪超时，属测试基建）；扩展侧 Cookie/UA 透传（A-2）；§8-F 列出的低优先级项（README 过期表述、UI 线程同步 RPC、批量路径缺失的伪文件名剔除等）。

---

## 七、修复与复测记录（2026-09-19）

针对 §5-A / §5-B / §5-C 三项问题完成修复，重新发布单文件 exe 并全量复测。

### 7-A 崩溃修复（§5-A）
- **根因**：批量任务连发时进度小窗密集创建，XAML 加载 + 原生背景挂载 + 窗口激活在同一帧内密集触发，命中 `Microsoft.UI.Xaml.dll` 原生故障（事件日志 `异常代码 0xc000027b`，fastfail），表现为未处理 `XamlParseException` 后进程直接终止——托管层 `try/catch` 与 `e.Handled=true` 均无法拦截原生 fault，只能从触发端消除。
- **修复**（`MainWindow.xaml.cs`）：
  1. **建窗最小间隔闸门**：两次实际建窗之间强制 ≥600ms（`MinWindowCreationGap`），把"建窗风暴"摊平为串行慢速创建；
  2. **激活延迟**：`Activate()` + `ForceForeground()` 推迟到下一调度周期执行，将"建窗（XAML 加载+原生背景挂载）"与"窗口激活"拆到不同帧；
  3. 保留原有 try/catch 兜底（失败仅记日志、任务仍可见于主列表）。
- **复测**：200 个唯一 URL 任务 ~10s 内连发（与首轮崩溃场景一致）→ **应用存活、无新增原生崩溃事件、日志 0 次 XamlParseException / 未处理异常**；全部任务正常完成。

### 7-B 小窗堆叠修复（§5-B）
- **修复**：为每个进度小窗分配**级联槽位**（`Slot`，取存活窗口未占用的最小槽位 0..5，关闭后自动释放）；`SizeAndPlaceProgressWindow` 在屏幕中心基础上按槽位向右下错开（每槽 +28×DPI / +20×DPI 物理像素）。第 0 号窗保持正中央，其余依次错开。
- **复测**：6 个并发小窗实测位于 **6 个不同坐标**（760,420 / 788,440 / 816,460 / 844,480 / 872,500 / 900,520），互不重叠；`ui_inspect.ps1` 复测 **ISSUES: 0**。

### 7-C 日志空白行修复（§5-C）
- **根因**：`Aria2Process.cs` 逐行捕获 aria2c stdout 时仅判 `e.Data != null`，空白行被写成 `"[aria2] "`（带前缀，绕过了 `App.Log` 的空白过滤）。
- **修复**：改为 `!string.IsNullOrWhiteSpace(e.Data)`，stdout/stderr 两条通道均跳过空白行。
- **复测**：修复后构建运行期间 `[aria2*]` 日志行 **0 条空白**（修复前同口径 97.5% 为空白）；日志体积与可读性恢复正常。

### 7-D 复测汇总（修复后构建，同一干净部署流程）

| 项目 | 结果 |
|------|------|
| 崩溃复现（200 唯一任务连发） | ✅ 应用存活，无原生崩溃事件，0 XamlParseException |
| 下载功能全场景（run.mjs） | ✅ **52 / 52 PASS** |
| E2E 功能套件（test.mjs） | ✅ **24 / 24 PASS** |
| UI 检测（ui_inspect.ps1） | ✅ ISSUES: 0；6 并发小窗 6 个不同位置（级联生效） |
| 日志健康 | ✅ 0 空白 `[aria2]` 行、0 未处理异常 |

> 复测中另发现并修正一处测试脚本自身缺陷：`perf_submit.mjs` 未 `await r.text()` 导致响应体解析恒为 null（误报 err=200）；且同 URL 跨轮次会被应用去重吞掉，已加 per-run salt 保证每轮 URL 唯一。

---

## 八、代码审计与修复记录（2026-09-19 第二轮）

对 v1.0.8 全量源码做了一轮独立审计（Core 引擎与 API、App/WinUI 层、浏览器扩展、单文件部署引导、README 声明核对），并对**运行中的实例**做黑盒测量取证。本轮修复了 3 项、修正了 1 项原判结论，全部复测通过。**版本号仍为 1.0.8。**

### 8-A 安全：本地 API 可被任意网页驱动（已修复）

**问题**：`ApiService` 对所有请求回 `Access-Control-Allow-Origin: *`，并允许 `X-Kokona-Secret` 自定义头。鉴权依赖自定义头 → 浏览器必然先发 CORS 预检 → 预检对**任何**来源都回 204 并允许该头 → **任意网页都能带密钥调用本地 API 并读取响应体**（`/api/download` 投递下载、`/api/tasks/{gid}/remove` 删任务、`/api/settings` 改下载目录）。同时实测确认：实时 `ApiSecret` 存在于**公开仓库**（`Hariteki/KokonaDownloader`，visibility=public）的历史提交中（逐提交 `git grep` 命中 8 个提交，HEAD 已清理但历史不可变）。

**修复**（`ApiService.cs`）：
1. 新增 `IsTrustedOrigin`：只放行浏览器扩展来源（`chrome-extension` / `extension` / `moz-extension`）与回环来源，其余来源直接 **403** 且不返回任何 CORS 头；用 `Uri.IsLoopback` 解析而非前缀匹配，避免 `http://127.0.0.1.evil.com` 这类绕过；
2. 受信任来源改为**回显具体 Origin**（不再 `*`）并声明 `Vary: Origin`；
3. 轮换 `ApiSecret`（`a938ca…0661` → 新值，已失效）。

**复测（部署后对真实 exe 实测）**：

| 检查 | 结果 |
|------|------|
| `OPTIONS`/`POST` + `Origin: https://evil.example` | ✅ **403**，无 ACAO 头 |
| `Origin: http://evil.example` / `http://127.0.0.1.evil.com` | ✅ 均 403（前缀绕过已挡住） |
| `Origin: chrome-extension://…` 预检 | ✅ 204，ACAO 回显该来源 |
| `Origin: http://localhost:8080` | ✅ 200，ACAO 回显该来源 |
| 无 Origin（扩展 SW/脚本） | ✅ 200，且响应不含通配 ACAO |
| **旧密钥** `a938ca…0661` | ✅ **401**（已失效） |

新增回归测试：`不受信任的网页来源被拒绝`、`无来源与回环来源仍可用`、`OPTIONS预检返回204且只回显受信任来源`（原断言 `*` 已更新）、`扩展来源CORS可用`（同步更新）。

### 8-B 性能：设置写入触发全量主题重算（已修复）

**问题**：`ThemeService.OnSettingsChanged` 不判断主题是否真的变化，而 `SettingsStore.Changed` 对**任何**设置变更都会触发；`ApiService.HandleSettingsPatch` 与 `SettingsWindow` 保存的 modifier 又都**无条件 `return true`**。于是"改并发数""点一次保存"这类与外观无关的操作，都会触发：重建约 120 个资源键（且写两遍：`_overrides` + 镜像进 `Application.Resources`，每次新建约 240 个画刷）+ 对每个窗口做 `RequestedTheme Dark→Light→Dark` 的**整棵可视树重求值**。

**修复**：`ThemeService.Apply()` 用 `ResolvedTheme`（record，按值比较）判定"主题未变则整轮跳过"（另设 `_appliedOnce` 防首帧误跳过）；两个设置写入点改为逐字段比对后返回"是否真的变了"。

**前后对比（同一测量脚本、同一口径，20 次内容完全相同的 `POST /api/settings`）**：

| 指标 | 修复前 | 修复后 |
|------|--------|--------|
| `ApplyTransparency` 调用次数 | 127（6.35 个窗口/次） | **0** |
| 应用 CPU 增量 | **4.75 s** | **0.27 s**（↓94%，17.9×） |
| 新增日志行 / settings.json 落盘 | 127 行 / 20 次 | **0 行 / 0 次** |

新增回归测试：`设置补丁内容未变时不触发变更事件`（3 次空改动补丁 → `Changed` 触发 0 次；真改动 → 1 次）。

### 8-C 内存：`_backdrops` 静态字典泄漏（已修复，范围修正）

**问题**：`ThemeService._backdrops` 以 `Window` 作**强键**，而 `Unregister`（窗口关闭时的最后一道清理）只从弱引用列表里摘除，从不释放该条目；`ReleaseBackdrop` 的唯一入口是 `ApplyTransparency` 的非复用分支，窗口关闭后已不在 `Windows` 列表中，那条路径永远不会再执行。

**范围修正（对比首轮审计结论）**：首轮报告称"每个关闭过的进度小窗都会泄漏"，**该表述不准确**。实测确认 `ProgressWindow` 自己先调 `WindowEffects.TryApplyAcrylic`，因此 `ThemeService.Register → ApplyTransparency` 走的是**复用分支**，`_backdrops` 根本不会写入条目；其控制器由 `WindowEffects.ConfigureForWindow` 自带的 `Closed` 处理器正确释放。**真正受影响的是未自行挂载背景的窗口：`SettingsWindow`（每次打开设置泄漏一个）与 `MagnetConfirmWindow`（每条磁力链接泄漏一个），以及退出时的主窗口。** 泄漏内容是托管侧窗口对象与其 Content 可视树（原生控制器本身已由 `ConfigureForWindow` 的 `Closed` 处理器 `Dispose`，故非原生资源泄漏）。

**修复**：`Unregister` 末尾调用 `ReleaseBackdrop(window)`（一行），并给 `ReleaseBackdrop` 里的 `window.SystemBackdrop = null` 补 try/catch——`Unregister` 是在窗口 `Closed` 时调用的，此时写该属性可能抛 `COMException` 并冲出事件处理器。

**验证口径说明**：本项为结构性修复，验证依据是代码路径 + 运行期 0 次"释放背景控制器失败"/0 次未处理异常，**未做内存测量**（未使用 dotnet-dump 对比 GC 堆）。

### 8-D 轮询负载：首轮结论修正（经实测后**不做截断**）

首轮审计把"轮询拉回全部历史任务状态"列为待修项（471 条 = 565 KB / 30–78 ms）。本轮补做拆解测量后**修正结论：不做 `tellStopped` 截断**，理由如下：

| 测量项 | 结果 |
|--------|------|
| 负载构成 | `files` 字段占 **71.9%**；进一步查明是 aria2 为**每条 split 连接**各登记一份重复 URI（实测单任务 9~16 条同 URL 的 `uris[]`，与 `split`/连接数一致） |
| `keys` 白名单裁剪 | 545 KB → 518 KB（仅 **5%**，因为 `files` 本身是必需字段） |
| **`tellStopped(0, 200)` 截断** | 保留的是**最旧**的 200 条（实测"最新 200 条"命中 **0/200**）→ 会隐藏最新任务，属**严重回归**，故否决 |
| 实际空闲开销 | 471 条历史、0 活动任务时：**2.81% 单核**（45 s 采样 1.27 s CPU），随历史条数线性增长 |
| aria2 自身行为 | `--max-download-result` 默认 1000 且**淘汰最旧**，故不存在">1000 条时新任务消失"的数据丢失问题（修正首轮的推测） |

**结论**：保留现行为；若后续确需降低，正确做法是 `tellStopped(max(0, numStopped - N), N)` 取"最新 N 条" + 用本地已积累快照回填更早条目（本轮不做，避免为 1% 级 CPU 收益改动核心轮询循环）。

### 8-E 复测汇总（修复后构建，单文件 exe 重新发布并部署）

| 测试项 | 结果 |
|--------|------|
| Core 单元/集成（逐类串行） | ✅ **139 / 139 PASS**（13 个类全通过） |
| 修复验证（`verify_fixes.mjs`，对部署后 exe） | ✅ **12 / 12 PASS** |
| 下载功能全场景（run.mjs） | ✅ **52 / 52 PASS** |
| E2E 功能套件（test.mjs） | ✅ **24 / 24 PASS** |
| UI 检测（ui_inspect.ps1） | ✅ **ISSUES: 0** |
| 新构建日志健康（23:03 之后） | ✅ 0 未处理异常 / 0 XamlParseException / 0 轮询异常 / 0 背景释放失败 |
| 部署引导 | ✅ StandaloneBootstrap 自动搬运新 exe（319,707,277 B，SHA256 `7AC72FD8…796F`）、孤儿 aria2 清理成功（旧 PID 18068 → 新 PID 26936） |

> **测试基建说明**：本轮首跑全量并发时出现 12 项失败（多为 1 ms 内即失败 + BT 用例 24/42 s 超时 + 测试宿主不退出并残留 aria2），**逐类串行复测后全部通过（139/139）**，证实为既有的并行 fixture 竞争（多个 fixture 同时拉起 aria2，15 s 就绪超时不够）而非应用缺陷。该基建问题（原 §5-D）仍未改造。

### 8-F 本轮**未**修复的低优先级项（建议后续迭代）

| 编号 | 位置 | 问题 |
|------|------|------|
| L-1 | `README.md` | 唯一公开文档有 3 处过期：链接的 `项目维护手册.md` 已被删除（GitHub 死链）；两处写"JSON-RPC over **WebSocket**"（实际全为 HTTP POST）；"核心库：…**计划**、通知"（无调度功能） |
| L-2 | `ProgressWindow.xaml.cs` | `OnOpenFolderClick` 走 `GetTaskAsync().GetAwaiter().GetResult()`，**在 UI 线程同步等 RPC**（aria2 无响应时窗口可冻结至 10 s 超时） |
| L-3 | `DownloadEngine.cs` | `DropUrlDerivedFileName` 只作用于单任务路径，批量 `AddTasksAsync` 漏掉（当前唯一批量调用方不传文件名，属**潜伏**缺陷） |
| L-4 | `TombstoneStore.cs` | `Unmark` 用 `Any(_hashes.Remove)` 短路，首个命中之后的墓碑不会被撤销 |
| L-5 | `extension/background.js` | `forwardDownload` 未回传 `confirm` 字段，导致磁力"请在确认窗口中确认"提示是**死分支** |
| L-6 | `StandaloneBootstrap.cs` | 每次冷启动无条件重解压 ~7 MB 并覆盖 304.9 MB 的 exe（实测拷贝 110 ms，非性能热点）；并发启动时 `ExtractAll` 撞共享冲突会静默中止引导 |
| L-7 | 仓库卫生 | 只有 tag `v1.0.0/1/3/4/5/6`，**v1.0.7 / v1.0.8 有 Release 但无 tag**；`Themes/ThemeSwatchPicker.*` 为死代码仍被打包；`tools\*.ps1` 陈旧 |
| L-8 | `DownloadEngine.cs` | 每轮轮询都拼诊断签名字符串（1000 任务时每轮数十 KB 垃圾） |
| L-9 | `ApiService.cs` | `AcceptLoop` 对每个请求 `Task.Run`，无并发上限（回环+鉴权，风险低） |
| L-10 | `MainWindow.xaml.cs` / `SettingsWindow.xaml.cs` | 若干 `async void` 事件处理器无 try/catch，异常被 `UnhandledException` 静默吞掉 |

> 另需**人工目视确认**一项：`release` 资产中的 `海兔下载器教程.pdf`（公开分发、且被 git 跟踪）含设置窗口截图，而设置窗口会显示连接密钥。脚本已解压可读文本流搜索，**未发现明文密钥**，但该 PDF 的多数流未能解压且截图内容无法用脚本判读，建议人工看一眼截图里是否露出旧密钥（旧密钥现已失效，故风险已降为"信息暴露"而非"可用凭据"）。

---

## 9. 第三轮：§8-F 低优先级项修复与复测（2026-09-20）

版本号保持 **1.0.8** 不变。本轮把 §8-F 的 L-1…L-10 全部处理完毕（L-7 经核实部分修正，见下），并重新构建、部署、全量复测。

### 9-A 修复明细

| 编号 | 位置 | 修复内容 |
|------|------|----------|
| L-1 | `README.md` | 删除指向已删文件 `项目维护手册.md` 的死链；架构图 "HTTP/WebSocket" → "HTTP"；技术表 "JSON-RPC over WebSocket" → "JSON-RPC（HTTP POST，本地回环）"；核心库描述去掉不存在的"计划/调度"功能 |
| L-2 | `ProgressWindow.xaml.cs` | `OnOpenFolderClick` 改 `async void` + try/catch；`GetLastTask()`（`.GetAwaiter().GetResult()` 同步等 RPC）→ `GetLastTaskAsync()`（异步 await，引擎未运行时直接返回 null），UI 线程不再被 RPC 阻塞 |
| L-3 | `DownloadEngine.cs` | 批量 `AddTasksAsync` 补上 `DropUrlDerivedFileName`（与单任务路径一致）；批量 meta 显示名占位符同步改用 `GuessFileNameFromUrl` |
| L-4 | `TombstoneStore.cs` | `Unmark` 由 `Any(_hashes.Remove)` 短路改为逐项 `Remove` 循环，批量撤销全部命中；**新增回归测试** `墓碑_批量撤销时全部命中条目都被移除`（旧实现下该测试失败：2 条命中剩 1 条） |
| L-5 | `extension/background.js` | `forwardDownload` 返回值透传 `{ ok, gid, duplicate, confirm }`；`manualSend` 的"请在确认窗口中确认"分支恢复可达（服务端对单磁力本就返回 `confirm:true`，E2E 已断言） |
| L-6 | `StandaloneBootstrap.cs` | **重写**：引导清单 `.kokona_bootstrap.json`（嵌入 payload SHA256 + 文件列表 + exe 哈希/大小/mtime）——三者一致且文件齐全则跳过解压；exe 仅内容哈希变化才复制（冷启动不再无条件覆盖 ~305 MB）；命名互斥量 `Local\KokonaDownloader_Standalone_Bootstrap` 串行化并发冷启动，后到者等锁后复查完整性直接复用 |
| L-7 | 仓库卫生 | **修正**：`Themes/ThemeSwatchPicker.*` 并非死代码——设置窗"主题配色"区在用（8 套内置调色板，点击即切换并持久化），不删除；v1.0.7/v1.0.8 缺 tag 待用户确认（受"禁止擅自发版"约定约束）；`tools\*.ps1` 陈旧按约定只报告不修改；本地 `backup\`、`.verify\old_exe_backup\`（319 MB 旧 exe）均 gitignore，不影响仓库，清理待用户决定 |
| L-8 | `DownloadEngine.cs` | 每轮轮询诊断签名由"每轮拼全量字符串"改为**无分配 FNV-32 整型指纹**（gid+state 折叠 + stopped 计数）；完整签名串仅在检测到变化、需要写日志时才构建 |
| L-9 | `ApiService.cs` | `AcceptLoop` 加 **64 槽请求门**（`SemaphoreSlim.Wait(0)` 非阻塞探测），超限立即 503 拒绝而非排队挂起；正常请求不受影响 |
| L-10 | `MainWindow.xaml.cs` / `SettingsWindow.xaml.cs` | `OnNewClick`/`OnPauseAllClick`/`OnResumeAllClick`/`OnItemDeleteClick`/`OnBatchDeleteClick`（整体 try/catch，失败时保留选中以便重试）与 `SettingsWindow.OnBrowseDir` 补异常防护 + 日志 |

### 9-B 过程中发现并修复的隐藏缺陷（引导静默失效）

部署验证时发现新构建**没有**触发 Standalone 迁移（目录无清单、文件 mtime 未变、进程直接从桌面目录运行）。逐层排查定位到：本 .NET 8 运行时下 `SHA256.Hash` **未终结前读取会抛 `CryptographicUnexpectedOperationException`**（用反射/独立 net8.0 程序实证），而 `HashPayload` 恰在 `TransformBlock` 循环后直接读 `.Hash` → 异常沿 `EnsureRuntimeComplete` 上抛被 `[ModuleInitializer]` 的空 catch 吞掉 → **整个引导静默中止**，应用退化为"原地运行"（因单文件 bundle 的临时解压目录自带 `resources.pri`，功能不受影响，但 exe 目录不再保持干净、清单机制完全失效）。

修复：`HashPayload` 先 `TransformFinalBlock` 再取哈希。复验：
- **全新状态**（删除 Standalone 目录后冷启动）：12 个文件全部解压 + 清单写入（payloadHash `a037f769…`、exeHash 与新构建一致）+ exe 复制，进程从 Standalone 拉起 ✅
- **跳过路径**（再次重启）：ping 就绪仅 **1.17 s**，12 个 payload 文件与 exe 的 mtime/内容均未变（仅清单自身按设计重写）✅

### 9-C 复测汇总（修复后构建，SHA256 `03ACF046…B215`，319,711,373 B）

| 测试项 | 结果 |
|--------|------|
| Core 单元/集成（逐类串行，含新增 L-4 回归测试） | ✅ **140 / 140 PASS**（ApiContractGaps 9、ApiService 14、BtIntegration 4、EngineIntegration 8、ExtensionContract 6、FileNameResolution 7、pure-unit 72、ResumeRange 2、StoreRobustness 18） |
| 修复验证 `verify_fixes.mjs`（CORS/来源白名单/主题重算短路） | ✅ **10 / 10 PASS**（+1 项密钥轮换 SKIP，未提供旧密钥） |
| 下载功能全场景 `run.mjs` | ✅ **52 / 52 PASS** |
| E2E 功能套件 `test.mjs`（含磁力 `confirm:true` 断言） | ✅ **24 / 24 PASS** |
| 扩展逻辑单元测试（cscript `tests\test_logic.js`） | ✅ **56 / 56 PASS** |
| UI 检测 `ui_inspect.ps1` | ✅ **ISSUES: 0**（进度小窗级联槽位复验：5 窗 5 个不同坐标，28/20 px 步进） |
| 单文件引导（全新解压 / 跳过路径 / 并发互斥） | ✅ 见 §9-B |

> 环境备注：本机 SDK 8.0.401 的 `SemaphoreSlim` 未暴露 `TryWait` 重载（反射实证），L-9 采用语义等价的 `Wait(0)`；FNV-64 基准常量超出 `long.MaxValue`，指纹改用 FNV-32（碰撞仅少一条诊断日志，无功能影响）。

---

## 10. 第四轮：代码审计（2026-09-20，只看不改）

版本号仍为 **1.0.8**。本轮**不修改任何代码**，只做审计并输出改进建议。

### 10-A 审计范围与方法

| 项 | 内容 |
|---|---|
| 范围 | Core 引擎/API ≈2,100 行、App(WinUI) ≈4,400 行 C# + ≈1,400 行 XAML、扩展 ≈800 行（JS/HTML/JSON）、构建脚本与测试基建 |
| 方法 1 | **主审精读**：对风险最高的文件**整篇通读**（`StandaloneBootstrap.cs` 261 行、`ApiService.cs` 接受循环/释放路径、`DownloadEngine.cs` 轮询与批处理路径、`MagnetConfirmWindow.xaml.cs`、`extension/background.js` 响应映射与消息处理、`MainWindow.xaml.cs` 异常路径、`.gitignore`/csproj 关键属性），其余文件按"分析器告警 + grep 定向取证"覆盖 |
| 方法 2 | **编译器静态分析**：`EnableNETAnalyzers=true`、`AnalysisLevel=latest`、`AnalysisMode=All` 全量重建（Core 18 类规则 / App 26 类规则），逐条人工复核真伪 |
| 方法 3 | **运行时实测**：句柄/内存/线程、窗口拓扑、日志取证、API 负载、并发门压力、引导全流程 |
| 方法 4 | **对照校验**：把代码行为与既有测试断言、`项目文档.md` 设计决策、README 声称逐条对照，区分"真缺陷 / 有意取舍 / 文档已声明" |

> **过程说明（影响可信度，须记录）**：本轮另尝试了"五路并行子代理深读"作为补充覆盖，但五个子代理均远超预算仍无产出（运行约 65 分钟后被主审中止，未返回任何报告）。因此**本文结论全部来自主审自身的方法 1–4**，不存在"未经复核的二手结论"；同时也意味着**覆盖率是抽样性质的**——未列出的文件不保证无问题，尤其 `MainWindow.xaml`(835 行)、`ThemeService.cs`、`WindowEffects.cs`、`popup.js`/`options.js` 只做了告警与定向取证，未逐行通读。

> 判定纪律：**文档里已声明的有意取舍不算缺陷**（1.2 s 去重窗口、快速退出、墓碑机制、纯磨砂主题、常驻进度小窗、单文件自解压、aria2 外部引擎、扩展不转发 Cookie/UA 等）；分析器告警也必须复核，误报要明确标注为误报。

### 10-B 静态分析结果与逐条复核

| 规则 | 命中 | 复核结论 | 处置建议 |
|------|------|----------|----------|
| CA1305 区域设置相关格式化 | 36 | **误报（已实测）**：在 ar-SA / ar-EG / fa-IR / bn-IN / hi-IN / th-TH / de-DE / tr-TR / zh-CN 下 `int/long.ToString()` 输出**全部为 ASCII 数字**（仅 `double` 受影响：`1.5` vs `1,5` vs `1٫5`）。aria2 的 `split`/`max-download-limit` 走整数路径，安全 | 保持现状；新增规则：**任何浮点写入 RPC/命令行必须显式 `InvariantCulture`** |
| CA1031 捕获通用异常 | 90+266 | 多数属有意兜底。实测 **124 处静默 catch**（含测试基建）；逐条复核后：`MainWindow` 4 处（小窗定位 385、DPI 回退 420、主题菜单图标 470、色板项 495）与 `MagnetConfirmWindow` 5 处（SetIcon/GetCursorPos/DPI/dn 解析）**均为有兜底值的尽力而为，判定可接受**；真正值得补日志的是 `TrayService.cs:64`（托盘菜单动作是用户动作，失败即"点了没反应"） | 仅对用户可感知路径补 `App.Log`；其余保留静默以维持现有降级行为 |
| CA2000 未释放局部对象 | 4 | `Aria2RpcClient` 两处 `new StringContent(...)` 未显式释放（`CallAsync`/`PostRawAsync`）。HttpClient 内部会在请求结束释放请求消息，实际泄漏风险低 | 改 `using var content = ...`，零成本消除歧义 |
| CA2213 未释放字段 | 9 | `ApiService._cts`/`_requestGate`、`DownloadEngine._pollCts` 等在 `Dispose` 中未释放。BCL 对象无句柄，实际影响低但不符合 `IDisposable` 契约 | 在 `Dispose` 中释放；顺带在 `ApiService.Dispose` 等待在途处理器结束 |
| CA1001 类型持有可释放字段却不可释放 | 2 | `MainWindow` 持有 `_dialogGate`(SemaphoreSlim) 与 `_showEvent`；主窗全程唯一实例，泄漏量可忽略 | 低优先级；可把信号量改为静态或实现 `IDisposable` |
| CA1806 忽略返回值 | 6 | `WindowEffects.SetDarkTitleBar/SetDwmColorAttribute` 忽略 `DwmSetWindowAttribute` 的 HRESULT、`TrayMenuHost` 忽略 `SetWindowLong` 返回值。旧系统不支持该 DWM 属性时**静默失效**（无日志、无回退提示） | 检查 HRESULT，失败时记录一次调试日志（并作为"深色标题栏不受支持"的证据） |
| CA2016 未转发 CancellationToken | 2 | 指向 `ApiService.cs:148` 的 `_requestGate.Wait(0)` | **误报**：此处刻意用 0 超时做非阻塞探测，不涉及等待 |
| CA5350 使用弱加密算法 | 2 | `BtHashUtil.FromTorrent` 用 SHA1 | **非缺陷**：BitTorrent v1 infohash 协议规定就是 SHA1，改动会破坏 BT 功能（已在代码注释中说明，勿"修"） |
| CA5392 P/Invoke 未指定搜索路径 | 50 | 单文件版把 exe 与运行时文件放在**用户可写**目录（`%LOCALAPPDATA%\KokonaDownloader\Standalone`）后运行 | 建议加 `[DefaultDllImportSearchPaths(DllImportSearchPath.System32)]`（防御纵深；攻击者需先取得该用户权限，非现实漏洞） |
| CA2007 未 `ConfigureAwait(false)` | 80（App 层） | Core 层已全部规范使用（0 命中）；App 层为 WinUI UI 代码，需回到 UI 上下文 | **非缺陷**，保持现状 |
| CA1859 返回类型可收窄 | 2 | `ThemeService.Solid` 返回 `Brush` 而实际为 `SolidColorBrush` | 微优化，可选 |

### 10-C 主审复核发现（人工精读 + 实测确认）

| 编号 | 严重度 | 位置 | 证据 | 问题与影响 | 改进建议 |
|------|--------|------|------|-----------|----------|
| N-1 | 中 | `MagnetConfirmWindow.xaml.cs:140-160` | `private async void OnActivatedForInitialFocus(...)` / `for (var attempt = 1; !_closed; attempt++) { await Task.Delay(100); if (StartBtn.Focus(...)) ...}` | **无 try/catch 的 `async void`**：`_closed` 只在循环开头判断，`await` 之后再未复查。窗口在 100 ms 重试间隔内被关闭/取消时，`StartBtn.Focus()` 会作用于已关闭窗口，异常沿 `async void` 抛到 UI 线程（若 `UnhandledException` 未标记已处理则有崩溃风险）。同批 L-10 修复覆盖了主窗/设置窗，**漏掉本处** | 给该方法加 try/catch + 日志；`await` 之后补 `if (_closed) return;` |
| N-2 | 中 | `src\KokonaDownloader.Core.Tests\` | 第三轮修复中仅 L-4 有回归测试（`墓碑_批量撤销时全部命中条目都被移除`）；`_requestGate`/FNV 指纹/批量伪文件名无任何测试引用；L-5 改的是 `background.js`，而 cscript 套件 `tests\test_logic.js` **只测 `logic.js`** | 修复没有护栏：后续重构极易静默回归（如 503 门被删、指纹碰撞导致日志丢失）。扩展侧改动更是完全无自动化覆盖 | ① 把 `background.js` 里的响应映射抽成 `logic.js` 的纯函数（`mapResponse(status, body)`）以便 cscript 覆盖；② 补 3 个 Core 单测：门饱和返回 503、指纹在状态变化时翻转、批量任务名剔除 URL 末段 |
| N-3 | 中低 | `src\KokonaDownloader.Core\Api\ApiService.cs:141-165、536-546` | `catch { break; }`（146-147）；`_ = Task.Run(async () => {...}, ct)`（159-163）；`public void Stop(){ _cts?.Cancel(); ... }` | 三个相关健壮性问题：① `GetContextAsync` 抛**任何**非取消异常都会永久跳出接受循环 → API 静默失效，且**无日志**；② 若 `ct` 在 `Wait(0)` 与 `Task.Run` 之间被取消，委托不会执行 → `finally` 不运行 → **门槽位永久丢失**（本实例不再 `Start()`，故现实可达性低，但属真实缺陷）；③ `Dispose` 不等待在途处理器，也不释放 `_cts`/`_requestGate` | ① 循环内区分取消与瞬时异常：记录日志并在非取消时短暂退避后继续；② `Task.Run` 外层加 try/catch，失败时 `Release()` 再回 503；③ `Dispose` 中 `_cts?.Dispose()`、`_requestGate.Dispose()`，并等待 `_loop` 与在途任务收敛 |
| N-4 | 低 | `src\KokonaDownloader.App\StandaloneBootstrap.cs:115-118` | `manifest.Files.All(f => File.Exists(root + f.Rel) && new FileInfo(root + f.Rel).Length == f.Size)` | 清单完整性**只比对文件大小**：磁盘损坏/半写导致"同尺寸损坏"时判定为完整，引导不再自愈，XAML 加载会持续失败且用户只能手删目录。文件名/数量错误可自愈，此路径不能 | 清单内增加每文件 SHA256（12 个文件约 7 MB，实测哈希毫秒级），或对 XAML 关键文件（`resources.pri`/`*.xbf`）做逐文件校验 |
| N-5 | 低 | `src\KokonaDownloader.App\StandaloneBootstrap.cs:36-39` | 注释：`// 尽力而为：解压彻底失败时让 XAML 初始化按原有方式报错，不在此处吞掉。` 紧接 `catch { }` | 注释与代码语义相反（实际就是吞掉），维护者会误判失败可见性；本轮正是靠"引导静默中止"这一现象才发现 `SHA256.Hash` 缺陷，注释加剧了排查难度 | 改写注释为"引导失败时静默降级为原地运行（依赖临时目录自带的 resources.pri），失败原因不在此上报" |
| N-6 | 低 | `src\KokonaDownloader.Core\Api\ApiService.cs:115` | `public string Version { get; } = "1.0.8";` | 版本号**硬编码**，与 `csproj` 的 `<Version>` 靠人工同步（csproj 注释也承认这点）。升级时漏改会导致 `/api/ping`、扩展版本校验、UiTests 断言互相矛盾 | 从程序集读取：`Assembly.GetExecutingAssembly().GetCustomAttribute<AssemblyInformationalVersionAttribute>()`（回退到 `AssemblyVersion`） |
| N-7 | 低 | 仓库根 `files/`、`infohash.txt`、`artifacts/` | `files/`=5 个测试夹具共 **78 MB**、`infohash.txt`=40 B，均**未跟踪且未被 `.gitignore` 覆盖**；`artifacts/transparency/`=19 张 PNG **2.2 MB 已被跟踪**，而 README/项目文档对其引用数为 **0** | ① 每次 `git status` 都被两条噪音污染，且无任何脚本引用 `files/`（已 grep 全仓：0 命中）→ 属遗留物；② `artifacts/` 记录的是**已从产品移除**的模式截图（Opaque/BlackTransparent），属于公开仓库里的无引用二进制重量 | ① `files/`、`infohash.txt` 写入 `.gitignore`（或改由脚本在临时目录生成夹具）；② `artifacts/` 若仅作历史证据，移到 `docs/` 并在文档中引用，否则删除 |
| N-8 | 低 | `src\KokonaDownloader.App\Tray\TrayService.cs:64` | `try { action?.Invoke(); } catch { }` | 托盘菜单动作（打开主窗/设置/退出等）异常被完全静默：用户点击"无反应"、日志无痕迹，属最难排查的一类问题 | 补 `App.Log($"[tray] 菜单动作异常: {ex.Message}")`，必要时弹通知 |

### 10-D 运行时实测（本轮新增证据）

| 观测项 | 结果 | 结论 |
|--------|------|------|
| 未处理异常 | 部署后 216 行日志中 0 条 `未处理`/`XamlParse`，仅剩测试夹具引发的预期 aria2 报错（errorCode=3/8） | 稳定 |
| L-8 轮询指纹（实测） | 全日志仅 29 条轮询诊断行，最后一条为 03:15:52；此后**空闲近 11 小时 0 条** | 变化检测工作正常且不再每轮拼串（原每轮数十 KB 垃圾已消除） |
| 并发门（实测） | 120 并发突发 → 75 个 200 + 45 个 503，耗时 100 ms | 与 64 槽设计一致，无排队挂起 |
| 内存/句柄 | 主进程 WS 62–83 MB、私有 161 MB、句柄 2,952、线程 50（运行 11 h）；aria2 WS 18.4 MB | 与既有基线（WS ~200 MB 峰值）一致 |
| 窗口句柄回收 | 关闭 6 个常驻小窗后句柄 2,952 → 2,845（**每窗约 18 个**），无增长迹象 | 常驻小窗无句柄泄漏 |
| API 负载 | 22 任务 → `/api/tasks` 9,072 B（约 412 B/任务） | 与既往 `files` 字段占比结论一致 |
| 引导（全新/跳过/并发） | 全新状态：12 文件解压 + 清单写入 + exe 复制；跳过路径：ping 就绪 1.17 s、零文件重写 | L-6 修复有效（见 §9-B） |

### 10-E 改进建议（按优先级）

**P0 — 建议下个版本必修（成本低、收益明确）**
1. **N-1**：`MagnetConfirmWindow` 焦点重试循环补 try/catch + `_closed` 复查（3 行代码，消除一条 UI 线程未处理异常路径）。
2. **N-2**：为第三轮修复补回归护栏（网关 503、指纹翻转、批量文件名 3 个 Core 单测；`background.js` 响应映射抽成 `logic.js` 纯函数以纳入 cscript 套件）。
3. **N-3**：`ApiService` 接受循环区分取消/瞬时异常并记日志；`Task.Run` 失败时归还门槽位；`Dispose` 释放 `_cts`/`_requestGate`。

**P1 — 建议同版本内顺手做（低风险）**
4. **N-4**：引导清单增加逐文件哈希（或至少校验 `resources.pri`/`*.xbf`），让"同尺寸损坏"可自愈。
5. **N-6**：`/api/ping` 版本号改为从程序集读取，消除双份版本源。
6. **CA2000 / CA2213 / CA1806**：`StringContent` 显式 `using`；`Dispose` 释放 `_cts`/`_pollCts`；DWM/`SetWindowLong` 检查返回值并记录一次日志。
7. **N-5 / N-8**：修正 `StandaloneBootstrap` 注释语义；托盘菜单动作补异常日志。

**P2 — 择机处理（卫生与加固）**
8. **N-7**：`.gitignore` 补 `files/`、`infohash.txt`；清理未被引用的 `artifacts/transparency/`（2.2 MB 公开仓库二进制）。
9. **CA5392**：P/Invoke 增加 `DefaultDllImportSearchPaths(SecuritySystem32)`，单文件版运行时目录位于用户可写路径，属防御纵深。
10. **门槛建议**：CI/本地构建启用 `AnalysisLevel=latest` + 选定规则集（至少 CA2000/CA2213/CA5392 报错），把本轮的分析器基线固化下来，避免回归。
11. **长期**：`MainWindow.xaml.cs`(955 行) 与 `DownloadEngine.cs`(698 行) 已接近"单文件职责过多"，可考虑按"任务列表交互 / 小窗编排 / 权限与节流"拆分；非必要不做大重构。

> 未列入建议的项：CA1305/CA2016/CA5350/CA2007 经复核为**误报或有意的协议/框架要求**，不应"修复"（尤其 `BtHashUtil` 的 SHA1 是 BT v1 infohash 协议规定）。

---
- `.verify\fulltest\server.mjs` — 测试网站（18080）
- `.verify\fulltest\run.mjs` — 下载全场景驱动（52 断言）
- `.verify\fulltest\bt_test.mjs` / `bt_monitor.mjs` — BT 磁力测试/监控
- `.verify\fulltest\perf_submit.mjs` — 性能负载提交
- `.verify\fulltest\ui_inspect.ps1` + `ui_inspect.json` — UI 重叠/越界检测
- `.verify\functest\test.mjs` — E2E（版本断言已更新 1.0.8）

第二轮（代码审计）新增，均位于 `.verify\audit\`：

- `rpc_size.mjs` — 轮询响应体积/延迟测量（对照 `keys` 裁剪与条数截断）
- `payload_breakdown.mjs` / `payload_attribution.mjs` / `uri_inspect.mjs` — 轮询负载按字段拆解与归因（查明 `files` 占 71.9%、系 split 连接重复 URI 所致）
- `tellstopped_order_and_pdf.mjs` — `tellStopped` 返回顺序判定（证明截断取的是最旧任务）+ 教程 PDF 密钥扫描
- `settings_write_cost.mjs` — 设置写入连带开销（修复前 4.75 s → 修复后 0.27 s）
- `secret_in_assets.mjs` — 公开资产中的密钥明文扫描
- `verify_fixes.mjs` — 部署后修复验证（12 项：CORS / 密钥轮换 / 主题重算短路）
- `deploy_fix.ps1` — 部署脚本（停旧实例 → 轮换密钥 → 替换 exe → 重启）
- `run_tests_serial.ps1` / `post_deploy_tests.ps1` — 逐类串行 Core 测试、部署后 run/E2E/UI 套件驱动

第三轮（§8-F 修复）新增，均位于 `.verify\audit\`：

- `deploy_round3.ps1` — 本轮部署脚本（停旧实例 → 替换 exe → 重启 → ping 等待；无密钥轮换）
- `bootstrap_repro.ps1` — 引导各步骤复现脚本（定位 HashPayload 异常用）
- `semtest\` — 一次性反射探针（实证本机 SDK 的 `SemaphoreSlim` 无 `TryWait` 重载、`SHA256.Hash` 未终结即读抛异常）
- `run_round3.log` / `test_round3.log` / `ui_round3.log` / `extlogic_round3.log` — 本轮复测原始输出

---

## 11. 第五轮：按 §10-E 建议落地改动（2026-09-20，版本号仍为 1.0.8）

本轮是第四轮审计的**执行轮**：§10-E 的 P0/P1/P2 全部落地，唯一有意保留的是第 11 项"长期拆分"（其原文即"非必要不做大重构"，本轮无重构需求）。改动共触及 15 个文件 + 新增 5 个文件（`+399/−79` 行），全部重新编译、发布、部署并复测。

### 11-A 改动清单

| 建议 | 编号 | 位置 | 实际改动 | 验证方式 |
|------|------|------|----------|----------|
| P0-1 | N-1 | `MagnetConfirmWindow.xaml.cs` | 焦点重试循环整体包 try/catch + `App.Log`；`await Task.Delay(100)` 之后立即 `if (_closed) return;` | 部署后日志出现 `[magnet] 确认窗口初始焦点设置成功（第 1 次尝试）`，且全程 0 条 UI 线程异常 |
| P0-2 | N-2 | Core.Tests / extension / tests | 新增 `ApiConcurrencyGateTests`（2）、`PollFingerprintTests`（6）、批量伪文件名回归测试（1）；`background.js` 的状态码映射抽成 `logic.js:mapDownloadResponse`；cscript 套件 56 → 77 项 | Core 149/149、cscript 77/77（见 §11-D） |
| P0-3 | N-3 | `ApiService.cs` | ① `AcceptLoop` 三分支：`HttpListenerException`（仅"仍在监听且未取消"时记日志）、`ObjectDisposedException`（静默）、其他（记完整异常）；② `Task.Run` **不再传 `ct`**（取消会让委托整体跳过、`finally` 不归还槽位），外层 catch → 记日志 + `ReleaseGate()` + 503；③ `Dispose` 释放 `_cts`/`_requestGate`，`ReleaseGate()` 吞掉关停竞态的 `ObjectDisposedException`；④ 新增可选构造参数 `maxConcurrentRequests`（默认 64）以便确定性测试 | 新单测 + 部署后压测 64×200 + 56×503（74 ms） |
| P1-4 | N-4 | `StandaloneBootstrap.cs` | `ManifestFile` 增加 `Hash`；新增 `VerifyFiles`（存在 + 尺寸 + SHA256 三重校验）与 `HashFileSafe`；校验失败即 `ExtractAll` 并**回写**新哈希，旧清单（无哈希）首次重解压后补齐 | 实测注入"同尺寸损坏"后自愈（§11-D 引导行） |
| P1-5 | N-6 | `ApiService.cs` + `src\Directory.Build.props`（新） | `Version = ReadAssemblyVersion()`（读 `AssemblyInformationalVersion`，截断 `+` 后源修订号，回退 `AssemblyVersion`）；版本四属性集中到 `Directory.Build.props`，App.csproj 内删除并留指引注释 | `/api/ping` 仍返回 `1.0.8`；全仓再无第二处版本源 |
| P1-6 | CA2000/CA2213/CA1806/CA1001 | `Aria2RpcClient.cs`、`DownloadEngine.cs`、`MainWindow.xaml.cs`、`WindowEffects.cs`、`TrayMenuHost.cs` | `StringContent` 改 `using var`；`DisposeAsync` 释放 `_pollCts`；`MainWindow` 实现 `IDisposable`（释放 `_showEvent`/`_dialogGate`，已释放时 `ShowDialogAsync` 直接返回 `None`）；DWM/`SetWindowLong` 返回值读回并**各记一次**日志（`Interlocked.Exchange` 防刷屏） | 编译期门槛（`.editorconfig`）+ 部署后日志中 0 条 `[effects] ... 设置失败`/`[tray] ... 未生效` |
| P1-7 | N-5/N-8 | `StandaloneBootstrap.cs`、`TrayService.cs`、`TrayMenuHost.cs` | 注释改为如实描述"引导失败即静默退化为原地运行"并补 `App.Log(ex)`；托盘气泡点击与菜单项回调的 catch 记日志，菜单里的 fire-and-forget 异步动作统一走 `RunLogged(what, start)` | 代码审查 + 编译；运行路径未自动化覆盖（见 §11-E） |
| P2-8 | N-7 | `.gitignore`、`artifacts/` | `.gitignore` 补 `files/`、`infohash.txt`；`artifacts/transparency/`（19 张 PNG，2.2 MB，文档引用 0 次）从索引与工作树删除，本地已备份到 `.verify\backup\artifacts_transparency_predelete\`（git 历史仍可取回） | `git status` 不再出现 `files/`、`infohash.txt`；`git ls-files artifacts` 为空 |
| P2-9 | CA5392 | `NativeImports.cs`（新） | 程序集级 `[assembly: DefaultDllImportSearchPaths(DllImportSearchPath.System32)]`，覆盖 user32/kernel32/shcore/dwmapi/comctl32/shell32 | 编译 0 警 0 错；运行期 DWM/`mica支持=True acrylic支持=True` 表明 dwmapi 正常加载 |
| P2-10 | 分析器门槛 | `src\.editorconfig`（新） | 四个项目路径作用域内把 CA2000/CA2213/CA1806/CA5392 设为 `severity = error`；测试项目与 UiTests 有意排除 | 一次性探针实证四条规则都会以 error 触发（§11-C） |
| P2-11 | 长期拆分 | — | **有意不做**：本轮改动均为局部修补，未新增职责集中度问题 | — |

### 11-B 新增/扩充的测试与脚本资产

| 资产 | 内容 |
|------|------|
| `src\KokonaDownloader.Core.Tests\ApiConcurrencyGateTests.cs` | 把闸门容量注入为 2，用两个"请求头已收完、`Content-Length: 100000` 却只发 32 字节"的裸 TCP 连接把槽位占满（处理器阻塞在读请求体），断言第三个请求**立即 503**；断开后轮询到 **200**，证明槽位归还。另有一项"未饱和时 20 连发全部 200"的对照 |
| `src\KokonaDownloader.Core.Tests\PollFingerprintTests.cs` | 6 项：同内容稳定、空轮询稳定、状态变化翻转、任务增减翻转、`NumStopped` 变化翻转（旧实现此处会误判"无变化"）、200 个不同任务集合无碰撞 |
| `FileNameResolutionTests.批量添加时同样剔除URL末段伪文件名` | L-3 的回归护栏：批量路径（一次贴多个链接）必须与单任务路径一样丢弃"只是把链接末段重复一遍"的伪文件名，落盘名取 `Content-Disposition` 真实名 |
| `extension\logic.js` → `mapDownloadResponse(status, body)` | 状态码 → `{ok,gid,duplicate,confirm}` 或 `{ok:false,code,message}`；新增 `busy`（503，客户端并发闸门满）分类，`background.js` 改为消费该函数（原先内嵌在 async 流程里，只能在浏览器里手测） |
| `tests\test_logic.js` | 56 → **77** 项：200/204/299 成功、gid 透传、duplicate/confirm 标志、401→`unauthorized` 且**保留内置文案**（服务端文案不可操作）、400→`rejected` 且透传服务端原因、503→`busy` 且服务端原因优先/空串回退内置、500/404→`http` 且文案带状态码 |
| `.verify\audit\bootstrap_corrupt_round5.ps1`（新） | N-4 的现场验证：按位取反 `resources.pri` 第 1000 字节（尺寸不变）→ 重启 → 断言哈希回到清单值；再重启断言走校验跳过路径（mtime 不变、清单哈希仍在） |
| `.verify\audit\run_tests_serial.ps1` | 类清单加入 `ApiConcurrencyGateTests`，并同步"纯单测"桶的排除过滤器（`PollFingerprintTests` 自动落入该桶） |
| `.verify\audit\probe_logic_map.js` | 一次性探针：用 `String.fromCharCode` 构造期望中文文案，实证 `logic.js` 里 `\u` 转义写法的正确性（避免 cscript 的 ANSI 读源码把中文变形） |

### 11-C 关于"分析器门槛"的实测结论（踩坑记录）

1. `AnalysisMode=Default` 下 CA2000/CA2213/CA1806/CA5392 **默认不报告**，但可以在 `.editorconfig` 里按路径 `dotnet_diagnostic.CAxxxx.severity = error` 单独打开。
2. **一旦某规则的严重度已在 `.editorconfig` 中设定，MSBuild 的 `WarningsAsErrors` / `-warnaserror` 就无法再升级它**。最初尝试用 `KokonaAnalyzerGate` 属性做门禁时，CA5392/CA1806 仍停在 `warning`、0 error；改为在 `.editorconfig` 直接写 `error` 后生效，MSBuild 侧的门禁属性已删除（避免留下"看起来有门禁其实无效"的配置）。
3. 门槛**非空转**的实证：临时放入 `P2GateProbe.cs`（未释放的 `StringContent` 局部量、实现 `IDisposable` 却不释放 `CancellationTokenSource` 字段、`" x ".Trim()` 丢弃返回值、无 `DefaultDllImportSearchPaths` 的 `[DllImport]`）→ 构建报 `error CA5392/CA1806/CA2000/CA2213` 四条；探针删除后 Core 干净重编译 0 警 0 错。
4. 属性写法坑：`[module: DefaultDllImportSearchPaths(...)]` 会报 `error CS0592`（该特性仅适用于 assembly/method），并且会**级联**触发约 10 条 XAML 编译错误（`WMC0001 Unknown type 'SquircleBorder'` 等，实为编译失败后的连带现象），极易误判为 XAML 被改坏。改用 `[assembly: ...]` 后 0 警 0 错。
5. 覆盖范围**故意不含** `KokonaDownloader.Core.Tests`（否则 `dotnet test` 会被测试里的临时对象绊住）与 `KokonaDownloader.UiTests`（本就过期）。

### 11-D 复测结果（全部本地实测，新 exe 已部署）

| 套件 / 观测项 | 结果 | 对比基线 |
|---------------|------|----------|
| Core 单元 + 集成（逐类串行 10 组） | **149/149 PASS**（0 失败 0 跳过） | 上一轮 140/140，净增 9 项 |
| 扩展逻辑（cscript） | **77/77 PASS** | 上一轮 56/56，净增 21 项 |
| `run.mjs` 下载全场景 | **52/52 PASS** | 持平 |
| `test.mjs` E2E | **24/24 PASS** | 持平 |
| `ui_inspect.ps1` | **ISSUES: 0**（5 个级联位、每窗 20 控件） | 持平 |
| `verify_fixes.mjs`（二/三轮修复回归） | **10/10 PASS** + 1 项 skip（未提供 OLD_SECRET）；20 次设置补丁 271 ms、新增主题重算 0 行 | 持平 |
| 并发门压测 | 120 并发 → **64×200 + 56×503**，74 ms | 上一轮 75/45；两者都与 64 槽一致（分配数取决于到达时序），无排队挂起 |
| 引导·同尺寸损坏自愈（N-4 新证） | 篡改 `resources.pri` 第 1000 字节（1,391,488 B 尺寸不变）→ 重启后哈希回到清单值，`HEALED` | 旧实现（只比尺寸）判定"完整"、不能自愈 |
| 引导·校验跳过路径 | 再次重启 ping 就绪 **1.08 s**，`resources.pri` mtime 不变；清单仍含 12 个逐文件哈希 + `payloadHash` | 与三轮基线（1.17 s）一致 |
| `/api/ping` 版本 | `1.0.8`（现由程序集特性读取） | 一致 |
| 运行日志 | 482 行中 `未处理`/`未生效`/`设置失败`/`引导失败`/`XamlParse`/`API 处理异常` **0 条** | 一致 |
| 部署产物 | 单文件 exe **318,204,760 B**，SHA256 `8A5D043A1ABE98358106AF210CF5AA11CA7E724D780E82203A3D9157D0DFA98D`（桌面版已替换） | 上一版 319,711,373 B；**小了 1,506,613 B，本轮未逐项归因** |

### 11-E 有意未做 / 未覆盖 / 遗留（诚实清单）

- **未做**：§10-E 第 11 项的窗体/引擎拆分（"非必要不做大重构"）。
- **未自动化覆盖**：`TrayMenuHost`（托盘菜单宿主窗口、菜单项动作）只在用户点开托盘菜单时执行，本轮新增的样式读回校验与 `RunLogged` 异常日志**只有编译期与代码审查证据**，缺运行时证据；需要人工点开托盘菜单（打开主窗 / 设置 / 暂停全部 / 退出）确认无 `[tray]` 失败行。
- **未归因**：新 exe 比上一版小约 1.5 MB，功能与负载测试全部持平，但本轮未对差异做逐项归因（不是本轮改动预期内的体积变化）。
- **文档已修但仓库外未动**：GitHub Release 的 exe/zip 资产仍是上一版，**未替换**；`v1.0.7`/`v1.0.8` 两个 tag 仍未创建（受"禁止擅自发版"约定约束，等用户确认）。
- **仍待人工**：`海兔下载器教程.pdf`（公开资产，288,628 B）中的设置窗截图可能显示已失效的旧密钥；脚本扫描未发现明文密钥，需人眼确认。
- **本地占盘未清**：`files/`（78 MB 测试夹具）已 ignore 但**未删除**，等用户决定；旧 exe 备份 319 MB 仍在 `.verify\old_exe_backup\`。
- **过程备注（可追溯性）**：编辑 `DownloadEngine.cs` 时曾发生一次误替换——把 `StartAsync` 的签名行替换成了重复的辅助方法（`edit` 的二次匹配），当场读回文件发现并修复，最终编译与 149 项测试均通过。记录以说明"改动过程中的自检手段是读回 + 编译 + 全量测试"。

---

## 12. 第六轮：开销专项审计（2026-09-20，只看不改）

本轮**未改动任何产品代码**（版本号仍 1.0.8，构建产物仍是 §11-D 那枚 exe），只针对"每轮轮询 / 每 tick UI / 每次写盘到底花多少钱"做定位与量化。前四轮关注正确性与健壮性，本轮回答四个问题：谁在花钱、花多少、修完能省多少、值不值得修。

**一句话结论**：以当前真实规模（7 条任务历史、1 个活动任务）衡量，应用**没有需要立刻修的浪费**——空闲 40 s 内日志 0 行、`tasks.json` 0 次重写、常驻 CPU 0.57–0.91 % 单核、WS 稳定在 212 MB。但开销的**增长曲线**集中在两处线性项：JSON 解析的三重物化（O-1）与全量历史轮询（O-2）。二者叠加时，若任务历史涨到 1000 条，仅"刷新任务列表"这一件事在 800 ms 节奏下就会吃掉约 **7 % 单核**（应用侧 4.3 % + aria2 侧 2.8 %）并以约 **5 MB/s** 的速率分配托管内存——这才是本轮所有建议针对的场景。

### 12-A 方法（以及为什么这些数字可信）

- **解析成本**：一次性控制台基准 `.verify\bench\parsebench\`（`net8.0`，`ProjectReference` 引用 Core 工程，用的是**产品里真实的模型类与 `JsonSerializerOptions`**）。把同一份**真实抓取**的 aria2 multicall 响应分别经三条路径解析，测 9 轮中位耗时与 `GC.GetAllocatedBytesForCurrentThread` 分配量，并在内部关闭 Server GC / 并发 GC 以减少噪声。三种策略解析出的**对象数量必须相等**（脚本末行打印 `COUNT-OK`，首次运行就抓出过我自己的 reader 游标 bug：`MISMATCH 1000/1000/0`）——避免"看起来快是因为少解析了东西"。
- **负载规模**：`dump_payload.mjs` 发出与应用**逐字节相同**的周期 multicall 并落盘；`seed_failures.mjs` 往 aria2 灌 1000 条"连 127.0.0.1:59999 秒失败"的任务，用来得到真实形态的长历史（不产生任何真实流量）；`probe_history.ps1` 负责在 **16999** 端口拉起/销毁一次性探针 aria2，全程不触碰应用自己的 16801（探针用独立 `--dir`、独立密钥、关闭 DHT/LPX/PEX）。
- **历史窗口的正确性**：`probe_windows.mjs` 比较字节数与 aria2 侧响应耗时；`check_tail_order.mjs` 在**在线引擎**（应用自己的 aria2，7 条历史）上逐 gid 证明偏移窗口取到的就是最新 N 条。这条是 §8-D 遗留问题的关键前置证据。
- **常驻开销**：`.verify\audit\idle_round6.ps1` 对同一进程做三分区采样（窗口可见 60 s → `WM_CLOSE` 最小化到托盘 60 s → 再启动一次 exe 触发单实例唤起 30 s），记录 CPU 时间增量、WS、私有内存、句柄、线程数，落盘 `idle_round6.csv`。**注意**：第三个分区事后用 Win32 `EnumWindows + IsWindowVisible` 复查，发现主窗当时**仍是隐藏**（那次唤起没生效，第二次唤起才生效，见 §12-E），所以它被标为"隐藏态 + 实例唤起"，不能当作"可见态复测"来读。
- **UI 算法**：parsebench 的 `ui` 模式复刻 `MainWindow.MergeTasks` + `ApplyView` 的算法（`Where`/`OrderByDescending`/逐项 `ReferenceEquals`/`List.Contains`/`IndexOf`/`Move`），分测"稳态轮"与"成员变化轮"。**该复刻不含 XAML 容器与绑定通知成本**，因此真实值只会更高。

### 12-B 实测数据

**表 1 · JSON 解析三路径对比**（同一份负载、同一模型类，9 轮中位数；分配量/轮）

| 负载 | 策略 | 中位耗时 | 分配 | 说明 |
|------|------|---------:|-----:|------|
| **4,271 B / 7 任务**（当前在线引擎真实响应） | A 现方案：`ReadAsStringAsync` → `JsonNode.Parse` → 逐节点 `JsonNode.Deserialize<T>` | 0.29 ms | 30 KB | 现实现 |
| | B `JsonDocument` + `JsonElement.Deserialize<T>` | 0.22 ms | 10 KB | 中间方案 |
| | C `Utf8JsonReader` 手写信封扫描 + `Deserialize(ref reader)` | 0.16 ms | 10 KB | 极限方案 |
| **458,075 B / 1000 任务**（探针 aria2 真实响应） | A | 34–38 ms | **3,995 KB** | 458 KB 负载放大成约 4 MB 托管垃圾（约 8.7 倍） |
| | B | 16–27 ms | **1,445 KB** | 分配 ↓64 %，耗时约 ↓35 % |
| | C | 7–14 ms | **1,445 KB** | 与 B **分配量完全相同**（对象本体就是下限），C 只再省掉 `JsonDocument` 的索引结构 |
| 587,169 B / 1000（同形态放大样本，交叉验证） | A / B / C | 31–37 / 16–27 / 7–14 ms | 3,995 / 1,445 / 1,445 KB | 分配比 **B/A = C/A = 0.36**，两轮独立样本一致 |

要点：① 现方案每轮把响应物化**三次**（>85 KB 时 `ReadAsStringAsync` 还会进 LOH）；② B 与 C 分配量相同，**手写 reader 相对 B 没有分配收益**，只换来一点 CPU；③ 1,445 KB/轮（1000 任务）是"任务对象本体"的地板，想再降必须**不再每轮重建全部任务对象**（本轮不建议，见 §12-D 不建议项）。

**表 2 · 常驻空闲开销三分区采样**（1 个活动任务、7 条历史，同一进程连续采样）

| 状态 | 窗口 | App CPU | aria2 CPU | App WS | WS 变化 | 私有内存 | 线程/句柄 |
|------|-----:|--------:|----------:|-------:|--------:|---------:|-----------|
| P1 主窗可见 | 60 s | **0.91 %** 单核 | 0.05 % | 211.8 MB | +0.9 MB | 118.6 MB | 稳定 |
| P2 已最小化到托盘（`WM_CLOSE`） | 60 s | **0.57 %** 单核 | 0.10 % | 212.1 MB | 0.0 MB | 118.9 MB | 稳定 |
| P3 隐藏态 + 第二个实例唤起（30 s） | 30 s | 0.88 % | 0.05 % | 212.0 MB | −0.1 MB | 118.8 MB | 稳定 |

要点：① **窗口不可见时仍有 0.57 %**（≈可见态开销的 63 %）——`_timer` 只在 `Closed` 里停，而关闭到托盘走的是 `AppWindow.Hide()`（`MainWindow.xaml.cs:199` vs `:221`），900 ms UI 刷新、`ApplySelectionVisuals`×2、`UpdateBtSummary` 全部照跑（O-3）；② 2.5 min 内 WS 波动 ≤0.9 MB，**无泄漏迹象**；③ 同期 `app.log` 40 s 内 **0 行新增**、`tasks.json` mtime 不变——第五轮的轮询指纹防抖与"设置写入短路"确实生效（`app.log.1` 5,243,031 B 说明 5 MB 轮转上界也在正常工作）。

**表 3 · 历史窗口策略对比**（探针 aria2，1000 条任务历史；周期 multicall 的字节数 / aria2 最快响应）

| 策略 | 字节/轮 | 相对现方案 |
|------|--------:|-----------:|
| 空历史（信封下限） | 170 B | — |
| **现方案** `tellActive` + `tellWaiting(0,1000)` + `tellStopped(0,1000)` + `getGlobalStat` | **458,363 B** | 1× |
| `tail 200`（`tell*(max(0,n-200),200)`） | 179,323 B | −61 % |
| `tail 50` | 53,133 B | −88 % |
| 仅 `tellActive` + `getGlobalStat` | 2,325 B | −99.5 % |
| `head 200`（朴素截断，**内容错误**） | 179,323 B | 字节数与 tail 相同，但拿到的是**最旧** 200 条 |

配套实测：① aria2 自身空转 1.33 % 单核（1000 条历史），被 800 ms 节奏轮询时升到 **4.1 %**；② 逐 gid 验证 `tellStopped(n-N, N)` **等于**全量响应的最后 N 条 ✅，而 `tellStopped(0, N)` 是最旧 N 条 ❌（与 §8-D 结论一致，本轮在在线引擎上复现：`full=680b…,…,9f26` / `tail=e762,d0ca,9f26`）；③ 单条任务的响应体积实测 **458 B**（纯 HTTP 失败任务，无 `files/uris` 展开），比 §8-D 记录的约 1.2 KB/任务（BT/split 场景）**更低**，所以本表的收益百分比属**保守**估计。

**表 4 · `ApplyView` 算法复刻**（不含 XAML 通知成本，9 轮中位数）

| 任务数 | 稳态轮（成员与顺序未变） | 成员变化轮（重建路径） | 比值 |
|-------:|------------------------:|----------------------:|-----:|
| 10 | 0.004 ms | 0.003 ms | 1× |
| 100 | 0.038 ms | 0.039 ms | 1× |
| 500 | 0.087 ms | 0.407 ms | 5× |
| 1000 | 0.190 ms | **1.691 ms** | 9× |
| 2000 | 0.291 ms | **5.225 ms** | 18× |

要点：稳态轮走的是 O(n) 快速返回（`MainWindow.xaml.cs:605-617`），**没问题**；`filtered.Contains()`（`:620`）+ `_tasks.IndexOf()`（`:624`）+ `Move` 组成的重建路径是干净的 O(n²)（每翻倍 ×4）。它只在成员/顺序变化的那几轮触发，但一次批量添加 100 条会让它连续多轮命中。

### 12-C 逐项开销清单

| 编号 | 级别 | 位置 | 现象与量化 | 建议（含代价） |
|------|------|------|-----------|----------------|
| **O-1** | 高 | `Aria2RpcClient.PostRawAsync`/`ParseResponse`（`:299`）+ `DownloadEngine` 取值处 | 每轮把响应物化三次：`ReadAsStringAsync`（>85 KB 进 **LOH**）→ `JsonNode.Parse` 全量 DOM → 逐元素 `JsonNode.Deserialize<T>`（内部再序列化一次）。1000 任务 = **4 MB 垃圾/轮、34 ms**；800 ms 节奏下 ≈ **5 MB/s、4.3 % 单核** | 改为 `resp.Content.ReadAsStreamAsync()` + `JsonDocument.Parse(stream)`（B 方案），信封用 `RootElement.GetProperty("result")` 走 `JsonElement.Deserialize<T>`。**分配 ↓64 %**、耗时约 ↓35 %，且彻底避开 LOH 字符串。改动局限在 1 个类 + 取值 4 处；**不建议**上手写 `Utf8JsonReader`（表 1 C 列：分配与 B 相同，白增复杂度） |
| **O-2** | 高（线性） | `DownloadEngine.PollLoopAsync` 的 multicall（`tellWaiting/tellStopped (0,1000)`） | 每轮拉回**全部**历史完整状态：1000 条 = 458 KB/轮（表 3），aria2 侧再多付 2.8 % 单核。§8-D 已否决朴素截断 | 若要做，**只能**用 `tell*(max(0,n-N), N)` 取尾部 + 用本地已积累快照回填更早条目（正确性已实测，表 3 ②）；或更省：**平时只拉 active + 前 K 条 waiting**，历史只在用户切到"已完成/失败"视图或 API 请求时按需拉。风险中等（涉及列表可见集合的一致性），建议排在 O-1 之后按实测数据再决定 |
| **O-3** | 中 | `MainWindow.xaml.cs`：`_timer` 仅在 `Closed` 停（`:199`），隐藏走 `AppWindow.Hide()`（`:221`） | 实测**隐藏后仍 0.57 % 单核**（表 2）：每 900 ms 仍执行 `RefreshAsync` → `MergeTasks` → `ApplyView` → `ApplySelectionVisuals(TaskList)` + `ApplySelectionVisuals(BtList)` → `UpdateBtSummary`。开机进托盘是默认路径 ⇒ 常驻浪费 | 订阅 `IsVisibleChanged`（或 `AppWindow.ComparingState`/隐藏处）停表，重新可见时 `Start()`；顺带把 `UpdateBtSummary`（`:632-638`，每轮 3 趟 LINQ + 分配）限定在 BT 页可见时执行。改动 <20 行，风险低 |
| **O-4** | 中低 | `ApplyView` 重建分支（`:619-627`） | O(n²)：n=1000 时 1.69 ms/轮、n=2000 时 5.23 ms/轮（表 4，**还不含**集合通知成本） | 用 `HashSet<参照>` 代替 `Contains`、按 gid 建索引代替 `IndexOf`；或改为"按旧→新一次遍历 + 批量 Reset"（成员变化轮只发生一次）。收益与规模成正比，改动局部 |
| **O-5** | 中 | `TaskStore`（`:109`） | 当前 `tasks.json` **650 条 = 259,681 B**（`WriteIndented = true`；紧凑形态 198,407 B，**−23.6 %**）。任何变更都整文件重写（美化输出更慢更大）；`_metas` 只靠显式 `RemoveMeta` 清理，aria2 侧淘汰的历史条目会永久留在本地库；`ScheduleSave`（`:82-91`）每次新建 `CancellationTokenSource` 且**从不释放**被取代的那个 | ① 去掉 `WriteIndented`（或只对导出用美化）；② 与 `--max-download-result` 对齐做本地剪枝（保留最近 N 条 + 仍有墓碑/元数据价值的条目）；③ 防抖 CTS 用 `Interlocked.Exchange` + `Dispose`。注：空闲期不写盘这点已验证正常（表 2 ③） |
| **O-6** | 中低 | `BtPieceGrid.Refresh`/`ParseBits`（`:205-247`） | 每次 `BitField` 变化都 `new bool[numPieces]` 并整段解码，再 O(numPieces) 聚到最多 100/400 个方块。分片数大的种子（如 8 GiB / 256 KiB ≈ 3.3 万分片）≈ **33 KB/行/轮**，再乘可见行数 | 已确认不会无谓触发（`TaskItemViewModel.BitField` 用 `Set` 短路，`:38`），所以只影响**正在下载的 BT 任务**——正是用户看到的那几行。建议：①缓存上次 hex 字符串（同一 hex 直接跳过）；②边解 hex 边累加每块计数，**不要物化 `bool[]`**。改动仅一个文件 |
| **O-7** | 中低 | `TombstoneStore.Mark/Unmark` → `Save()`（`:165`）；`NotifiedStore`（`:50`） | 每次标记/取消都整文件重写（最多 1000 条，美化输出），无防抖；`NotifiedStore` 用 `List<string>.Contains` 做 O(n) 查找且每条通知重写全文件（`notified.json` 已 15,866 B）；启动时 `PurgeSessionFile` 对 session 每个空白分隔 token 做一次"插值 + UTF8 编码 + SHA256 + hex + ToLower" | 墓碑/通知集合改 `HashSet<string>`；写盘加 500 ms 防抖（与 `TaskStore` 同款）；启动清理时先把 session 里的 token 收集到 `HashSet` 再比对，避免同一 token 反复哈希 |
| **O-8** | 中 | `DownloadEngine:385` `idle = stats.NumActive == 0 && stats.NumWaiting == 0` | aria2 把**做种**任务计入 active/waiting ⇒ 只要还在做种，`idle` 永假，轮询永远 800 ms 全量。做种可持续数天，期间**零下载**却按下载节奏轮询 | 识别"全部活动任务都是做种"（快照里 `IsSeeding` 全为真）时把间隔拉到 3–5 s（做种速度显示精度可降）。注意 `IdlePollIntervalMs = 2500` 目前只覆盖"完全空闲"这一种情况 |
| **O-9** | 低 | `DownloadEngine:255-279` | `_btHashIndex` 每轮新建一个 `ConcurrentDictionary` 并整体替换，**即使本轮没有任何 BT 任务**（当前 6 个桶 + 每个桶的 entry 分配） | 仅当 `raws` 中存在 `InfoHash` 非空的任务时重建；或复用普通 `Dictionary` + 轮询线程独占（轮询里读它的只有本线程） |
| **O-10** | 低 | `ApiService:310`（`GET /api/tasks`） | 该路由直接 `GetAllTasksAsync()`（`:748`，**再发一次**全量 multicall）+ 每任务 `ToTaskInfo` + 两次 `OrderBy` 排序 + 再映射成 DTO 列表；而 `FindActiveDuplicatesAsync` 已经在用 `TryGetRecentSnapshot(1200)`（`:503`）。`/api/stats` 同理（`:313` → 独立 RPC） | 两个读路由改走 `TryGetRecentSnapshot(...)`，取不到再回落 RPC（一行级）。当前扩展只会周期性打 `/api/stats`（`background.js:93`），所以这是"按需省"而不是"正在浪费" |
| **O-11** | 低 | 杂项 | `App.Log` 每行一次 `new FileInfo` stat + `File.AppendAllText`（open/write/close）；`TaskChanged` 每个变化任务一条日志（实测批量失败时一秒十几行）；`TaskItemViewModel.FormatSize` 每次分配 `units` 数组（每任务每轮约 4 次调用）；`StatusDotBrush`/`StatusGlowBrush` 是每次求值新建画刷（仅在 `State` 变化与容器重新实例化时求值，影响小）；`NextTaskNumber` 用 `DateTime.Now.ToString` + `long.Parse` 往返；`TryGetRecentSnapshot`/`_latestSnapshotTicks` 用 `DateTime.Now.Ticks`（**非单调时钟**，系统改时间/时区跳转会让快照判定瞬时失效） | 都是"顺手改"级别：日志改 `StreamWriter` 常驻 + 批量 flush 或至少缓存 `FileInfo`；`units` 提为 `static readonly`；`NextTaskNumber` 直接算 long；时间戳改用 `Environment.TickCount64` 或 `Stopwatch.GetTimestamp()`。按 1,000 任务估算，格式化垃圾约 100 KB/轮（相对 4 MB/轮 的解析占比较低，故列低优先） |
| **O-12** | 信息 | `StandaloneBootstrap`（309 行） | 每次启动做清单校验；第五轮已加逐文件哈希后，**跳过路径**实测 ping 就绪 1.08 s（§11-D），代价主要在首次/损坏时的全量 SHA256 | **不建议再动**：引导自愈价值已由 §9-B/§11-D 的复现验证支持；此处仅登记，避免下轮误把它当"新发现" |

### 12-D 改进路线图（若下一轮要动手，按这个顺序）

**P0 — 收益明确、改动小、风险低**
1. **O-1** RPC 解析改 `JsonDocument`（流式，不要 `ReadAsStringAsync`）：分配 −64 %，去掉 LOH 字符串。表 1 已给出收益，且 B/C 同分配 ⇒ 选简单的 B。
2. **O-3** 主窗隐藏时停 900 ms 定时器 + `UpdateBtSummary` 按 BT 页可见性门控：实测可见 0.91 % → 隐藏应降到接近 0（当前 0.57 %）。
3. **O-10** `/api/tasks`、`/api/stats` 走快照：两行改动，把外部读取的 RPC 降到 0。

**P1 — 需要一点设计，收益随规模增长**
4. **O-2** 历史窗口化：**必须**用 `tell*(max(0,n-N),N)` 取尾部 + 本地快照回填（表 3 ②已实测等价性）；或"平时不拉 stopped，按需再拉"。这条会动到核心轮询循环，建议在 O-1 之后重测数据再决定是否值得。
5. **O-8** 做种期退避（3–5 s）：对"长期做种"用户是最直接的一刀，改动只在 `idle` 判定附近。
6. **O-5** `TaskStore`：紧凑写入 + 与 aria2 淘汰策略对齐的剪枝 + 防抖 CTS 释放。当前 260 KB / 650 条，一年不剪就奔着 MB 级去。
7. **O-6** `BtPieceGrid` 去掉 `bool[]` 物化（顺带缓存上次 hex）。

**P2 — 卫生类**
8. **O-4** `ApplyView` 用 `HashSet`/gid 索引消掉 O(n²)。
9. **O-7** 墓碑/通知存储 `HashSet` + 写盘防抖。
10. **O-9 / O-11** 杂项（`_btHashIndex` 条件重建、单调时钟、日志与格式化分配）。

**明确不建议做（本轮复核后仍维持）**
- ❌ `tellStopped(0, N)` 朴素截断——表 3 与 §8-D 两次实测都证明它取回的是**最旧** N 条，会造成"新任务不显示"的严重回归。
- ❌ 手写 `Utf8JsonReader` 解析——相对 `JsonDocument` **没有分配收益**（表 1 B/C 同为 1,445 KB），只增加游标出错面（本轮我自己就踩了一次，见 §12-A）。
- ❌ 为 `tasks.json` 引入数据库或分文件——260 KB 量级不值得，先把美化输出与剪枝做对。
- ❌ 调大/调小 aria2 的 `--max-download-result`——它本身会淘汰最旧条目，不存在无界增长（§8-D 结论）。
- ❌ 引导哈希/自检的"优化"——第五轮刚补强，成本已在 1 s 级，收益不成立。
- ❌ "每轮新建 1000 个任务对象"的零拷贝改造（对象复用/字段级 diff）——那是把 Core 侧数据结构改成可变状态机的量级改动，当前证据不支持。

### 12-E 本轮未覆盖 / 不确定（诚实清单）

- **表 1 只测了解析**，不含 `ToTaskInfo` 映射、`DownloadTaskInfo` 新建、LINQ 排序、UI 侧 `vm.Update` 的字符串格式化。也就是说 1,445 KB/轮是**下界**，真实每轮托管分配更高（估计 +10~30 %，未测）。
- **没有在在线应用上复现 1000 任务场景**（会污染用户真实任务列表与墓碑库），因此"应用侧每轮 CPU"用的是离线基准 + §3.6/§8-D 的历史基线（471 条 = 565 KB/轮、30–78 ms、空闲 2.81 %）交叉推断，两者在 30–38 ms 量级吻合。
- **探针 aria2 的 1000 条任务大多停在 `waiting`（840）而非 `stopped`（155）**，因为并发上限只有 5。这说明"全量轮询"的问题不只在 stopped，`tellWaiting(0,1000)` 同样会随排队规模线性膨胀；但窗口化对 waiting 的语义影响（用户重排队顺序）本轮**未**评估。
- **表 4 是算法复刻**，不含 `ObservableCollection` 通知、容器 realize/虚拟化、绑定更新——真实的重建轮只会更贵。
- **未做分配归因分析**（ETW / `dotNetRuntime` 事件或 `dotMemory` 快照），所有分配数字来自 `GetAllocatedBytesForCurrentThread`；它能给出可信的相对比例，但无法给出分代分布。
- **三分区采样只有 2.5 分钟**，足以看出"隐藏后仍在刷新"和"无泄漏迹象"，**不足以**断言长期无泄漏（第四轮 11 h 观察的结论仍以前者为准）。
- **`App.Log` 的突发成本未量化**：空闲态实测 0 行/40 s，因此只在批量添加/aria2 报错风暴时才值得测。
- **表 2 的 P2 分区里 aria2 CPU 反而略升（0.05 → 0.10 %）**，样本太小（60 s、62 ms 累计），不足以支持任何结论。
- **附带观察（未定性，建议后续核实）**：为了把主窗恢复成可见，我先后用了两次"再启动一次 exe → 单实例转发 `Local\KokonaDownloader_ShowWindow`"这条唤醒路径（`App.xaml.cs:76` + `MainWindow.xaml.cs:188`）。**第一次没生效**（事后 `EnumWindows` 复查主窗仍 `hidden`），**第二次生效**（`VISIBLE 0x2D0548 '海兔下载器'`）；两次全程都只有 1 个进程，说明单实例互斥与转发本身没问题，可疑的是 `App.ShowMainWindow` 里 `AppWindow.Show() → Restore() → Activate() → ForceForeground()` 在"取消关闭后隐藏"这一状态下的时序。本轮**未**据此下结论（只发生一次、无日志佐证），但建议下轮在 `ShowMainWindow` 里补一条日志、并把 `Restore()` 提到 `Show()` 之前再观察。
- **收尾状态**：审计结束后已把主窗恢复为可见（当前 `VISIBLE`），探针 aria2（16999）与一次性 aria2 目录（`.verify\bench\probe_dl\`）随脚本 `finally` 一并销毁，未留下监听端口；本轮读出的 RPC 密钥缓存文件已删除（脚本改为每次从 `aria2c.exe` 命令行现取）。

### 12-F 本轮新增度量资产（全部在 `.verify\` 下，不参与构建/发布）

- `.verify\bench\dump_payload.mjs` — 发送与应用完全相同的周期 multicall，报告字节数/耗时并可落盘为基准负载。
- `.verify\bench\seed_failures.mjs` — 向指定 aria2 批量灌入"秒失败"任务以构造长历史（零真实流量）。
- `.verify\bench\poll_loop.mjs` — 固定节奏轮询，便于在轮询进行时采样 aria2 CPU。
- `.verify\bench\probe_windows.mjs` — 历史窗口策略对比（现方案 / 带偏移 tail / 朴素截断 / 仅 active）。
- `.verify\bench\check_tail_order.mjs` — 在在线引擎上逐 gid 证明 `tellStopped(n-N,N)` == 最新 N 条。
- `.verify\bench\probe_history.ps1` — 拉起/销毁 16999 端口的一次性探针 aria2，串起以上步骤并采样 aria2 CPU/WS。
- `.verify\bench\parsebench\`（`parsebench.csproj` + `Program.cs`）— 解析三路径基准（`COUNT-OK` 自检）与 `ui` 模式的 `ApplyView` 算法基准。
- `.verify\bench\payload_now.json` / `payload_probe_1000.json` / `payload_1000.json` — 真实负载样本（4,271 B / 458 KB / 587 KB）。
- `.verify\audit\idle_round6.ps1` + `idle_round6.csv` — 可见/隐藏/再可见三分区的进程级采样。
- 备注：脚本需要 aria2 的 RPC 密钥，仓库里**没有**该字段，一律从运行中 `aria2c.exe` 的命令行现取（`Get-CimInstance Win32_Process -Filter "Name='aria2c.exe'"` + 正则）。审计过程中曾把它短暂落盘为 `.verify\bench\.rpcsecret`，**收尾时已删除**；`.verify\` 本身已 gitignore，不会进仓库。

---

## 13. 第七轮：按 §12-D 落地 P0 三项（2026-09-21，改码 + 复测 + 实测）

> 用户指令："按 P0 三项开始改"。范围＝O-1（RPC 解析改流式 `JsonDocument`）＋ O-10（`GET /api/tasks`、`GET /api/stats` 复用轮询快照）＋ O-3（主窗隐藏时停界面刷新定时器）。
> 沿用第五轮"先不动远程代码"的决定：**未提交 git、未打 tag、未替换 GitHub Release 资产，版本号仍为 1.0.8**。
> 改动落在 5 个源文件（+313 / −61 行）与 2 个新测试文件；除 §13-C 记录的两处外无其他行为变化。

### 13-A 改了什么

| 编号 | 文件 | 改法 | 为保持语义而做的约束 |
|------|------|------|----------------------|
| **O-1** | `Core/Engine/Aria2RpcClient.cs` | 新增 `PostForDocumentAsync`：`ReadAsStringAsync` → `ReadAsStreamAsync` + `JsonDocument.ParseAsync(stream)`，**整份响应只解析一次**；删除 `ParseResponse(string)`，代之以 `ExtractResult` / `ThrowIfError`。新增 `MultiCallBufferedAsync` → `Aria2MultiCall : IDisposable`（`Count` / `HasResult` / `IsFault` / `DeserializeList<T>` / `Deserialize<T>`，自动展开 multicall 的 `[value]` 包装，fault 子调用给空列表），轮询用 `using var call` 在同一作用域内把数据取完。`JsonOpts` 改 `internal static readonly` 供两条路径共用 | 旧的 `MultiCallAsync`（`JsonNode` 版）**保留**给添加/暂停/删除这类小回执，与新路径共用 `BuildMultiCallPayload`；`error` 报文仍抛 `Aria2RpcException` 且 code/message 原样透传（缺省 `-1` / `未知错误`）；HTTP 非 2xx 分支才把正文读成字符串（错误体很小，正常响应可能上百 KB）；`finally { resp.Dispose(); }` 顺手补上第五轮 CA2000 在这条路径的残留（`HttpResponseMessage` 此前从不释放）。**有意接受的差异**：顶层 JSON 字面量为 `null` 时现在抛 `JsonException`（原先是 `Aria2RpcException(-32700, …)`），aria2 正常报文不会走到 |
| **O-10** | `Core/Engine/DownloadEngine.cs` | 新增 `volatile GlobalStat? _latestStats`，与任务列表**同一轮、同一时间戳**发布；`GetAllTasksAsync` → `TryGetRecentSnapshot(ReadSnapshotMaxAgeMs) ?? OrderTasks(GetAllTasksLiveAsync())`；`GetGlobalStatAsync` → `TryGetRecentStats(...) ?? RPC` | 排序抽成 `OrderTasks` 供快照/实时两条路径**共用**，保证列表顺序与旧版逐字段一致；判重预检 `FindActiveDuplicatesAsync` **坚持实时读**（`TryGetRecentSnapshot(1200) ?? GetAllTasksLiveAsync()`），不能让 1.2 s 之外的更宽快照二次命中；`Pause/Resume/PauseAll/ResumeAll/SetGlobalSpeedLimit/SetTaskSpeedLimit` 之后一律 `InvalidateSnapshot()`，避免"扩展发完指令立刻回查"读到指令前的状态 |
| **O-3** | `App/MainWindow.xaml.cs`、`App/App.xaml.cs` | `_timerRunning` + `StopTimerForHidden()`（幂等，写日志）/ `ResumeRefreshTimer()`（幂等，写日志）/ `NotifyHidden()`。停表三条路：点 X 收托盘（`OnWindowClosing`）、开机 `--minimized` 静默启动（`App`）、`RefreshAsync` 头部兜底（覆盖"外部直接 Hide"这类没通知到我们的路径）。恢复两条：`Activated` 事件、`App.ShowMainWindow` 显式调用（双保险）。顺带把 `ApplySelectionVisuals(TaskList/BtList)` 限定为"各自容器可见才刷"，`UpdateBtSummary()`（三趟 LINQ + 拼串）只在 BT 页可见时执行 | 安全性核对过：`OnFilterChanged`（`MainWindow.xaml.cs:719`）先翻 `BtPage.Visibility` 再调 `ApplyView()`（`:720`），所以切到 BT 页时概要条必然刷到新数据；引擎侧轮询与托盘图标不受影响（那是另一条线） |
| 附带 | `App/WindowEffects.cs` | 新增 `IsWindowVisible` P/Invoke 与 `WindowVisibleByAnySignal(Window)`：`AppWindow.IsVisible` 或 Win32 实际可见，**任一为真即算可见**（原因见 13-C-1） | 停表判据改用它，避免"屏幕上有窗口却再也不刷新"的僵局 |

### 13-B 中途发现：快照窗口比空闲轮询间隔还窄，导致"复用"整件事根本没生效

本轮最有价值的一条，因为**代码写完是自洽的、测试也全绿，但实测收益是 0**：

- `ReadSnapshotMaxAgeMs` 第一版写死 2000 ms，而轮询在空闲态会退避到 `IdlePollIntervalMs = 2500` ms。于是静默态下每次读都判"快照太旧"→ 退回实时 RPC，O-10 等于没做。
- 静默态 200 次 `GET /api/tasks` 的对照（同机、同一负载 588,200 B）：改前构建 aria2 CPU **62 ms**，改后（窗口仍 2000）aria2 CPU **78 ms** —— 一点没省，反而略高。
- 改为 `ReadSnapshotMaxAgeMs = IdlePollIntervalMs + 500`（= 3000 ms，注释里写清"必须严格大于空闲轮询间隔"这个不变量），并新增回归测试 **`读接口的快照新鲜度窗口必须宽过空闲轮询退避`**（反射读两个私有常量，断言 `window > idlePoll` 且 `window <= idlePoll + 1500`），把这个跨字段的隐式不等式钉死。
- 复测后 aria2 CPU 归零（见 13-E 表 1），证明复用真的生效了。
- 教训（写给自己）：**"缓存复用"类改动必须用"被缓存方是否停止工作"来验收**，只看调用方延迟会被本地回环与客户端开销掩盖（node 侧 `fetch` 往返本身就要 ~17 ms）。

### 13-C 停表改动暴露的两个问题（都已处理 / 记录）

1. **`AppWindow.IsVisible` 与真实窗口可见性会不一致（实测复现）**。外部（本轮是脚本）直接对 HWND 调 `ShowWindow(SW_RESTORE)` 时，Win32 认为窗口可见，而 WinUI 的 `AppWindow.IsVisible` 仍是 `false`（它只认自己的 `Hide()/Show()`）。第一版兜底停表只看这个标记，结果是**窗口明明在屏幕上、界面却再也不刷新**（`app.log`：`16:15:05 恢复刷新定时器` → `16:15:22 又停表`）。修法＝`WindowEffects.WindowVisibleByAnySignal`。验收（`externalshow_round7.ps1`，最终构建）：隐藏后用裸 Win32 复原 → 12 s 内**没有**再出现停表日志，且 `Activated` 触发了恢复（日志计数 stop 9→10、start 9→10，之后无新增 stop）。
2. **单实例唤醒路径本身不可靠（本轮 3 次连续失败，比第六轮更严重）**。`App.ShowMainWindow` 的 `AppWindow.Show() → Presenter.Restore() → Activate() → ForceForeground()` 在"点 X 收进托盘"之后有时**就是不显示窗口**；本轮同一状态下连试 3 次全部失败，只能靠 Win32 `ShowWindow` 救回（`app.log` 无相关日志，无法进一步定位）。第六轮 §12-E 只记到"1 败 1 成"，本轮 3 连败说明这不是噪声。本轮**未改**这条路径（不在 P0 范围），已写进《项目文档.md》§10.2 未修复清单。
   与 O-3 相乘的观感是"用户以为程序丢了"，但停表本身不会冻住界面：窗口不显示时没人看界面，窗口一旦显示，`Activated` 就会恢复刷新——实测那 3 次唤醒失败后靠 Win32 复原，日志随即出现"恢复界面刷新定时器"，CPU 采样随后也出现 900 ms 心跳。

### 13-D 回归护栏与全套复测

**新增 11 条测试（2 个文件）**

- `src/KokonaDownloader.Core.Tests/RpcParsePathTests.cs`（7 条，自建 `FakeRpc` HttpListener，不依赖 aria2）：文档态 multicall 解析出任务列表与统计；**文档态与旧 `JsonNode` 版结果一致**（钉住两条路径等价，便于将来安全删除旧路径）；子调用返回 fault 结构按空列表处理；error 报文两条路径都抛 `Aria2RpcException`；HTTP 非 200 时异常带上状态码与响应正文；响应不是合法 JSON 时抛 `JsonException`；1000 任务大负载解析正确。
- `src/KokonaDownloader.Core.Tests/SnapshotReadTests.cs`（5 条，真引擎 + 真 aria2）：列表读接口复用快照而不额外发起 RPC（先 `KillNow()` 冻结发布时间戳，再连续读，aria2 侧请求数不变）；**新鲜度窗口必须宽过空闲轮询退避**（见 13-B）；统计读接口复用同一轮发布的统计；添加任务后快照立即作废且读接口能看到新任务（`SnapshotInvalidationTests`）；暂停指令后读接口不会回查到旧状态（用 2 MB 慢速源保证此刻任务还没结束）。
- `.verify\audit\run_tests_serial.ps1` 的分类清单与"纯单元"排除过滤同步更新（`RpcParsePathTests` 属不依赖 aria2 的一类，落在"纯单元"那次运行里）。

**全套复测（本轮结束时重跑，全部通过）**

| 套件 | 结果 | 说明 |
|------|------|------|
| Core 串行分类（12 次运行） | **161 / 161，0 失败** | 与 `dotnet test --list-tests` 列出的 **161 条用例**（20 个测试类）逐类对账完全一致 ⇒ 无遗漏 |
| `cscript //nologo tests\test_logic.js` | **77 / 77** | 扩展侧逻辑（含 503/400/404 映射、`err.code` 恒为字符串） |
| `.verify\fulltest\run.mjs` | **52 / 52** | 真应用 + 真下载（A–H 全部分区，含 BT/磁力/限速/设置 API） |
| `.verify\functest\test.mjs` | **24 / 24** | 断点续传 range、暂停/继续/删除、非法请求 |
| `.verify\audit\verify_fixes.mjs` | **10 / 10（+1 SKIP）** | 第五轮 CORS/设置补丁护栏；SKIP 是密钥轮换校验（未提供 `OLD_SECRET`） |
| `.verify\fulltest\ui_inspect.ps1` | **ISSUES: 0** | 7 个窗口：主窗 79 控件、5 个进度小窗各 20、磁力确认窗 18 |

### 13-E 运行时实测（最终构建）

**表 1：读接口 A/B（静默态，同一负载，同机先后各测一次）**

同一台机、同一负载、先后各跑一次；两边都等到引擎静默（`numActive == 0 && numWaiting == 0`）才开始，任务集合完全相同（200 次读合计响应 **672,200 B**，两边逐字节同长度）。脚本：`ab_round7.ps1`（只换 exe，不重新发布）。

| 指标 | round-5 构建（旧读路径） | round-7 构建（快照 + `JsonDocument`） | 变化 |
|------|--------------------------|----------------------------------------|------|
| 200 次 `GET /api/tasks` 总耗时 | 344 ms | **148 ms** | **−57 %** |
| 平均延迟 | 1.33 ms | **0.53 ms** | **−60 %** |
| 中位 / p90 / 最大 | 1.02 / 2.02 / 22.19 ms | **0 / 1.01 / 14.03 ms** | 中位归零 |
| 50 次 `GET /api/stats` 总耗时 | 32.2 ms（0.64 ms/次） | **12.1 ms（0.24 ms/次）** | **−62 %** |
| 该窗口内 **应用**进程 CPU | 500 ms | **234 ms** | **−53 %** |
| 该窗口内 **aria2** 进程 CPU | 125 ms | **0 ms** | **−100 %**（读路径不再产生任何 RPC） |

- **aria2 CPU 归零**是 O-10 生效的唯一硬判据：调用方延迟会被本地回环往返掩盖（node `fetch` 自身约 17 ms），而 aria2 侧只要收到一个请求就不可能是 0。
- 应用侧仍剩下的 234 ms，主要是应用自己的周期轮询（每 2.5 s 一轮）+ HTTP 管线，与这 200 次读**无关**；作为对照：同一构建在 hammer 前多跑一轮的结果是 250 ms / aria2 0 ms（`redeploy_new_round7`）。
- 负载随时间变大不影响结论：两边读到的任务集合与响应长度完全相同。
- 与 13-B 的旧数据合起来看（同机不同时刻，仅作趋势参考）：窗口 2000 ms（复用未生效）时 aria2 CPU 78 ms / 应用 CPU 312 ms；窗口 3000 ms（生效）后 0 ms / 234 ms。

**表 2：三分区空闲 CPU（`idle_round7.ps1`，与六轮 `idle_round6.ps1` 同结构）**

| 分区 | 六轮（改前） | 七轮（改后，最终构建） |
|------|--------------|------------------------|
| P1 主窗可见 60 s | 547 ms / **0.91 %** | 812 ms / **1.35 %** |
| P2 收进托盘 60 s | 344 ms / **0.57 %** | 78 ms / **0.13 %**（**−77 %**，本项主收益） |
| P3 重新可见 30 s | 266 ms / 0.88 % | 1453 ms / **4.84 %**（一次性追赶，见下） |

- P1 两次差 0.44 个百分点属噪声量级（各只测一次、时刻不同），**不作为结论**。
- P3 的 4.84 % 是"隐藏期间停表 → 恢复显示后一次追赶 + 窗口重新合成/重绘"的**一次性**代价，不是新的常驻成本：在同一构建上用 500 ms 切片复测（`wakecost_round7.ps1`），唤醒后 0–10 s 平均 0.94 %，随后 10 s 降到 0.47 %，与可见稳态（1.72–1.88 %）同量级甚至更低。
- 换算：托盘常驻每小时约省下 **16 s** 单核（0.44 个百分点 × 3600 s），代价是每次唤起多花几十到几百毫秒。

**表 3：隐藏态到底停没停（`wakecost_round7.ps1`，每状态 10 s × 20 个 500 ms 切片，进程 CPU 增量）**

| 状态 | 总 CPU | 平均 | 备注 |
|------|--------|------|------|
| 可见稳态 | 172 ms / 10 s | 1.72 % | 心跳来自 900 ms 定时器 |
| 隐藏稳态（第 1 次） | **0 ms / 10 s** | **0.00 %** | 20 个切片全为 0 |
| 隐藏稳态（第 2 次） | 172 ms / 10 s | 1.72 % | **19 个切片为 0，单点 156 ms**；本轮未定位该单点（候选：托盘进度小窗重绘、引擎事件回调进 UI 线程）。60 s 分区平均仍是 0.13 % |
| 唤醒后 0–10 s | 94 ms | 0.94 % | 追赶 |
| 唤醒后 10–20 s | 47 ms | 0.47 % | 已回落到隐藏量级 |

- **采样粒度提醒**：`Process.TotalProcessorTime` 在本机约 **15.6 ms 一格**，500 ms 细粒度切片只能看量级（出现值恒为 16 / 31 / 47 / 94 / 156 ms 这类台阶）。要下结论请看 30–60 s 分区（表 2）。
- 停表/恢复的**功能性证据**（`app.log`）：`16:24:23` 同秒 stop+start（唤醒路径成功）、`16:25:38` stop → `16:25:55` start，之后 40 s 可见期间**没有**新的 stop；外部 `ShowWindow` 复原那一轮（13-C-1）同样只留下 stop→start 一对，无二次停表。

**正确性侧写（O-10 的语义保持）**

- `snapshot_vs_live_round7.mjs`（最终构建，收尾时复跑）：同一任务集合下，快照路径与实时路径（临时把快照作废逼出实时读）返回**逐字节一致**的响应（3,361 B / 8 条）⇒ 排序与字段无差异。
- `api_shape_round7.mjs`（最终构建）：连续 60 次 `GET /api/tasks` 全部 `200 / 3,361 B / [{`，解析后都是 `array(8)`（顺带纠正一个 PS 5.1 假象：`@(Invoke-RestMethod …).Count` 写成内联表达式会得到 1，脚本里必须先赋值再取 `.Count`——之前那条"引擎里只有 1 个任务"就是这么来的）。
- 收尾状态：桌面 exe SHA256 `46D772E6…DFF2`、`/api/ping` 仍报 `1.0.8`、未带密钥的 `GET /api/tasks` 回 **401**（仅 `/api/ping` 免鉴权）。

### 13-F 本轮新增资产（全部在 `.verify\` 下，不参与构建/发布）

- `.verify\audit\api_hammer_round7.ps1` — 顺序打 N 次 `GET /api/tasks` + 50 次 `/api/stats`，输出延迟分位数与该窗口内 app/aria2 的 CPU 增量（**aria2 CPU 是否为 0 是快照复用的判据**）。
- `.verify\audit\ab_round7.ps1` — 只换 exe（不重新发布）：round-5 备份 ↔ round-7 新构建，各自等到静默态后跑同一把 hammer。
- `.verify\audit\redeploy_new_round7.ps1` — 发布 + 部署到桌面目录 + 静默态等待 + hammer。
- `.verify\audit\idle_round7.ps1` + `idle_round7.csv` — 可见/隐藏/再可见三分区采样（含 `EnumWindows` 可见性快照）。
- `.verify\audit\wakecost_round7.ps1` — 钉住长生命周期 PID，用 `EnumWindows`+`IsWindowVisible` 校验每次转换后的真实可见性，再按 500 ms 切片采样。
- `.verify\audit\externalshow_round7.ps1` — 13-C-1 的验收脚本（隐藏 → 裸 Win32 复原 → 确认界面继续刷新）。
- `.verify\audit\api_shape_round7.mjs` / `snapshot_vs_live_round7.mjs` / `logcheck_round7.mjs` — 响应形状、快照 vs 实时负载、日志标记计数。
- 注：本轮所有 `*_round7.ps1` 都做了**非 ASCII 字节扫描**（0 个非 ASCII 字节）。原因：PowerShell 5.1 以 CP936 读取 `-File` 脚本，脚本里的中文会让解析器"吃掉"紧跟的引号，报 `The string is missing the terminator` —— 本轮真的踩了一次。

### 13-G 有意未做 / 遗留

- **未动的开销项**：O-2（历史尾部窗口，必须带 offset）、O-4（`ApplyView` 的 O(n²) 重建）、O-5（`TaskStore` 美化写盘 / 不剪枝 / 防抖 CTS 不释放）、O-6（`BtPieceGrid` 的 `bool[]`）、O-7（墓碑/通知整文件重写）、O-8（做种期不退避）、O-9（`_btHashIndex` 无条件重建）、O-11、O-12。§12-D 的"明确不建议做"清单继续有效。
- **唤醒路径缺陷**（13-C-2）已进《项目文档.md》§10.2；本轮只报告未修。
- **版本与发布**：版本号仍 1.0.8；未提交、未打 tag、未替换 GitHub Release 资产（受"先不动远程代码"约束，需用户另行指示）。
- **可回退**：旧 exe 备份 `.verify\old_exe_backup\KokonaDownloader_round5_deployed.exe`（318,204,760 B，SHA256 `8A5D043A1ABE98358106AF210CF5AA11CA7E724D780E82203A3D9157D0DFA98D`）；新构建 `dist\KokonaSingleFile\KokonaDownloader.exe`（318,208,856 B，**+4,096 B**，SHA256 `46D772E6DD8789FF41E489E15B84E092B6DEA30C9A867E39F671CCE28E2CDFF2`，已部署到 `%LOCALAPPDATA%\KokonaDownloader\Standalone\` 并正在运行）。
- **遗留人工目视项不变**：托盘菜单宿主路径的运行时表现、`海兔下载器教程.pdf` 是否仍显示旧 API key、`files/`（78 MB）未删、`tools\*.ps1` 与 `src\KokonaDownloader.UiTests\` 已过期（按约定只报告不改）。

---

## 14. 第八轮：最终打包件的"真实场景"全链路回归（2026-09-24）

- **被测对象**：`dist\KokonaSingleFile\KokonaDownloader.exe`，318,208,856 B，SHA256 `46D772E6DD8789FF41E489E15B84E092B6DEA30C9A867E39F671CCE28E2CDFF2`，`/api/ping` 报 1.0.8（即 §13 第七轮 P0 三项改动之后的**最终打包件**，不是源码调试态）。
- **配套组件**：`dist\KokonaExtension\`（未打包 MV3 扩展，与应用同版本）＋ 应用内嵌解压出的 `aria2c.exe`（1.37.0，UA `Transmission/2.92`）。
- **本轮唯一目标**：把"点击链接 → 扩展拦截 → 调用本地 API → aria2 下载 → 落盘 → UI 列表"整条链路按**真实用户操作**跑通，而不是只打 API；重点回答"历史各轮遇到过的那些问题还会不会复现"。
- **约束**：按用户指示，本轮**不做 GitHub 同步**（不提交、不打 tag、不替换 Release 资产），也不产出截图（改为程序化取证）。

### 14-A 测试站与取证手段

自建测试站 `.verify\e2e\site\server.mjs`（Web `127.0.0.1:18099` ＋ 内置 BT tracker `127.0.0.1:26969`，0 个非 ASCII 字节），页面与端点：

| 页面 / 路由 | 覆盖的真实场景 |
|------------|--------------|
| `/pages/basic` | 普通直链、CD 改名、重定向链（`/fake/23_377276` → 302 → 301 → 真实 zip）、查询串取名（`/queryname?file=download.bin`）、UTF-8 名（`filename=`+`filename*`）、仅 `filename*`、非法字符/控制字符/`../` 路径穿越/`C:\` 绝对路径/空格括号/无扩展名/双扩展名/208 字符长名、百分号空格名 |
| `/pages/failures` | 404/403/500/502/416、Content-Length 撒谎（截断）、"前两次 404 第三次 200"的抖动端点、只在带本站 Referer 时才 200 的资源 |
| `/pages/resume` | 10 MiB 支持 Range、8 MiB **不支持** `Accept-Ranges`（慢速滴流）、24 MiB 滴流（限速靶）、50 MiB 大文件（多连接）、Chunked 无 `Content-Length` |
| `/pages/magnet` | 合法 magnet（`dn`+`tr`）、无 `dn` 的 magnet、全零 infohash 的假 magnet、`.torrent` 直链、`target=_blank`、div 内嵌 magnet |
| `/pages/center` `/pages/bulk` | 5 连点批量、8 连点批量（验证并发上限与去重） |
| `/pages/js` | JS 触发：`location.href`、`window.open`、隐藏 `a[download].click()`、`setTimeout` 后跳转 |
| `/pages/compat` | **不应该**被接管的：`blob:`、`data:`、普通导航锚点、表单 POST、指向客户端自身 API 的链接 |
| `/status/<code>` `/retry/<token>` `/retry-reset/<token>` `/cd/<case>` `/conflict/<name>` `/form/echo` `/bulk/itemN.bin` | 错误码、抖动重试、13 种 Content-Disposition 形态、同名冲突、表单回显、批量小文件 |
| `site\manifest.json` | 16 个文件的 size+sha256＋torrent `{infohash 49c7c3b2700c698ff2f00e85b077a84fe1ee97ad, 20 MiB}`，全部断言以它为唯一真值 |
| `site\requests.log` | 每个请求一行 JSON（method/url/status/range/referer/ua/ts），可用 UA 区分 **浏览器** 与 **aria2（Transmission/2.92）** |

取证手段（每一条都能交叉验证，不依赖"看起来对"）：

1. **真实浏览器**：`msedge.exe`（v153）＋ `--load-extension=dist\KokonaExtension`，用 CDP `Input.dispatchMouseEvent` 在真实坐标上点击真实 `<a>`，不是 `fetch()` 模拟。
2. **扩展侧真值**：经 `chrome.downloads.search` 读下载项状态（`complete`/`interrupted`）、`chrome.storage.local.kokona_settings` 读扩展实际生效的设置。
3. **服务端真值**：`requests.log` 区分"浏览器请求"与"引擎请求"，据此证明"扩展拦截成功后浏览器确实放弃"以及"引擎确实用了 Range"。
4. **引擎侧真值**：应用日志里的 `[aria2]` 原始输出（含 aria2 的 errorCode 与 `Invalid range header` 这类细节）。
5. **落盘真值**：`size` + `sha256` + 是否残留 `.aria2` ＋ 是否误落到浏览器默认 `Downloads`。
6. **UI 真值**：UIA（`winx.ps1`，全 ASCII）枚举窗口/控件、按名字点击工具栏、导出控件树文本，用于托盘隐藏/唤醒/刷新计时器验证。
7. **反向取证**：`%APPDATA%\KokonaDownloader\app.log`（`API 新建任务` / `TaskChanged` / `轮询 active=` 行）用于定位"任务到底有没有被创建""多久之后才恢复"。

### 14-B 阶段与结果总表

所有阶段都遵循同一套前置：把 `defaultDownloadDir` 改到 `%TEMP%\kokona_e2e\<runTag>\<stage>` 并**重启客户端**（因为见缺陷 1，运行中改目录不生效）、`globalSpeedLimit=0`、Edge 用独立 profile（不碰用户自己的 msedge）。

| 阶段 | 内容 | 断言 | 结果 |
|------|------|-----:|------|
| **0 setup** | 环境与前置：设置重定向、profile 干净、扩展握手、popup 连接、`/api/ping`、401 鉴权 | 17 | **16 PASS**（E10 为测试脚本自身缺一次 Enter，已修） |
| **A basic** | 直链 1 MiB/10 MiB、CD 改名、同名冲突、5 连点、8 连点并发、负向取证 | 44 | **29 PASS**（15 项失败=缺陷 1＋缺陷 2，见下） |
| **B names** | 15 种文件名形态（重定向/查询串/UTF-8/穿越/绝对路径/非法字符/控制字符/无扩展名/双扩展名/长名/空格） | 150 | **146 PASS**（2 项为测试脚本期望值过时，2 项=缺陷 3） |
| **DIR** | 运行中修改下载目录（缺陷 1 的定向复现） | 12 | **7 PASS**（DIR3/4/7/9 复现缺陷 1；DIR6 为测试脚本自身时序 bug） |
| **C failures** | 404/403/500/502/416、截断、抖动 404 恢复、Referer 转发、失败任务重下 | 36 | **34 PASS**（2 项失败=缺陷 6；4 项为说明书式 SKIP） |
| **G/H compat** | blob:/data:/导航/表单/自身 API、自动接管开关、popup 手动发送、右键菜单 | 42 | **42 PASS（全绿）** |
| **D resume** | 暂停/续传（Range 与无 Range）、Chunked、限速、双击去重、工具栏全部暂停/开始、O-3/O-10 | 46 | **43 PASS**（3 项失败全部是缺陷 4；2 项 SKIP 为墓碑行为说明） |
| **F magnet/BT** | 本地做种机＋tracker 的真实 BT：合法/无 dn/假 infohash magnet、`.torrent` 直链、取消确认 | 16 | **15 PASS**（1 项 SKIP：同一 infohash 已在任务列表时，`.torrent` 点击被"重复种子"守卫合并——设计如此） |
| **I/J hidden** | 托盘隐藏、隐藏时引擎继续、隐藏时 CPU 停表（O-3）、再次启动唤回、唤醒后刷新恢复、重启持久化、UIA 树转储 | 19 | **18 PASS**（1 项失败=缺陷 8：重启后完成态任务从 API 列表消失） |
| **站点自检** | 测试站自身的 52 项断言（fixture sha256、Range/Chunked 语义、tracker 协议字节级解析等） | 52 | **52 PASS** |
| **既有回归（同机同 exe）** | `audit\verify_fixes.mjs` / `fulltest\run.mjs` / `functest\test.mjs` / `fulltest\ui_inspect.ps1` | — | 修复回归 **10/10**、E2E 功能 **24/24**、UI 检测 **ISSUES: 0**、run.mjs **52/52**（首跑 42/49 是本轮测试脚本把 `globalSpeedLimit` 留在 262144 未复位所致，复位后全绿——同一原因也让 D2 的 8 MiB 滴流任务变慢） |

<!-- STAGE_TABLE_END -->

### 14-C 缺陷清单（全部有日志/落盘证据，可复现）

> 严重度：P1＝会造成数据丢失或用户明确可见的错误；P2＝行为错误但可恢复；P3＝体验/一致性。

**缺陷 1（P1，数据丢失）运行中修改"默认下载目录"对已启动的引擎无效，且连带破坏同名保护与"打开文件夹"**

- 现象：把默认下载目录从 A 改到 B，新建任务仍写进 A。`DIR3 task dir=C:\Users\lumin\Downloads configured=…\dirx_145754`；`DIR4 new=False old=True`；重启客户端后才生效（`DIR10` PASS）。
- 根因（源码级）：`Aria2RpcClient.BuildOptions` 仅在 `req.Directory` 非空时才下发 `dir`；`ApiService` 自动接管路径传 `dir=null`，于是 aria2 用它**启动时**的 `--dir=<启动那一刻的 DefaultDownloadDir>`（`Aria2Process.cs:45`）。
- 连带后果一（数据丢失）：`ApplyUniqueFileName` 在新目录里查重名，而文件其实写进旧目录 ⇒ 不触发 `(1)` 改名；又因 aria2 带 `--allow-overwrite=true`，**重复下载同一链接会直接覆盖上一次的文件**。定向实验：payload 不含 `filename` → 下两次只有 1 个文件；显式给 `filename` → 得到 `e2e_known_name.bin` 与 `e2e_known_name (1).bin` 两个文件。`DIR7 before=['e2e_conflict_me.bin'] after=['e2e_conflict_me.bin']`。
- 连带后果二：UI 的"打开文件夹/删除文件"按**设置里的目录**解析，与实际落盘目录不一致时会指向错的地方。
- 阶段 A 的 15 项失败全部属于本条（`files=[]` 但浏览器 Downloads 里出现了同名文件）。

**缺陷 2（P1，数据丢失）自动接管的任务（payload 无 `filename`）不做重名保护，重复下载会覆盖旧文件**

- 现象：从浏览器点同一个链接两次，第二次直接覆盖第一次的文件，列表里也只有一个任务名（`A4 got=e2e_conflict_me.bin`，期望 `e2e_conflict_me (1).bin`）。
- 根因：扩展在 `downloads.onCreated` 时 `item.filename` 通常还是空的，于是 `buildDownloadPayload` 不带 `filename`；而 `ApplyUniqueFileName` 在无 `filename` 时**按设计直接返回**，把改名交给 aria2，aria2 又带着 `--allow-overwrite=true`。
- 说明：这正是"历史问题清单"里的老面孔，本轮用受控对照实验再次确认它与"文件名由谁决定"强相关。

**缺陷 3（P3，能力边界，可判为上游限制）仅带 `filename*=UTF-8''…` 的 Content-Disposition 被忽略**

- 现象：`/cd/utf8only`（只有 `filename*`）落盘名仍是 URL 末段 `utf8only`；`/cd/star`（`filename=`＋`filename*`）正确得到 `中文报告 2026.pdf`。
- 定性：**不经浏览器**、直接用 API 下发同一 URL 也复现，属于 aria2 的 CD 解析范围，不是应用或扩展的回归；报告在此记录，不改。

**缺陷 4（P2，误导性残留文件）对不支持 Range 的服务器执行"暂停→继续"，会留下一个"大小正确但内容损坏"的同名文件**

- 现象：`D2` 中 8 MiB 无 `Accept-Ranges` 资源被暂停后继续，任务最终 `failed`，但磁盘上 `e2e_norange_8mb.bin` 大小恰为 8388608（正确大小）而 sha256 不匹配，且残留 `.aria2`。
- 引擎侧证据（`app.log`）：`errorCode=8 Invalid range header. Request: 5439488-6291455/8388608, Response: 0-8388607/8388608` —— 暂停后 aria2 按 `--continue=true` 发 Range，服务端不支持 Range 于是回整个文件，aria2 判定非法并放弃。
- 影响：任务状态是"失败"（没有假装成功），但文件已经以**最终文件名**躺在下载目录里，用户很可能当成完整文件使用。
- 建议：任务失败时把残留文件改名为 `*.partial`（或删除并只保留 `.aria2`），这是廉价且能立刻消除误用的改动。

**缺陷 5（P3，一致性）服务器错误码链接的处理是"竞态"：有时客户端出现失败任务，有时什么也没有**

- 现象：点 `/status/404|403|500|502|416`，多次运行观察到两种结果——(a) 客户端完全无感知，浏览器下载列表里也没有条目；(b) 扩展先接管再让任务失败（`errorMessage=Resource not found`）。
- 原因：扩展只在下载项仍处于 `in_progress` 时才接管，而 Chromium 对 HTTP 错误响应几乎立刻把该项置为 `interrupted`，两者是赛跑。
- 影响：用户点了链接却"什么都没发生"（既不下载也没有任务），属体验问题，不会造成数据损坏。

**缺陷 6（P3，恢复延迟）引擎对 404 只重试 1 次，短暂抖动要等数分钟才自动恢复**

- 现象：`/retry/<token>`（前两次 404、第三次 200）。引擎只发 2 次请求（`17:38:55` 两次 404）→ 任务 `Failed`；随后**没有任何用户操作**，到 `17:49:30`（约 10.5 分钟后）同一批任务自行变成 `Completed`，文件名也正确解析为 `e2e_retry_ok.bin`（`app.log` 与 `requests.log` 双向可查）。
- 结论：客户端确实会自愈，但**自愈时延远超用户耐心**；阶段 C 里以"5 分钟内未恢复"判为 FAIL 是刻意保留的严格口径，此处按证据修正定性。

**缺陷 7（P2，重启后历史不一致）重启客户端后，已完成任务从 `/api/tasks` 消失，但 UI 里还看得到**

- 现象（`stage_hidden` I6）：重启前 `before=['e2e_big_50mb.bin']`，重启后 `/api/tasks` 里已完成任务为 0（`after=[]`）；同一时刻 UIA 导出的主窗文本里**仍能找到**该文件名。
- 与 `DIR9 before=25 after=8` 相互印证：重启后剩下的基本都是失败任务（来自 aria2 的 session 恢复），完成态历史不再通过 API 暴露。
- 影响：任何依赖 `/api/tasks` 的集成方（脚本、第三方面板）在重启后会"看不到历史"，而 UI 与 API 表现不一致本身也是维护陷阱。
- 说明：这需要先与产品预期对齐（"历史是否应该跨重启保留"），因此本轮只报告。

**缺陷 8（P3，历史遗留，仍未修）被托盘隐藏后用"再次启动"唤回窗口不可靠（本轮复测 1 次成功，属间歇性）**

- 详见 §13-C-2 与《项目文档.md》§10.2。本轮 `stage_hidden`：`WM_CLOSE` 正确隐藏（窗口不可见、进程存活）、隐藏期间引擎继续下载且最终 `sha256` 一致、隐藏时 CPU 停表（O-3）与唤醒后刷新恢复均 PASS；"再次启动唤回窗口"**一次尝试即成功**（与第七轮 3 连败不同 → 判定为间歇性），兜底手段裸 Win32 `ShowWindow(SW_RESTORE)` 依然有效。

### 14-D 交叉验证：历史问题清单的"是否还会发生"

| 历史问题 | 本轮结论 | 证据 |
|---------|---------|------|
| 重定向后的假文件名（`23_377276` 之类） | **不再复现**，取到真实 zip 名 | `B1` 通过；`app.log` 有"忽略 URL 末段伪文件名 …交由 aria2 按响应头/重定向解析" |
| 查询串被当成文件名 | **不再复现**（落到真实文件名） | `B2` 名称断言通过（仅测试脚本对尺寸的期望过时） |
| `../` 路径穿越、`C:\` 绝对路径、非法字符、控制字符 | **不再复现**，全部被规整在下载目录内 | `stage_names` 对应 8 项全 PASS，落盘路径均在配置目录内 |
| 截断传输被当成"完成" | **不会**：任务 `failed`、残留 `.aria2`、绝不出现 100% | `C6` |
| 浏览器里留下半截文件 | **不会**：拦截后浏览器项被取消/擦除，`Downloads` 无泄漏 | 各阶段 `browser_neutral` 断言 |
| 同链接并发去重 | **正常**：双击只产生 1 个任务 | `E2`（受阶段 D 重跑影响，见 14-B） |
| Range 断点续传 | **正常**：暂停/继续后 sha256 一致 | `D1`（含 `Range` 请求取证） |
| Chunked（无 `Content-Length`） | **正常** | `D5` |
| 全局限速 | **生效**（稳态贴住上限，见阶段 D 重跑） | `D4` |
| 托盘隐藏后引擎继续下载 | **正常**（隐藏期间进度持续增长，最终文件 sha256 一致） | `stage_hidden` I2/O3/I4 |
| 隐藏时 CPU 停表（O-3） | **正常**：隐藏 6 s 采样 ≤600 ms，唤醒后 UI 文本继续变化（刷新计时器恢复） | `winx cpu` + UIA 文本两次转储 |
| 读接口复用轮询快照（O-10） | **正常**：隐藏态连续 8 次读取完全一致，且读路径不再打 aria2 | `stage_hidden` / `stage_resume` |
| 设置页开关（自动接管） | **正常**：关闭时浏览器自己下载（真值取自 `chrome.storage.local` 与落盘）、开启后重新接管 | `stage_compat` G2/G6 与 `diag_toggle.py` |
| `blob:` / `data:` / 导航 / 表单 / 自身 API | **全部正确放行**，无接管、无自环 | `stage_compat` G1/G3/G4/G5（42/42） |
| magnet / BT 全链路 | **正常**：点 magnet → 扩展 toast → 客户端弹"磁力链接下载确认" → 点"开始下载" → 本地做种机 20 MiB 全量传完，落盘 **sha256 与 fixture 完全一致**、无 `.aria2` 残留；假 infohash 绝不假装成功；确认窗点"取消"不建任务 | `stage_magnet` 15/15（真值：本地 aria2c 做种机 + 内置 tracker `127.0.0.1:26969`） |
| 右键菜单"使用海兔下载器下载" | **正常**：UIA 命中菜单项并完成下载（sha256 一致） | `stage_compat` H2 |
| popup 手动发送 URL | **正常** | `stage_compat` H1 |

### 14-E 本轮新增资产（全部在 `.verify\e2e\` 下，不参与构建与发布）

- `site\server.mjs`（测试站＋tracker，含 `--emit-fixtures` 生成 20 MiB BT 载荷与 `.torrent`）、`site\manifest.json`、`site\selftest.mjs`（52 项站点自检）、`site\requests.log`（逐请求 JSON 日志）。
- `winx.ps1` — 纯 ASCII 的 Win32/UIA 助手：`list` / `hide` / `show` / `close-window` / `click-button` / `click-text` / `close-dialog` / `text-of` / `cpu`。
- `chain_lib.py` — 基础设施：`Result`、`App`（API 客户端 + 任务快照 + 状态等待）、`Winx`、`Edge`（CDP 驱动真实浏览器、SW 侧 `chrome.*` 查询、`activate` 前台化）、`Chain` 相关工具、站点/引擎请求日志解析、`ensure_download_dir`（改设置＋重启）。
- `chain_ops.py` — `Chain`：`capture_link`（真点击→任务出现→终态→字节校验→浏览器侧中性化）、`engine_hit`（UA/Range 取证）、`verify_bytes`。
- `stage_setup.py` / `stage_basic.py` / `stage_dir.py` / `stage_names.py` / `stage_failures.py` / `stage_resume.py` / `stage_compat.py` / `stage_magnet.py` / `stage_hidden.py`。
- `diag_toggle.py` — 自动接管开关的定向诊断（含 `chrome.storage.local` 真值）。
- 结果文件：`.verify\e2e\out\stage_*_results.json`（逐条 PASS/FAIL/SKIP＋证据字符串）与 `ui_tree_after_wake.txt` / `ui_tree_after_restart.txt`。
- `diag_bt.py` — BT 拓扑定向诊断（做种机的 payload/控制文件/校验状态）；`chain_cleanup.py` — 收尾还原（设置回滚＋重启、停测试站/Edge profile/多余做种机、删掉漏进真实 `Downloads` 的测试文件）。
- 本轮踩到并已修好的**测试脚本自身问题**（记下来免得下次重犯）：CDP 输入事件对**后台标签页无效**（打开 popup 后必须 `Target.activateTarget` 把页面标签拉回前台，否则点击全部静默失效）；`winx.ps1` 里 UIA 的按钮点击要按 `ControlTypeProperty`（不是 `ControlTypeIdProperty`）过滤，且客户端的"全部暂停/全部开始"是 **Text 元素**而非 Button（新增 `click-text` 命令，向上找可 `Invoke` 的祖先）；`PowerShell` 的 `>` 重定向写 UTF-16LE（读取端要按 BOM 判断编码）；BT 做种机必须放**完整 payload + `--check-integrity=true`**，否则 aria2 报 `File exists, but a control file (*.aria2) does not exist` 拒绝做种；站点 tracker 路由原先未写请求日志（已补）。

### 14-F 人工验收清单（建议用户自测顺序，约 20 分钟）

1. **默认目录（缺陷 1）**：设置里把下载目录改到 `D:\dl_test`，**不要重启**，直接点一个普通链接下载 → 观察文件是否真的进了 `D:\dl_test`；再做一次"点两次同一链接"看是否出现 `(1)` 文件。
2. **同名覆盖（缺陷 2）**：不改目录，点同一链接下载两次，确认第二个文件是 `xxx (1).bin` 而不是覆盖。
3. **暂停/继续（缺陷 4）**：找一个不支持断点续传的站点（或本仓库 `.verify\e2e\site` 的 `/files/e2e_norange_8mb.bin`），下载中暂停再继续，确认要么能续上，要么失败后不留"看起来完整"的同名文件。
4. **托盘与唤醒（缺陷 7)**：点关闭按钮（应最小化到托盘，进程不退），等 30 秒，再从开始菜单/桌面图标启动一次，确认窗口能回来；若没回来，用托盘菜单打开。
5. **BT**：用 `site\fixtures\e2e_bt_20mb.torrent`（配本地做种）或任意真实种子/磁力，确认弹"磁力链接下载确认"、点"开始下载"后进度与做种方一致、完成后种子校验通过。
6. **右键菜单**：在链接上右键选"使用海兔下载器下载"，确认任务出现在列表（本轮 UIA 自动化未能可靠命中 Chromium 原生右键菜单，需人工确认）。
7. **重启后的历史**：完成一个下载后重启客户端，确认列表里仍能看到它（本轮观察到完成态任务在重启后从 API 列表消失，`: `DIR9 before=25 after=8`，需与产品预期对齐）。

### 14-G 有意未做 / 遗留（本轮明确的边界）

- **未做 GitHub 同步**：按用户指示本轮**不提交、不推送、不打 tag、不替换 Release 资产**；改动仅在工作区（第七轮 P0 改动的 5 个文件 + 本轮新增的测试脚本），稍后由用户自行决定。
- **未做截图**：用户指示跳过全部截图任务，改为程序化取证（UIA/Win32 可见性、CPU 切片、请求日志）。
- **未修任何产品代码**：本轮只测试、只记录，发现的问题（缺陷 1/2/4/5/6/7 与历史 O-2/O-4～O-9/O-11/O-12）均未动手修改。
- **UIA 无法可靠驱动 Chromium 原生右键菜单**：`stage_compat` 的 H2 以 `MENU_NOT_FOUND` 记录并转人工；扩展侧手动发送（popup `#urlInput`+`#sendBtn`）已自动化验证通过。
- **测试期设置需还原**：本轮把 `defaultDownloadDir` 指到 `%TEMP%\kokona_e2e\145754\*`、`globalSpeedLimit=0`；收尾已按 `state.json` 记录的原始值还原（`C:\Users\lumin\Downloads`、`1048576`）。

### 14-H 结论（一句话版）

**最终打包件（`46D772E6…DFF2`，1.0.8）在真实浏览器全链路下是"能用的"**：插件的拦截/放行边界、API 契约、aria2 的落盘与完整性、BT 全链路、托盘与停表都能按设计工作，历史问题清单里除"仅 `filename*` 的 CD"这一上游边界外**全部不再复现**；本轮 9 个阶段 + 4 套既有回归共 **400 余条断言，绿色率 96.5%**，失败项全部落在**两条数据安全类缺陷**和几条体验类问题上：

1. **必须修（P1，数据丢失）**：① 运行中改"默认下载目录"不生效并连带使同名保护失效；② 无 `filename` 的自动接管任务重复下载覆盖旧文件。这两条只要其中一条命中，用户就会**丢文件**，且都是输入极普通的场景（改设置、点两次同一链接）。
2. **建议修（P2）**：③ 无 Range 续传失败后留下"大小正确但内容损坏"的同名文件（改成 `*.partial` 即可）；④ 重启后完成态历史从 API 消失（先对齐产品预期）。
3. **可延后（P3）**：⑤ 错误码链接处理竞态；⑥ 404 自愈要等约 10 分钟；⑦ 单实例唤醒窗口间歇性失效（第七轮已记录）。

以上全部**只报告未改**，等待用户确认后再动代码与 GitHub 同步。

## 15. 第九轮：按 §14-H 落地修复 + 复测（2026-09-24，改码 + 打包 + 全链路复测）

### 15-A 本轮范围与用户决策

- 用户指令：**开始修复**。GitHub 同步仍然**不做**（不提交、不推送、不打 tag、不换 Release 资产），等用户自测后自行决定。
- 仍**跳过全部截图任务**（用户稍后自测）；本轮取证一律用程序化手段：真实 aria2 集成测试、`/api/tasks` 真值、UIA 文本转储、逐请求日志。
- 两条 P2 的处置方式由用户在选项中拍板：
  - §14-C 缺陷 7（重启后完成态历史从 API 消失）→ **实现本地历史**（客户端自己持久化，UI 与 `/api/tasks` 都看得到）。
  - §14-C 缺陷 4（无 Range 续传失败留下"大小正确但内容损坏"的同名文件）→ **失败即隔离为 `*.partial`**。
- 本轮动手：缺陷 **1、2、4、5、7、8**（编号沿用 §14-C）；**不修**缺陷 3（仅 `filename*` 的 CD，上游边界）；缺陷 6 经探针重新定性（见 15-C）。

### 15-B 修复明细（逐条：改法 → 代码位置 → 回归护栏）

**缺陷 1（P1）运行中改"默认下载目录"不生效**

- 改法：引擎不再把目录当启动快照。`DownloadEngine` 新增运行期默认值区（`DefaultDownloadDir` / `DefaultConnections` / `UpdateRuntimeDefaults(...)` / `WithRuntimeDefaults(req, isBt)`），**每一次新建任务都显式下发 `dir`**（`BuildOptions` 里 `dir` 不再受"非空才下发"约束），并把 `MaxConcurrentDownloads` 通过 `aria2.changeGlobalOption` 热应用。
  - 代码：`Core/Engine/Models.cs`（`NewTaskRequest.AllowOverwriteExisting`）、`Core/Engine/Aria2RpcClient.cs`（`BuildOptions` 改为 `public static`，便于单测）、`Core/Engine/DownloadEngine.cs`（新 region「运行期默认值 / 本地历史 / 失败残留隔离」）、`Core/AppHost.cs`（`OnSettingsChanged` 热应用 `DefaultDownloadDir` / `DefaultConnections` / `MaxConcurrentDownloads`）。
  - `ApplyUniqueFileName` 改用 `_defaultDir` 扫描 ⇒ 同名保护终于看得见真正的目标目录。
- 护栏：`RoundEightFixTests.改默认目录后新任务立刻落到新目录`（引擎起来后改目录、新任务必须落进新目录且 `Dir` 报告新目录）。

**缺陷 2（P1）重复下载同一链接覆盖旧文件**

- 改法：**逐条**下发 `allow-overwrite`：普通 HTTP 任务 `false`（aria2 自动改名 `x.1.ext`，旧文件不动），BT/磁力 `true`（BT 依赖复用目录里的同名分片）。客户端已知可靠文件名时仍自己改成 `x (1).ext`（保持既有观感），名字只来自响应头时交给 aria2——两条路径都不覆盖。
- 护栏：`RoundEightFixTests.同名文件已存在时新下载自动改名而不是覆盖` + 单测 `新建HTTP任务必须逐条关掉allow_overwrite` / `BT任务保持allow_overwrite_以便复用目录中的同名文件`。
- 探针依据见 15-C（Q1）。

**缺陷 4（P2）无 Range 续传失败留下"看起来完整"的损坏文件**

- 改法：轮询发现"失败 + 非 BT + **不可续传类**失败"时执行隔离 `QuarantineUnresumableFailureAsync`：
  1. 路径先 `Path.GetFullPath` 归一（aria2 上报的 `files[0].path` 是正斜杠写法）；
  2. `x` → `x.partial`（占用则 `x.partial.1…`），**连同 `.aria2` 控制文件一起改名**——留着挂在原名下的控制文件会让 aria2 以为还能 `--continue`；
  3. `UpdateFinished` 把隔离后的路径写进留档；
  4. `removeDownloadResult` 清掉 aria2 的失败结果。**必须清**：失败结果还在时，用户点"继续"会让 aria2 按内存里的分位图往（此时已改名的）文件里"继续写"，得到一个满是空洞的假完整文件，比原来更糟。
  - 判定 `IsUnresumableFailure`：`errorCode == 8`（Invalid range header）**或**文案命中 `Invalid range header` / `Unable to continue download` / `cannot be continued`。普通网络中断**不算**（那类断点是有效的，不能隔离）。
  - 副作用（有意）：隔离后 aria2 不再认识这个 gid，"继续"会被 API 拒（`400 task_action_failed`），"重新下载"改走本地留档（见缺陷 7）。
- 护栏：`RoundEightFixTests.服务器不支持Range时续传失败_残留被隔离为partial且仍可重新下载`（用 `TestEnv.FileServer(ignoreRange: true)` 复现"收到 Range 也回整份 200"的服务器；断言：原名下什么都不剩、`*.partial` 存在、`*.partial.aria2` 跟着走、历史行仍是 `Failed` 且带原因、`redownload` 能重新下完且字节一致）。

**缺陷 7（P2）重启后完成态历史从 `/api/tasks` 消失**

- 为什么必须客户端自己做：探针实测 aria2 的 `--save-session` 在 4 个已完成任务下写出 **0 字节**（aria2 明确不持久化已完成下载），`aria2.getNumStoppedDownloads` 这个方法根本不存在。
- 改法：`TaskStore` 的 meta 增加**终态快照**字段（`TotalLength/CompletedLength/FilePath/Dir/ErrorCode/ErrorMessage`）与 `Finished()` / `RemoveMany(...)`；引擎侧建 `_history`（内存行，上限 `MaxHistoryRows = 2000`，按完成时间淘汰最旧并连带删 meta）；`GetAllTasksAsync` 与 `/api/tasks`、UI 三处统一走 `WithHistory(live)`（**同一 gid 以 aria2 实时状态优先**）；`RedownloadAsync` 在 aria2 不认识 gid 时回落 `HistoryFallback`；`RemoveAsync` 支持删除已完成/失败任务（用 meta 里的路径删文件，并同步清 `_history`）。
- 顺带：`PruneStaleMetasOnce` 每会话一次，只清理"10 分钟前建立、既无终态也没 FinalState、aria2 已不认识"的僵尸 meta——**有终态快照的绝不清**（那是历史的数据源）。`StopAsync` 用 `try/finally` 保证 `SaveNow()` 一定执行（否则最多丢 500 ms 防抖窗口内的完成记录）。
- 护栏：`RoundEightFixTests.已完成任务在引擎重启后仍以历史行出现在列表里`（换一个 RPC 端口重开引擎，仍能看到 `Completed` 行、任务号一致、`TotalLength` 正确、`DownloadSpeed=0`、URL 仍在以便重新下载）。

**缺陷 5（P3）错误状态链接有时什么都不会发生**

- 改法：`extension/logic.js` 的接管规则 1 放松——`state === 'complete'` 仍不接管；`interrupted` 若是**刚刚发生**（不早于扩展 Service Worker 启动、也不是隔天以前的历史项）就接管。原因：MV3 冷启动时，对 `error` 响应的下载项会在 `onCreated` 回调跑起来之前就被 Chromium 翻成 `interrupted`，严格只认 `in_progress` 就会漏掉整批。规则 2（`filename` 非空 ⇒ 浏览器已真的落盘，绝不接管）保持严格，避免"客户端下一份、浏览器也留一份"。
- 护栏：`tests\test_logic.js` 断言数 78 → **81**（新增 `interrupted` 新鲜/陈旧/带 filename 三种子情形）。

**缺陷 8（P3）被托盘隐藏后"再次启动"唤回窗口不可靠**

- 改法：`WindowEffects.RevealAndActivate(Window)` 用真实 Win32 序列唤醒并**验证**：`IsIconic` → `ShowWindow(SW_RESTORE=9)` 否则 `SW_SHOW=5`；仍不可见再 `ShowWindow(9)` 一次；`BringWindowToTop` + `SetForegroundWindow`；最后用 `IsWindowVisible && !IsIconic` 复查，不达标就重试一次并写日志（"唤醒主窗口后窗口仍不可见或被最小化…"）。`App.ShowMainWindow` 里的 `AppWindow.Show()` / presenter `Restore()` 都包 try/catch；第二实例唤醒路径的裸 `catch { }` 换成记录异常类型与消息（下次真出问题日志里能看到）。

**缺陷 6（P3）临时错误的重试策略（重新定性后修）**

- 见 15-C 的对照探针：真正的缺陷**不是**"404 只重试 1 次太保守"，而是"临时 5xx 一次都不重试"。
- 改法：aria2 启动参数加 `--retry-wait=2 --max-tries=5`（`Aria2Process.cs`，附实测注释）。404 维持 fail-fast，不在客户端偷偷重试死链。

**其它同批改动**

- `ErrorMessage` 回落 `errorReason`：aria2 很多失败只填 `errorReason` 不填 `errorMessage`，`ToTaskInfo` 现在会补上（历史行终于能解释"为什么失败"）。
- `Aria2RpcClient.BuildOptions` 从 private 改 `public static`（只为了能被单测直接断言，不改变调用方）。

### 15-C 探针取证（`.verify\fixtest\`，不入库、不参与构建）

修之前先做探针，避免"照着猜测改参数"。两个脚本都是**真实 aria2c**（`vendor\aria2\aria2c.exe`）+ 本地 HTTP 服务，启动参数与产品一致（`--split/--min-split-size/--continue/--allow-overwrite/…`）。

**`aria_probe.mjs`（RPC 16999 / HTTP 18090）——决定缺陷 2、7 的修法**

| 场景 | 实测结果 |
|------|---------|
| 同一 URL + `out=a.bin` + **`allow-overwrite=false`**（拟修复） | 旧文件**完好不动**，新下载自动叫 **`a.1.bin`** |
| 同上但**不带 `out`** | aria2 按 `Content-Disposition` 取名（`srv_big.bin`），重名同样自动编号 |
| 同一 URL + `allow-overwrite=true`（旧的全局参数） | **静默覆盖**旧文件（数据丢失的直接原因） |
| 4 个任务完成后 `aria2.saveSession` | 会话文件 **0 字节**；`aria2.getNumStoppedDownloads` **方法不存在** |

**`retry_probe.mjs`（两组对照，服务器前 2 次故意失败）——决定缺陷 6 的修法**

| 场景 | 当前产品参数（无 `--retry-wait`） | `--retry-wait=2 --max-tries=5` |
|------|-----------------------------------|--------------------------------|
| 404 → 200 | 服务器只见 **1 次**请求，`errorCode=3 Resource not found` | 同样 **1 次**请求，`errorCode=3` |
| 503 → 200 | 服务器只见 **1 次**请求，`errorCode=29 The response status is not successful` | **3 次**请求后 `complete`（同一个任务内自愈） |

结论：① 404 被 aria2 当**永久**错误，任何 `--retry-wait` 都无效 ⇒ 客户端"不自动重试 404"是合理设计，只能由用户点"重新下载"；② 真正的产品缺陷是**临时 5xx 完全不重试**（连一次都没有），补 `--retry-wait=2 --max-tries=5` 后同一任务内自愈；③ 已完成任务不进会话文件 ⇒ 历史必须由客户端自己持久化。

### 15-D 单元 / 集成回归（本轮新增 11 条，全部对着真实 aria2）

新增 `src\KokonaDownloader.Core.Tests\RoundEightFixTests.cs`：

| 测试 | 覆盖 |
|------|------|
| `新建HTTP任务必须逐条关掉allow_overwrite` | `allow-overwrite=false`、`continue=false`、`dir` 必发、`split`/`out` 条件 |
| `BT任务保持allow_overwrite_以便复用目录中的同名文件` | BT 路径仍 `true`，且不下发 `split` |
| `不可续传失败的判定只认错误码8与相应文案` | 两个重载：code=8 / 三条文案命中；普通网络中断与 404 **不算** |
| `终态快照持久化后重开仍能读出历史行` | meta 终态字段落盘 + 重开读回 + `Finished()` 倒序 + `RemoveMany` 计数 |
| `失败任务的完成长度按已完成写入_成功任务按总长写入` | `total=0` 时不谎报"完整" |
| `改默认目录后新任务立刻落到新目录` | 缺陷 1（真实 aria2） |
| `同名文件已存在时新下载自动改名而不是覆盖` | 缺陷 2（旧文件字节必须不变，新文件另存） |
| `已完成任务在引擎重启后仍以历史行出现在列表里` | 缺陷 7（换 RPC 端口重开引擎） |
| `服务器不支持Range时续传失败_残留被隔离为partial且仍可重新下载` | 缺陷 3/4（隔离 + 清结果 + 重新下载字节一致） |
| `会话文件里早已完成的条目不会在重启后被重新排队` | **本轮修"重启幽灵"时新查出的缺陷**：`--input-file` 会把会话里已完成的条目当新任务重新加入（新 gid、`排队中 0 B`，还可能重下一遍）；断言重启后列表里**只有一条**该文件的行且是完成态（`SessionPurgeTests` 之外，走完整引擎） |
| `启动前过滤会话文件_已删除与早已完成的条目都不再复活` | `TombstoneStore.PurgeSessionFile(sessionFile, extra)`：墓碑哈希 ∪ 临时传入的"已完成"哈希；按"URI 行 + 缩进选项行"成块解析，用块内 `dir=` 参与 `SHA256(url|dir)` 计算；断言删掉 2 条（已删除的 + 早已完成的）、保留 1 条，且不带 `extra` 再跑一次过滤数为 0（`extra` 不落盘） |

配套改动：`TestEnv.FileServer` 新增 `ignoreRange`（收到 `Range` 也回整份 200），用来复现"没有断点续传能力的服务器"；`_rangeRequests` 计数改到"收到 Range 就算"，与是否回 206 解耦。

写这批测试与全链路复测时踩到六个坑（都值得记下来）：

1. **读接口可能走实时 RPC**，比引擎的轮询循环更早看到 `Failed/Completed`；而"隔离残留 / 写历史"发生在**轮询循环**里 ⇒ 断言前必须 `WaitUntilAsync` 等引擎真正处理完，否则测的是竞态而不是行为。
2. **aria2 上报的 `files[0].path` 用正斜杠**（`C:/Users/…`），与 `Path.Combine` 的结果直接比字符串会误判"路径不对"。已在比较前 `Path.GetFullPath` 归一，并顺手让隔离后的历史路径统一成规范写法（`QuarantineUnresumableFailureAsync` 先归一再改名）。
3. **复用上一轮测试沙箱会让断言集体误判**（本轮第一次全链路就栽在这）。`state.json` 里存了 `runTag`，所以重跑会**继续用同一个 `%TEMP%\kokona_e2e\<tag>`**，里面还躺着上一轮 197 MB 的产物；而第九轮起 aria2 逐条带 `allow-overwrite=false`，凡是"重名就自动改名"的路径都会立刻生效：`names` 一片 `got=X.1`（因为 `basic` 阶段已经把 `e2e_cinebench.zip` 下过一遍），`DIR6` 因为 `dirx_*` 里有旧 `e2e_logo.png` 而拿不到预期文件名、白等 120 s。现在 `stage_setup` 一上来就把 `%TEMP%\kokona_e2e\*` 清空（只碰这个沙箱目录），并把"按名字精确断言"改成"按 `name_key` 归一 + 按任务自己的 `filePath` 校验字节"（见 15-G）。**教训：只要产品行为里出现了"自动改名"，测试沙箱就必须是每轮全新的，否则你测到的是历史文件而不是本轮行为。**
4. **测试站自己也会带崩整套**（第八轮留下的雷）。`handleTracker` 里引用了 web 侧每个请求各自构造的闭包 `done()`，`ReferenceError` 抛出后，外层 catch 又无条件 `res.writeHead(...)`——此时响应头已经发过，于是再抛 `ERR_HTTP_HEADERS_SENT`，站点启动后第一次 tracker announce 就整个进程退出（`exit code: 1`），紧接着所有阶段以 `ECONNRESET` 连锁失败。修成：tracker 用 `logRequest` 自己记日志、catch 先判 `res.headersSent`、`uncaughtException` 兜底打栈但 `EADDRINUSE`/`EACCES` 仍然直接 `exit 1`（"这个实例根本没起来"绝不能装作没事）。顺带发现第八轮那句"tracker 请求现在会记日志"其实**从来没生效过**。
5. **限速这类"平均行为"不能用 `max()` 当判据**。`D4` 要验证 `globalSpeedLimit=262144` 真的生效：第一版扫全部采样的最大值，被 aria2 起步爬坡的头几秒坑了（实测峰值 368,968 B/s）；改成"只看后一半"之后，同一份代码、同一台机器上仍量出 373,600 B/s 的超限样本（16 条连接 + 令牌桶粒度）。最终判据改成**稳态中位数 ≤ 限速值 × 1.2**（实测稳态中位数 213k～240k，完全不限速时约 699k，区分度仍然很大），峰值只作为观测值写进 detail。
6. **本地历史上线之后，所有"绝对数量"判据都会变成假报警源**。`/api/tasks` 现在跨重启、跨多次运行累积（本轮跑到后面已经是 900 多行历史），于是 `G2`"这个 URL 一条任务都没有"、`DIR9c`"同名只有一行"、`B4`"目录里就该有这些文件"这类断言全部开始误判——它们以前成立，恰恰是因为旧版本**会**丢历史、沙箱**会**被复用。本轮统一改成**相对判据**：点击前 `app.snapshot()` 比"新增"（`chain_ops.no_task(before=…)`、`browser_neutral` 对比 `browser_before`）、按任务自己的 `filePath` 定位文件（`D2` 的隔离件、`B4` 的尺寸）、`DIR9c` 只要求"同名行里不得有非完成态的幽灵"。完整对照见 15-F 第 3 条。

* 全套：`dotnet test src\KokonaDownloader.sln -c Release` → **172 / 172 通过**（0 失败，6 m 1 s，串行；日志 `.verify\fixtest\coretests_round9final.log`）。
* 扩展逻辑单测：`cscript //nologo tests\test_logic.js` → **81 / 81 PASS**。

### 15-E 重新打包与全链路复测

**新构建**：`dist\KokonaSingleFile\KokonaDownloader.exe` → **318,221,144 字节**，SHA256 `7BDDA869FC7FDE2EB798CF6FCEC7B814DFD7895AD372E5B7EEC80F484F4E9055`（`Get-FileHash` 现测，与《项目文档.md》§12 一致；打包日志 `.verify\fixtest\publish_round9.log`）。

**全链路（真实 Edge + 未打包扩展 + 真 aria2 + 真落盘）—— 最终干净跑（日志 `.verify\fixtest\e2e_round9g.log`）**

| 阶段 | 第九轮 | 第八轮 | 说明 |
| --- | --- | --- | --- |
| `setup` | **17 / 17** | 16 / 17 | 含"沙箱先清空"与"改目录后热生效" |
| `basic` | **68 / 68** | 29 / 44 | 第八轮 15 条红是当时未修的目录/覆盖缺陷，本轮转绿 |
| `names` | **155 / 155** | 146 / 150 | 含 RFC 5987、无扩展名、`x (1)` / `x.1` 两条改名路径 |
| `dir` | **14 / 14** | 7 / 12 | 含 `DIR9b`/`DIR9c`（重启后完成态仍在、且不再被会话文件重新排队） |
| `failures` | **41 / 41**（+4 skip） | 34 / 36（+4 skip） | 新增 `C13`：临时 503 必须在**同一任务内**自愈 |
| `resume` | **52 / 52**（+1 skip） | 43 / 46（+2 skip） | 含 `D2` 隔离契约、`D3` 工具栏暂停/继续、`E2` 判重 |
| `compat` | **42 / 42** | 42 / 42 | `G2` 换成"只比点击前快照的新增"后仍全绿 |
| `magnet` | 14 / 15（+1 skip） | 15 / 15（+1 skip） | 唯一红是 `F0a` 前置条件（见下），磁力/BT 行为项全绿 |
| `hidden` | **19 / 19** | 18 / 19 | 含 `I3`"再次启动能把隐藏窗口叫回来"（缺陷 7 的活体证据） |
| 合计 | **422 通过 / 6 跳过 / 1 失败**（共 428 条） | 350 通过 / 7 跳过 / 24 失败（共 381 条） | 两轮的分母不同（本轮新增/重写了大量断言），总数不宜直接对比；可比的是同一阶段名下的红转绿 |

唯一失败的那条是**测试自己的前置条件**，不是产品行为：`F0a` 要在磁力阶段开始前确认"没有遗留 BT 任务会遮蔽测试"，它发现一条上一轮遗留的 `[METADATA]0000…`（还在取元数据），删掉之后立刻复查仍然看到一行。后续单独观察（本轮收尾时直接打 API）确认：这类行**确实会被删除**，只是 `aria2.remove` 对"取元数据阶段"的 BT 任务是**打标记、等下一轮轮询才摘掉**，30 s 内两次删除 + 复查仍有残余，属于异步未收敛的竞态。已把 `F0a` 改成"最多等 30 s、每轮重删，判据本身不放宽"（`.verify\e2e\stage_magnet.py`），但**重跑磁力阶段需要在同一轮里先把 BT 做种机拉起来**——本轮收尾时我单独重跑了一次 `stage_magnet`，因为 51500/51501 做种端口没监听，`F1`（磁力完成）跟着变红（`e2e_round9h_magnet.log`，13/15）。**那次重跑作废，本表的磁力数字取自做种机正常的 `e2e_round9g.log`。**

过程中的其它跑次也一并留下，避免"只挑好看的那次讲"：

* `e2e_round9d.log`：产品修完之后的第一次全链路，**410 通过 / 8 失败**。8 条红全部是测试脚本自己的判据问题（`names` 3 条无扩展名改名、`D3`/`E2` 连锁、`D4` 峰值、`G2` 绝对判据），逐条见 15-F 第 3 条与 15-D 坑 5/6。
* `e2e_round9e.log`：只重跑 `names`/`resume`/`compat`（沙箱未清空）→ `names` 155/155、`compat` 42/42、`resume` 51/52，暴露出 `D2` 写死文件名的问题（15-G 收尾条目）。
* `e2e_round9f_resume.log`：同一个沙箱跑到第三遍，`resume` 掉到 44/52 —— 沙箱复用会污染所有"时序 + 文件名"判据，这也直接促成最终那一轮必须从 `setup` 重新开始。

**既有回归（同机同 exe）**

| 套件 | 结果 | 备注 |
| --- | --- | --- |
| `.verify\fulltest\run.mjs` 下载全场景 | **52 / 52** | 日志 `.verify\fixtest\fulltest_round9.log`；**注意：`run.mjs` 自己会启动 18080 站点，另外再起一份会 `EADDRINUSE`**（本轮第一次就这么废掉的） |
| `.verify\functest\test.mjs` | **24 / 24** | 日志 `.verify\fixtest\suites_round9.log` |
| `.verify\audit\verify_fixes.mjs` | **10 / 10**（+1 skip） | skip 仍是密钥轮换（未提供 `OLD_SECRET`） |
| `.verify\fulltest\ui_inspect.ps1` | **ISSUES: 0** | 7 个窗口：主窗 **81** 控件、5 个进度小窗各 20、磁力确认窗 18（`ui_inspect_round9.log`）；这次枚举前刚用"再启动一次 exe"把隐藏中的窗口叫回来，顺带二次验证缺陷 7 的修复 |
| `tests\test_logic.js`（扩展纯函数） | **81 / 81** | 与第八轮持平 |

**收尾**：`python .verify\e2e\chain_cleanup.py` → 设置已还原（`dir=C:\Users\lumin\Downloads`、`limit=1048576`）、客户端以原目录重启、测试站/Edge 配置/做种机全部停止、从用户 `Downloads` 清掉 6 个泄漏的测试产物（`e2e_cinebench.zip`、无扩展名的 `e2e_no_extension_at_all` 等）；日志 `.verify\fixtest\cleanup_round9.log`。测试沙箱 `%TEMP%\kokona_e2e\145754`（约 986 MB）**保留未删**，供事后核对；`setup` 下一轮会自动清空。

### 15-F 对 §14 的几处说法修正（诚实清单）

1. **§14-C 缺陷 7 里"UI 里还看得到"这句被过度解读了。** 第八轮的 UIA 转储（`out\ui_tree_after_restart.txt`）里确实能找到 `e2e_big_50mb.bin`，但它那一行的状态是 **`排队中` / `0 B`**（不是"已完成"）：aria2 是用 `--input-file=<会话文件>` 启动的，重启时会话里剩下的条目会被**当作新任务重新加入**（新 gid、进度归零）。也就是说当时 UI 与 API 其实**都没有**"已完成"这一行——UI 只是把一个已经下完 50 MB 的链接重新排成了"排队中 0 B"。`stage_hidden` 的 I6 断言只做名字子串匹配，于是把一个**另一种缺陷**（重启把已完成任务重新排队，可能真的重新下一遍）误判成"历史还在"。本轮：① I6 改成"名字出现 **且** 空状态文案不得出现"，并新增 `stage_dir` 的 `DIR9b`/`DIR9c` 以 `/api/tasks` 为真值（重启前那条已完成任务必须仍以完成态在列表里、带 `filePath`；同名行里**不得出现非完成态的幽灵**——不能要求"全表只有一行"，因为本地历史跨次运行累积，见 15-D 坑 6）；② 本地历史上线后，完成态行由客户端自己提供，不再依赖 aria2 会话恢复的"复活"副作用。
2. **§14-C 缺陷 6 的"约 10.5 分钟后自行 Completed"本轮未能复现。** 探针显示同一 URL 的 404 只发 1 次请求（两种参数都一样），也没有任何客户端侧的定时重投机制。§14 那条观察很可能是当时另有人为操作（阶段脚本的 `redownload`）造成的，**不写入结论**；相关定性见 15-C。
3. **第八轮有几条"通过"其实是踩在旧缺陷上的**，第九轮把产品行为改对之后，它们必须换判据才能继续成立。四条实测对照：
   - `B4`（批量 8 个文件尺寸都对）第八轮"通过"，实际是 `chain.files()` 扫到了**上一轮**留在同一目录里的 `item*`；本轮清空沙箱后它直接给了 `[]`。现在改用 8 个任务自己的 `filePath` 量尺寸。
   - `G2`（关掉自动接管后客户端不该建任务）第八轮"通过"，靠的是"重启后历史会消失"这个缺陷；本地历史上线后 `/api/tasks` 里永远留着同名历史行，绝对判据必然假报警。现在改成"只比点击前快照的**新增**"。
   - `I6`（重启后 UI 还认得这条已完成任务）第八轮"通过"，实际看到的是被会话文件重新排队的 `排队中 0 B`（见本条第 1 点）。
   - `D3`/`E2` 这对最典型：`allow-overwrite=false` + 判重 + 本地历史都生效以后，`D3` 因为 `wait_new` **看到第一条就返回**而把"两条都成功"数成一条；随后 D3 的 50 MB/24 MB 大任务留在"未结束"状态，`E2` 再点同一个链接被客户端**正确地**拒绝重复添加，于是报 `tasks=0`。日志铁证（`app.log`）：`00:34:59`/`00:35:00` 两条 `API 新建任务` 都成功，`00:35:01`/`00:35:25` 两次 `API 跳过重复任务（客户端已存在）`。修法＝新增 `wait_new_all()`（等每个 pattern 都出现）、D3 跳过分支负责清理大任务、E2 加"这个 URL 上没有未结束任务"的前置断言。**这不是产品缺陷，但如果不查日志就会当成缺陷去"修"，把正确的判重行为改坏。**
   同批次还有 `D4`：限速断言用 `max()` 扫全部采样，把 aria2 起步爬坡的头几秒也算进"稳态"，实测峰值 368,968 B/s（限 262,144）；最终改为**稳态中位数 ≤ 限速 × 1.2**（详见 15-D 坑 5）。

### 15-G 本轮新增 / 修改的测试资产

- `src\KokonaDownloader.Core.Tests\RoundEightFixTests.cs`（新，9 条）。
- `TestEnv.cs`：`FileServer.AddFile(..., ignoreRange: false)` 新参数与 Range 计数修正。
- `.verify\fixtest\aria_probe.mjs` / `retry_probe.mjs`（探针，不入库）。
- `.verify\e2e\site\server.mjs`：新增 **`/retry5xx/<token>`**（前 2 次 503、第 3 次 200，`e2e_retry5xx_ok.bin`）作为"临时 5xx 必须同一任务内自愈"的哨兵。
- `.verify\e2e\stage_resume.py`：D2 段按新契约重写（不再要求"再点一次继续就能完成"，改为断言"原名下什么都不剩 + 残留改名 `*.partial` + 历史行带原因 + 继续被拒 400 + 重新下载能拿到字节一致的文件"）。
- `.verify\e2e\stage_dir.py`：`DIR7` 改到**当前**默认目录里数文件（第八轮它数错了目录，等于没测）；`DIR9` 后新增 `DIR9b`。
- `.verify\e2e\stage_basic.py`：新增 `name_key()`——重名保护有两条合法路径（客户端 `x (1).ext` / aria2 `x.1.ext`），断言两者都算通过；A4/C2 改用它。**（收尾时上移到 `chain_ops.py` 成为唯一实现，并把"重名但无扩展名"的 `x.1` 写法也纳入；`stage_basic` 改为 import）**
- `.verify\e2e\stage_failures.py`：C7b/C9 明确"fail-fast 且带原因"＋"用户点重新下载可恢复（最多 3 次）"；新增 **C13**（临时 503 必须同一任务内自愈 + 落盘 256 KiB 完整）。
- `.verify\e2e\stage_hidden.py`：I6 的 UI 断言加上"空状态文案不得出现"（见 15-F-1）。
- `.verify\e2e\site\server.mjs`：修 15-D 坑 4——tracker 的日志改用 `logRequest`；catch 先判 `res.headersSent` 再决定怎么收尾；`uncaughtException`/`unhandledRejection` 打栈但 `EADDRINUSE`/`EACCES` 直接 `exit 1`。站点自检随后 **52/52**，`/retry5xx/<token>` 实测回 `503 → 503 → 200`。
- `.verify\e2e\stage_setup.py`：整套开跑前清空 `%TEMP%\kokona_e2e\*`（只碰这个沙箱；见 15-D 坑 3）。
- `.verify\e2e\chain_ops.py`：`name_key()` 上移至此成为唯一实现；`capture_link` 的名字断言与 `verify_bytes` 都改用它，并且 `verify_bytes` **优先用任务自己报的 `filePath`** 去校验字节（重名改名后按名字扫会去比旧文件）。
- `.verify\e2e\stage_basic.py`（收尾）：`B4` 改用 8 个批量任务自己的 `filePath` 量尺寸（第八轮那条"通过"其实是扫到了上一轮留下的 `item*`）；`N2` 改成"只比 `Chain.browser_before` 快照的**新增**"（用户的真实下载目录里有历史轮次的同名残留，绝对判据会假报警）；`C2` 的期望名改成不带编号的 5 个原始名并新增 `C2b`（5 个名字必须互不相同 ⇒ 没有任何东西被覆盖）。
- `.verify\e2e\stage_dir.py`（收尾）：`DIR6` 的快照改在发请求**之前**取（反过来的话新任务可能已经进快照，于是永远匹配不上，本轮实测白等 120 s），并加上"`task.dir` 必须等于显式传入的目录"这条硬判据。
- `src\KokonaDownloader.Core.Tests\xunit.runner.json`：`parallelizeTestCollections` `true` → **`false`**（端口 TOCTOU 造成的随机失败，见 15-D 与《项目文档.md》§11.6）；`.verify\audit\run_tests_serial.ps1` 的类清单与"纯单测"排除过滤器同步加入 `RoundEightEngineTests`。
- `.verify\e2e\chain_ops.py`（收尾第二批）：新增 `wait_new_all()`（连点多个链接时必须等**每个** pattern 都出现，`wait_new` 看到第一条就返回会少报）；`name_key()` 补上"重名但无扩展名"的 `x.1` 写法（`names` 的 `queryname.1`/`utf8only.1`/`e2e_no_extension_at_all.1` 三条就是这么误判的）。
- `.verify\e2e\stage_resume.py`（收尾）：`D3` 改用 `wait_new_all` 并在跳过分支负责清理 50 MB/24 MB 大任务；`E2` 加"这个 URL 上没有未结束任务"的前置断言（先等/再删，最多 60 s）；`D4` 稳态判据改为"后一半样本的中位数 ≤ 限速 × 1.2"；`D2` 定位残留改用**任务自己的名字**（第三遍复用沙箱时原名已被上一轮的完整文件占住，写死原名会把"隔离成功"读成"隔离失败"，实测 `original=True sidecar=True`）。
- `.verify\e2e\stage_dir.py`（收尾第二批）：`DIR9b` 优先挑"本轮目录里文件真的存在"的完成行（同名历史行会跨轮累积）；`DIR9c` 判据改为"同名行里不得有非完成态的幽灵"，不再要求全表只有一行（见 15-D 坑 6）。
- `.verify\e2e\stage_compat.py`（收尾）：`no_task()` 增加 `before=` 快照参数，`G2` 传入点击前的快照（本地历史上线后 `/api/tasks` 一定还留着同名历史行，绝对判据必然假报警）。
- `.verify\e2e\stage_magnet.py`（收尾）：`F0a` 的前置条件改成"删完最多再等 30 s、每轮重删"（`aria2.remove` 对"取元数据阶段"的 BT 任务是异步摘除，删完立刻复查必然假报警）；判据本身不放宽。
- 测试基建坑（写给下一轮的自己）：`run.mjs` **自己就会 `import` 并启动 18080 测试站**，另外再起一份会撞 `EADDRINUSE` 把整轮废掉（本轮第一次就这么废的，日志 `.verify\fixtest\suites_round9.log`）；单独重跑 `stage_magnet` 之前必须确认 BT 做种机（51500/51501）在监听，否则 `F1` 会假红（`.verify\fixtest\e2e_round9h_magnet.log`，该次作废）。
- 删除 `.verify\e2e\tmp_peek.py`（第八轮留下的临时脚本）。

### 15-H 有意未做 / 遗留

- ~~**缺陷 3（仅 `filename*=UTF-8''` 的 Content-Disposition）** 仍是 aria2 上游边界；真要修得在扩展里用 `webRequest` 缓存响应头再显式传 `out`，本轮不做。~~
  **← 这条结论是错的，第十轮（§16）已推翻并修好。** 错因是我自己的测试夹具：`/cd/utf8only` 的 ext-value 里圆括号没按 RFC 6266 百分号编码（`attr-char` 不含 `(` `)`），属于**非法 ext-value**；aria2 对非法 ext-value 的处理是整条 `Content-Disposition` 作废。合法的 `filename*`（含 CJK、空格、语言标记、跟 302）aria2 本来就能解。而且当时 `stage_names.py` 的 `B4` 断言写的就是"期待解析失败"，所以这个错误结论一直显示为绿。详见 §16-A。
- **404 不做客户端自动重试**（15-C 的定性）。测试侧相应调整为"必须 fail-fast + 用户可恢复"。
- **历史行上限 2000**：超出按完成时间淘汰最旧（连带删掉它的 meta），UI 不额外提示。
- 历史行的"继续/暂停"按钮按状态自然隐藏；对历史行调用它们会被 API 以 `400` 拒绝（隔离后的任务本就不该被"继续"）。
- **全链路唯一残留红：`F0a`**（磁力阶段前置条件）。已定性为测试自己的异步删除竞态并改了脚本，但**改完之后的复跑作废**（缺做种机），所以本表仍记 14/15 而不是"补跑后全绿"。下一轮把做种机拉起来再跑一次就能确认。
- 测试沙箱 `%TEMP%\kokona_e2e\145754`（约 986 MB）**故意保留未删**，供事后核对；下一轮 `stage_setup` 会自动清空。
- **本地历史带来一个新的可见后果**（本轮实测）：反复测试后 `/api/tasks` 已有 **900 多行**历史（`app.log`：`本地历史已恢复 908 条已完成/失败记录`），上限 2000 之前**列表会一直变长**，UI 目前没有分页/按时间折叠。真实用户一年里不太会撞上限，但这条应写进产品预期（与《项目文档.md》§5.2"本地历史"一致）。
- 部署：`dist\KokonaSingleFile\KokonaDownloader.exe` 已复制到 `C:\Users\lumin\Desktop\KokonaSingleFile\`（目录原先不存在，本轮新建；两份 SHA256 一致），供用户自测；当前运行中的实例是 `dist\` 那份（同一构建）。
- **仍未做 GitHub 同步**：按用户指示不提交、不推送、不打 tag、不替换 Release 资产；本轮全部改动只在工作区。
  （第十轮末尾按用户新指示完成同步，见 §16-H。）


## 16. 第十轮：专项复测"临时链接解析不到真实文件名" + 修复（2026-09-25）

### 16-A 先把上一轮的错误结论撤掉（本轮最重要的一条）

§15-H 里那句"缺陷 3 是 aria2 上游边界"**是错的**，而且错在我这边：

- 我给的夹具 `.verify\e2e\site\server.mjs` 的 `utf8only` 是
  `attachment; filename*=UTF-8''%E6%8A%A5%E5%91%8A%20(1).pdf`
  —— `%20` 编码了，但 **`(1)` 是裸写的**。RFC 6266 的 ext-value 只允许 `attr-char` 集合里的字符，**圆括号不在里面**，所以这条头是**非法 ext-value**。
- aria2 对非法 ext-value 的处理不是"忽略这一段"，而是**把整条 `Content-Disposition` 判为无效**：对照实验 c3（`filename="fallback.bin"; filename*=UTF-8''…(1).pdf`）里，aria2 连旁边那条完全合法的 `filename="fallback.bin"` 都不认，直接落回 URL 末段（既不是 `fallback.bin` 也不是真名）。
- 浏览器（含 Edge）对同一行是**宽容**的：它照样把 `报告 (1).pdf` 显示给用户。所以用户看到的现象正是"浏览器里名字对、客户端里名字错"。
- 更糟的是 `.verify\e2e\stage_names.py` 的 `B4` 断言写的就是"名字应保持 URL 末段 `utf8only`"——**我把自己的夹具缺陷固化成了绿色断言**，所以它连续几轮都没报警。本轮处理：夹具保留（它就是现实里大量存在的畸形头），断言改成"必须拿到真名"，并补了合法编码版做对照。

### 16-B 复测方法：23 种响应头形态 × 3 个层次

| 层次 | 目的 | 工具 |
| --- | --- | --- |
| L1 裸 aria2 | 判定"到底是不是上游限制" | 打包里**同一份** `aria2c.exe`（1.37.0），与产品一致的全局参数，独立 RPC 16802 |
| L2 产品 API | 用户在客户端里实际看到什么 | `POST /api/download`（16800，修复前的 v1.0.8 打包件） |
| L3 修复原型 | 验证"宽松解析 + 预解析下发 `out`"能不能救回来 | 同一站点，先 `Range: bytes=0-0` 预解析再喂名字 |

- 探针站：`.verify\nameprobe\server.mjs`（127.0.0.1:18098，256 KiB 确定性载荷，sha256 `725c51b0d67b…`，支持 `Range`→206，含 302 临时链接路由）。
- 探针脚本：`.verify\nameprobe\probe_name.py`（`--mode aria2|product`），表格化：`fmt.py` / `fmt_product.py`；证据 JSON 在 `.verify\nameprobe\out\`。
- 判定：`got == want`，或去掉 aria2/客户端自动改名后缀（`x (1).pdf` / `x.1.pdf`）后相等。

### 16-C 决定性对照：同一条头，只差"括号有没有编码"

| 用例 | `Content-Disposition` | 打包内 aria2 1.37.0 自己解到的名字 |
| --- | --- | --- |
| c19 | `filename*=UTF-8''%E6%8A%A5%E5%91%8A%20%281%29.pdf`（括号已编码） | ✅ `报告 (1).pdf` |
| c1 | `filename*=UTF-8''%E6%8A%A5%E5%91%8A%20(1).pdf`（括号裸写） | ❌ 落回 URL 末段 `c1_star_only_utf8` |
| c21 | `filename*=UTF-8''report%20%281%29.pdf`（ASCII，已编码） | ✅ `report (1).pdf` |
| c20 | `filename*=UTF-8''report (1).pdf`（ASCII，括号裸写） | ❌ 落回 URL 末段 |

⇒ **跟 CJK 无关，跟"只有 `filename*`、没有 `filename=`"也无关，只跟 ext-value 是否合法有关。** 上一轮的"上游限制"定性到此作废。

### 16-D aria2 1.37.0 实测能力边界（L1，不加任何我方补丁）

- **能解**：只有 `filename*`（CJK / 空格 / `UTF-8'zh-cn'` 语言标记 / `inline` / 小写 `utf-8` / 零宽字符前缀 / `%25` 字面量还原 / 跟完 302 重定向之后）。
- **不能解**：
  1. **非法 ext-value**（裸圆括号最常见）——而且**整条头作废**（见 16-A 的 c3）；
  2. **缺 disposition 类型**（只有 `filename*=…`，没有 `attachment`/`inline`）——c17；
  3. **`filename=` 里裸写 UTF-8 字节**——落盘名变乱码 `ä­æ_c18.pdf`（c18）。加上 `--content-disposition-default-utf8=true` 后同一份 aria2c 立刻正确（`中文_c18.pdf`，实测）；
  4. 名字只出现在 **query 里**（S3 式 `?response-content-disposition=…`）——落回 URL 末段（本轮明确不处理，见 16-H）。

### 16-E 本轮改动（能修的都修了）

| # | 改动 | 位置 | 作用 |
| --- | --- | --- | --- |
| 1 | 新增**比 aria2 宽容**的 RFC 6266 解析器：非法 ext-value 照收、优先 `filename*`、只取 basename、拒绝控制字符 / Windows 非法字符 / `CON`·`COM1` 保留名、剥首尾零宽与 BOM、超长截断且保留扩展名、`filename=` 一律**不**做百分号解码（与浏览器/aria2 一致）、`filename=` 里的裸 UTF-8 字节还原 | `src/KokonaDownloader.Core/Engine/ContentDispositionParser.cs` | 救 c1/c3/c17/c18/c20 一类 |
| 2 | 新增**预解析器**：`Range: bytes=0-0` 探测（跟 302）→ 解析头 → 作为 `out` 下发；门控：只在"无显式文件名 + http/https"时考虑，**带签名参数且有扩展名**的链接直接跳过；**只认 200/206**（404 错误页挂的 `Content-Disposition` 不算名字）；任何失败返回 `null` ⇒ 行为与修复前逐位相同 | `RemoteNameResolver.cs` | 让宽容解析真正落到产品里 |
| 3 | aria2 启动参数加 `--content-disposition-default-utf8=true`；并把参数组装抽成 `Aria2Process.BuildArgs()` 以便单测断言 | `Aria2Process.cs` | 裸 UTF-8 头不再乱码（c18） |
| 4 | 单任务与批量两条添加路径都接上预解析（`WithPreresolvedFileNameAsync`） | `DownloadEngine.cs` | 与 15-B 的两条路径保持一致 |
| 5 | **引擎掉线自愈**：`AddUriWithGuardAsync` / `AddUriBatchWithGuardAsync`（连接类异常→重启→重试一次→仍失败才抛中文错误）+ 轮询循环看门狗 `RestartEngineIfNeededAsync` | `DownloadEngine.cs` | 修"aria2 一掉线，之后每次添加恒 500 且永不恢复" |
| 6 | `NewTaskRequest.WithFileName()`（该类是 `init`-only 类不是 record，用不了 `with`） | `Models.cs` | 支撑 #2 |
| 7 | 测试侧：站点新增 `starencoded` / `nodisptype` / `rawutf8` 三个形态；`B4` 断言改为期待真名并新增 `B4b/B4c/B4d`；站点自测 S14 键表同步；`TestEnv.FileServer` 加 `RequestCountFor()` 以证明"预解析真的多发了一次请求" | `.verify\e2e\site\server.mjs`、`stage_names.py`、`selftest.mjs`、`.verify\e2e\chain_lib… TestEnv.cs` | 防止错误结论再次被固化成绿色 |

### 16-F 修复后实测

- **单元/集成**：`src/KokonaDownloader.Core.Tests/RoundTenNameTests.cs` 共 **44** 条——解析器 18 形态、可用性拒绝 8 条、路径穿越/超长 2 条、门控 11 条、启动参数 2 条、错误响应守卫 1 条，外加 5 条**真实 aria2 端到端**：非法 ext-value 落真名、缺类型落真名、预解析请求计数（无扩展名 ≥2 次 / 显式命名恰好 1 次）、签名链接不预解析但合法头仍由 aria2 解对、**把 aria2 进程杀掉之后添加任务能自动重启引擎并下完**。
- **Core 全套**：见下一段同批数据（本轮最终数以收尾复跑为准）。
- **产品级探针（新构建 + 干净下载目录）**：`.verify\nameprobe\out\probe_product_192431.json` → **20/21 命中服务器真名**。唯一"不符"是 c8：服务器名字里带 U+200B 零宽字符，我方**故意剥掉首尾零宽**（落盘名是它的可见等价形式）；c18（裸 UTF-8 头）本轮变 ✅；c1/c3/c17/c20 全部变 ✅；c10/c13/c16 属"名字不可用/安全优先"，仍按 URL 末段（见 16-H）。
- **全链路 9 阶段复测**（本轮最终一次，扩展副本已由 `stage_setup` 的 `P0` 强制同步并逐文件核对）：`setup 18/18` · `basic 68/68` · `dir 14/14` · `names 189/189` · `failures 41/41`（skip 4）· `compat 42/42` · `hidden 19/19` · `resume 53/53`（skip 1）· `magnet 17/17`（skip 1）= **461/461，skip 6，失败 0**。
  - 覆盖面诚实说明：drip 夹具改动只影响 `e2e_slow_24mb.bin` / `e2e_norange_8mb.bin` 两个夹具，而全链路里**只有 `resume` 引用它们**（`grep` 核对过阶段脚本），所以其余 7 个阶段沿用夹具改动之前那一次的结果；受影响的 `resume` 与本轮新增清理判据的 `magnet` 都在改动之后**重跑过**。
  - 两处 skip 都是产品行为而非漏测：`resume` 那条是"同 infohash 已在下载 → 重复种子保护合并"，`magnet` 的 `F5`（.torrent 链接）同理被合并进已在跑的 BT 任务。
- **下载功能全量 `run.mjs`**：**52/52**（上一轮 50/52 的两条红是 `stage_resume` 把 `globalSpeedLimit=256 KiB/s` 留在了用户设置里，导致 B3 50MB 跑到 77% 超时；本轮先跑 `chain_cleanup.py` 还原设置再跑，全绿）。限速用例实测 `elapsed=27.9s`、真实平均 `376,171 B/s` ≤ 上限×1.6。
- **GitHub 同步（本轮按用户指示执行）**：`main` 推送 `e3a1926..d8015d1`（含"第四轮审计落地"与"第十轮"两个提交）；Release `v1.0.8`（id 391289091）原地替换两项资产：`KokonaDownloader_108.exe` 319,711,373 → **318,237,528 B**、`KokonaExtension_108.zip` 37,834 → **39,369 B**（均 http=201，替换后回读资产表核对大小）；教程 PDF（288,628 B）未动。**未另打 `v1.0.9` tag**：版本号仍是 1.0.8（全链路 `P1` 判据也锁在 1.0.8），沿用 9-18 已有的"原地替换 exe 资产"先例；如果以后想让"哪次发布对应哪份产物"可追溯，建议改成"版本号 +1 + 新 tag + 新资产"，代价是要同步改 `P1` 之类的版本断言。
- **Core 全套（最终一次复跑）**：**216/216**，耗时 6 m 34 s；扩展逻辑 `cscript //nologo tests\test_logic.js`：**81/81**；站点自检：**56/56**。
- **新构建**：`dist\KokonaSingleFile\KokonaDownloader.exe` → **318,237,528 字节**（比第九轮 +16,384 B），SHA256 `6A5BCF9622BDCD7AAD569CFF179F5D8A65255B8C130647008E5450534F4938AC`，`/api/ping` 仍 `1.0.8`。运行期直接查子进程命令行确认参数真的下发了（不是只看源码）：`Get-CimInstance Win32_Process -Filter "Name='aria2c.exe'"` → 含 `--content-disposition-default-utf8=true`。

### 16-G 本轮过程错误（诚实清单，全部是测试/工具自己的问题）

1. **夹具自己写了非法 ext-value**，据此得出"上游 aria2 限制、超出范围"并写进 §15-H；同时 `B4` 断言把错误结论固化成绿色。教训：**"上游限制"这类结论必须用最小差分实验钉住**（本轮的 c19/c1、c21/c20 就是最小差分：同一条头只差一个编码）。
2. `Get-Process aria2c | Stop-Process` 把**客户端自己的引擎**一起杀了 ⇒ 之后所有添加恒 500（"目标计算机积极拒绝"）。顺带暴露了一条真缺陷：**引擎掉线后既不自动重启、错误文案还是原始 SocketException**——本轮已修（16-E #5）并加了杀进程的单测。
3. `dotnet publish` 因为**运行中的 exe 正占用**而失败（`GenerateBundle` 抛 `IOException: The process cannot access the file …`），但我把命令包装成 `… ; "exit=$LASTEXITCODE"`，作业退出码是 0，**看起来像成功**。教训：包装命令会吞掉真实退出码，必须读日志或用 `$LASTEXITCODE` 判定。
4. `Content-Disposition` 在 .NET 属于**实体头**（在 `resp.Content.Headers` 里），只查 `resp.Headers` 永远查不到——我的解析器单测全绿、集成测试却全红，才把它逼出来。**集成测试的价值就在这里**：纯单测只能证明"字符串→名字"，证明不了"能不能拿到那条头"。
5. 预解析最初会**从 404 错误页的 `Content-Disposition` 取名**（下载站的"文件不存在.html"也会被当落盘名）⇒ 加状态守卫（只认 200/206）并补单测。
6. 用 PowerShell `(Get-Content -Raw).Replace(…) | Set-Content -Encoding UTF8` 改源码把 UTF-8 文件写成乱码（PS 5.1 按 ANSI 读），只能整文件重写。改文本一律用 `edit`/`write`。
7. 判"命中"时把自动改名后缀（`x (1).pdf` / `x.1.pdf`）算成不匹配，第一轮表格统计偏低；`fmt*.py` 的 `stem()`/`same()` 修正后才与人工核对一致。
8. **`dist\KokonaExtension` 长期是陈旧副本（本轮新发现，影响面最大的一条）**：全链路是拿真实 Edge `--load-extension=dist\KokonaExtension` 跑的，但**只有 `build.ps1` 会把 `extension\` 拷进 `dist\`**，第八轮起我们都是直接 `dotnet publish`，于是 `dist` 里还是 **9-13/9-14 的旧扩展**（`logic.js` 6,507 B，而 `extension\logic.js` 已经 8,971 B）。也就是说：**第八、九轮里凡是"验证扩展行为"的链路证据（接管规则放松、重复点击去重等），实际跑的都是改动之前的扩展代码**。本轮同一条链路的 `dist` 副本同样陈旧（本轮产品改动都在 Core，命名类结论不受影响，但涉及扩展的那部分必须重跑才算数）。修法：`stage_setup.py` 现在开跑前强制 `extension\` → `dist\KokonaExtension` 全量同步，并新增 `P0` 逐文件核对（数量 + 大小，对不上直接红）。
9. **测试判据本身有两处写错了**（第十轮全链路第一次跑出 3 条红，全部是测试的问题，不是产品的）：
   - `stage_names.py` 的 `B6/B7`：把"目录里不许出现 `evil_dotdot.bin`/`evil_abs.bin` 这个名字"当成了安全判据。真正的性质是**文件不许落到下载目录之外**；我方解析器现在把 `../../x`、`C:\Windows\x` 收成 basename 正常落进目录里（第九轮之所以"目录里也没有"，是因为当时 aria2 整条头都不要、落回 URL 末段——巧合被当成了判据）。已改为"目录外四处必须没有 + 目录内必须有且 `filePath` 就在目录里"，正向断言更强。
   - `stage_resume.py` 的 `D4`：用"限速窗口里的峰值 < 700 KiB/s"证明"限速确实在起作用"，对照对象选错，还依赖上一个不限速任务有没有排空（实测峰值 747,201 B/s > 716,800 假红）。第一版修法（解除限速后再采一轮做对照）**也是错的**：解除时那条任务已经快下完，只剩 1 个样本（289,877 vs 239,275），样本量撑不起"2 倍"结论。最终改成**先测基线**：同一条任务先不限速采 8 s（实测中位数 825,588 B/s），再打 256 KiB/s 采 14 s（中位数 261,634 ≤ 上限+20%），两个中位数同轮直接对照，并要求基线本身就在红线之上（否则说明上限是从上一个阶段漏下来的，对照无效）。
10. **测试站的"慢速文件"夹具根本没在限速**（本轮最后才发现，是 `D4` 三次返工的真正原因）：`site/server.mjs` 的 drip 节流只包在 `status === 200` 的响应里，而 aria2 拿到 `Accept-Ranges` 就一分为十六发 Range，**每条 206 完全绕过节流**——24 MiB 的"慢速文件"实测 **24 MB/s** 秒完，于是打完限速之后一个样本都采不到（`limited median 0 B/s`）。修法是改成**按文件共享的令牌桶**（同一文件不管几条连接，聚合速率都是 `dripBytes/dripMs`），并新增站点自检 `S30`：拿 256 KiB 的 Range 请求计时，必须 ≥ 200 ms——把"206 也要过桶"这条夹具自身的不变量钉死。站点自检 **56/56**。教训和 16-G #1 同源：**夹具的名字（`slow_24mb`）不等于夹具的行为，夹具自身也要有断言**。
11. **磁力阶段 `F1` 连红两轮，第十轮用取证脚本 `.verify/e2e/diag_magnet_rpc.py` 钉死了真因（不是产品回归，是测试留下的引擎状态）**。取证链：
    - 直连客户端自己的 aria2 RPC（**端口/密钥从子进程命令行读**：`Get-CimInstance Win32_Process -Filter "Name='aria2c.exe'"`；RPC 路径必须是 `/jsonrpc`，写成 `/rpc` 会恒 400——我第一版就栽在这里，把一条好端端的链路读成"引擎全崩"）；
    - `tellActive` 里躺着两条**客户端列表里根本看不到**的条目：一条是上一轮下完 20 MiB **仍在做种**的载荷（`infoHash=49c7c3b2…`），一条是元数据没下完的磁力孤儿（`infoHash=0000…0000`、`totalLength=0`、连接数飙到 50）；
    - 于是新加的磁力 `addUri` 先被收下（正常返回 gid），下一秒就被 aria2 丢掉（aria2.log：`InfoHash 49c7c3… is already registered`），客户端拿到的 gid 在 `tellStatus` 里查无此人（`No such download for GID#…`）→ 界面上是一条**永远 0% 的幻影行**。上一轮我只看到 F5 那条"预期内"的 already-registered 日志，就把它记成"疑似环境时序"，这轮的复跑证明**它每一步都可复现**。
    修法（全部在测试侧）：`stage_magnet.py` 新增 `F0z`（进阶段就绕过客户端、直接从引擎侧 `forceRemove` 掉所有 BT 条目）与 `F9`（阶段收尾同样清干净，不再指望"上一轮恰好没留下东西"），复跑 **F1 直接变绿**（磁力 → 确认窗 → 下载完成 → 20 MiB 载荷逐字节校验一致）。
12. **`.aria2` 控制文件的判据抢跑**（同一条 F1 的第二个假红）：BT 任务收尾进入做种时 aria2 会重写/删除控制文件，"完成的那一刻"去目录里看必然看到残留（几分钟后同一目录已经干净）。改成最多等 60 s 的收尾窗口，判据不放宽。
13. **顺带记两条产品事实**（本轮取证确认，非缺陷、此前文档没写清）：
    - 客户端默认 `BtTrackersEnabled = true`：每 24 h 从社区列表拉 ~20 条公共 tracker，用 `--bt-tracker=` 下发给引擎，**每条磁力的 `announceList` 因此都会带上这 20 条公共 tracker**（实测：本地 tracker + 20 条公共）。好处是连通性，代价是 infohash 会被广播到公共 tracker——隐私敏感的下载应先关掉这个开关。
    - aria2 的 infohash 注册表**跨引擎重启存在**（`--save-session` 指向 `%APPDATA%\KokonaDownloader\engine\aria2.session`）：只要同哈希的任务还在（哪怕已经下完在做种），同哈希磁力就会被静默丢弃；产品目前**不会**把这种情况显示成错误，界面是一条 0% 的幻影行。这是已知可用性缺口（修法要在添加后校验 gid 是否还活着并把原因写进错误文案），本轮未改，留作后续。→ **第十一轮已改；并且第十一轮本机实测发现重复磁力的形态是"aria2 生成 `already registered` 错误任务"而不是静默丢弃，见 §17-C/§17-E。**

14. **`.ps1` 助手脚本没写 BOM，Windows PowerShell 5.1 按 GBK 解码把脚本"读串了行"**：`.verify\replace_assets_round10.ps1` 顶部是中文注释，UTF-8 无 BOM；5.1 用系统 ANSI 码页解码时，一个 UTF-8 多字节序列会把紧随的换行吞进第二个字节 ⇒ 行号整体错位、`param()` 被并进注释，报 `Unexpected token ')'`（我一开始还以为是脚本本身写错）。同一份文件在 PowerShell 7 里解析正常。修法：给所有含中文的 `.ps1` 补 UTF-8 BOM（已就地补上并用 `[scriptblock]::Create()` 验证可解析），并优先用 PowerShell 7 跑。教训：**中文注释也是测试基建的一部分，编码要和判据一样较真**（同一条坑在 §16-G #6 已经踩过一次，那次是改源码）。


### 16-H 有意未做 / 遗留

- **c10：名字只在 query 里**（S3/OSS 式 `?response-content-disposition=attachment%3Bfilename%3D%22%E4%B8%AD%E6%96%87.pdf%22`）——仍落回 URL 末段。要解得在预解析里二次解析该参数（含 URL 二次编码），本轮不做。
- **c16：普通记法里的百分号字面量**（`filename="%E6%8A%A5%E5%91%8A%20(1)"`）——按 RFC 6266 **不解码**，落盘名保留字面量（与浏览器、aria2 三者一致，属正确行为而非缺陷）。
- **c13：`..%2F..%2F` 编码穿越**——只取 basename（`evil_c13.pdf`），安全优先。
- **签名链接的取舍**：带 `signature/sign=/expires=/token=` 等参数**且 URL 里已有扩展名**时不预解析（避免多碰一次性签名 URL）；这类链接若响应头 ext-value 非法，仍会落回 URL 末段。**无扩展名**时仍会预解析（没有别的可兜底）。
- 引擎自愈目前只覆盖**添加**路径与轮询看门狗；暂停/恢复/删除等端点遇到引擎掉线仍会直接报错（下一轮可把守卫上移到 RPC 层）。→ **第十一轮已关闭，见 §17-B。**
- **磁力"幻影行"**（见 16-G #11）：同 infohash 已在引擎注册时 aria2 会静默丢弃新任务，产品界面是一条 0% 且永不报错的行。候选修法：添加后延迟校验 gid 是否仍在 `tellActive/tellWaiting/tellStopped`，查不到就把任务标为失败并写明"同一磁力已在下载/做种"。本轮**未改**（需要动轮询与状态机，且要新的集成测试兜底）。→ **第十一轮已关闭，见 §17-C。**
- **公共 tracker 广播**（见 16-G #13）：`BtTrackersEnabled` 默认开启，磁力会向 ~20 条公共 tracker announce。要不要默认关、或在 UI 上明示，属于产品决策，本轮只记录不改。
- 历史行上限 2000、UI 不分页（与 §15-H 相同）；测试沙箱 `%TEMP%\kokona_e2e\*` 由下一轮 `stage_setup` 自动清空。

## 17. 第十一轮：引擎自愈上移 + 磁力"幻影行"收敛

### 17-A 本轮范围

只做 §16-H 里明确点名的两条遗留项（用户指示：**本轮不推送 GitHub**，不 commit / 不 push / 不打 tag / 不替换 Release 资产）：

1. 引擎自愈目前只覆盖**添加**路径与轮询看门狗 → 上移到 RPC 层（见 17-B）；
2. **磁力"幻影行"**（同 infohash 已在引擎注册时界面留一条 0% 永不报错的行）→ 收敛成带原因的失败行（见 17-C）。

### 17-B 引擎自愈上移到 RPC 层

| # | 改动 | 文件 |
|---|---|---|
| 1 | 新增通用守卫 `WithEngineGuardAsync<T>`：`IsEngineUnreachable` 命中 → `RestartEngineIfNeededAsync` → **重试一次** → 仍失败才抛中文 `InvalidOperationException`；`AddUriWithGuardAsync` / `AddUriBatchWithGuardAsync` 退化为它的薄封装（第十轮 #5 的行为不变） | `DownloadEngine.cs` |
| 2 | 暂停 / 继续 / 暂停全部 / 继续全部 四个端点全部改走守卫（动作名分别"暂停任务/继续任务/暂停全部任务/继续全部任务"），不再把 `SocketException` 原样冒到 HTTP 层 | 同上 |
| 3 | 删除路径：`remove` / `removeDownloadResult` 走守卫（"删除任务"/"清理任务结果"）；**语义保持"这一行必须消失"**——引擎侧最终仍失败时只记日志"删除任务时引擎未成功执行（继续本地清理）"，不挡住本地元数据/历史清理、不向 HTTP 层抛错 | 同上 |
| 4 | 重新下载：`tellStatus` 接上自愈（重启后重读一次，仍失败才回落本地历史 `HistoryFallback`） | 同上 |

### 17-C 磁力"幻影行"收敛

两个部件，汇到同一个用户可见结果：

1. **添加后存活校验**：`TrackMagnetLiveness(gid, magnet, taskNumber)` 登记 `_pendingMagnetAdds`（超时 `MagnetLivenessTimeoutMs = 6000`）；轮询每轮拿到 `tellActive/tellWaiting/tellStopped` 后**先**跑 `ResolvePendingMagnetAdds`（必须在 `followedBy` 处理之前——元数据任务一旦被消费，它的 gid 就会从 aria2 列表里消失，之后必然被误判成"被丢弃"）。
2. **`already registered` 兜底**：轮询里对 `status=error` 且 `errorMessage` 含 `already registered` 的条目，不再是"删行 + `TaskRemoved`"（修复前用户只看到"行没了"），改为清掉 aria2 里的噪音结果 + 写一条失败行。

两条路径都调 `FailDuplicateBtAdd`：`UpdateFinished` 落终态 + `RememberHistory` + 推 `TaskChanged`，界面与 `/api/tasks` 都会看到 `state=failed`、`errorMessage=同一磁力已在下载或做种中：引擎未注册本次添加，无需重复添加`。引擎重启时 `_pendingMagnetAdds` 随 `_lastStates` 一起清空（旧登记在新引擎里已无意义）。

### 17-D 顺带修掉的真实缺陷（不在计划内，被新测试逼出来）

`Aria2Process.Start()` 原先先赋值 `_process = new Process{…}` 再 `Start()`：**一次启动失败**（路径不存在、端口被占等）就会把 `_process` 留成"从未成功启动的进程对象"，此后读 `IsRunning`（内部访问 `HasExited`）永远抛 `InvalidOperationException: No process is associated with this object.`。后果是**引擎自愈彻底失效**——第一次重启失败之后每个端点都只会报"自动重启失败: No process is associated with this object."；更糟的是 `PollLoopAsync` 里的 `if (!_process.IsRunning)` 不在 `try` 内，轮询任务会被这条异常直接打死（列表从此不再刷新）。修法：`IsRunning` / `ExitCode` 改为 try/catch 安全实现，`Start()` 失败时 `_process = null` 再抛。

### 17-E 修复后实测

- 新增 `src/KokonaDownloader.Core.Tests/RoundElevenSelfHealTests.cs`（3 条）：
  - `引擎掉线时暂停与恢复走自愈而不是抛出原始网络异常`：构造"RPC 端口无人监听 + aria2 路径不存在 ⇒ 重启必然失败"的引擎，断言 Pause/Resume/PauseAll/ResumeAll 四个端点都抛中文 `InvalidOperationException`（消息含"下载引擎不可用"）且日志里"正在自动重启"出现 **≥4 次**（证明每次都真去重启过，不是直接放弃）。
  - `引擎掉线时删除任务仍完成本地清理且不抛异常`：断言行消失语义下 `RemoveAsync` 不抛异常，日志含"删除任务时引擎未成功执行"。
  - `同批次重复磁力未被引擎注册时必须标失败并写明原因`：真实 aria2 + 真实磁力（一次提交两条相同 infohash），断言恰好有一条失败行、`errorMessage` 含"同一磁力"/"已在下载或做种中"，另一条仍活着（Active/Waiting/Paused/Seeding/Completed），且失败行**写进磁盘 `tasks.json` 的终态**（`FinalState=Failed`——aria2 里没有它，重启后只能靠本地留档带回来）。
- **分支取证（本机实测，不是推断）**：失败行在 **2721 / 2724 ms**（两次复跑一致）出现，日志为 `alreadyRegisteredErrorTask=True livenessFallback=False` ⇒ 本机 aria2 对重复磁力的真实形态是**生成一个 `already registered` 的可见错误任务**（即 17-C 的第 2 条路径），**不是**静默丢弃。预留的"6 s 存活校验"分支（静默丢弃形态，§16-G #11 里测验脚本观察到的那种）本轮**没有在本机复现**，只做了代码路径覆盖，报告不把它记成"已实测"。测试同时钉住两条不变量：失败行必须来自这两条已知路径之一；若走存活校验路径，耗时必须 ≥5.5 s（防止"兜底被别处提前触发"造出假绿）。
- Core 全套（**本轮全部改动冻结后**的最终复跑）：**219 / 219 PASS**，耗时 6 m 54 s，失败 0（第十轮 216 条，本轮 +3 条全部来自 `RoundElevenSelfHealTests`；连跑两次结果一致）。
- 扩展逻辑 / 站点自检 / 全链路 e2e：本轮改动**全在 Core**（另加两个只写日志的分支标记），按 §16-F 的"覆盖面诚实说明"体例，这些链路未复跑，沿用第十轮结果（`cscript //nologo tests\test_logic.js` 81/81、站点自检 56/56、全链路 461/461 skip 6）。
- **GitHub 同步：本轮按用户指示不执行**（未 commit / 未 push / 未打 tag / 未替换 Release 资产）；本地 `main` 仍是第十轮收尾的 `c093654`（"docs: 第十轮收尾"），版本号仍 `1.0.8`。

### 17-F 本轮过程错误（诚实清单，两条都是测试自己的问题）

1. **分支取证的判据打错了位置**：第一版把"aria2 错误任务"标记打在 `dupFailGids` 分支、把"存活校验"标记打在两条分支共用的 `FailDuplicateBtAdd` 里，于是后者在两条路径上都会输出 ⇒ 一次正常的失败被判成红（`alreadyRegistered=True liveness=True`）。教训同 §16-G #1：**判据要打在分支入口**，不能打在共用函数里。修法：每个分支加独有日志行（"…以 already registered 错误拒绝" / "…{timeout}ms 后仍未出现在 tell* 中（引擎静默丢弃）"）。
2. **"失败行必须进本地历史"的断言写成立刻读磁盘**：`TaskStore` 是 **500 ms 防抖落盘**（`ScheduleSave`），而内存历史（`_history`）当时已经有那一行 ⇒ 界面/API 断言全绿、磁盘断言 `Expected: "Failed" Actual: null` 假红。修法：与 `RoundEightFixTests` 一致，改成等到磁盘上真出现终态（带超时），语义不放宽（"必须落盘"这条不变量仍被钉住）。

### 17-G 本轮有意未做 / 遗留

- §16-H 其余各条**原样保留**：c10（名字只在 query 里）、c16（普通记法百分号字面量不解码，属正确行为）、c13（编码穿越只取 basename，安全优先）、签名链接不预解析的取舍、公共 tracker 广播默认值（产品决策）、历史行上限 2000 / UI 不分页。
- 磁力"静默丢弃"形态（存活校验分支）只有代码路径覆盖，缺本机可复现的夹具；要在别的 aria2 版本/环境下验证，需要能稳定造出"addUri 返回 gid 但 tell* 查无此人"的场景（例如把 `MagnetLivenessTimeoutMs` 调小并人为延后注册）。

## 18. 第十二轮：版本号 1.0.9 + 单文件重打包 + 复测

### 18-A 本轮范围

用户指令：「现在修改版本号为 1.0.9 并打包单文件 exe 再进行复测。并写入测试结果到文档。」拆成四件事：升版本号 → 打包单文件 exe → 复测 → 写文档。**本轮不改产品逻辑**（除版本号），**不做 GitHub 同步**（未 commit / 未 push / 未打 tag / 未替换 Release 资产——沿用第十一轮的约束，本轮用户也未要求）。第十一轮的两条产品改动（引擎自愈上移到 RPC 层、磁力"幻影行"收敛）代码已随本轮 1.0.9 首次打包，明细见 §17。

### 18-B 版本号 1.0.9 落地（6 个文件 / 8 处）

| # | 位置 | 改动 |
|---|---|---|
| 1 | `src\Directory.Build.props`（**唯一来源**） | `Version` 1.0.9 / `AssemblyVersion`·`FileVersion` 1.0.9.0 / `InformationalVersion` 1.0.9 |
| 2 | `extension\manifest.json` | `"version": "1.0.9"`（扩展在界面上的版本显示走客户端 `/api/ping`，其余不用改） |
| 3 | `.verify\e2e\stage_setup.py` | 2 处：`P1 client /api/ping answers 1.0.9`、`v1.0.9` 文案断言 |
| 4 | `.verify\functest\test.mjs` | `ping reports version 1.0.9` |
| 5 | `.verify\fulltest\run.mjs` | `A1 ping 200 且版本=1.0.9` |
| 6 | `tools\verify_singlefile_main.ps1`（2 处）、`tools\verify_desktop_launch.ps1`、`tools\verify_singlefile_fallback.ps1` | 版本断言同步；主脚本另有三条陈旧判据修正（见 18-E） |

复核：全仓 `grep 1\.0\.8`（排除 `obj\`）余下的命中**全部是历史注释/变更记录**（`Directory.Build.props` 顶部"以前 1.0.8 同时写死"、`StandaloneBootstrap.cs` 的"v1.0.8 第二/四轮审计修复"、两个验证脚本里"SplashWindow 已在 1.0.8 移除"），无遗漏的版本锁。

### 18-C 打包

- 命令沿用 `项目文档.md` §7.2（必须带 `-p:WindowsAppSDKSingleFileVerifyConfiguration=false`）。首次 `dotnet publish` 直接写 `dist\KokonaSingleFile` 时失败：`GenerateBundle → IOException: The process cannot access the file … KokonaDownloader.exe because it is being used by another process`——用户当时正跑着从该目录启动的**管理员权限**实例（`Stop-Process` / `taskkill /F /T` 均 `Access is denied`）。处理：先发布到暂存目录 `dist\_stage_r12`（成功，exit 0），用户关闭后删除旧目录、`Move-Item` 换入、再把 exe 同步到 `C:\Users\lumin\Desktop\KokonaSingleFile\`；判定读 `$LASTEXITCODE`（不重犯 §16-G #3"包装命令吞退出码"）。
- 产物：`dist\KokonaSingleFile\KokonaDownloader.exe` → **318,245,720 B**（比第十轮 318,237,528 **+8,192 B**，就是版本号资源的增量），SHA256 `BB496128B98CBE1766B5CB132D806D9CB903C5D1522102E29EFF5760633B61D3`，`FileVersion=1.0.9.0`、`ProductVersion=1.0.9`，mtime `2026-09-26T00:13:46Z`。桌面部署目录同大小同哈希。
- `dist\KokonaExtension` 全量重建：12 个文件、`manifest.json` 版本 1.0.9，与 `extension\` 逐文件（相对路径 + 大小）核对一致——即全链路 `P0` 判据的"dist 副本陈旧"问题本轮不存在（该副本是 graph 上链路唯一加载的扩展）。

### 18-D 复测数据（本轮实际跑过的）

| 套件 | 结果 | 证据 |
|---|---|---|
| 单文件嵌入资源 | **12/12 PASS** | exe 二进制内直接搜 `standalone/…` 名字：8 个 xbf（含 `Themes/ThemeSwatchPicker.xbf`）+ `resources.pri` + `aria2c.exe` + `icons/tray.ico` + `icons/tray.png` |
| 单文件·测试 A（干净临时目录只放 exe） | PASS ×4 | `ping: {"ok":true,"app":"KokonaDownloader","version":"1.0.9"}`；**exe 目录保持干净（0 个解压文件外泄）**；运行时目录 `payload=12 exeSizeMatchesDist=True`；近期日志无 `XamlParseException` |
| 单文件·测试 B（dist 目录，散文件布局） | PASS | 窗口 `hwnd=11666766 visible=True size=940x560 pos=(104,104)`；ping 200 / v1.0.9 |
| 单文件·测试 C（只读目录） | PASS ×2 | 原进程 Exit(0)，实际存活进程路径 = `%LOCALAPPDATA%\KokonaDownloader\Standalone\KokonaDownloader.exe`；ping 200 / v1.0.9；运行时目录文件数 **before=14 → after=14**（清单校验通过 ⇒ 不重解压） |
| 引导清单取证 | 通过 | `.kokona_bootstrap.json`：`ExeHash=bb496128…b61d3` **等于** `dist` 产物的 SHA256 ⇒ 运行的就是这份打包件；`Files.Count=12`、`PayloadHash=a037f769b69f73ce252a8374e5c13586e4258e070ffb3f3f59f4a7a893159a73`、`ExeSize=318245720`、`ExeMtimeUtc=2026-09-26T00:13:46Z` |
| Core 全套（`dotnet test`, Debug, 串行） | **219 / 219 PASS** | 失败 0，耗时 **6 m 53 s**（与第十一轮 219/219 一致；本轮只改版本号，无产品代码改动） |
| 扩展逻辑（`cscript //nologo tests\test_logic.js`） | **81 / 81 PASS** | 与第十一轮一致 |

**未复跑（诚实清单）**：`.verify\fulltest\run.mjs`（52 项）、`.verify\functest\test.mjs`（24 项）、`.verify\e2e` 九阶段全链路（461 项）与站点自检（56 项）**本轮无法复跑** —— 本机**没有 node**（`where node` 无结果、`C:\Program Files\nodejs` 不存在），`run.mjs` / `test.mjs` / `site\server.mjs` 都依赖它；`KokonaDownloader.UiTests`（UI 自动化 + 截图）与 UI 控件级检测本轮也未跑。这些链路的最近有效结果仍是第十轮的 52/52、24/24、461/461（skip 6）、56/56，**不能记为 1.0.9 已验证**。

### 18-E 本轮修掉的测试资产问题（诚实清单）

1. **`tools\verify_singlefile_main.ps1` 三条判据已腐烂**（首次运行三条假红，`MISS standalone/SplashWindow.xbf` / `FAIL: splash not seen in test A` / `FAIL: only 0 files`）。定位：全是判据陈旧，不是产品缺陷——(a) 启动动画 SplashWindow 已在 1.0.8（09-18）从产品移除（`项目文档.md` §12「移除启动动画」，`src` 内已无源码，`obj\` 里那份只是残留编译产物）；(b) 自第九轮起 `StandaloneBootstrap` 的语义就是"载荷一律解压到 `%LOCALAPPDATA%\KokonaDownloader\Standalone` 并从那里重新拉起，**exe 目录保持干净**"，而旧判据断言的是"exe 旁 ≥13 个文件 + splash 可见"，与产品语义**正好相反**。已按当前语义重写：嵌入名单去 SplashWindow（12/12）、测试 A 改为"ping 1.0.9 + exe 旁 0 文件泄漏 + 运行时目录 12 文件且 exe 与 dist 同尺寸"，把"exe 目录保持干净"钉成正向不变量。
2. **PS 5.1 无 BOM 的坑再次复现**（同 §16-G #14）。`tools\verify_singlefile_main.ps1` 用 `powershell -File` 直接跑会解析崩溃（`Missing using directive` / `Unexpected token` / here-string `@'` 不被识别）：UTF-8 无 BOM + 首行中文注释，5.1 按 GBK 解码时末字节把换行吞掉，与下一行粘连。本轮同时发现 `tools\verify_singlefile_fallback.ps1` **实际已因此坏掉**——`$dist` 未被赋值，首跑直接 `FAIL: dist exe missing:`（路径为空）。两个文件都已补 UTF-8 BOM（首字节 `EF BB BF`，用 .NET `UTF8Encoding($true)` 重写）后复跑正常。仓内 `tools\*.ps1` 现已全量审计：**再无"含非 ASCII 且无 BOM"的文件**。
3. 版本号 1.0.9 的判据只改到"能改到"的地方：`.verify\e2e`、`.verify\fulltest`、`.verify\functest` 里的断言已同步，但因缺 node 本轮无法用它们验证——**已同步的断言不等于已通过的断言**。

### 18-F 有意未做 / 遗留

- **GitHub 同步**：本轮按用户既有约束不执行（未 commit / 未 push / 未打 tag / 未替换 Release 资产）。Release 上仍是 `v1.0.8` tag 与 1.0.8 资产；版本号与 tag 的对应关系仍按 §16-F 的建议留给用户决定。
- **`tools\probe_splash_args.ps1`**：整体针对已移除的 SplashWindow（探测 splash 窗口尺寸/参数），已无意义，本轮未删未改。
- **`tools\verify_desktop_launch.ps1`（测试 D）**：L1/L46-57 仍在断言"桌面目录启动能看到 splash 窗口（336/420 尺寸）"，且它跟踪的是 `Start-Process` 返回的**原进程 PID**——而当前产品里该进程必然 `Exit(0)` 并把活干完的进程换到 `%LOCALAPPDATA%`，因此这条脚本**一跑必假红且探不到窗口**。本轮只同步了它的版本断言（1.0.9），**未重写**：它的有效内容已被 `verify_singlefile_main.ps1` 的测试 A/B 覆盖（干净目录 + dist 目录两种布局各自的启动与窗口可见性），重写等于重复投资。
- **`tools\verify_singlefile_fallback.ps1`（测试 C）的前提已过时但不影响结论**：脚本名与注释写的是"icacls 拒绝写 exe 目录 ⇒ 回退到 `%LOCALAPPDATA%`"，但当前产品**根本不会尝试写 exe 旁**（`StandaloneBootstrap` 只在 exe 旁已有 `resources.pri` 时提前返回，否则直接走 `%LOCALAPPDATA%`），所以这条测试实际证明的是"**只读目录里的 exe 也能正常启动并服务 API**"这一不变量（本轮实测通过），而不是"回退分支"。断言里的 `>=13 files` 也偏松（实际 14 = 12 载荷 + exe + 清单）。
- §16-H / §17-G 其余遗留**原样保留**（c10 名字只在 query 里、c16 百分号字面量不解码、c13 编码穿越只取 basename、签名链接不预解析、公共 tracker 广播默认值、历史行上限 2000 / UI 不分页、磁力"静默丢弃"形态缺夹具）。


